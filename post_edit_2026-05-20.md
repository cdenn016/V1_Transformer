# Post-Edit Log — 2026-05-20

## Session — Supplementary §B Covariance Dynamics debate (thirteenth in the audit series)

### Trigger

User request continuing the supplementary chapter sweep:
> continue supplementary followup

Followed by, after the debate verdict:
> yes implement commit and push

### Process

Full 5-phase debate (rounds=2, judge=on) under the `red-blue-debate` skill. Artifacts in `docs/debates/2026-05-19-supplementary-covariance-dynamics/`. Verdict: RED_WINS-narrow.

The compound claim covered six sub-claims (α Gaussian KL closed form, β Σ-derivative, γ sandwich-product transport, δ coefficient `-2` on `Σ_i^{-1}`, ε fixed-point and regime analysis, ζ Hessian positive-definiteness). All six sub-claims verified against external canon (Red conceded in rebuttal):
- α → [Bishop 2006 §2.3.6 Eq. 2.121; Murphy 2012 §2.3.2; Cover-Thomas 2006 §8.6 Eq. 8.69].
- β → [Petersen-Pedersen Matrix Cookbook §9.1 Eq. 75, §9.4 Eq. 100].
- γ → [Bishop 2006 §2.3.3] + CLAUDE.md sandwich-product hard constraint.
- δ → direct algebra using `Σ_j β_{ij} = 1`.
- ε → simplex constraint identity + homogeneous-limit reduction.
- ζ → envelope-theorem extension to F_i Hessian (Blue's rebuttal closed Red's attack at sub-claim ζ).

The math is six-for-six against canon. The "complete and self-contained" prong fell on five-to-six concrete editorial breaks: undefined `α_i` at line 275; section-number drift at line 337 ("Section 3.6" → §3.7 State-Dependent Prior Precision); undefined `Λ_o` at line 385 (semantically = R^{-1}); undefined `⊠_{sym}` notation at line 380; zero internal citations across 207 lines covering four canonical correspondences; alignment-dominated regime gated by `α_i ≪ 1` against the framework default `α_i = 1` not flagged.

Aggregate exceeds the precedents of Debate 3 (3 gaps) and Debate 12 (4 gaps).

### Fixes applied — six editorial corrections to `Attention/GL(K)_supplementary.tex`

1. **Line 275 — define `α_i` at first use in §B**: changed "with unit coefficient `α_i = 1`" to "with the self-coupling weight `α_i` on the prior term held at its canonical default `α_i = 1`; see main paper Section `\ref{sec:state_dependent_precision}` for the state-dependent generalization".

2. **Line 337 — fix section-number drift**: replaced "cf.\ main text, Section~3.6" with "cf.\ main text Section~`\ref{sec:state_dependent_precision}`" so the cross-reference resolves to the correct section regardless of future renumbering.

3. **Line 385 — define observation precision**: replaced "and `Λ_o` negligible relative to the inter-agent coupling" with "and the observation precision `R^{-1}` negligible relative to the inter-agent coupling (cf. Eq.~`\eqref{eq:Sigma_gradient_final}`)". Substituted `R^{-1}` directly to match the §B-internal notation at line 253.

4. **Lines 377-381 — explicit Hessian form**: replaced the undefined `⊠_{sym}` notation with the explicit second-variation form `Hess[H, H] = (1/2) tr(Σ_1^{-1} H Σ_1^{-1} H) = (1/2) ‖Σ_1^{-1/2} H Σ_1^{-1/2}‖_F²`, with positive-definiteness immediate from the Frobenius-norm expression. Added a sentence noting the F_i Hessian extension to a β-weighted convex combination via the envelope-theorem identity (Eq.~`\eqref{eq:autograd_envelope_gap}` of main paper).

5. **Inline citations added at three canonical correspondences**:
   - Line 201 — Gaussian KL closed form: appended `\citep{bishop2006pattern,murphy2012machine,CoverThomas2006}`.
   - Lines 223-225 — matrix-calculus derivatives: added "using the standard identities `∂ log|Σ|/∂Σ = Σ^{-T}` and `∂ tr(AΣ)/∂Σ = A^T` `\citep{petersen2012matrix}`".
   - Line 234 — Gaussian linear pushforward: added `\citep[§2.3.3]{bishop2006pattern}`.

6. **Lines 352-353 — flag alignment-dominated regime as theoretical-only**: appended one sentence: "This regime is studied as a theoretical limit; the canonical experimental configuration of the main paper uses `α_i = 1`, in which case the prior term remains present at coefficient `1/2` and the fixed-point form `\eqref{eq:sigma_fixed_point_beta}` applies."

### Bib additions — `Attention/references.bib`

One new entry added after `CoverThomas2006` (line 538):
- `petersen2012matrix` — Petersen-Pedersen Matrix Cookbook (v. 20121115, Technical University of Denmark).

(Existing entries verified: `bishop2006pattern` at line 2516, `murphy2012machine` at line 2523, `CoverThomas2006` at line 531 — all from prior debate edits.)

### Style compliance

- Prose paragraphs throughout the manuscript edits.
- No `\;`, `\,`, `\!` spacing macros (caught and removed `\,` in Edit 4 Hessian display before commit).
- No banned phrases.
- No self-referential drafting history.
- Citations use existing bibkey style.
- Cross-references verified:
  - `sec:state_dependent_precision` exists at main paper line 896.
  - `eq:autograd_envelope_gap` exists at main paper line 872.
  - `eq:Sigma_gradient_final` exists locally in §B at line 270.
  - `eq:sigma_fixed_point_beta` exists locally in §B at line 289.
  - `eq:gaussian_KL` exists locally in §B at line 220.

### Cross-document reference convention note

The supplementary is built standalone (no `xr` package, no `\externaldocument`). Cross-references via `\ref{...}` to main paper labels (e.g., `\ref{sec:state_dependent_precision}`, `\ref{eq:autograd_envelope_gap}`) produce `??` when the supplementary is built without the main paper's aux file. This is the convention already established by prior debate edits in §A and elsewhere in the supplementary. The user can later add the `xr` package or merge the documents at submission time.

### Verdict-queue status update

This is the thirteenth debate in the gauge-transformer audit series. Closed queue:
1. §5 transformer reduction (RED_WINS).
2. Softmax-β stationarity (RED_WINS).
3. Sub-claim A flat bundle (BLUE_WINS — §5 reduction sub-claim).
4. Sub-claim B degenerate Σ (BLUE_WINS).
5. Sub-claim C QK^T identification (BLUE_WINS).
6. Sub-claim D V identification (BLUE_WINS).
7. Canonical F vs surrogate (RED_WINS).
8. Multi-head block-diagonal (BLUE_WINS).
9. Route 1 untied carving (RED_WINS).
10. FFN softmax-gradient correction (RED_WINS).
11. §3 Gauge-Covariant VFE (RED_WINS narrow).
12. Supplementary §A General Mathematical Framework (RED_WINS narrow).
13. Supplementary §B Covariance Dynamics (RED_WINS narrow).

The main paper §3–§5 and supplementary §A–§B are now fully audited at the editorial standard established by the prior debates. Optional follow-ups remain: §C Gauge Frame Gradients (lines 388–610), §D Variational Gradient Descent (lines 611–665), Participatory_it_from_bit.tex §Theory.

## Session — `use_prior_bank=True` decode debate + τ disambiguation

### Trigger

User request:

> perform a /red-blue-debate on the theory and implementation of use_prior_bank = True in the /transformer /vfe transformer

Followed by, after the verdict came back RED_WINS on the P1 sub-proposition (the formula match):

> i thought tau was supposed to be the learnable priorbank temperature?
>
> skip and remove ambiguity

### Debate outcome

Artifacts at `docs/debates/2026-05-20-vfe-use-prior-bank-decoder/`. The
judge graded:

- P1 (runtime formula matches `logits = -KL(q || π_v)/τ`): **LOST for blue**, because the literal CLAUDE.md text reads as bare `τ = decode_tau` while the runtime applies `-c · KL / tau` with `c = exp(decode_log_scale)` learnable.
- P2 (Law 3 — same Gaussian manifold for encode/infer/decode): **PARTIAL** (holds in `diagonal_covariance=True`; full-cov branch projects to diagonal at decode).
- P3 (no-neural-networks): **CONCEDED to blue** (no nn.Linear / MLP / activation on the `True` path).
- P4 (canonical derivation): **PARTIAL for blue** (Bishop §4.2 + Friston Form-3 composition is canonical; derivation absent from manuscripts).

The user's clarification reframed P1: the intent is for τ to **be** the learnable decode softmax temperature (analogous to attention's learnable τ = κ · √K), parameterized via `decode_log_scale` for positivity. Under that reading the runtime *is* canonical; the strike was a documentation ambiguity, not a soundness defect.

### Edits — τ disambiguation

Four-file edit pass making τ unambiguously the learnable decode softmax temperature:

1. **`CLAUDE.md` "Hard Constraints" block** — appended to the PriorBank-decode sentence: "here `τ` is the learnable decode softmax temperature, parameterized as `τ = decode_tau · exp(-decode_log_scale)` with `decode_log_scale` an `nn.Parameter` clamped to `[-3, 3]`, analogous to the learnable attention `κ` in `τ_attn = κ √K`."

2. **`transformer/vfe/prior_bank.py` module docstring** (lines 22–34) — replaced the "deviation from canonical" framing with the explicit identification: τ = decode_tau · exp(-decode_log_scale), and the parallel to attention's κ.

3. **`transformer/vfe/prior_bank.py` parameter init comment** (the `decode_log_scale` block in `__init__`) — restated as "Learnable decode softmax temperature" with the same parameterization formula.

4. **`transformer/vfe/prior_bank.py` `decode()` method docstring** — added a paragraph clarifying that the `tau` symbol in `ℓ_{i,v} = -KL/τ` is the learnable composite, the `tau` keyword argument is the fixed `decode_tau` component.

5. **`transformer/vfe/config.py:235–243`** (`decode_tau` doc) — explained that `decode_tau` is the *fixed* component, the full τ is learnable via the formula.

6. **`transformer/vfe/config.py:338–348`** (`use_prior_bank` doc) — updated to state τ is the learnable composite, noted the diagonal-projection caveat for the full-cov branch (P2 partial), affirmed no nn.Linear on the True path.

### Runtime behavior

Unchanged. The implementation already computed `logits = -exp(decode_log_scale)/decode_tau · KL_diag(q || π_v)`; this is now correctly identified as `logits = -KL/τ` with `τ = decode_tau · exp(-decode_log_scale)`. No code logic, parameter, optimizer-group, or state-dict change.

### Open items (not addressed in this pass)

- **P2 (full-cov branch diagonal projection)**: `prior_bank.py:478-479` extracts the diagonal of `sigma_q` and `sigma_p` even when both are full-covariance, dropping off-diagonal mass from the canonical full-cov KL. The `config.py:338-348` edit now notes this caveat, but the code path is still a documented approximation rather than the canonical sandwich-KL.
- **P4 (canonical derivation as labelled theorem)**: the Bishop §4.2 + Friston Form-3 composition (substitute `q` for the point observation in the Gaussian discriminant, take expected log-likelihood, identify with the FEP accuracy summand under per-token Gaussian likelihood) is not currently present as a labelled appendix in any `Attention/*.tex` file. Worth adding to discharge the canon-§1 policy that novel constructions need explicit derivation from canonical primitives.

## Session — P2 investigation + P4 appendix

### Trigger

User followup:

> investigate P2 and fix P4

### P2 — block-diagonal full-cov decode (investigation only, no code change)

Investigation written to `docs/debates/2026-05-20-vfe-use-prior-bank-decoder/06_P2_investigation.md`. Findings:

- The diagonal projection at `prior_bank.py:494` discards information **only** in the combination `gauge_fixed_priors=True` AND `diagonal_covariance=False`. The other three mode combinations are structurally exact: in `diagonal_covariance=True` paths the `torch.diagonal(...)` call is a no-op on a `(K,)` tensor; in the `(gauge_fixed_priors=False, diagonal_covariance=False)` combination the per-token σ is diagonal-embedded into a `(K,K)` tensor at `_diag_to_full_block`, so all off-diagonal entries are zero by construction.
- In the affected combination, sigma_p is **block-diagonal** with dense within-block content `A_h diag(s_h) A_h^T`. The exact full-cov KL decomposes additively across blocks, with per-block cost `O(d_h³)` (block inverse + log-det) plus `O(d_h²)` per position for the trace and quadratic. For the user's active scale (K=100, 5 blocks of d_h=20, V=10000, B=16, N=128), exact-block decode is ~20× the diagonal cost: ~4·10¹⁰ FLOPs/forward vs ~2·10⁹.
- Approximation error magnitude: roughly `-0.5·d_h·log(1-ρ²)` per block where ρ is the within-block correlation. At `phi_scale=0.001` (active init), within-block correlation is small and the approximation is tight; the gap grows as φ_v moves away from zero during training.
- Recommendation: add an opt-in `exact_full_cov_decode` flag to `VFEConfig`, default `False`, gated to `(gauge_fixed_priors=True, diagonal_covariance=False)`. Per the CLAUDE.md "pure path under appropriate toggles" doctrine. Not implemented in this session; user opted for investigation only.

The investigation also catalogued super-block sizing under `cross_couplings` (the `d_h³` cost is paid in the largest super-block, not the average) as a separate cost note for any future implementation.

### P4 — canonical-derivation appendix in supplementary

Added a new labelled section to `Attention/GL(K)_supplementary.tex` at lines 1326–1444:

> `\section{Derivation of the Prior-Bank Decode from Canonical Primitives}`
> `\label{sec:supp_decode_derivation}`

The appendix has five subsections deriving `ℓ_{i,v} = -D_KL(q_i* || π_v)/τ` from canonical primitives:

1. **Gaussian generative classifier (Bishop §4.2)** — the Bayes-optimal discriminant for class-conditional Gaussians, recovering linear discriminant analysis in the tied-covariance specialization.
2. **Variational plug-in** — substituting the recognition distribution q for the point observation x, scoring class v by `E_q[log p(x|C_v)]`. Cited to Blei2017 Eq.5.
3. **Identification with `-D_KL`** — using the closed-form Gaussian KL (Bishop App. B) to show `E_q[log p(x|C_v)] = -D_KL(q || π_v) - H(q) + C` where H(q) and C are v-invariant, so cancel under softmax.
4. **Identification with the accuracy term of F** — same logit recovered from Friston Form-3 `F = E_q[-log p(o|s)] + KL(q||p)` under a per-token Gaussian observation likelihood.
5. **Scope** — explicit limits: (i) the derivation justifies the readout form, not the M-step objective (CE, not full F); (ii) exact only when q and {π_v} lie on the same statistical submanifold (the diagonal-covariance specialization is closed; the full-cov branch's diagonal projection at decode is an approximation, cross-referencing P2); (iii) the τ in the derivation is the learnable composite documented in the τ-disambiguation edits earlier in this log.

Citations all resolve in `Attention/references.bib`: `Bishop2006` (lines 964–969), `Friston2010` (line 569), `Blei2017` (line 955).

Compliance:
- No banned phrases (`crucially`, `notably`, etc.) in the new section.
- No banned LaTeX spacing macros (`\;`, `\,`, `\!`) — pre-existing file is also clean of these; a 34-instance accidental introduction during drafting was stripped before the file was finalized.
- Equations carry standard end-of-display punctuation per the project's style.
- LaTeX dry-compile (pdflatex -draftmode) passes with only the expected first-pass undefined-reference warnings.

### Net status of the original debate

| Sub-prop | Verdict | After this pass |
|---|---|---|
| P1 (formula match) | LOST → reinterpreted | RESOLVED via τ-disambiguation; runtime IS `-KL/τ` with τ learnable, documentation now matches |
| P2 (Law 3 same manifold) | PARTIAL → resolved | `exact_full_cov_decode` opt-in flag implemented; see "Exact full-cov decode" session below |
| P3 (no neural networks) | CONCEDED to blue | No change needed |
| P4 (canonical derivation) | PARTIAL → labelled | WON; derivation now in `Attention/GL(K)_supplementary.tex §sec:supp_decode_derivation` |

## Session — exact_full_cov_decode opt-in toggle

### Trigger

> build the opt-in toggle and code

### Implementation

New optional config flag `exact_full_cov_decode: bool = False` on `VFEConfig` (config.py:362-377) gates a per-block Cholesky-based full-cov KL decode in `VFEPriorBank` that closes the Law-3 partial documented as P2.

Files changed:

1. **`transformer/vfe/config.py`** — added `exact_full_cov_decode` dataclass field with docstring describing the cost (~10–20× the diagonal decode) and the gating prerequisites; added validation in `__post_init__` that raises `ValueError` if the flag is on without `gauge_fixed_priors=True`, `diagonal_covariance=False`, and `use_prior_bank=True` (the three prerequisites for the flag to do real work).

2. **`transformer/vfe/prior_bank.py`** —
   - `__init__`: stash `self.exact_full_cov_decode` from cfg (line ~107).
   - `decode()`: early-return into `_decode_exact_full_cov(...)` when the flag is on and `sigma_q.dim() == 4`; otherwise keep the existing diagonal path.
   - New `_decode_exact_full_cov(mu_q, sigma_q, mu_p, sigma_p, tau)` method (between `encode()` and `decode()`): per gauge block `h`, computes `Cholesky(Σ_p_v_h + 1e-6 I)`, derives `Σ_p_v_h^{-1}` via `cholesky_inverse`, then assembles flattened lhs/rhs feature vectors so the full sum-over-blocks KL reduces to a single `(B,N,F) @ (F,V)` matmul without forming any `(B,N,V,d_h)` intermediate. The feature layout per block is `lhs_h = [vec(Σ_q_h + μ_q_h μ_q_h^T), μ_q_h]` and `rhs_h = [vec(Σ_p_v_h^{-1}), -2 Σ_p_v_h^{-1} μ_p_h]`. v-only contributions `μ_p_h^T Σ_p_v_h^{-1} μ_p_h + log|Σ_p_v_h|` are summed into a `(V,)` prior-bias. Final logits are `-0.5 · scale/τ · (combined + prior_bias)` matching the diagonal path's softmax temperature convention.

### Smoke tests (passing)

- **Validation**: `ValueError` raised when `exact_full_cov_decode=True` is combined with `diagonal_covariance=True`, with `gauge_fixed_priors=False`, or with `use_prior_bank=False`. Error message names every incompatible flag.
- **Agreement at φ=0**: with `phi_scale=0` (so `A_v = I` and Σ_p has zero within-block off-diagonals) and a `Σ_q` stored as a `(K,K)` diagonal-only tensor, the exact and diagonal decode paths agree to ~1e-8 in softmax probability (numerical zero).
- **Divergence at φ=0.5**: with `phi_scale=0.5` and a `Σ_q` carrying within-block off-diagonal mass, exact and diagonal softmax probabilities differ by ~0.9, recovering the full-cov KL contribution that the diagonal projection discards.
- **Gradient flow**: `loss.sum().backward()` produces finite gradients on both `μ_q` and `Σ_q` through the Cholesky / `cholesky_inverse` path.
- **Regression**: `tests/transformer/test_vfe_package.py` — 47/47 pass.

### Cost (FLOPs, B=16, N=128, V=10000, K=100, 5 blocks of d_h=20)

- Diagonal decode (default path): ~2·10⁹ FLOPs/forward.
- Exact decode (this flag): ~4·10¹⁰ FLOPs/forward.
- Cholesky cache: V·Σ_h d_h³ ≈ 4·10⁸ FLOPs once per forward.

User opts in by setting `exact_full_cov_decode=True` in the entry-point config dict; the path is otherwise dormant.

### Net status of the original debate

| Sub-prop | Verdict | After this pass |
|---|---|---|
| P1 (formula match) | LOST → reinterpreted | RESOLVED via τ-disambiguation; runtime IS `-KL/τ` with τ learnable |
| P2 (Law 3 same manifold) | PARTIAL → resolved | `exact_full_cov_decode=True` discharges strictly; default stays at the documented O(V·K) approximation |
| P3 (no neural networks) | CONCEDED to blue | No change needed |
| P4 (canonical derivation) | PARTIAL → labelled | WON; derivation in `Attention/GL(K)_supplementary.tex §sec:supp_decode_derivation` |

---

## Session — Supplementary §C Gauge Frame Gradients debate (fourteenth in the audit series)

### Trigger

User request continuing the supplementary chapter sweep:
> continue supplementary followup

Followed by, after the verdict:
> begin fixes and investigate the codebase

### Process

Full 5-phase debate (rounds=2, judge=on) under the `red-blue-debate` skill. Artifacts in `docs/debates/2026-05-20-supplementary-gauge-frame-gradients/`. Verdict: **RED_WINS (substantive, scoped to §C.5)**.

This is the first debate in the series where actual mathematical errors (FD-verified wrong formulas) are identified, not editorial gaps. Both teams independently performed finite-difference verification and converged on three FD-confirmed formula errors in §C.5:

- **Line 599** `d exp_φ(T_a) = exp(φ) · Ψ(ad_φ)(T_a)` with `Ψ(z) = (e^z−1)/z` is **mathematically wrong**: relative error 2.09 against the true Fréchet derivative. The form combines left placement of `exp(φ)` with right-trivialized generator — not the canonical Hall 2015 left- or right-trivialized form.
- **Line 590** Killing-metric formula off by exactly 2× relative to the line-587 definition `g(X, Y) = -(1/2) B(X, θ(Y))`.
- **Line 606** pullback metric omits the `exp(φ)^⊤ exp(φ)` factor for non-compact `φ ∈ gl(K)`. §C.5 is specifically titled "Gauge Frame Preconditioning for GL(K)" (non-compact), so the omission is in the chapter's stated scope.

Sub-claims α, β, δ, ε all survived primary-source verification by both teams. Sub-claim γ-integral (Higham forms) is correct as object, with the symbol `dexp_φ` misapplied between §C.2 (right-trivialized) and §C.2.2 (full Fréchet).

### Fixes applied — five corrections to `Attention/GL(K)_supplementary.tex`

1. **Line 599 (mathematical revision)**: replaced `d exp_φ(T_a) = exp(φ) · Ψ(ad_φ)(T_a)` with the canonical right-trivialized form `D_φ(exp)[T_a] = Ψ(ad_φ)(T_a) · exp(φ)` per [Hall 2015 §2.7 / Theorem 5.4], with the left-trivialized equivalent noted via the operator identity `Ψ_R(z) = e^z Ψ_L(z)`.

2. **Line 606 (mathematical revision)**: restored the missing `exp(φ) exp(φ)^⊤` factor: `G_{ab}(φ) = tr(Ψ(ad_φ)(T_a)^⊤ · exp(φ) exp(φ)^⊤ · Ψ(ad_φ)(T_b))`, with explicit notes that the factor reduces to `I` for compact `φ ∈ so(K)` and grows as `exp(2φ)` for non-compact symmetric `φ`.

3. **Line 587 / 590 (mathematical revision)**: dropped the `-(1/2)` prefactor from the definition `g(X, Y) = -(1/2) B(X, θ(Y))` to make it `g(X, Y) = -B(X, θ(Y))`, reconciling the definition with the line-590 formula `g̃_{ab} = 2K tr(T_a^⊤ T_b) - 2 tr(T_a) tr(T_b)`. Citation added to [Knapp 2002 Prop. 1.93]. This matches the existing implementation in `transformer/core/gauge_preconditioner.py:277, :360-361` exactly.

4. **§C.2.2 lines 471–486 (notation unification)**: clarified that the integral form `D_φ(exp)[ξ] = ∫_0^1 e^{tφ} ξ e^{(1-t)φ} dt` is the FULL Fréchet derivative per [Higham 2008 Eq. 10.15], and the block-matrix identity per [Higham 2008 Algorithm 10.27] gives the same. Added a sentence recovering the right-trivialized generator `dexp_φ(ξ)` of Eq. eq:dexp_series via `dexp_φ(ξ) = D_φ(exp)[ξ] · e^{-φ}`.

5. **Line 556 (cross-reference fix)**: replaced broken `\ref{sec:glk_lm}` (which resolved to main paper §4.2 experimental section, not a polar-decomposition derivation) with a direct citation to [Higham 2008 §8] plus an inline polar-decomposition surjectivity argument: for `A = UP` with `U ∈ SO(K)`, `P ∈ Sym_{++}`, choose `φ_i` with `exp(φ_i) = UP^{1/2}` and `φ_j` with `exp(-φ_j) = P^{1/2}`.

### Codebase audit — formula propagation investigation

Per the verdict's optional code-mode follow-up, investigated whether `transformer/aif/` or `transformer/vfe/` consumes the wrong formulas directly.

**Files inspected:**
- `transformer/core/gauge_preconditioner.py` (the actual implementation of all four preconditioner modes).
- `transformer/vfe/config.py` (the gating of which preconditioners are enabled).

**Per-formula propagation status:**

1. **Line-590 Killing metric**: `gauge_preconditioner.py:277` implements `metric = 2.0 * K * gram - 2.0 * trace_outer`, exactly matching the manuscript line-590 formula. The docstring at line 209 already says `g̃(X, Y) = -B(X, θ(Y))` (without the `-(1/2)` factor). My manuscript edit (Fix 3 above, dropping `-(1/2)` from line 587) **reconciles the manuscript with the existing code**. The per-block variant at `gauge_preconditioner.py:344-396` uses the same convention with per-block `d_h`. **No code change needed.**

2. **Line-606 pullback metric**: `gauge_preconditioner.py:552-554, 586+` implements `G_ab(φ) = (Ψ T)^⊤ · gram · (Ψ T)` which OMITS the `exp(φ) exp(φ)^⊤` factor — **same bug as manuscript line 606**. However, `transformer/vfe/config.py:591-600` raises `ValueError` if `phi_preconditioner='pullback'` is requested. The valid options are `'clip', 'cartan', 'killing', 'killing_per_block'` — none use the wrong pullback formula. **The pullback preconditioner is dormant code; the bug never executes in production.**

3. **Line-599 dexp form**: The actual differential is computed via `torch.matrix_exp` autograd in production paths. The wrong manuscript formula only appears in the docstring at `gauge_preconditioner.py:547` (inside the disabled pullback function). **No active code path is affected.**

**Bottom line — no active code path is affected by the §C.5 formula errors.** The pullback preconditioner is the only code that mirrors the manuscript bugs, and it is explicitly disabled at the config level. If someone re-enables `phi_preconditioner='pullback'` for a non-compact GL(K) gauge frame, they will hit the dormant bug; the corrected formula must be added to `build_pullback_metric_tensor` (insert `exp(φ) exp(φ)^⊤` between the two `Ψ(ad_φ)` factors) and the docstring at line 547 corrected before re-enabling.

### Bib additions — `Attention/references.bib`

One new entry added in the LIE GROUPS section (after `Hall2015`):
- `Knapp2002` — Knapp, "Lie Groups Beyond an Introduction," Birkhäuser 2002 (Progress in Mathematics Vol. 140).

### Style compliance

- Prose paragraphs throughout the manuscript edits.
- No `\;`, `\,`, `\!` spacing macros (caught and removed `\!` at line 608 before commit).
- No banned phrases.
- Cross-references verified: `Hall2015`, `Higham2008`, `Knapp2002`, `Gallier2020`, `culver1966existence` all exist in `references.bib`.

### Verdict-queue status update

This is the fourteenth debate in the audit series. Closed queue:
1. §5 transformer reduction (RED_WINS).
2. Softmax-β stationarity (RED_WINS).
3. Sub-claim A flat bundle (BLUE_WINS).
4. Sub-claim B degenerate Σ (BLUE_WINS).
5. Sub-claim C QK^T identification (BLUE_WINS).
6. Sub-claim D V identification (BLUE_WINS).
7. Canonical F vs surrogate (RED_WINS).
8. Multi-head block-diagonal (BLUE_WINS).
9. Route 1 untied carving (RED_WINS).
10. FFN softmax-gradient correction (RED_WINS).
11. §3 Gauge-Covariant VFE (RED_WINS narrow).
12. Supplementary §A General Mathematical Framework (RED_WINS narrow).
13. Supplementary §B Covariance Dynamics (RED_WINS narrow).
14. Supplementary §C Gauge Frame Gradients (RED_WINS substantive).

The main paper §3–§5 and supplementary §A–§C are now fully audited. Optional follow-ups: §D Variational Gradient Descent (lines 611–665), Participatory_it_from_bit.tex §Theory.

---

## Session — pullback metric code repair in `transformer/core/gauge_preconditioner.py`

### Trigger

User request after the §C debate verdict and the codebase audit (which found the pullback metric bug propagated into `gauge_preconditioner.py:552-554` but the preconditioner was explicitly disabled at the config level):

> fix the 'pullback' code

### Process

The §C debate verdict's optional code-mode follow-up identified the `build_pullback_metric_tensor` function as containing the same wrong formula as supplementary line 606 (now corrected in commit `e4481f7c`). The function was dormant (the `pullback` preconditioner is gated by `vfe/config.py:591-600` raising `ValueError`), but a separate code path through `transformer/training/optimizer.py::RiemannianAdamW(metric='pullback')` consumes the same `build_pullback_metric_tensor`, so the bug was latent in the outer optimizer.

### Investigation

Tracing the chain `psi[a,c]` → "c-th coordinate of `Ψ(ad_φ)(T_a)`" by verifying with a 2x2 explicit case (phi = c·T_2 in a basis with `[T_1, T_2] = T_2`):
- `ad_φ_std = [[0, 0], [-c, 0]]` (standard operator matrix, rows=output).
- Code's `ad_X = [[0, -c], [0, 0]]` (matches `(ad_φ_std)^T`).
- Therefore `psi = Ψ(ad_X) = Ψ(ad_φ_std)^T`, giving `psi[a, c] = (Ψ(ad_φ))^c_a`.

The legacy code `G = psi^T @ gram @ psi` was the WRONG matrix product ordering for this convention; it should have been `psi @ H @ psi^T` where `H[c,d] = tr(T_c^T · T_d · M)` with `M = exp(φ) exp(φ)^T`. The legacy tests passed despite this because (a) at φ=0 both orderings reduce to gram, and (b) no test verified against the canonical Frobenius pullback at non-zero φ.

### Fixes applied to `transformer/core/gauge_preconditioner.py`

1. **`apply_pullback_natural_gradient` docstring (line 541+)**: replaced the wrong dexp form with the canonical right-trivialized `D_φ exp[T_a] = Ψ(ad_φ)(T_a) · exp(φ)` per [Hall 2015 §2.7 / Theorem 5.4], and added the correct pullback metric `G_ab(φ) = tr(Ψ(ad_φ)(T_a)^T · Ψ(ad_φ)(T_b) · exp(φ) exp(φ)^T)`.

2. **`build_pullback_metric_tensor` (line 586+)**: implemented the corrected metric. Three changes:
   - Compute `phi_mat = Σ_a φ^a T_a` and `M = exp(phi_mat) @ exp(phi_mat)^T` in matrix space (per token).
   - Build φ-dependent Gram-like tensor `H_{cd}(φ) = tr(T_c^T · T_d · M) = einsum('cil,dij,...jl->...cd', generators, generators, M)`.
   - Use the correct matrix-product ordering `G = psi @ H @ psi^T` (replacing the legacy `psi^T @ gram @ psi`).
   - Documented the convention `psi[a, c]` = c-th coord of `Ψ(ad_φ)(T_a)` inline so future readers don't fall into the same trap.

3. **Module-level docstring (lines 34-46)**: updated to reference the corrected metric and the operator identity `Ψ_R(z) = e^z Ψ_L(z)`.

4. **`transformer/vfe/config.py:159-163, :600-606`**: updated the `pullback` exclusion comments to reflect that the metric math has been corrected on 2026-05-20 and the corrected formula is already in use via `RiemannianAdamW(metric='pullback')`. The inner E-step `_update_phi` path still requires structure-constants wiring before this preconditioner can be selected via `phi_preconditioner='pullback'` (separate refactor).

### Tests

Added two correctness tests in `tests/transformer/test_gauge_preconditioner.py::TestPullbackNaturalGradient`:

- `test_metric_matches_frobenius_pullback_noncompact`: verifies the metric against the canonical Frobenius pullback computed via Higham 2008 Algorithm 10.27 block-matrix Fréchet derivative, using the FULL elementary `E_{ij}` basis of `gl(3)` (closed under the Lie bracket). PASSES at relative tolerance 1e-3 with `series_order=20` and `‖φ‖ ≈ 0.25`.
- `test_metric_compact_reduces_to_psi_gram_psi`: verifies that on the compact subalgebra `so(K)` (skew-only φ), the new metric reduces bit-exactly to the legacy `Ψ^T gram Ψ + 1e-6 I` form (since `exp(φ) exp(φ)^T = I` for skew φ). PASSES at relative tolerance 1e-5.

The new non-compact correctness test caught a hidden bug in the test setup itself: an initial attempt at the test used a partial basis (sym diagonal + skew off-diagonal of gl(3)) that is NOT closed under the Lie bracket — `[E_11, E_12-E_21] = E_12+E_21` is symmetric off-diagonal, outside the basis. The structure-constants projection silently approximated, breaking the test. Fixed by switching to the full `{E_{ij}}` elementary basis (n_gen = K² = 9). This is documented inline as a warning for future test authors.

All 39 tests in `test_gauge_preconditioner.py` and `test_riemannian_adam_pullback.py` pass.

### Status

The pullback metric is now mathematically correct for arbitrary `φ ∈ gl(K)`, both compact (so(K)) and non-compact (Sym(K)) directions. The outer optimizer `RiemannianAdamW(metric='pullback')` now produces correct natural gradients without any latent bug. The inner E-step `phi_preconditioner='pullback'` mode is still gated by the missing `structure_constants` wiring through `VFEEStep._update_phi`; re-enabling there is a separate refactor.

---

## Session — Supplementary §D Variational Gradient Descent debate (fifteenth in the audit series)

### Trigger

User request continuing the supplementary chapter sweep:
> next debate

Followed by, after the verdict:
> yes

### Process

Full 5-phase debate (rounds=2, judge=on) under the `red-blue-debate` skill. Artifacts in `docs/debates/2026-05-20-supplementary-variational-gradient-descent/`. Verdict: **RED_WINS (narrow editorial scope)**, matching §A and §B precedent. Both teams converged on the same verdict label.

Sub-claims α through ε (natural-gradient projections, whitening + trust region, affine-invariant SPD retraction, SO(3) `Ψ_L` closed form, trivialization relation) all verified against canonical sources [Amari 2016 §2.3/§4.3; Pennec 2006 §3; Bhatia 2007 §6.1; Absil-Mahony-Sepulchre 2008 §5.4.6; Hall 2015 §2.7 / Proposition 2.25]. Sub-claim α additionally verified via sympy. Sub-claim ζ (preconditioner cross-references at §D.3 line 665) failed on three concrete defects, all conceded by both teams:

- **Stale pullback formula at line 665**: `G_ab(φ) = ⟨Ψ(ad_X)(T_a), Ψ(ad_X)(T_b)⟩` — the OLD uncorrected form. The §C debate (commit `e4481f7c`) corrected §C.5 line 606 to include the `exp(φ) exp(φ)^⊤` factor for non-compact GL(K); §D.3 line 665 was never updated.
- **Broken cross-reference**: "App.~C.3" should be "App.~C.5" (`sec:glk_preconditioning`). The Cartan-involution-modified metric derivation lives at §C.5 line 558, not §C.3 (KL Gradient Through Transport at line 488).
- **Missing canonical citations** for §D.2 SPD retraction Eq. (637-639): [Pennec 2006], [Bhatia 2007 §6.1], [Absil-Mahony-Sepulchre 2008 §5.4.6].

The verdict is RED_WINS-narrow, not RED_WINS-substantive (like §C), because the codebase audit from the §C debate already established that no active code path consumes the §D.3 line-665 formula: the inner E-step `phi_preconditioner='pullback'` is gated by `vfe/config.py:591-600`, and the outer `RiemannianAdamW(metric='pullback')` uses the corrected `build_pullback_metric_tensor` from `transformer/core/gauge_preconditioner.py`. Manuscript-only drift.

### Fixes applied — three corrections to `Attention/GL(K)_supplementary.tex`

1. **Line 665 inline pullback formula (substantive update)**: replaced `G_ab(φ) = ⟨Ψ(ad_X)(T_a), Ψ(ad_X)(T_b)⟩` with the corrected formula matching §C.5 line 606:
   ```
   G_ab(φ) = tr(Ψ(ad_φ)(T_a)^⊤ · exp(φ) exp(φ)^⊤ · Ψ(ad_φ)(T_b))   (Eq. eq:pullback_metric)
   ```
   Updated the surrounding prose to note that the position-dependent factor `exp(φ) exp(φ)^⊤` captures the exponential amplification of `D exp` in the non-compact sector. Added forward-reference `\ref{eq:pullback_metric}` to the §C.5 derivation.

2. **Line 665 cross-reference fix**: replaced "App.~C.3" with `Appendix~\ref{sec:glk_preconditioning}` so the reference resolves to §C.5 regardless of future renumbering.

3. **§D.2 line 636 citations**: added `\citep{Pennec2006,Bhatia2007,Absil2008}` to the affine-invariant exponential map statement, grounding the SPD retraction in the canonical literature.

### Bib additions — `Attention/references.bib`

Two new entries added after the existing `Absil2008` in the RIEMANNIAN OPTIMIZATION section:
- `Pennec2006` — Pennec, "Intrinsic Statistics on Riemannian Manifolds," JMIV 25(1) 2006.
- `Bhatia2007` — Bhatia, "Positive Definite Matrices," Princeton 2007.

`Absil2008` already existed at line 1017.

### Style compliance

- Prose paragraphs throughout the manuscript edits.
- No `\;`, `\,`, `\!` spacing macros.
- No banned phrases.
- Cross-references verified: `sec:glk_preconditioning` at supplementary line 559, `eq:pullback_metric` at line 607.

### Verdict-queue status update

This is the fifteenth debate in the gauge-transformer audit series. Closed queue:
1. §5 transformer reduction (RED_WINS).
2. Softmax-β stationarity (RED_WINS).
3. Sub-claim A flat bundle (BLUE_WINS).
4. Sub-claim B degenerate Σ (BLUE_WINS).
5. Sub-claim C QK^T identification (BLUE_WINS).
6. Sub-claim D V identification (BLUE_WINS).
7. Canonical F vs surrogate (RED_WINS).
8. Multi-head block-diagonal (BLUE_WINS).
9. Route 1 untied carving (RED_WINS).
10. FFN softmax-gradient correction (RED_WINS).
11. §3 Gauge-Covariant VFE (RED_WINS narrow).
12. Supplementary §A General Mathematical Framework (RED_WINS narrow).
13. Supplementary §B Covariance Dynamics (RED_WINS narrow).
14. Supplementary §C Gauge Frame Gradients (RED_WINS substantive — 3 FD-verified math errors + codebase fix).
15. Supplementary §D Variational Gradient Descent (RED_WINS narrow).

The main paper §3–§5 and supplementary §A–§D are now fully audited. Optional follow-up: Participatory_it_from_bit.tex §Theory.

---

## Session — /vfe purity audit vs Participatory_it_from_bit.tex (sixteenth–nineteenth debates)

### Trigger

User invoked `/red-blue-debate` on the claim that the `transformer/vfe/` implementation is complete and theoretically/mathematically pure for language modeling, with the scope restricted to language-modeling aspects of PIFB (no mass-analogy, no pullback-to-spacetime, no meta-agent emergence). Then directed: "verify and fix all findings, commit and push."

### Process

Four sequential debates, each full 5-phase (rounds=2, judge=on, ultrathink agents):

1. `2026-05-20-vfe-module-purity-for-pifb/` — overall claim. **Verdict: RED_WINS** on the per-dim Bayesian α gap.
2. `2026-05-20-single-config-mutual-exclusion/` — follow-up A. **Verdict: RED_WINS** on `omega_direct` ↔ `exact_full_cov_decode` mutual exclusion.
3. `2026-05-20-cross-layer-phi-handoff/` — follow-up B. **Verdict: BLUE_WINS** — φ cascade works via `beliefs.phi`; `priors.phi` is intentionally inert.
4. `2026-05-20-wilson-observable-scope/` — follow-up C. **Verdict: RED_WINS** (literal trigger), manuscript-side remediation.

### Fixes applied

#### 1. PIFB per-coordinate α derivation (original-debate fix)

The verdict identified: PIFB:1308-1313 (`eq:state_dependent_alpha`) prescribes scalar `α_i* = c₀/(b₀ + KL(q_i||p_i))`; `transformer/vfe/e_step.py:50-66` and `:498-542` implement per-K-dimension `α_k* = c₀_k/(b₀_k + KL_k)` with `raw_c0, raw_b0` of shape `(K,)`. The two are algebraically inequivalent for K > 1 (no `VFEConfig` flag recovers the scalar form). The /vfe e_step.py module docstring at lines 62-66 itself notes the per-dim form is "not currently derived in the manuscript."

User-directed remediation per their question to me: "derive the per-dim form in the manuscript" — applied as new paragraph `\paragraph{Per-coordinate precision for diagonal Gaussians.}` inserted into PIFB §"State-Dependent Prior Precision" between line 1319 (end of envelope-reduction paragraph) and line 1321 (`\paragraph{Product-rule chain at autograd time.}`).

Content: derives the per-coordinate form from standard Gamma-Normal conjugacy on a product of univariate Gaussians (cite `[Bishop2006 §10.2]`). The diagonal Gaussian factorizes as `q = Π_k q_k`, the KL decomposes additively into per-coordinate terms, and an independent Gamma prior on each coordinate's precision yields the per-coordinate stationarity condition `D_KL^(k) + b₀^(k) - c₀^(k)/α^(k) = 0`, with closed form `α^(k)* = c₀^(k)/(b₀^(k) + D_KL^(k))`. Boxed as new equation `eq:state_dependent_alpha_per_coord`. The scalar form `eq:state_dependent_alpha` is shown to be the special case of tying `(c₀^(k), b₀^(k))` across `k` AND unioning the independent Gamma priors into a single joint trace-precision prior. Per-head form noted as the gauge-equivariant intermediate (one Gamma triple per gauge block) under the `GL(d_head)^H ⊂ GL(K)` multi-head reduction at PIFB:1785, reducing to scalar for `H=1` and to per-coordinate for `d_head=1`. Product-rule chain `eq:alpha_product_rule_itfb` carried through per-coordinate with `(α*)^2 b₀/c₀ ∂D_KL/∂θ → (α^(k)*)^2 b₀^(k)/c₀^(k) ∂D_KL^(k)/∂θ`.

Banned LaTeX spacing macros (`\,`, `\!`) removed after first pass.

This retires the "not currently derived in the manuscript" caveat at `transformer/vfe/e_step.py:50-66`.

#### 2. CLAUDE.md — omega_direct × full-cov sandwich KNOWN GAP (follow-up A fix)

Verdict identified `config.py:566-573` (omega_direct requires `diagonal_covariance=True`) and `config.py:484-507` (`exact_full_cov_decode=True` requires `diagonal_covariance=False`) as a structural mutual exclusion at config construction time. The two canonical forms (group-level Lie-group retraction per PIFB:2566-2570 and Nakahara-style (0,2)-sandwich per PIFB:1619-1626 / Nakahara 2003 §10.3) cannot coexist in a single `VFEConfig`.

Verdict offered two paths: implement the open `NotImplementedError` at `non_flat.py:500-505` (full-cov pairwise KL kernel — research-grade kernel addition) OR document the limitation in `CLAUDE.md`. Applied the documentation fix per the lower-surgery option: added a new `**KNOWN GAP — omega_direct × exact full-cov sandwich**` section to `CLAUDE.md` immediately after the existing `KNOWN GAP — RoPE × MahalanobisNorm` paragraph, naming the two validation guards and the open `NotImplementedError` that would close the gap.

#### 3. stack.py docstring rewrite — φ via beliefs.phi (follow-up B fix)

Verdict (BLUE_WINS): `priors.phi` has zero downstream readers in `transformer/vfe/` (grep-verified). The φ cascade happens via `beliefs.phi`: `block.py:172` returns updated `beliefs.phi`, `stack.py:94-95` re-passes the BeliefState, and `e_step.py:779` reads `phi = beliefs.phi`. The `priors.phi = initial_priors.phi` write at `stack.py:145` is operationally inert.

Verdict-recommended action: rewrite the `stack.py:18-34` "matching mechanism for phi if desired" hedge into a positive statement explaining the principled σ/φ asymmetry and the operative `beliefs.phi` cascade. Do NOT add a `prior_handoff_phi` toggle (would gate a parameter no consumer reads).

Applied: split the original docstring block into two `Implementation note` paragraphs. The first preserves the σ documentation (`mean-only cascade vs canonical hierarchical VI (σ)`). The second is new (`φ cascade is via beliefs, not priors`) and documents: the `beliefs.phi` cascade chain through `e_step.py:779`, `block.py:172`, `stack.py:94-95`; the σ-vs-φ asymmetry as principled (prior σ enters `KL(q||p)`, prior φ does not enter the E-step KL functional); pointer to the debate verdict for adjudication. Module imports verified post-edit.

#### 4. PIFB:868 and PIFB:874 — manuscript scope qualification (follow-up C fix)

Verdict (RED_WINS, manuscript-side remediation): PIFB:868 ("the form actually implemented as the optional `holonomy_penalty` regularizer") and PIFB:874 ("The implementation exposes α as the `cocycle_relaxation` configuration parameter") are direct manuscript claims about implementation features absent from `transformer/vfe/` (grep returns zero matches in `/vfe`) but present in the broader codebase (`transformer/core/connection.py`, `transformer/analysis/holonomy.py`, `transformer/analysis/holonomy_metrics.py`, etc.).

Per the canonical sources [Wilson 1974, Kogut-Susskind 1975, Creutz 1983] Wilson loops are defined on closed graphs; autoregressive transformer DAGs have no closed loops (per PIFB:876 itself). PIFB:824, 826, 878, 880 explicitly label the Wilson constructions as Regime II content. Adding Wilson machinery to /vfe would import a term PIFB:880 declares degenerate in the Regime I limit.

Applied: two-line scope qualification at PIFB:868 and PIFB:874. PIFB:868 now reads "...is the form provided as the optional `holonomy_penalty` regularizer in the Regime II research branches of the broader codebase (`transformer/core/`, `transformer/analysis/`); the Regime I language-modeling path `transformer/vfe/` ships the edge-relaxed transport itself (`non_flat.py`, bitwise-flat at W=0) but not the Wilson regularizer, which is degenerate on the autoregressive DAG used there per the discussion below." PIFB:874 now reads "...The Regime II research branches in `transformer/core/` expose α as the `cocycle_relaxation` configuration parameter; the Regime I language-modeling path `transformer/vfe/` keeps the connection field δ_ij initialized at zero (`non_flat.py` with `use_non_flat_transport=True` starts at W = 0, δ ≡ 0, recovering the vanishing-holonomy theorem at step 0) and does not expose a separate α knob, since the Regime II homotopy is a research extension rather than a Regime I LM dependency."

### Verification

- `Attention/Participatory_it_from_bit.tex`: `pdflatex Participatory_it_from_bit.tex` compiles to a 187-page PDF without LaTeX errors. The "missing Fig_*.png" warnings are pre-existing (figure files are not in the repo) and unrelated to my edits.
- `transformer/vfe/stack.py`: imports cleanly (`from transformer.vfe.stack import VFEStack` succeeds). `VFEConfig(vocab_size=100)` constructs successfully (with the expected orphan-LR warning for the default single-layer config).
- Banned LaTeX spacing macros (`\;`, `\,`, `\!`) audited and removed from my insertions.
- Banned Claude-isms ("key insight", "crucially", "notably", "importantly", etc.) audited — none present in my insertions.

### Debate artifacts

Durable in `docs/debates/`:
- `docs/debates/2026-05-20-vfe-module-purity-for-pifb/` (original, 6 files)
- `docs/debates/2026-05-20-single-config-mutual-exclusion/` (follow-up A, 6 files)
- `docs/debates/2026-05-20-cross-layer-phi-handoff/` (follow-up B, 6 files)
- `docs/debates/2026-05-20-wilson-observable-scope/` (follow-up C, 6 files)

Each debate carries the 5-phase set: `00_claim.md`, `01_evidence.md`, `02_red_opening.md`, `02_blue_opening.md`, `03_red_rebuttal.md`, `03_blue_rebuttal.md`, `04_verdict.md`, `05_action.md`.

### Verdict-queue status update

This session adds four debates to the audit series:
16. `/vfe` purity vs PIFB language-modeling core (RED_WINS — per-dim α gap).
17. `omega_direct` × exact full-cov sandwich (RED_WINS — structural mutual exclusion).
18. Cross-layer φ handoff (BLUE_WINS — cascade works via `beliefs.phi`).
19. Wilson observable scope (RED_WINS literal — manuscript-side remediation).

All four findings are fixed in this round. The single-config full-cov + omega_direct kernel implementation at `non_flat.py:500-505` (open `NotImplementedError`) is recorded as a deferred research-grade follow-up; the per-construction reading remains operative.
