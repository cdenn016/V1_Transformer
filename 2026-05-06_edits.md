# 2026-05-06 Edits

Edits to `Attention/Participatory_it_from_bit.tex` driven by two rounds of multi-agent peer review (reports archived in `Attention/peer_review_2026-05-06/`). Each section below addresses one of seven issues raised across the rounds. Investigators and verifiers ran in parallel for both rounds; line-number citations refer to the manuscript at edit time.

## Issue 1 — Envelope-theorem / reduced free-energy clarification

### Why

The boxed pointwise free energy (Eq. 32 at `eq:pointwise_free_energy`, line ~802) and the field free energy (Eq. 39 at `eq:free_energy_functional_final`, line ~828) display $\sum_{ij} \beta^*_{ij} \mathrm{KL}(q_i \| \Omega_{ij}q_j)$, which is the autograd-convention form $\langle E\rangle_{\beta^*}$, not the reduced free energy $-\tau \log Z_i$. Section 3.4 ("The Envelope Theorem and the Reduced Free Energy") at lines 867-905 already disambiguates the two conventions; but a reader stopping at the boxed Eq. 39 reads it as the reduced free energy, which it is not. The two objectives share critical points but differ off-equilibrium by $\tau \sum_j \beta^*_{ij}\log(\beta^*_{ij}/\pi_j)$.

### Approach

Inserted clarifying notes after Eq. 32 and after the boxed Eq. 39. Each note explicitly labels the displayed object as the joint (un-minimized, autograd-convention) free energy with $\beta_{ij}, \gamma_{ij}$ as free variational parameters, and points the reader to `eq:free_energy_reduced` in `\ref{sec:envelope_theorem}` for the reduced form. The notes also state the off-equilibrium difference explicitly. No equation was altered. Verified algebraically by sympy that $F_{\mathrm{align}}(\beta^*) = -\tau\log Z$ and $\langle E\rangle_{\beta^*} = -\tau\log Z - \tau\,\mathrm{KL}(\beta^* \| \pi)$.

## Issue 2 — Regime I / Regime II distinction for connection and curvature

### Why

The connection $A^{(i)}_\mu = U_i^{-1}\partial_\mu U_i$ derived from the smooth gauge frame $\phi_i$ together with the cocycle $\Omega_{ij} = U_iU_j^{-1}$ forces $F^{(i)}_{\mu\nu} = 0$ identically by the Maurer–Cartan identity (Theorem `thm:vanishing_holonomy`). Yet roughly twelve passages elsewhere in the manuscript treated $F_{\mu\nu}$, frame-twist holonomy, Yang-Mills kinetic terms, and gauge-curvature linguistic conjectures as if they had nontrivial dynamical content under the same parameterization. This is a real internal inconsistency that the verifier rated high severity.

### Approach

Adopted explicit two-regime distinction. Inserted an opening paragraph in Section `\ref{sec:connection_forms}` defining **Regime I** (pure-gauge connection, $F = 0$ identically; what the implementation realizes) and **Regime II** (independent connection variable; transport becomes path-ordered, $F$ becomes dynamical). Annotated downstream passages as Regime II content where they appear:

- "Gauge Field Strength: Path-Dependent Information Transport" subsubsection — opening sentence states that nonzero $F$ scenarios are Regime II, and Regime I forces $F = 0$.
- "Curvature Structure" gauge-field-curvature paragraph — paragraph rewritten to flag the Regime II conditional.
- Yang-Mills curvature-penalty regularizer in the boxed free energy at line ~865 — relocated into Regime II since the integrand is structurally zero in Regime I.
- The "Gauge Curvature Conjecture" linguistic prediction (line 2625) — explicitly conditional on Regime II, with the $F = \partial \phi - \partial \phi + [\phi,\phi]$ typo at line 2628 corrected to use $A$.
- Section `\ref{sec:signature_resolution}` regime-status paragraph at the head, noting that the signature mechanism stays in Regime I (uses $\mathrm{tr}(A_\mu A_\nu)$, not $F$) but relies on the frame-as-state reading of $\phi$.

## Issue 3 — Two roles for the gauge frame (gauge redundancy vs cognitive state)

### Why

The same symbol $\phi_i$ was used in two incompatible roles. In its **transport role** (gauge redundancy) the manuscript said frames are "arbitrary choices with no physical content" (line 235); in its **state role** (cognitive ontology) the manuscript said "there is something it is like to occupy gauge frame $\phi_i$" (line 2747) and treated frame-dependent pulled-back metrics $G_i^{(q)}$ as load-bearing in the qualia and signature constructions. A referee will object that the framework cannot have both readings simultaneously without disambiguation.

### Approach

Inserted "Two roles for the gauge frame" paragraph in Section `\ref{sec:agents_as_sections}` (the Principal Bundles section) explicitly distinguishing **Role A** (transport, gauge-covariant, $\phi_i$ redundant) from **Role B** (cognitive state, $\phi_i$ contributes to ontology, gauge-equivalent frames yield distinct phenomenologies). Established convention that "gauge-invariant" and "gauge-covariant" carry their standard mathematical meanings, and "frame-dependent" is used for Role-B quantities to signal dependence on cognitive state rather than on labelling. Added cross-reference labels (`sec:agents_as_sections`, `sec:qualia`, `sec:consensus_metric`) and a Role-B reminder at the top of the qualia subsection. The 1546 escape hatch — which had appealed to the gauge-invariant Yang-Mills action density $\mathrm{tr}(F F)$ as the gauge-invariant content — was rewritten: $\mathrm{tr}(F F) = 0$ identically under Regime I cocycle, so it supplies no escape hatch. Gauge-invariant ontology is instead routed through the consensus / Haar-averaged metric of Section `\ref{sec:consensus_metric}` (which now carries an explicit label).

## Issue 4 — Yang-Mills naming and sign-convention conflicts

### Why

The bundle pullback's horizontal block $g_{\mathcal{C},\mu\nu} = \mathrm{tr}(A_\mu A_\nu)$ was labelled the "Yang-Mills kinetic metric" throughout (`g^{\mathrm{YM}}` in equations). It is not Yang-Mills: Yang-Mills is the curvature invariant $\mathrm{tr}(F_{\mu\nu}F^{\mu\nu})$, which presupposes a base metric and is gauge-invariant; the manuscript's $\mathrm{tr}(A_\mu A_\nu)$ is connection-dependent and gauge-noninvariant. Separately, sign convention: for compact $\mathfrak{so}(n)$ the standard positive-definite inner product is $-\mathrm{tr}(AB)$, with $\mathrm{tr}(A^2) \le 0$ on skew-symmetric $A$. The Lorentzian worked example uses $T = \mathrm{diag}(1,-1)$, which is symmetric (in $\mathfrak{sl}(2,\mathbb{R})$, not $\mathfrak{so}(2)$) and gives $\mathrm{tr}(T^2) = +2 > 0$. Under the standard compact-form sign convention the construction's signs would reverse. These convention choices were unstated.

### Approach

Renamed `g^{\mathrm{YM}}` to `g^{\mathrm{tw}}` ("frame-twist") at the bundle metric definition (lines 1530, 1535, 1538, 1546, 1617, 1672) and added a sentence at the definition specifying the bilinear form: $\langle A, B\rangle := \mathrm{tr}(AB)$ on $\mathfrak{gl}(K, \mathbb{C})$, indefinite, distinct from the standard positive-definite $-\mathrm{tr}(AB)$ on compact forms. In the worked example added a sentence stating that $T = \mathrm{diag}(1,-1) \in \mathfrak{sl}(2,\mathbb{R}) \setminus \mathfrak{so}(2)$ and that this non-compact choice is what makes $\mathrm{tr}(T^2) > 0$ available. Updated all "Yang-Mills kinetic" descriptive prose in the signature-resolution subsection and downstream cross-references (Lahav–Neemeh discussion, Conclusion's open-problems summary) to use "frame-twist quadratic form".

## Issue 5 — Drop the unsupported "complex KL" claim

### Why

The Concrete-Pathway bullet at line 1623 stated: "Extend gauge frames to $\phi \in \mathfrak{gl}(K, \mathbb{C})$ and distribution parameters to complex values. The KL divergence generalizes naturally to complex exponential families." This is mathematically unjustified: KL is defined for nonnegative probability densities, and complex-valued "densities" require one of (i) quantum relative entropy on density matrices, (ii) Born rule with $|\psi|^2$, (iii) real $2K$-realification, or (iv) signed/complex measures (under which KL is not standard). Grep confirmed the manuscript does not specify which is intended. Worse, the line directly contradicts line 1631 (which states the Fisher-Rao fiber metric remains positive semi-definite). The worked example only complexifies the gauge frame $\phi$, never the distributions $q_i$.

### Approach

Replaced the offending bullet with: "Complexified gauge frames acting on real Gaussian beliefs", explicitly noting that $q_i$ remain real Gaussians on real $\mathbb{R}^K$, the KL divergence on the fiber stays in standard real form, and a genuinely complexified probability theory is a stricter step that this manuscript does not adopt (parked as separate future work). This also fixes the Yang-Mills-pathway sentence's "non-compact structure" overclaim by noting in surrounding text that real $\mathrm{GL}(K, \mathbb{R})$ is non-compact yet still produces positive-definite transport on real Gaussian fibers (Sylvester).

## Issue 6 — Lorentzian-signature framing: worked-example-honest

### Why

The worked example at lines 1633-1671 was already honest about its three postulates (designate temporal direction, multiply gauge component by $i$, take real part). But several other passages — the Introduction's Level-3 note, Section `\ref{sec:gauge_group_choice}`, the "Why Compact Gauge Groups Force Riemannian Geometry" subsubsection, the "Concrete Pathway" subsubsection title, the "Resolution Pathway" item in `\ref{sec:open_problems}`, and the Gravitational-Effects section — described $\mathrm{GL}(K, \mathbb{C})$ as providing a "concrete pathway" or "resolution" of the signature problem, contradicting the worked-example's careful disclaimers. Worst single overclaim was at line 2890: "yields indefinite pullback metrics without ad hoc signature assignments", which directly inverts what the worked example shows.

### Approach

Renamed "The Concrete Pathway" subsubsection to "Postulates Required for an Indefinite Pullback". Replaced "Resolution Pathway" item at line 2890 with "Existence-toy status" and rewrote its body to enumerate the necessary group-prerequisite (lifting compactness) separately from the sufficient postulates (imaginary frame component, real-part projection). Reworded the Level-3 note in the Introduction, the `\ref{sec:gauge_group_choice}` cross-reference, the "Why Compact Gauge Groups Force Riemannian Geometry" subsubsection, and the "Inertial Mass and Gravitational Effects" passage to consistently say the compact-group lift is necessary but not sufficient for indefinite signature, that real $\mathrm{GL}(K, \mathbb{R})$ is non-compact yet positive-definite, and that the worked example obtains its result only with the additional postulates.

## Issue 7 — Mass-from-precision: stiffness vs inertia, and Hessian symmetry

### Why

The Hessian $M_{\mu\mu} = \partial^2 \mathcal{F}/\partial\mu\partial\mu$ derived in Section `\ref{sec:mass}` is, in the harmonic-oscillator analogy, a *stiffness* $k$ on belief configuration space, not a mass $m$. For $L = \tfrac12 m \dot{x}^2 - \tfrac12 k x^2$ the dispersion is $\omega^2 = k/m$, so the Hessian-of-potential gives stiffness; the precision plays the mass role only after a kinetic-metric postulate is in place. The manuscript supplied that postulate at lines 1283-1290 ("Velocity-Quadratic Metric Form") but did not flag it as the load-bearing assumption. Separately, the off-diagonal-block caveat at lines 1240-1242 said $M_{\mu\mu}$ is "symmetric only when $\beta_{ik} = \beta_{ki}$ and $\Omega_{ik} = \Omega_{ki}^\top$". By Schwarz's theorem any Hessian of a scalar functional automatically satisfies block-symmetry $M_{ik} = M_{ki}^\top$; the displayed formula at line 1232 already does (sympy-verified). What reciprocity actually buys is the stronger condition for $M_{\mu\mu}$ to act as a *symmetric bilinear form* on stacked $(\mu_i)_i$, which is what licenses the conservative-Hamiltonian reading.

### Approach

Inserted "What the Hessian gives, and what is added" paragraph at the top of Section `\ref{sec:mass}` distinguishing stiffness from mass and stating that the Hessian-as-mass identification is contingent on the kinetic-metric postulate of the Velocity-Quadratic subsection. Added a label `sec:velocity_quadratic` to that subsection and rewrote its opening paragraph to make explicit that the kinetic form is a postulate, not a consequence of $\mathcal{F}$. Rewrote the "Off-diagonal block caveats" paragraph at lines 1240-1242 to separate (i) automatic Hessian block-symmetry $M_{ik} = M_{ki}^\top$ (which always holds by Schwarz's theorem) from (ii) the symmetric-bilinear-form condition $M_{ik} = M_{ki}$ (which requires reciprocity and licenses the conservative-Hamiltonian reading). Replaced lingering `sec:empirical_mass` references with the existing `sec:mass` label.

## Reports archived

Eight peer-review reports + two verifier reports archived under `Attention/peer_review_2026-05-06/`:

- Round 1 (general manuscript review): `01_math_rigor.md`, `02_empirical.md`, `03_writing_style.md`, `04_concept_relwork.md`, `05_verifier.md`
- Round 2 (free energy / curvature / gauge): `06_freeenergy_entropy.md`, `07_curvature_consistency.md`, `08_gauge_redundancy.md`, `09_verifier_round2.md`
- Round 3 (YM / complex-KL / signature / mass): `10_ym_sign_convention.md`, `11_complex_kl.md`, `12_lorentzian_framing.md`, `13_mass_stiffness.md`, `14_verifier_round3.md`

## Issue 8 — Meta-agent emergence: variational principle + threshold-based implementation

### Why

Three rounds of investigation (reports 15-18 in `Attention/peer_review_2026-05-06/`) confirmed that the manuscript contained a real and pervasive overclaiming defect on meta-agent emergence. The "Consensus Detection and Meta-Agent Formation" subsection at line 1313 honestly disclosed that meta-agent formation is implemented via discrete threshold-based detection (ad-hoc thresholds $\Gamma_{\min} = 0.5$, $N_{\min} = 2$), and the next paragraph stated explicitly that "in a more principled implementation, meta-agent formation would be governed directly by free energy gradients..." But thirteen other passages elsewhere in the manuscript treated the threshold-based demonstration as a derivation of emergence: "spontaneously generate," "first direct evidence," "No hierarchical structure was imposed," subsection title "Participatory Structure Validated," figure caption claiming the structure "emerged spontaneously... without imposed architectural priors," and so on. A referee reading the abstract or the figure caption first would form an expectation about derivation that the line-1313 disclosure then contradicts.

The math investigation also showed that ~70% of the user's proposed variational-RG / information-bottleneck reformulation was already implicit in the manuscript: line 1328 already stated the free-energy-improvement criterion as future work; the existing aggregation formulas for $\bar{\mu}_I, \bar{\Sigma}_I$ at line 1340 were the leading-order forward-KL barycenter with the dispersion term dropped; and the additive frame formula $\phi_I = \sum w_i \phi_i / \sum w_i$ was the first-order BCH approximation to the group-valued Karcher barycenter. Promoting this content from future-work footnotes to the lead variational principle of the section therefore earned a substantial fraction of the strong-emergence claims without rerunning simulations or adding new derivation. The remaining defect in the variational-RG proposal — the user's prose said "precision-weighted consensus" while the equation as written gave forward-KL moment-matching with a dispersion term — was resolved by sympy verification: the existing manuscript formulas correspond to forward-KL with the dispersion term dropped as $\mathcal{O}(\varepsilon)$ in the high-coherence regime, and the rewrite documents this approximation explicitly.

### Approach

Two coordinated phases:

**Option B-lite (substantive reorganisation).** Renamed and restructured the "Consensus Detection and Meta-Agent Formation" subsection. The new subsection has two subsubsections:

1. `sec:meta_agent_variational` — the variational principle, with three equations: the free-energy improvement criterion ($\mathcal{F}_{\text{parent}} + C(I) < \mathcal{F}_{\text{sep}}$, eq:meta_agent_FE_criterion), the parent state as gauge-covariant variational barycenter (eq:meta_agent_barycenter, with closed Gaussian forms eq:meta_agent_mu_barycenter and eq:meta_agent_sigma_barycenter), and the parent frame as group-valued Karcher barycenter (eq:meta_agent_frame_barycenter). The savings analysis shows the free-energy gain from collapsing $\mathcal{O}(|I|^2)$ pairwise alignment KLs into $|I|$ child-parent KL terms scales as $\sim |I|(|I|-1)\varepsilon$ in the high-coherence limit, giving the threshold detector a principled scaling-law interpretation.
2. `sec:meta_agent_threshold` — the threshold-based detector, now framed as a practical surrogate for the variational criterion, motivated by the observation that in the high-coherence regime $\Gamma$ tracks the same coherence factor that controls the savings. The thresholds $\Gamma_{\min} = 0.5$, $N_{\min} = 2$ are demoted from "ad-hoc" to "implementation details, not theoretical requirements."

The "Gauge-Covariant Belief Construction" subsection now opens by referencing eq:meta_agent_barycenter and explains that the implementation drops the dispersion term as a leading-order approximation valid in the high-coherence regime. The frame construction is reframed as the first-order BCH approximation to the Karcher mean, exact for abelian or commuting frames and valid to $\mathcal{O}(\|\phi_i\|^2)$ on the convexity ball for compact $G$; the GL$^+$($K$) obstruction (no bi-invariant Riemannian metric, signature (2,1,1) on $\mathfrak{gl}(2)$ verified directly) is documented.

**Option A (textual sweep).** All thirteen overclaiming passages were rewritten to either reference the variational principle and its threshold-based surrogate, or to qualify emergence claims with the post-detection conditioning. Notable changes:

- Opening of `\subsection{The Participatory Universe...}` (was line 1305): "agents spontaneously organize... arise naturally from variational free energy minimization" → "when coherent agent clusters satisfy the variational free-energy improvement criterion of `sec:meta_agent_variational`, they aggregate... The simulations of this paper trigger the aggregation through the threshold-based surrogate of `sec:meta_agent_threshold` rather than through a continuous-time evaluation of the variational criterion."
- Line 121 `\textbf{Validated.}` header split into `\textbf{Validated.}` (transformer + WikiText-103) and `\textbf{Demonstrated in working code (single illustrative run).}` (Ouroboros Tower).
- Line 2066 ("We report spontaneous emergence...") → "We report detector-mediated emergence..."
- Figure caption at 2174 ("emerged spontaneously... without imposed architectural priors") → "produced by the threshold-based consensus detector... acting on agent state variables driven by the gauge-theoretic dynamics; the energy functional and initialization do not hard-code a hierarchical depth, scale assignments, or a tree topology, but the detector that nucleates meta-agents is imposed."
- Line 2195 ("first direct evidence... can spontaneously generate") → "Conditional on the threshold-based consensus detector..., these results show that gauge-theoretic active inference with softmax coupling weights produces a stable thirteen-scale hierarchical organization... We do not claim free-energy-driven spontaneous emergence at this resolution, since the detector is imposed rather than derived."
- Line 2204 ("No hierarchical structure was imposed... emerged purely from") → "The energy functional and the initialization do not encode a hierarchical depth, scale assignments, or a tree topology; in that restricted sense, no hierarchical \emph{architecture} is hard-coded. The hierarchy in Fig.~\ref{fig:hierarchy} is nucleated, however, by the threshold-based consensus detector."
- Subsection title at 2265 ("Participatory Structure Validated") → "Participatory Loop Demonstrated under Threshold Detection"
- Body of the demonstrated-bullets list at 2270: each bullet now explicitly conditions on the threshold detector triggering.

All eleven new section/equation labels (`sec:meta_agent_formation`, `sec:meta_agent_variational`, `sec:meta_agent_threshold`, `sec:meta_agent_state_construction`, plus seven `eq:meta_agent_*`) are defined in the variational subsection and are referenced 26 times across the manuscript. Banned-pattern audit confirms: 0 banned phrases introduced, 4 banned spacing macros (all pre-existing in regions not touched today). The Information-Bottleneck Layer 0 of the user's proposal was deliberately not promoted in this revision; the math investigation flagged a sign issue and an undefined Markov pair, and the report classifies it as research direction not yet ready to enter the manuscript.

### Reports archived

`Attention/peer_review_2026-05-06/15_metaagent_textual.md`, `16_variational_rg_math.md`, `17_existing_metaagent_construction.md`, `18_verifier_round4.md`.

## Issue 8 follow-up — Information-bottleneck refinement (Level-1 footnote)

### Why

The Issue-8 fix promoted the variational free-energy improvement criterion (Layer 1) and the gauge-covariant variational barycenter (Layer 2) and the group-valued Karcher barycenter (Layer 3) but deliberately did not promote the user's Information-Bottleneck Layer 0. The math investigation flagged two specific defects in the user's IB draft: a sign issue ($+\beta I$ drives erasure rather than compression; standard IB has rate minus $\beta$ times predictive information) and an undefined Markov pair (no specification of what plays the role of $Y$, the predictive target, in the gauge-theoretic context). These had to be resolved before IB could enter the manuscript responsibly.

### Approach

Added a short paragraph in `sec:meta_agent_variational` after the FE improvement criterion, explicitly framing IB as a research-direction refinement of Layer 1 rather than a load-bearing principle. The paragraph fixes both defects:

- **Sign correction.** Adopted the standard Tishby Lagrangian $\mathcal{L}_{\mathrm{IB}} = I(T;X) - \beta_{\mathrm{IB}} I(T;Y)$, with the sign convention stated explicitly in prose as "rate minus $\beta$ times predictive information" so the convention is unambiguous to a reader unfamiliar with IB.
- **Markov pair specified.** $X$ = joint constituent state, $T = (q_I, s_I, U_I)$ = parent regarded as stochastic encoding of $X$, $Y = X_{t+\Delta t}$ = future constituent state under one step of natural-gradient flow on $\mathcal{F}$. This is the predictive-information-bottleneck reading (Bialek--Nemenman--Tishby) and is the canonical Markov pair for variational dynamics; the paragraph cites the original IB paper~\cite{tishby1999information}, the predictive-information work~\cite{bialek2001predictability}, and the Gaussian-IB closed-form solution~\cite{chechik2005information}.

The paragraph also explicitly connects IB to the FE improvement criterion: reading $C(I)$ as the IB rate term $\beta_{\mathrm{IB}}^{-1}I(T;X)$ and the free-energy savings as a proxy for $I(T;Y)$, the IB Lagrangian is the variational closure of~\eqref{eq:meta_agent_FE_criterion}. It then identifies three ingredients still required before IB graduates from research direction to derivation: a precise natural-gradient transition kernel for $X \to Y$, a treatment of the gauge-frame component of $T$ under encoder noise (group-valued Gaussian-IB analogue), and an empirical comparison of the IB-optimal coarse-graining with the threshold-detector hierarchy. The framing is honest: IB *would* license a strictly stronger emergence claim than~\eqref{eq:meta_agent_FE_criterion} alone supports, but only after the three ingredients are supplied; the present manuscript stands on the FE-improvement criterion.

Three new bibliography entries added (`tishby1999information`, `chechik2005information`, `bialek2001predictability`); one new equation label (`eq:meta_agent_IB`) with two cross-references in the same paragraph. Banned-pattern audit clean: 0 banned phrases, 0 new banned spacing macros.

## Issues 9-15 — Discussion containment, connection convention, Fisher inverse, Hessian symmetry, cross block, observation gradient, Gaussian curvature, MI definition

User-supplied list of seven additional defects (numbered 1-9 in the user's message; #10 was cut off). Investigation followed the same pattern as previous rounds: each cited line read directly, formula verified by hand or sympy, fix applied surgically.

### Why and approach (per issue)

**Issue 9 — Philosophical containment.** Two Discussion-section overclaims survived the Issue-8 sweep. Line 2247 ("Wheeler's universe creates itself because information geometry, when properly formulated, is self-organizing.") and line 2289 ("This shows that neural networks are the biological and computational instantiation of the deeper underlying geometry") were rewritten. The first now reads as an interpretive reading conditional on the demonstration regime ("we offer this as an interpretive reading rather than a derivation"); the second is restricted to "the gauge-theoretic construction reproduces dot-product attention as an explicit limit" with explicit acknowledgment that biological implementation is empirically open.

**Issue 10 — Gauge-connection convention inconsistency.** The manuscript previously wrote $A^{(i)}_\mu = U_i^{-1}\partial_\mu U_i$ (left Maurer--Cartan) and immediately quoted the Yang-Mills gauge-transformation law $A^{(i)} = \Omega A^{(j)} \Omega^{-1} + \Omega \partial_\mu \Omega^{-1}$ as the overlap relation. Under the cocycle $\Omega_{ij} = U_i U_j^{-1}$ and $A = U^{-1}dU$, the correct overlap relation is the chain-rule identity $\theta^{(i)}_\mu = \mathrm{Ad}(U_j^{-1})[\Omega_{ij}^{-1}\partial_\mu \Omega_{ij}] + \theta^{(j)}_\mu$, which is not the standard Yang-Mills law. Renamed $A$ to $\theta$ in the connection-form subsection to mark it as a Maurer--Cartan frame-gradient (not a dynamical Yang-Mills connection), wrote the correct overlap relation explicitly with a $\mathrm{Ad}(U_j^{-1})$ adjoint action, and noted that the Yang-Mills law is recovered in Regime II, not in Regime I. Removed a duplicate `eq:connection_form` label.

**Issue 11 — Frame-twist vs. Yang-Mills naming, strengthened.** Already partially addressed by the prior $g^{\mathrm{YM}} \to g^{\mathrm{tw}}$ rename. The connection-form rewrite above now also makes the sigma-model framing explicit: the $\mathrm{tr}(\theta_\mu \theta_\nu)$ object is a pullback of a Lie-algebra bilinear form along the section $U: \mathcal{C} \to G$, structurally a nonlinear-sigma-model kinetic term, not a Yang-Mills curvature invariant.

**Issue 12 — Natural-gradient covariance missing factor of two.** The Fisher block on the covariance sector is $\tfrac{1}{2}(\Sigma^{-1} \otimes \Sigma^{-1})$; its inverse is $2(\Sigma \otimes \Sigma)$, so the natural gradient on $\Sigma$ is $\tilde{\nabla}_\Sigma \mathcal{F} = 2\Sigma (\nabla_\Sigma \mathcal{F})\Sigma$, not $\Sigma (\nabla_\Sigma \mathcal{F})\Sigma$ as the manuscript previously wrote. Fix applied at both occurrences (main text and appendix), with the factor-of-two derivation made explicit in prose.

**Issue 13 — Hessian block-symmetry.** The user reiterated this as a remaining concern; verified that the prior fix (commit 04c1de5b's Issue-7 work) already distinguishes automatic block-symmetry $M_{ik} = M_{ki}^\top$ (Schwarz's theorem) from the stronger bilinear-form symmetry needed for a conservative-Hamiltonian reading. No change required.

**Issue 14 — Cross mean-covariance block reverted to orthogonal formula.** The cross-block at line 1261 used $\Omega_{ik}, \Omega_{ik}^\top$ (the $\mathrm{O}(d)$ form) instead of $\Omega_{ik}^{-T}, \Omega_{ik}^{-1}$ (the general $\mathrm{GL}(d)$ form). Rewritten to use the precision-transport rule $\tilde{\Lambda}_{q_k} = \Omega_{ik}^{-T}\Lambda_{q_k}\Omega_{ik}^{-1}$ explicitly, with the variation $\delta\tilde{\Lambda}_{q_k} = -\Omega_{ik}^{-T}\Lambda_{q_k} V \Lambda_{q_k}\Omega_{ik}^{-1}$ derived in line. Earlier-drafts $\mathrm{O}(d)$ form noted as the orthogonal special case.

**Issue 15 — Covariance appendix likelihood-gradient mistake.** The appendix at line 3414 said the observation term contributes "an $O(\Sigma_i^{-1})$ correction that we neglect..." For the Gaussian likelihood $-\mathbb{E}_{q_i}[\log p(o_i \mid \theta)] = \tfrac{1}{2}(o_i - \mu_i)^\top \Lambda_{o_i}(o_i - \mu_i) + \tfrac{1}{2}\mathrm{tr}(\Lambda_{o_i}\Sigma_i) + \mathrm{const}$, the contribution is in fact a constant precision $\partial/\partial\Sigma_i = \tfrac{1}{2}\Lambda_{o_i}$, not $O(\Sigma_i^{-1})$. Rewrote the paragraph, included $\Lambda_{o_i}$ in the boxed gradient, and added it to the fixed-point equation `eq:sigma_fixed_point_beta` with the noted observation-free limit recovered when $\Lambda_{o_i} = 0$.

**Issue 16 — Constant negative sectional curvature for Gaussians.** Lines 476 and 505 stated the Gaussian manifold has constant negative sectional curvature. This is true for the univariate case and certain location-scale families but not for the general multivariate Gaussian, whose curvature is nonpositive and not constant across all tangent planes. Rewrote both passages to specify nonpositive sectional curvature, distinguishing the SPD covariance factor (a symmetric space of nonpositive curvature) from the special low-dimensional cases where constant negative curvature holds. The qualitative consequence (geodesics diverge, uncertainty grows) follows from nonpositivity and is preserved.

**Issue 17 — Mutual information of marginals undefined.** The cross-scale information-flow definition at line 1423 read $\mathcal{I}_{s\to s+1} = \sum_{I,i \in I} \mathrm{MI}(q_i^{(s)}, q_I^{(s+1)})$. Mutual information is defined for a joint distribution; given only marginals there is no joint and $\mathrm{MI}$ is ill-defined. Replaced with the gauge-covariant transported KL, $\sum \mathrm{KL}(q_i^{(s)} \| \Omega_{i,I}[q_I^{(s+1)}])$, which is well-defined as a divergence between two distributions on the same fiber after gauge-covariant transport. Added an `eq:cross_scale_information_flow` label and a parenthetical noting why the previous MI notation was ill-formed and what the replacement preserves operationally. Added `sec:epistemic_collapse` label for the cross-reference.

### Audits

- 0 banned phrases (one "notably" introduced and removed before commit)
- 0 new banned spacing macros (one $\,$ introduced and removed before commit; 4 pre-existing remain in untouched regions)
- No duplicate labels (one `eq:connection_form` duplicate removed)
- All cross-references resolve


## Issues 18-25 — aggregation circularity, frame Karcher chart-dependence, Lorentzian cross-derivatives, SL(2,C) precision, conditional uniqueness, NG-vs-geodesic, validated language, smooth masks

User-supplied list of eight further defects.

### Why and approach

**Issue 18 — meta-agent Gaussian aggregation.** The covariance formula $\bar{\Sigma}_I = \sum_i w_i \Omega \Sigma_i \Omega^\top / \sum_i w_i$ is neither the moment-matched mixture covariance (which carries a $(\tilde{\mu}_i - \bar{\mu}_I)(\tilde{\mu}_i - \bar{\mu}_I)^\top$ dispersion term), nor the precision-weighted product-of-experts ($\bar{\Lambda}_I = \sum w_i \tilde{\Lambda}_i$); it is an implementation heuristic that agrees with the variational forward-KL barycenter at $\mathcal{O}(\varepsilon)$ when constituent post-transport means coincide. The weight $w_i = \chi_i \exp[-\mathrm{KL}(q_i \| \bar{q}_I)]$ depends on $\bar{q}_I$, which is itself being constructed (circular). The rewrite at `sec:meta_agent_state_construction` now states all three aggregation interpretations explicitly (mixture / product-of-experts / heuristic), names the implementation as the heuristic, and resolves the circularity by stating that the formula is implicit and is solved by fixed-point iteration with the previous-iteration estimate $\bar{q}_I^{(s),(t-1)}$ inside the weights at step $t$.

**Issue 19 — Lie-algebra average for frames.** The implementation $\phi_I = \sum w_i \phi_i / \sum w_i$ averages in Lie-algebra coordinates and is chart-dependent for nonabelian groups. The principled construction is the group-valued Fr\'echet/Karcher mean $U_I = \arg\min_U \sum_i w_i d_G(U, U_i)^2$, with $\phi_I = \log U_I$ on a chosen branch. Strengthened the existing BCH-approximation note in `sec:meta_agent_state_construction` to state the principled construction first, the additive average as a local computational shortcut whose chart-dependence is bounded by the BCH error, and a note that a reviewer reading "Lie-algebra average" as globally gauge-covariant would object.

**Issue 20 — Lorentzian worked example suppresses cross-derivatives.** The connection $A_\tau = i(\partial_\tau \psi_\tau)T$, $A_x = (\partial_x \psi_x)T$ at line 1706 ignores $\partial_\tau \psi_x$ and $\partial_x \psi_\tau$. The displayed result requires the separable ansatz $\psi_\tau = \psi_\tau(\tau)$, $\psi_x = \psi_x(x)$. Added an explicit separability-assumption sentence to the worked example, with the suppressed cross-derivative terms named.

**Issue 21 — SL(2,C) / Lorentz embedding imprecision.** The original "$\mathrm{SO}(1,3)$ embedded via $\mathrm{SL}(2,\mathbb{C}) \cong \mathrm{Spin}(1,3) \subset \mathrm{GL}(4,\mathbb{C})$" mixed representations. $\mathrm{SL}(2,\mathbb{C})$ acts on two-component Weyl spinors and lives in $\mathrm{GL}(2,\mathbb{C})$; the vector representation of $\mathrm{SO}^+(1,3)$ sits in $\mathrm{GL}(4,\mathbb{R}) \subset \mathrm{GL}(4,\mathbb{C})$. Rewrote both occurrences (the worked-example tail at line 1728 and the Concrete-Pathway item at line 1673) to keep the spinor double cover and the vector representation distinct, naming each by its dimension and home in the complexified group. Used $\mathrm{SO}^+(1,3)$ (proper orthochronous) and $\mathrm{Spin}^+(1,3)$ for precision.

**Issue 22 — Conditional Uniqueness Theorem appendix.** Three sub-defects: (A) the line-3646 statement of the f-divergence form integrated against $q_i$ rather than against $(\Omega_{ij}q_j)$, the standard $\int p f(q/p) d\mu$ form. Corrected with an explicit acknowledgment that the earlier pass was non-standard; the theorem statement at line 3734 already used the correct form. (B) the prose at line 3644 saying forward KL is uniquely required by "well-behaved coordination" was too broad. Rewritten to state that uniqueness is conditional on the specific stationary target form~\eqref{eq:geometric_mean_target_app}; weakening the target form reopens the divergence choice. (C) the geometric-mean exponent $1/2$ implicitly assumed $B := \sum_j \beta_{ij} = 1$. Generalised the theorem to $1/(1+B)$ exponents, with the $1/2$ form recovered when $B = 1$, and updated the proof's Step 2 to expose the $B$-dependence.

**Issue 23 — Natural-gradient flow is not geodesic flow.** Line 2255 claimed "Natural gradient descent is not a choice but rather it is the geodesic flow on the statistical manifold." This conflates gradient flow ($\dot\theta = -\mathrm{grad}_g \mathcal{F}$) with geodesic flow ($\nabla_{\dot\theta}\dot\theta = 0$); they satisfy different equations. Replaced with the correct steepest-descent characterisation, with explicit acknowledgment that conflating the two is a common informal slip we wish to avoid.

**Issue 24 — Validated participatory structure language.** Line 2299 ("this demonstrates the participatory structure: agents co-create reality through multi-scale feedback. The universe... is not a pre-existing structure...") was further softened. The simulation establishes a computational mechanism consistent with the participatory picture, not the ontology; the universe-construction reading is offered as an interpretive frame, not a validated conclusion.

**Issue 25 — Smooth sections vs hard masks.** The free energy uses $\chi_i(c) \in \{0, 1\}$ as hard support indicators, while the bundle language requires smooth sections over open domains; hard masks introduce discontinuities at support boundaries that conflict with the smooth-section assumption and make derivatives distributional. Rewrote the line-885 paragraph to define $\chi_i: \mathcal{C} \to [0, 1]$ as a smooth bump-function presence (compactly supported partitions of unity rather than indicator functions), and explicitly noted that the earlier $\chi_i \in \{0, 1\}$ phrasing should be read as a heuristic for the limit of a sharply localised bump rather than a hard indicator.

### Audits

- 0 banned phrases
- 0 new banned spacing macros (one introduced and removed before commit; 4 pre-existing remain in untouched regions)
- No duplicate labels
- All cross-references resolve

## Issues 26-35 — full FE structure, gauge invariance precision, GL(K) caveat, exp coverage, dim confusion, sign convention, gauge avg, Lorentz local/global, env-agent equivalence

User-supplied list of ten further defects. The most significant is Issue 26 (a real math error in the prior fix of Issue 1).

### Why and approach

**Issue 26 — full/reduced/autograd FE inconsistency.** The earlier Issue-1 fix had said the autograd surrogate $\langle E\rangle_{\beta^*} = \sum_j \beta^*_j E_j$ and the reduced free energy $-\tau\log Z$ "share critical points but differ off-equilibrium by the attention-entropy term." This is wrong: they have different gradient fields, since $\nabla_x \langle E\rangle_{\beta^*} = \sum_j \beta^*_j \nabla_x E_j + \sum_j E_j \nabla_x \beta^*_j$, while $\nabla_x F_{\mathrm{red}} = \sum_j \beta^*_j \nabla_x E_j$ alone (envelope theorem). The discrepancy is $-\tau^{-1}\mathrm{Cov}_{\beta^*}(E, \nabla_x E)$, which is generally nonzero, so the surrogate and the reduced energy do not share stationary points. Rewrote the corresponding paragraphs (after Eq. 32, after Eq. 39, and the "Autograd versus reduced-free-energy gradients" paragraph) to state this correctly. Promoted the full functional (with explicit attention entropy term) as the canonical free energy and clarified that the autograd-on-the-surrogate route is a different objective whose discrepancy with the reduced functional is a covariance.

**Issue 27 — boxed Eq. 39 missing the attention entropy term.** Without $\tau\sum_{ij}\beta_{ij}\log(\beta_{ij}/\pi_{ij})$, minimising the boxed functional over $\beta$ gives a hard arg-min, not the softmax. Added the entropy term explicitly to both the pointwise functional Eq. 32 and the boxed field functional Eq. 39, with both the belief and model channels paired with their attention priors $\pi_{ij}, \pi^{(s)}_{ij}$. The variational variables $\{\beta_{ij}\}, \{\gamma_{ij}\}$ are now listed explicitly in the functional's argument list.

**Issue 28 — gauge invariance global vs local.** The manuscript previously presented $U_i \mapsto U_i g$ (same $g$ for all agents) as "the" gauge invariance; this is global diagonal redundancy, not the full local gauge freedom of a principal bundle (independent $g_i$ per agent). Rewrote the "Gauge Covariance: The Fundamental Principle" subsection to keep both invariances cleanly separated: the global diagonal redundancy is the working notion used in body derivations, while the full local $U_i \mapsto U_i g_i$ with the corresponding $\Omega_{ij} \mapsto g_i^{-1}\Omega_{ij}g_j$ and $q_j \mapsto \rho(g_j^{-1})q_j$ is the stronger statement under which pairwise KLs remain invariant. Stated standard naming convention (gauge-invariant vs gauge-covariant) explicitly.

**Issue 29 — GL(K) is not the full natural Gaussian symmetry.** The full Gaussian family on $\mathbb{R}^K$ is preserved under the affine group $x \mapsto Ax + b$. The manuscript's choice of $\mathrm{GL}(K)$ is the origin-preserving linear subgroup -- a modelling choice (matching the bilinear attention reduction and treating means as frame coordinates), not "the" natural symmetry. Rewrote the gauge-group-choice paragraph to state this explicitly: $\mathrm{GL}(K)$ is the linear sector of the natural Gaussian symmetry; the translation sector $b$ is available but not invoked.

**Issue 30 — exponential parameterisation does not cover all $\mathrm{GL}^+(K, \mathbb{R})$.** For $K \geq 2$ the real matrix exponential $\exp: \mathfrak{gl}(K, \mathbb{R}) \to \mathrm{GL}(K, \mathbb{R})$ is not surjective onto the identity component: matrices with negative real eigenvalues of odd algebraic multiplicity admit no real logarithm. Rewrote the line-565 passage to acknowledge this: the parameterisation includes a neighbourhood of identity and is sufficient for the implemented dynamics, but is not a global parameterisation of $\mathrm{GL}^+(K, \mathbb{R})$.

**Issue 31 — observable-dimension argument conflates base and fiber dimensions.** The earlier text wrote as if the induced base-manifold metric $G_i(c)$ has $\dim(\mathcal{B}) = K(K+3)/2 = 296{,}064$ eigenvalues (for $K=768$). This is wrong: $G_i(c)$ is a tensor on $T_c\mathcal{C}$, an $n \times n$ matrix where $n = \dim(\mathcal{C})$, with at most $n$ eigenvalues. The differential $d\sigma_i: T_c\mathcal{C} \to T_q\mathcal{B}$ samples only an $n$-dimensional subspace of the much-larger fiber tangent space. Inserted "What lives where: base vs fiber dimensions" paragraph clarifying this; rewrote the internal-sector characterisation to refer to fiber-tangent directions not sampled by the section, not to eigen-directions of the base-manifold pullback.

**Issue 32 — sign convention for $\mathrm{tr}(A_\mu A_\nu)$ on compact groups.** For $\mathfrak{so}(N)$, $\mathrm{tr}(A^2) \le 0$, so the positive-definite inner product is $-\mathrm{tr}(AB)$. The manuscript previously hand-waved between $+\mathrm{tr}$ (used in the Lorentzian worked example) and $-\mathrm{tr}$ (needed for compact-group Riemannian) without a clean global convention. Rewrote the bundle-metric definition to introduce a single bilinear form $\kappa(A, B)$ pinned by the choice of compact vs non-compact form: $\kappa = -\mathrm{tr}$ for compact $\mathfrak{so}(N), \mathfrak{su}(N)$ (Riemannian); $\kappa = +\mathrm{tr}$ for $\mathfrak{gl}(K, \mathbb{C})$ and non-compact forms with $\mathrm{tr}(T^2) > 0$ available (indefinite). The body uses the first convention; the worked example uses the second.

**Issue 33 — gauge-averaged metric is not well-defined.** For constant $g$, $\mathrm{tr}(A_\mu A_\nu)$ is already invariant by trace cyclicity, so the Haar average is trivial. For local $g(c)$, an honest gauge-orbit average would integrate over all maps $g: \mathcal{C} \to G$ -- an infinite-dimensional functional integral requiring a regulator/gauge fixing. Rewrote the gauge-averaged metric subsection to state both limitations explicitly: the construction is retained as a heuristic for what gauge-invariant content the horizontal block could be reduced to under a chosen regulator, but not as a finite regulator-free gauge-invariant metric.

**Issue 34 — Lorentz local vs global.** The local frame group of an indefinite metric with signature $(1,1)$ is $\mathrm{O}(1,1)$ at each non-degenerate point, with proper orthochronous component $\mathrm{SO}^+(1,1)$. The full metric is globally $\mathrm{SO}^+(1,1)$-isometric only when coefficients are constant or absorbable by a coordinate change. Softened the worked-example claim from "the subgroup preserving this metric is $\mathrm{SO}(1,1)$" to a precise statement about local frame transformations vs global isometry, naming both possibilities.

**Issue 35 — environmental-agent KL equivalence with observation likelihood is partial.** $\mathrm{KL}(q_i \| q_e)$ for Gaussians has an extra $-\tfrac{1}{2}\log|\Sigma_i|$ entropy term that the negative log-likelihood lacks. Mean gradients match ($\Lambda_o(\mu_i - c_k)$), covariance gradients differ by $-\tfrac{1}{2}\Sigma_i^{-1}$ -- not constant in $\Sigma_i$. The earlier prose claimed "full equivalence at the level of dynamics"; corrected to: equivalence holds at mean-gradient level for any $\Sigma_o > 0$, full variational equivalence requires either replacing KL by cross-entropy $-\mathbb{E}_{q_i}[\log q_e] = \mathrm{KL} + H(q_i)$ or restricting to fixed-covariance dynamics where the discrepancy is constant.

### Audits

- 0 banned phrases
- 0 new banned spacing macros (4 introduced and removed before commit; 4 pre-existing remain)
- No duplicate labels
- All cross-references resolve

## Issue 36 — Constant pairwise transport $\Omega_{ij} = \Omega$ contradicts $\Omega_{ij} = U_iU_j^{-1}$

### Why

The transformer derivation at line ~2393 specialised to ``$\Omega_{ij} = \Omega$ for all $i,j$'' as the route from the gauge-covariant attention to standard dot-product attention. This is structurally inconsistent with the vertex-frame parameterisation $\Omega_{ij} = U_iU_j^{-1}$: the self-pair gives $\Omega_{ii} = I$, and the cocycle $\Omega_{ij}\Omega_{jk} = \Omega_{ik}$ at $i = k$ forces $\Omega_{ij}\Omega_{ji} = I$. Pair-independence then yields $\Omega^2 = \Omega$, and invertibility forces $\Omega = I$. The only constant pair-independent transport consistent with the vertex-frame law is the trivial one. Therefore the derivation as written did not actually obtain dot-product attention from the stated transport model; the trick of ``absorbing $\Omega^{-1}$ into learned projections'' was substituting a different object -- a shared query-key bilinear compatibility -- without naming it as such.

### Approach

Separated two distinct objects in the transformer reduction. (i) The relative gauge transport $\Omega_{ij} = U_iU_j^{-1}$, which carries the gauge-covariant content of the framework, collapses to $\Omega_{ij} = I$ when all token-agents share a frame ($U_i = U$ for all $i$ at the single base point). (ii) A shared query-key bilinear compatibility $M \in \mathrm{GL}(d_k)$ acts on the means in the cross term of the Mahalanobis attention score; in the standard transformer, $M = W_QW_K^\top$ is learned. Replaced the ``constant gauge specialisation'' paragraph with a structural caveat showing that pair-independent nontrivial $\Omega$ is impossible under the cocycle, and rewrote the dot-product derivation to use $\mu_i^\top M \mu_j$ in the cross term from the start (formerly written as $\mu_i^\top \Omega^{-\top}\mu_j$). The factorisation $W_QW_K^\top = \sigma^{-2}M$ now factors a learned bilinear similarity rather than a gauge-transport operator. Updated three other passages that had repeated the ``constant gauge'' framing (lines 585, 2319, 2327, 2349) to use the corrected three-limit sequence: isotropic covariances, trivial pairwise transport (via shared frames), and learned shared bilinear compatibility $M$.

### Audits

- 0 banned phrases
- 0 new banned spacing macros (4 pre-existing remain in untouched regions)
- All cross-references resolve

## Cleanup — remove self-referential drafting history from manuscript prose

### Why

Per user feedback: prose phrases referencing the manuscript's own revision history -- "earlier drafts wrote X," "the original calculation said Y," "the corrected reading," "we previously claimed," etc. -- are not appropriate inside a manuscript. They make the document read as a corrigendum to itself rather than as a finished paper. This stylistic preference now lives as a feedback memory.

### Approach

Swept the manuscript for self-referential drafting language and rewrote each occurrence as if the corrected version were the only version. Specifically removed or rewrote:

- L131 ("Where earlier drafts described this as 'dissolving'...") -- replaced with a direct statement of the relocation framing.
- L833 ("Some earlier passages wrote the alignment block...") -- rewrote as a forward-looking caveat about an autograd surrogate that is sometimes used in implementations.
- L885 ("Earlier drafts wrote $\chi_i \in \{0,1\}$...") -- rewrote as a note that the informal $\{0,1\}$ usage elsewhere is a heuristic limit.
- L991 ("Earlier passages of this section claimed full equivalence...") -- rewrote as a direct statement of the mean-gradient-only equivalence.
- L1141 ("...used in earlier drafts hold only under $\mathrm{O}(d)$ and are corrected below") -- rewrote as a forward statement.
- L1234, L1281 ("the form used in earlier drafts") -- removed the parenthetical reference.
- L1432 ("The mutual-information notation used in earlier drafts...") -- rewrote as a methodological note that we avoid the MI notation because it requires a joint coupling.
- L1586 ("Earlier drafts presented the induced metric...") -- rewrote as "a common informal construction" framing.
- L1630 ("expression used in earlier drafts") -- rewrote as "the bare score-function expression."
- L1825 ("Earlier passages of this manuscript wrote as if...") -- rewrote as a direct statement of the base-vs-fiber distinction.
- L2327, L2349, L2403, L2413 (the constant-$\Omega$ rewrite from Issue 36) -- removed all "earlier drafts" / "the role $\Omega^{-\top}$ played in earlier drafts" framings; the corrected derivation now reads as the only version.
- L3439 ("Earlier drafts of this appendix dropped this contribution... that classification was wrong") -- rewrote as a direct statement of the constant-precision contribution.
- L3663 ("an earlier, less-careful pass of this appendix wrote the integrand against $q_i$...") -- removed the historical disclaimer; the convention now reads as the only convention.

The edit-log document at the project root remains the appropriate place to record what changed and why; the manuscript itself no longer narrates its own revision history.

### Audits

- 0 banned phrases
- 0 new banned spacing macros (4 pre-existing remain)
- All cross-references resolve
