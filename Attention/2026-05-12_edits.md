# 2026-05-12 Edits: Attention-Entropy Term Added to VFE

## Motivation

Audit (3 parallel investigators + verifier) confirmed that both the `transformer/vfe/`
package and the legacy `transformer/core/` + `transformer/train.py` paths implement
the **entropy-suppressed surrogate** `Σ β·KL(q‖Ω q)` rather than the full manuscript
free energy. The manuscript (`Attention/Participatory_it_from_bit.tex`,
`\label{eq:free_energy_functional_final}` near line 1234–1242, sec
`sec:complete_free_energy`) defines the canonical functional with explicit attention
entropy:

```
F = ... + Σ_ij [ β_ij·KL(q_i‖Ω q_j) + κ · β_ij · log(β_ij / π_ij) ] + ...
```

Manuscript line 1261 explicitly warns: the entropy-suppressed surrogate "does not share
stationary points" with the canonical F; its gradient differs by
`-τ⁻¹ Cov_{β*}(KL, ∇KL)`. Practical impact in code:

- Reported scalar F biased by `+κ·H(β)` (skews early stopping, ELBO logging).
- `learnable_kappa` gradient missing `-H(β/π)` term.
- (μ, Σ, φ) gradients envelope-correct at fixed β; `λ_soft·(β·KL.detach()).sum()`
  already incidentally provides the manuscript's covariance correction for these.

Slow channel (`s, r, γ, λ_h`) out of scope per user.

## Changes

### `transformer/vfe/config.py`

Added `include_attention_entropy: bool = True` flag after `learnable_kappa`. Default
ON; disable to recover entropy-suppressed surrogate.

### `transformer/vfe/e_step.py`

Two insertions:

1. Inside `_update_phi` after the existing `alignment_loss` (product-rule decomposition
   of `β·KL`), added:
   ```python
   if self.cfg.include_attention_entropy:
       beta_safe = beta_phi.detach().clamp(min=1e-30)
       entropy_value = (beta_safe * beta_safe.log()).sum()
       alignment_loss = alignment_loss + _kappa * entropy_value
   ```
   `_kappa = self.effective_kappa` is the live tensor (Parameter with gradient when
   `learnable_kappa=True`). `β.detach()` makes envelope theorem give zero
   (μ, Σ, φ) gradient at softmax fixed point; the live κ multiplier provides the
   correct `-H(β)` κ-gradient.

2. Diagnostic metric `attention_entropy_loss` added alongside `attention_entropy` at
   the metrics-collection site (was line 424). Reports `κ · Σ β·log β` (the
   actual F contribution, with sign).

### `transformer/vfe/trainer.py`

Logging line picks up the new metric:
```python
if 'attention_entropy_loss' in metrics:
    msg += f" | F_H {metrics['attention_entropy_loss']:.3f}"
```

### `transformer/train.py`

- Added `include_attention_entropy: bool = True` parameter to
  `compute_free_energy_loss`.
- After `belief_align_loss` assembly (was around line 418), added
  `belief_entropy_loss = M_beta * κ_scalar · Σ β·log β / dim_scale` where `κ_scalar`
  is read from `model.blocks[0].ffn.kappa` with safe fallback to 1.0. Scalar κ here
  (not live tensor) — this term is for F-value correctness only; κ-gradient flow
  lives in the E-step path (`variational_ffn.py`).
- `total_loss` assembly now includes `belief_entropy_loss`.
- Metrics dict reports `loss/belief_entropy`.

### `transformer/core/variational_ffn.py`

- Added `include_attention_entropy: bool = True` constructor parameter; stored as
  `self.include_attention_entropy`.
- Docstring math spec at lines 27–30 extended with the κ·Σβ·log(β/π) term.
- Three alignment-loss assembly sites (the `(β_h, kl_h)`, `(β_phi_h, kl_h)`, and
  `(β_phi, kl_matrix)` variants near lines 1035, 1225, 1257) each appended a
  detached-β entropy term `self.kappa * (β_safe · log β_safe).sum()` gated by the
  flag.

## Files NOT modified

- `transformer/vfe/model.py` M-step loss — M-step only computes
  `ce_loss + mass_phi · ||φ||²`; β-related terms live exclusively in the E-step
  `alignment_loss`. No change.
- Manual gradient kernel `compute_vfe_gradients_gpu` — operates on (μ, Σ); entropy
  contribution is zero by envelope theorem (β detached). No change.
- Existing tests — no test asserts a specific F value; the `train_vfe.py` smoke test
  only requires `loss.isfinite()`. New behavior preserved.

## Theoretical correctness

For β = softmax(-KL/κ) at the iteration fixed point:

- **κ gradient** (when `learnable_kappa=True`): manuscript envelope-theorem result is
  `∂F/∂κ = Σ β·log(β/π) = -H(β/π)`. The new code's contribution
  `_kappa * Σ β.detach()·log β.detach()` differentiates as
  `∂[_kappa · S]/∂_kappa = S = (β·log β).sum()` — matches manuscript.
- **(μ, Σ, φ) gradient**: detached β gives zero gradient from the new term — matches
  manuscript envelope-theorem result; the existing `λ_soft · (β · KL.detach()).sum()`
  term continues to provide the manuscript's off-equilibrium covariance correction
  `-τ⁻¹ Cov_{β*}(KL, ∇KL)`.
- **Reported F value**: equals manuscript F up to the dropped additive constant
  `κ · log N_valid` (uniform-prior assumption, π = 1/N_valid). Presence-absorbed
  prior `π̃_ij = χ_ij π_ij / Σ_k χ_ik π_ik` is a follow-up refinement.

## Follow-up correction (same day): τ_eff = κ · √K, per-head live κ in legacy path

The first pass used `κ` as the entropy weight, but the softmax in code uses
`β = softmax(-KL / (κ · √K))`, so the manuscript-consistent effective
temperature is `τ_eff = κ · √K`. Without the √K, β is not the stationary point
of F — envelope theorem fails. Corrected:

- `transformer/vfe/e_step.py`: entropy weight now `_kappa · √embed_dim` (single-
  effective-block treatment; multi-irrep configs assume uniform K per block).
- `transformer/core/variational_ffn.py` per-head sites (1041, 1235): entropy weight
  now `kappa_h · √d_h` where `kappa_h = self._get_kappa_h(h, d_h)` (live tensor
  when `learnable_head_kappa=True`).
- `transformer/core/variational_ffn.py` consolidated multi-head site (1271): when
  `learnable_head_kappa=True`, builds a per-head live `kappas` tensor via
  `_get_kappa_h` and weights `(κ_h · √d_h) · entropy_h` per head; else uses
  scalar `self.kappa · √d_h_canon`.
- `transformer/train.py`: removed spurious `/dim_scale` from entropy term; the
  per-component-normalized form is `M_beta · κ · (β·log β).sum().mean()` since
  `τ_eff · entropy / √K = κ · entropy`.

## Second-pass correction: attached β at all 4 E-step entropy sites

After the first pass committed, a unit test (`tests/test_entropy_envelope.py`)
swept 6 (λ_soft, β-attach) configurations and found that the detached-β entropy
combined with the existing `λ_soft = 1` default gave the **wrong gradient** —
mismatched the manuscript envelope-theorem prediction by ~10× the signal in
||∂F/∂μ||, ||∂F/∂Σ||, and |∂F/∂κ|.

**Root cause.** The `λ_soft · (β · KL.detach()).sum()` term contributes
`−τ⁻¹ · Cov_β(KL, ∇KL)` to the gradient — exactly the manuscript line 1261
"entropy-suppressed surrogate offset". With β detached in the entropy term,
nothing cancels it. Two consistent configurations exist:

- `λ_soft = 1` AND attached β in entropy: autograd-through-softmax produces the
  canceling `+τ⁻¹ · Cov`. Net gradient matches manuscript ∇F_red at softmax β.
- `λ_soft = 0` AND detached β in entropy: pure envelope-clean form; no off-
  equilibrium correction.

Chose option 1 (attached β) because it is robust to any user `λ_soft` setting
and is the literal autograd implementation of the manuscript F functional.

**Files changed (second pass).**

- `transformer/vfe/e_step.py`: `beta_phi.detach().clamp(...)` → `beta_phi.clamp(...)`.
- `transformer/core/variational_ffn.py` three entropy sites: same `.detach()` removal.
- `tests/test_entropy_envelope.py` (new): asserts production config matches
  envelope-theorem prediction to tolerance 1e-10.

**Verification.** `python tests/test_entropy_envelope.py` reports
`λ_soft=1, β.attached (PROD)` MATCH at 5.86e-19 / 2.31e-18 / 8.67e-19, hard
assertion PASS.

## Third-pass: direct-form F functional (λ_soft-independent)

The unit test from the second pass revealed that `λ_soft=1, β.attached` matches
but `λ_soft=0, β.attached` does **not** — yet the active `train_vfe.py` config
has `lambda_soft = 0.0`. The two-term product-rule structure couples the
correctness of the entropy term to the value of `λ_soft`.

Resolution: when `include_attention_entropy=True`, replace the product-rule
split with the direct manuscript F functional, scaled by `lambda_align`:

```
F = (β · KL).sum() + τ_eff · (β · log β).sum()       # all attached
alignment_loss = lambda_align · F
```

`lambda_soft` is ignored in this branch. The unit test now sweeps `λ_soft ∈
{0, 0.5, 1, 7.3}` and `λ_align ∈ {0.5, 2, 10}` — all match envelope predictions
at machine precision (~1e-18 for μ, ~1e-13 for Σ, ~1e-17 for κ).

Touched 4 entropy sites:
- `transformer/vfe/e_step.py`: `_update_phi` alignment_loss now branches on
  `include_attention_entropy`; entropy path uses direct F.
- `transformer/core/variational_ffn.py`: three sites restructured the same way.
- `tests/test_entropy_envelope.py`: adds `direct_form=True` config and
  multi-`λ_soft` / multi-`λ_align` assertions.
- `tests/smoke_test_entropy.py` (new): live training smoke test, 30 steps each
  on entropy={on, off}; asserts loss finite, `attention_entropy_loss` metric
  present, and entropy term measurably affects backward (`Δ loss > 0`).

Also exposed two attribute holes in `transformer/vfe/e_step.py:VFEEStep`:
`include_attention_entropy` and `embed_dim` were referenced via `self.cfg.X`
but VFEEStep's pattern is to copy individual cfg fields to `self.X` in
`__init__`. Added the missing assignments.

## 2026-05-13 follow-ups: skip_attention hardening

### `block_config.py` — incompatibility warning

Added `UserWarning` in `BlockConfig.__post_init__` when `skip_attention=True`
is combined with any of the detaching EM modes (`em_phi_p`, `em_phi_q`,
`implicit_ift`). Those modes detach σ_p and/or φ inside the FFN's E-step
and rely on the attention sublayer as the sole autograd path to σ_embed
and φ_embed. With `skip_attention=True` the sublayer is gone, so σ and φ
embeddings stay frozen at init — a silent failure that the previous
revert-of-STE-bridge commit (`46bf4b52`) made unavoidable. The warning
tells the user explicitly that they need `'straight_through'` or
`'ift_phi'` for a clean skip-attention configuration.

### FFN β surfaced when `skip_attention=True`

Attention heatmap plots stopped firing every eval step under
`skip_attention=True` because `block.forward(return_attention=True)`
returned `(beta=None, kl=None)` for that branch — the attention sublayer
was the β source. The FFN's VFE E-step computes its own per-iteration β
internally; we now expose it.

- `transformer/core/variational_ffn.py`: in `_finalize_e_step`, store the
  final-iteration β onto `self._last_beta` unconditionally (stacked over
  heads, detached). Mirrors the existing `_last_beta_for_implicit`
  pattern but is available for any em_mode and any
  `return_beta_history` setting.
- `transformer/core/blocks.py`: in `GaugeTransformerBlock.forward`, when
  `return_attention=True` AND `skip_attention=True` AND the attention β
  stayed None, set `beta = self.ffn._last_beta`. `kl_matrix` stays None
  (the heatmap consumer at `publication_metrics.plot_attention_heatmap`
  reads only β).

Result: `model.forward_with_attention(...)` now returns a populated β
even under `skip_attention=True`, and the eval-step
`save_attention_visualization` produces per-layer per-head heatmaps from
the FFN's internal attention pattern.

## 2026-05-13: decouple σ step size from μ LR

Before this change `sigma_lr` (config: `E_sigma_q_lr`) was plumbed only as a
**trust-region clamp** on the whitened tangent `δσ/σ`, while the actual σ
step size was the learnable `self.lr` (i.e., the μ LR). Sweeping
`E_sigma_q_lr` produced no visible effect unless the value was small enough
to bind the clamp — exactly the symptom the user reported.

Fix: thread `sigma_lr` as the *step size* into the σ retraction, and add a
separate `sigma_trust` parameter (config: `E_sigma_q_trust`, default 5.0)
for the clamp magnitude. The σ retraction is now:

```
σ_new = σ · exp( E_sigma_q_lr · decay_factor_t · clamp(δσ/σ, ±E_sigma_q_trust) )
```

`decay_factor_t` is the same per-iteration cosine annealing that the μ step
uses, recovered as `effective_lr / self.lr` so μ and σ share the schedule
without sharing the step magnitude.

Files touched:
- `transformer/core/variational_ffn.py` — constructor adds `sigma_trust`
  arg (default 5.0); stores `self.e_step_sigma_trust`; `_retract_sigma`
  now computes `_sigma_step = self._fixed_sigma_lr * decay_factor` and
  passes it as the actual step size; `_get_sigma_trust` returns
  `self.e_step_sigma_trust` (instead of the legacy `_fixed_sigma_lr`).
- `transformer/core/block_config.py` — new `E_sigma_q_trust: float = 5.0`
  dataclass field; threaded through `from_config` and to
- `transformer/core/blocks.py` — FFN construction passes
  `sigma_trust=cfg.E_sigma_q_trust`.
- `transformer/train_publication.py` — config dict gains
  `'E_sigma_q_trust': 5.0`; updated comment clarifies E_sigma_q_lr is now
  the actual σ step size.
- `CLAUDE.md` — mathematical-reference section updated with the corrected
  σ retraction formula and the decoupling note.

Test: `tests/test_sigma_lr_decoupling.py` — sweeps `sigma_lr` ∈ {1e-4,
0.015, 0.15} on a single diagonal retraction step and asserts the σ
update magnitude scales linearly with `sigma_lr` (10× LR → 9.96× Δσ;
0.015 / 1e-4 ratio → 149.91× Δσ, matching the ~150× expected for small
whitened tangent).

Migration note for the active `196_K=200_GL(25)_skip` config: with
`E_mu_q_lr=0.3` and the old code, the effective σ step was
`min(0.3·whitened, 0.015) ≈ 0.0045` per iteration (whenever the clamp
bound). Under the new code the effective σ step is `0.015·whitened`,
~3× larger. If σ training becomes unstable, drop `E_sigma_q_lr` to
`~0.005` or `0.003`.

## Out of scope (follow-up items)

- **Presence-absorbed prior `π̃_ij`**: my code uses uniform π = 1/N and drops
  the additive `τ · log N` constant. For learnable κ this shifts κ-gradient by
  `√K · log N` per row — a systematic offset, not a per-batch noise. Acceptable
  approximation; π̃ refinement is a follow-up.
- Mixed-irrep configs in `variational_ffn.py` consolidated site.
- Slow channel (`s, r, γ, λ_h`) — user excluded.

## Verification

Syntax check via `ast.parse` on all five modified files: PASS.

Next: deploy verifier agent to confirm
(a) entropy term added at documented lines,
(b) β.detach() used (envelope-clean for μ,Σ,φ),
(c) κ multiplier is the live tensor where κ-gradient flow is needed,
(d) flag defaults True,
(e) no double-counting between vfe/e_step.py and legacy train.py paths.
