# 2026-05-21 — Participatory_it_from_bit.tex introduction calibration

Edits applied in response to three red-blue debates on the introduction (verdicts in `docs/debates/2026-05-21-pifb-intro-*/04_verdict.md`):

- `2026-05-21-pifb-intro-prior-art` — REMAND
- `2026-05-21-pifb-intro-pan-agentic` — REMAND
- `2026-05-21-pifb-intro-epistemic-status` — RED_WINS

## Manuscript edits to `Attention/Participatory_it_from_bit.tex`

### §1.1 line 74 — prior-art calibration

Replaced the universally quantified "no rigorous mathematical framework has united these threads" claim with a calibrated paragraph that cites Sengupta, Tozzi, Cooray, Douglas \& Friston 2016 (Towards a Neuronal Gauge Theory) and the geometric Bayesian-mechanics literature (Sakthivadivel 2022, Kirchhoff-Parr-Palacios-Friston-Kiverstein 2018) as existing FEP-plus-gauge work, then narrows the priority claim to "neither line of work delivers a computational instantiation in which 'it' (transformer-style attention) demonstrably emerges from 'bit' (variational message passing on a principal $\mathrm{GL}(K)$ bundle with section fields $\phi_i \in \mathfrak{gl}(K)$ and inter-agent transport $\Omega_{ij} = \exp(\phi_i)\exp(-\phi_j)$). We supply that instantiation." Verified via primary-source WebFetch of Sengupta 2016 (PLoS Biology 14(3):e1002400).

### §1.4 line 100 — binary dichotomy softened

Replaced "two radical possibilities: either *physics produces cognition*, or *cognition produces physics*" with a three-family articulation that admits dual-aspect monism, Russellian / structural panprotopsychism, and the interacting-agent formalism. The new paragraph reads "rather than deriving physics from cognition or cognition from physics, we exhibit information-geometric analogues of physics-like structures within an interacting-agent formalism," aligning with the §1.5 line 117 framing.

### §1.5 line 119 — explicit panprotopsychist positioning + statistical-manifold clarification

Two substantive changes to the "Pan-agentic multi-scale ontology" paragraph.

First, clarified the substrate position: the base manifold $\mathcal{C}$ is a parameter / index manifold of contexts (the structural role of Whitehead's extensive continuum), not a non-agent physical thing. This eliminates the textual contradiction with §sec:lahav_convergence line 3414.

Second, clarified that the statistical manifold associated with an agent is whichever is appropriate to its scale: "multivariate Gaussian on $\mathbb{R}^K$ in the working implementation of Sections~\ref{sec:framework}--\ref{sec:transformers}, but more generally any exponential family, mixture family, or in the quantum case the manifold of density operators with quantum relative entropy as the divergence (the manuscript does not develop the quantum case)." This is the load-bearing clarification: the Gaussian belief tuple is a simplification of the working architecture, not a commitment of the ontology.

Third, explicit panprotopsychist taxonomy. Added "Positioned against the panpsychism taxonomy, the framework is a *panprotopsychist* position in the sense of Chalmers" citing \cite{Chalmers2013consciousness, Chalmers2016}. Reformulated the comparison to Lahav-Neemeh (denies pre-agentic substrate), substance idealism (does not require minds), and constitutive panpsychism (does not posit phenomenal micro-properties combining into macro-experience).

### §1.6 line 125 — "consistent with" framing for the 0D attention recovery

Replaced "Standard scaled dot-product attention is recovered" with "Standard scaled dot-product attention is consistent with a zero-dimensional isotropic-Gaussian limit of the KL-consensus construction under three explicit reductions (isotropic covariances, trivial-frame transport, learned bilinear compatibility $M$) plus the standard normalization and bias assumptions". Added an explicit sentence separating the analytic limit (Section~\ref{sec:transformers}) from the empirical sweep (Section~\ref{sec:scaling_validation}, which trains the general gauge model rather than the trivial-frame reduction), matching the §sec:transformers line 1601 phrasing.

### §1.7 line 142 — Lorentzian-postulate enumeration expanded from 3 to 6

Listed all six body-flagged postulates from §sec:worked_signature: (a) Regime I / $F_{\mu\nu} \equiv 0$ pure-gauge restriction; (b) non-compact generator choice $T = \mathrm{diag}(1,-1) \in \mathfrak{sl}(2,\mathbb{R})$; (c) $+\mathrm{tr}(AB)$ bilinear-form sign convention; (d) separable ansatz $\psi_\tau = \psi_\tau(\tau), \psi_x = \psi_x(x)$; (e) imaginary temporal gauge-frame component $\phi_\tau = i\psi_\tau T$; (f) real-part projection $G^{\mathrm{Lor}}_{\mu\nu} := \mathrm{Re}(G_{\mu\nu})$ (no Wick counterpart). Brought §sec:open_problems line 3505 enumeration into alignment with the same six items.

### §1.7 — added consensus-metric regulator-gap entry

Added a new bolded entry "**Consensus metric is regulator-dependent.**" recording that the gauge-orbit-averaged consensus metric of Section~\ref{sec:consensus_metric} is the framework's only candidate gauge-invariant geometric observable but remains regulator-dependent and is presented as a heuristic structural target rather than a finite gauge-invariant observable. This was the load-bearing missing-scope item identified by the epistemic-status debate (RED_WINS).

### §1.8 — added Gauge-Theoretic Free Energy and Bayesian Mechanics paragraph

Added a new Related Work paragraph between Quantum Information Approaches and Observer-Relative Approaches, citing Sengupta, Tozzi, Cooray, Douglas \& Friston 2016; Sakthivadivel 2022; Kirchhoff, Parr, Palacios, Friston \& Kiverstein 2018; Sengupta \& Friston 2018; and Caticha 2019. Differentiates the present construction by explicit principal-bundle section fields $\phi_i$, the inter-agent transport operator $\Omega_{ij}$, the covariance sandwich-product action, the multi-agent KL-coupled free energy, and the computational transformer instantiation.

### §1.8 line 176 — "uniquely combines" qualified + per-conjunct calibration

Replaced "Our approach uniquely combines" with "Our approach combines, for the first time at the bundle-construction level we develop here". Carried forward the §1.5 line 125 bilinear-$M$ qualification into conjunct (iv) and scoped conjunct (iii) to the fast-channel participatory bootstrap (single seed, $\gamma_{ij} = 0$) to match §1.7 line 127 / §sec:results.

### §sec:base_manifold (line 489) — substrate-clause repair

Reclassified the base manifold $\mathcal{C}$ from "noumenal substrate" to "parameter / index space of contexts." Updated the subsection title and definition. The new wording is "$\mathcal{C}$ is a smooth $n$-dimensional manifold serving as the parameter / index domain over which agent sections (beliefs, priors, gauge frames, generative models, hyper-priors) are defined" and adds the clarification "This is consistent with the §sec:lahav_convergence statement that no observer-independent physical substrate sits underneath the agents: $\mathcal{C}$ is a domain of inquiry, not a thing." The §sec:lahav_convergence line 3414 statement is now consistent with §sec:base_manifold.

## Bibliography edits to `Attention/references.bib`

Appended five new entries (Sengupta2016NeuronalGauge, SenguptaFriston2018, Sakthivadivel2022, KirchhoffParrPalaciosFristonKiverstein2018; Caticha2019 already present and reused; Chalmers2013consciousness and Chalmers2016 already present and now cited from §1.5).

## Follow-up debate spawned

The pan-agentic verdict prescribed a sub-debate on whether the "electron is a scale-0 agent" example is load-bearing or illustrative, against Hudson's theorem. With the §1.5 statistical-manifold clarification applied (electron beliefs may live on the density-operator manifold rather than the Gaussian-on-$\mathbb{R}^K$ manifold), the follow-up debate's question becomes: does the panprotopsychist + general-statistical-manifold reading survive the structural-incompatibility attack, and is the §1.7 "no quantum extension" disclaimer sufficient bounding?

## Debate D (electron scale-0 load-bearing under statistical-manifold generality) — REMAND

The follow-up debate at `docs/debates/2026-05-21-pifb-electron-scale-0-loadbearing/` ran with both teams citing primary sources (Petz 1996, Uhlmann 1976, Amari-Nagaoka 2000 ch. 7, Bengtsson-Życzkowski 2017 §15, Umegaki 1962, Nakahara 2003 §10, Čencov 1982, Friston-Wiese-Hobson 2020) and adjudicated REMAND with four prescribed edits, all applied below.

### Edit (§1.5 — explicit scope-split recasting)

Recast the universally quantified §1.5 clause to distinguish the ontological commitment from the constructive scope. The new paragraph states: "The ontological commitment is that every system at every scale carries gauge-frame structure plus participation in variational dynamics on a statistical manifold appropriate to its scale and nature: multivariate Gaussian on $\mathbb{R}^K$ at the classical-mesoscale level instantiated by the working architecture …; more generally any exponential family or mixture family at intermediate classical scales; and the manifold of density operators $\mathcal{D}(\mathcal{H})$ with Umegaki quantum relative entropy as the divergence at the scale-0 quantum level (constructive instantiation deferred; see Section~\ref{sec:open_problems})." Adds explicit Nakahara 2003 §10 citation for the structural reading of "gauge-theoretic dynamics" with scale-dependent structure group ($\mathrm{GL}(K,\mathbb{R})$ classically, $U(\mathcal{H})$ at scale-0). The electron declaration is demoted from declarative-illustrative ambiguity to "Under this ontology an electron is a scale-0 agent in the structural-gauge-theoretic sense above … the constructive instantiation of the scale-0 gauge-bundle on $\mathcal{D}(\mathcal{H})$ is an open research-program item."

### Edit (§sec:open_problems Quantum Extension — Petz metric-selection paragraph)

Added a new paragraph acknowledging that the Riemannian metric on $\mathcal{D}(\mathcal{H})$ is not uniquely determined by monotonicity under CPTP maps (Petz 1996), with Bures (Uhlmann 1976), Kubo–Mori / BKM (Amari–Nagaoka 2000 ch. 7), RLD, max as canonical representatives. The selection is an open research-program parameter, distinct from the classical case where Čencov 1982 gives Fisher–Rao uniqueness.

### Edit (§1.7 line 150 — disclaimer extended to scope the ontology)

Extended the "No quantum extension" entry to explicitly scope both the construction and the ontology: "The §sec:cognitive_first pan-agentic ontology commits to scale-0 agents in the structural-gauge-theoretic sense; the constructive instantiation on the density-operator manifold $\mathcal{D}(\mathcal{H})$ — selection of the Petz monotone metric …, exhibition of inter-agent unitary transport and $\beta$-coupling, demonstration of reduction to the classical-Gaussian dynamics at higher scales — is open. The §sec:cognitive_first ontology and the construction are therefore explicitly scope-split: the ontological commitment is in-scope, the constructive scale-0 instantiation is research-program target."

### Bibliography additions

Appended three new entries to `Attention/references.bib`: Petz1996, Uhlmann1976, AmariNagaoka2000. Čencov1982 already present.

---

## §Theory revision pass — pifb-theory-rock-solid debate fallout

A 3-round panel=full debate on the claim "§Theory of `Attention/Participatory_it_from_bit.tex` is rock solid and publication ready" returned RED_WINS (unanimous 3/3 first-pass judges: canon-strict, code-truth, scope). Verdict at `docs/debates/2026-05-21-pifb-theory-rock-solid/04_verdict.md`; action items at `docs/debates/2026-05-21-pifb-theory-rock-solid/05_action.md`. Seven of the eight action items were applied in this pass; item 5 (§1.19 mass-analogy reframe) is deferred to a separate agent investigation.

### Item 7 — Figure 1 caption (line 1411) and body sentence (line 1406)

Replaced the figure caption's "demonstrating that attention mechanisms are geometric necessities rather than arbitrary architectural choices" with a body-faithful sentence: "This emergent pattern arises from variational free energy minimization of the consensus-energy ansatz of Section~\ref{sec:mixture_derivation} rather than from learned attention weights, exhibiting the role attention plays under the source-independence factorization adopted there." Similarly replaced the body's "necessary consequences of information-geometric principles" line with a parallel ansatz-faithful sentence. Both edits align the figure-caption-and-body framing with the manuscript's own line-1044 status note ("consensus-energy ansatz rather than a generative-model derivation") and line-1064 disclosure ("substantive structural assumption, not a tractability simplification"). Closes operationalization sub-claim 4 partial wound.

### Item 2 — TODO marker (line 1880)

Removed the literal `\textbf{TODO:}` token. Rewrote the deferred-empirical-test sentence as a clean limitation acknowledgment that does not use future-work language: "We do not claim a dynamical test of $\omega^2 \propto m_{\text{eff}}^{-1}$ … because the present construction sets the inertia tensor equal to the potential Hessian by postulate (Section~\ref{sec:velocity_quadratic}) and therefore makes the scaling a definitional consequence of that identification rather than an empirical dispersion law; no operationally independent measurement is reported in this manuscript." Closes operationalization sub-claim 6.

### Item 8 — Residual-subgroup inline restatement (line 610)

Extended the Convention paragraph in §1.5 (Associated Bundles) to inline the residual-constant-per-agent subgroup restriction from §sec:dual_role_rigor when invoking the lattice gauge transformation $\Omega_{ij} \mapsto g_i \Omega_{ij} g_j^{-1}$: "… are stated relative to this convention, with the per-vertex $g_i \in G$ restricted to the constant-per-agent residual subgroup of Section~\ref{sec:dual_role_rigor} for Role-B quantities that are not invariant under the full local gauge." Closes operationalization sub-claim 4 hygiene gap surfaced by blue's sur-rebuttal.

### Item 6 — β=0 vs β=β* convention tension (lines 1967, 2015)

The §1.19 "Differentiation convention" paragraph and the "Remark on the consensus simplification" paragraph contained adjacent claims that the boxed mass-matrix forms were "stated in the isolated-agent limit ($\beta_{ij} = 0$)," yet the displayed Eqs. eq:mass_mu_diagonal and eq:mass_sigma_diagonal both carry explicit $\sum_k \beta_{ik}$ and $\sum_j \beta_{ji}$ contributions inconsistent with $\beta = 0$. Committed to the envelope-theorem fixed-$\beta^*$ convention throughout: "The boxed forms below are written for general $\beta_{ij}^* \geq 0$ under this convention and reduce to the isolated-agent limit by setting $\beta_{ij}^* = 0$ in the sums." Edited the consensus-simplification remark similarly: "the boxed form above is the consensus-point restriction, with the envelope-theorem fixed-$\beta^*$ convention of the 'Differentiation convention' paragraph above. The isolated-agent specialization is recovered by setting $\beta_{ij}^* = 0$ in the sums, where the off-consensus distinction also vanishes trivially." Closes operationalization sub-claim 3 internal-consistency wound.

### Item 3 — Cross-scale shadow framing (lines 547–552)

The cross-scale shadow paragraph in §1.4 had the rigid-link $\sigma^2 \to 0$ identification framed as "the deterministic-decoder limit of a ladder-VAE-style hierarchical prior~\cite{Sonderby2016}." Ladder VAE passes a finite-variance Gaussian conditional and does not take the $\sigma^2 \to 0$ Dirac limit; the canon-strict judge cited this as decisive evidence for RED_WINS on sub-claim 1. Removed the wrong-domain Sønderby comparison from §Theory (the Sønderby cite remains in Appendix~\ref{app:augmented_joint} where the comparison is correct as a finite-$\sigma^2$ reduction). Recast the §Theory paragraph to label the rigid-link specialization as a "structural commitment of the framework rather than a derivation from any peer-reviewed hierarchical-VI precedent," retaining the appendix-A pointer for the augmented-joint generative model and the variational-principle continuity with~\cite{Friston2010, Friston2017, parr2022active}. Closes operationalization sub-claim 1 wound.

### Item 1 — Conditional Representation Theorem renaming

Three coordinated renamings to resolve the body-vs-appendix naming inconsistency surfaced by blue's appendix read:

- Renamed LaTeX label `app:conditional_uniqueness` → `app:conditional_representation` (six occurrences across §Theory body and the appendix `\label`). All `\ref{app:…}` calls propagate consistently.
- Renamed theorem label `thm:uniqueness_app` → `thm:representation_app` (one definition, two body refs added). The theorem environment title `Conditional representation` was already correct.
- Renamed body-text invocations of "the conditional uniqueness theorem" → "the conditional representation theorem" at lines 1044 and 1118.
- Added the standard real-analyticity-of-$f$ hypothesis to the body's assumption summary at three locations (lines 1044, 1118, 1251), matching the appendix's Theorem~\ref{thm:representation_app} hypothesis statement.
- Updated the appendix abstract sentence at line 4296 from "The uniqueness of the term is conditional and follows from three assumptions" to "The representation result is conditional and follows from three structural assumptions on the divergence" and added an explicit clause stating the theorem's regularity conditions on the generator $f$.

Closes operationalization sub-claim 3 internal-consistency wound (body invoking "uniqueness theorem" vs appendix self-titling "Representation Theorem"; body listing (i)-(iii) vs appendix proof requiring real-analyticity).

### Item 4 — Companion-paper delegations inlined from GL(K) papers

The §Theory section carried fourteen in-text references to `\cite{Dennis2025trans}` (the GL(K) attention companion: `Attention/GL(K)_attention.tex` + `Attention/GL(K)_supplementary.tex`, unpublished). Per the operationalization's self-containedness requirement (sub-claim 5), the companion-paper cross-cites at load-bearing reduction steps were either removed (where the content is already inlined in PIFB §Theory itself) or replaced with inlined statements pulled from the GL(K) papers:

- **Line 636** (§1.7 Agents as Smooth Sections): removed the "We follow the convention of the gauge-theoretic transformer companion paper" framing; the convention statement that follows ($\gamma_{ij} = 0$ in simulations, $p_i$ realized by PriorBank) stands on its own.
- **Line 943** (§1.10 Gauge Group Choice): replaced "adapted from the gauge-theoretic transformer companion paper" with "written here in the present notation"; the $\mathrm{GL}(K)$ Gauge Invariance Theorem is sourced from `[Amari2016]` directly.
- **Line 1042** (§1.13 Mixture-of-Sources Construction): removed "adapted from the gauge-theoretic transformer companion paper"; the construction is presented self-contained at the source.
- **Line 1209** (cocycle / vanishing-holonomy lemma): removed the "(the proof here follows the companion paper)" parenthetical; the proof is inlined at line 1223 of the manuscript ("Direct computation. The consecutive inverse-exponential pairs cancel identically").
- **Line 1294** (state-dependent prior precision): removed the "companion treatment of the same construction" cite; the closed-form $\alpha_i^*$ derivation is inlined in the subsection.
- **Line 1352, 1365** (per-coordinate $\alpha$ and product-rule chain): removed the "companion paper adopts the per-coordinate form" cite, replacing with "in practice the per-coordinate form is the implemented choice"; removed the "companion paper carries the corresponding chain-rule slot" cite, replacing with an inline statement of where the prefactor enters $\partial\mathcal{F}/\partial\mu_i$.
- **Line 1607** (transformers as zero-D limit): removed the "and the underlying construction developed in the companion paper" trailing cite; the construction is developed in §1.16 and §1.17 of PIFB itself.
- **Line 1615** (trivial-frame reduction): replaced "The remainder of this subsection follows the development in the gauge-theoretic transformer companion paper" with "We now develop the trivial-frame route" since the content that follows is the actual derivation, not a delegation.
- **Line 1623** (multi-head lift): replaced the punt "is treated in the companion paper" with a four-sentence inlined derivation of the multi-head lift, including the block-diagonal direct-product identification $\mathrm{GL}(d_{\text{head}})^H \subset \mathrm{GL}(d_{\text{model}})$ in a new labeled equation (\ref{eq:multihead_gauge_group}) and the off-diagonal complement codimension count.
- **Line 1676** (Rényi $\alpha$-divergence): replaced "The companion paper carries the reference implementation" with "In the reference implementation the order $\alpha$ is exposed as a per-block configuration parameter so that different layers can interpolate between mode-seeking and mean-seeking behaviour without modifying the architecture."
- **Line 1702** (thin-SVD multi-head kernel factorization): removed the "(see also the companion paper)" parenthetical; the thin-SVD development is at Eqs.~\ref{eq:thin_svd_decomposition}–\ref{eq:low_rank_lift} of PIFB itself.
- **Line 1818** (gauge group identification at end of trivial-frame route): replaced the trailing cite with an explicit back-reference to Eq.~\ref{eq:multihead_gauge_group} introduced above.
- **Line 1875** (multi-head + RoPE + FFN-as-GLU summary): replaced the catch-all "derived in detail in companion paper" punt with three new paragraph-level inlined correspondences pulled from `GL(K)_attention.tex` §sec:multihead, §sec:rope, §sec:ffn_nonlinearity:
  - "Multi-head attention as a block-diagonal gauge restriction" — restates the $\bigoplus_a \mathfrak{gl}(d_{\text{head}}) \subset \mathfrak{gl}(d_{\text{model}})$ identification with explicit codimension and the architectural choice that the standard output projection $W_O$ mixes head outputs *after* attention rather than restoring the off-diagonal complement before it.
  - "Rotary position embeddings as position-dependent gauge frames" — exhibits the RoPE rotation $R(\theta_m) \in \mathrm{SO}(2)^{d_k/2}$ as the position-dependent specialization $\Omega_{ij} = \exp(\phi_i^{(\mathrm{pos})})\exp(-\phi_j^{(\mathrm{pos})})$ restricted to the abelian $\mathrm{SO}(2)^{d_k/2}$ subgroup, with explicit relative-position dependence $R(\theta_{j-i})$.
  - "Feed-forward networks as Boltzmann-gated linear units" — displays the per-edge update map $\Delta\mu_i\big|_j \propto e_{ij} \cdot \mathrm{softmax}_j(-\|e_{ij}\|^2_{\Sigma_j^{-1}}/2\tau)$ in a new labeled equation (\ref{eq:vfe_glu}), identifies the SiLU / Swish form as the binary-source ($n_s = 2$) limit, places SiLU and GELU in the same exponential family as Gaussian-PDF-gate vs Gaussian-CDF-gate, and states the cancellation of the softmax-gradient correction against the entropy term under the canonical entropy-augmented $\mathcal{F}$.

After this pass, the §Theory section (lines 180–2070) contains zero `\cite{Dennis2025trans}` references. Remaining cites are confined to §Implementation (line 2622) and Appendix~\ref{app:conditional_representation} (line 4285), both outside §Theory's scope for the publication-readiness claim. Closes operationalization sub-claim 5 wound.

### Item 5 — deferred

The §1.19 mass-analogy subsection (lines 1877–2069, "Statistical Precision as Configuration-Space Stiffness") was identified by the verdict as wounding operationalization sub-claims 1 and 4 through the line-2064 admission that $\omega^2 \propto k/m$ is "a definitional consequence of the postulate rather than an independent dynamical scaling." Item 2 above removed the in-section `\textbf{TODO:}` marker and rephrased the deferred-empirical-test sentence as a limitation acknowledgment, partially addressing the wound; a deeper reframing of the subsection (either as a pure stiffness-Hessian computation without the $\omega^2 \propto m_{\text{eff}}^{-1}$ scaling claim, or with a separate operationally independent kinetic-metric postulate) is deferred to a dedicated agent investigation per user request.

## Item 5 follow-up — dedicated agent investigation outcomes (PM)

Following the deferral above, a four-agent investigation (manuscript reviewer, codebase-feasibility auditor, info-geometer, verifier) was dispatched against the PIFB mass-analogy TODO. Audit artifacts at `docs/audits/audit-2026-05-21-pifb-mass-todo.md` (V13 verifier consolidation) and `MAgent_Model-main/docs/audit-2026-05-21-mass-todo-{feasibility,VERIFIER}.md` (correct-codebase investigation and verifier). All twelve load-bearing claims VERIFIED unanimously: (a) the manuscript's $k = m = M_{\mu\mu}$ identification is by postulate at line 2088 and admitted at line 2068; (b) Amari-Nagaoka §3.2 makes the Hessian-of-KL-equals-Fisher identification doctrinal on exponential families; (c) Arnold 1989 §22 requires independent $(0,2)$-tensors for the small-oscillations dispersion to have empirical content; (d) the MAgent_Model-main codebase has a real symplectic leapfrog with momentum state and dual mass conventions, but the "independent" Ouroboros convention pulls $\Lambda_m$ from `agent.precision_p` (`dynamics.py:1186`), so a $\Sigma_p$ sweep would propagate into the inertia without a one-line patch; (e) the historical $R^2 = 0.9998$ claim (now removed) traced to a tautological polyfit in `gauge_agent/figures.py:236` against the same $\bar\Lambda_p$ tensor it was meant to predict. User confirmed all data prior to the planned MAgent_Model-main re-runs should be treated as placeholder.

Two surgical PIFB edits applied in response, both restricted to the §1.19 / §sec:phenomenological_interpretation slice (no scope creep into the meta-agent-emergence single-run or the WikiText-103 scaling subsections, which are separate studies).

### Item 5a — line 1988 "validated" → "postulated" (notation-hierarchy paragraph)

The "Notation hierarchy" paragraph at line 1988 carried the trailing phrase "(e.g., for the Newtonian-analogy $\omega^2 \propto 1/m$ scaling validated in Section~\ref{sec:mass})". This directly contradicted the same-section admission at line 2088 ("the harmonic-oscillator scaling $\omega^2 \propto k/m$ is a definitional consequence of the postulate rather than an independent dynamical scaling… no operationally independent measurement is reported") and the §sec:phenomenological_interpretation limitation acknowledgment. Rewrote as "(e.g., for the Newtonian-analogy $\omega^2 \propto 1/m$ scaling postulated in Section~\ref{sec:velocity_quadratic}, whose definitional status under the kinetic-metric postulate is discussed there and whose operationally-independent empirical test is flagged as future work at the same location)". Cross-reference target moved from sec:mass (where the postulate is implicit) to sec:velocity_quadratic (where it is explicit and where the empirical-test caveat lives).

### Item 5b — line 3052 residual `\textbf{TODO:}` token removed (§sec:phenomenological_interpretation)

Item 2 above removed the literal `\textbf{TODO:}` token from line 1880 in the morning pass, but the parallel token at line 3052 ("\textbf{TODO:} A computational test of $M_{\mathrm{eff}} \propto \Sigma_p^{-1}$ with $\omega^2$ and $M_{\mathrm{eff}}$ measured as operationally independent quantities is deferred to future work") survived. Rewrote as a clean limitation acknowledgment parallel to the line-1904 wording: "We do not claim a computational test of $M_{\mathrm{eff}} \propto \Sigma_p^{-1}$ in which $\omega^2$ and $M_{\mathrm{eff}}$ are measured as operationally independent quantities; the within-framework reading above identifies the Fisher mass with the Hessian by construction on this slice (Section~\ref{sec:mass}, Section~\ref{sec:velocity_quadratic}), so the scaling is a definitional consequence of that identification rather than an empirical dispersion law, and no operationally independent measurement is reported in this manuscript." Zero `\textbf{TODO:}` tokens remain in the manuscript after this pass.

### Codebase changes deferred

The four-step implementation plan for an operationally-independent test (one-line `Lambda_m` decoupling patch on `gauge_agent/dynamics.py:1186`; `scratch/omega_vs_sigma_p_sweep.py` wrapper; `scratch/fit_omega_from_trajectory.py` FFT estimator; report) is recorded in user memory and the audit artifacts and will be executed in a separate session within the `MAgent_Model-main` repo per user instruction. No code in this V13 tree is affected by this manuscript pass.

### Verification

After all edits, §Theory of `Attention/Participatory_it_from_bit.tex`:
- Contains zero `\textbf{TODO:}` tokens.
- Contains zero `\cite{Dennis2025trans}` references.
- Resolves the body-vs-appendix theorem-naming inconsistency: body refers to "conditional representation theorem" and `Theorem~\ref{thm:representation_app}`; appendix theorem is labelled `thm:representation_app` and titled "Conditional representation." Appendix label is `app:conditional_representation`.
- Closes the β=0 vs β=β* convention tension in §1.19: the boxed forms are committed to the envelope-theorem fixed-$\beta^*$ convention with the isolated-agent limit recovered as $\beta_{ij}^* = 0$.
- Inlines the residual-constant-per-agent subgroup restriction at the §1.5 Convention paragraph where the lattice gauge transformation $\Omega_{ij} \mapsto g_i \Omega_{ij} g_j^{-1}$ is introduced.
- Recasts the §1.4 cross-scale shadow as a structural commitment of the framework rather than as a ladder-VAE-derived construction.
- Inlines the multi-head lift $\mathrm{GL}(d_{\text{head}})^H \subset \mathrm{GL}(d_{\text{model}})$, the RoPE identification as position-dependent abelian gauge, and the FFN-as-Boltzmann-gated-linear-unit correspondence at paragraph level in §1.16.

Two new LaTeX labels introduced: `eq:multihead_gauge_group` (line 1626) and `eq:vfe_glu` (line 1908). All previously-existing `\ref{thm:uniqueness_app}` and `\ref{app:conditional_uniqueness}` calls were renamed consistently; verified via Grep that no orphan references remain. The Sønderby2016 citation remains correctly used in Appendix~\ref{app:augmented_joint} (line 4571) as a finite-$\sigma^2$ comparison, removed from §Theory where it had been wrong-domain.

---

## §Implementation revision — emergence-simulator supersession and items 3/4/6/7/8/9/10

Per user direction following the pifb-implementation-rock-solid debate (RED_WINS, 3/3 first-pass judges + chief reconciliation; verdict in `docs/debates/2026-05-21-pifb-implementation-rock-solid/04_verdict.md`): the §Implementation section's references to the prior emergence-simulator codebase are obsolete because the simulator code has been superseded by `C:\Users\chris and christine\Desktop\MAgent_Model-main`. The pass below (i) replaces every "the simulations of this paper" / "in our experiments" prose hook in §Implementation with either a faithful description of the new `MAgent_Model` reference implementation or a `\textbf{TODO}` marker where the new code diverges from the manuscript's canonical specification, and (ii) applies action items 3, 4, 6, 7, 8, 9, 10 from the debate verdict in the same pass. Item 5 (cross-scale "transport" relabeling) is deferred per user instruction.

### Theory-consistency survey of `MAgent_Model-main/gauge_agent/`

Two parallel investigation agents surveyed the new codebase against §Implementation (lines 2101–2304). Findings:

- **MATCH** (faithful realization): variational barycenter mean and dispersion-dropped covariance (`meta_agents.py:303–306`), saddle-point coherence weights $w_i = \chi_i \exp(-\mathrm{KL})$ with fixed-point iteration (`meta_agents.py:282–321`), five-term FullVFE assembly with attention entropy (`free_energy.py:481–487`, `:306–349`), natural-gradient + Lie-group $\Omega$ update (`dynamics.py:391–411, 475–545`), Σ-sector affine-invariant SPD geodesic (`dynamics.py:1396–1548`), thresholds $\Gamma_{\min}=0.5$, $N_{\min}=2$ (`meta_agents.py:48–49`), depth-1 cross-scale transport on both fibers (`meta_agents.py:412–465`, `ouroboros.py:260–268`), self-referential top-scale closure (`ouroboros.py:323–423`), manifold sampling (`manifolds.py`, `manifold_system.py`), KL-collapse epistemic-death detector (`meta_agents.py:131–147`).
- **DIVERGE** (code does not realize the canonical form): threshold-detector form (`meta_agents.py:55–91` uses `1-KL` not `exp(-V/τ)` and two factors not three, missing presence factor `P`); frame averaging (`meta_agents.py:343–359` uses extrinsic Euclidean weighted mean not log-Euclidean BCH-first-order); Ouroboros multi-generation weighting (`ouroboros.py:210–295` uses pooled-prior single-slot form not additive sum-of-KLs with separate $h^{(s,k)}$ fields and $\lambda_0$); non-equilibrium indicators ($\Phi_E$ proxy, $\Phi_I$ missing $H[p]$ contribution, $V_\nabla$ cross-scale-energy proxy not per-agent gradient-norm variance; $E_{\min}$ not enforced).
- **NOT-IMPLEMENTED** (manuscript construct, no code): variational FE-improvement criterion (Eq. 2123 — code uses threshold surrogate), IB Lagrangian (Eq. 2133 — research-direction only), per-scale timescale slowdown.
- **CODE-ONLY** (extension beyond §Implementation): RG numerical pipeline including β-functions and fixed-point analysis (`renormalization.py::RenormalizationGroupFlow._analyze_flow`), trust regions, numerical monitor, verifier, non-flat lattice connection.

Overall verdict: the new codebase is **partially kosher with theory** — roughly half MATCH, plus several documented DIVERGE / TODO points. Items where the code matches are written up; items where the code diverges or is absent are flagged `\textbf{TODO}` with explicit reasoning so the page makes the gap visible.

### Manuscript edits to `Attention/Participatory_it_from_bit.tex`

#### §Implementation preface (new) — Reference implementation and scope

Added a new `\paragraph{Reference implementation and scope of this section.}` paragraph at the top of §Implementation naming `MAgent_Model` (`gauge_agent/` package) as the open-source reference codebase. Enumerates the operative modules (`free_energy.py`, `dynamics.py`, `meta_agents.py`, `ouroboros.py`, `numerical_monitor.py`, `verifier.py`) and states the policy: where the reference implementation matches a construction, it is invoked as the operative form; where it diverges or is unrealized, the section marks the gap as `\textbf{TODO}`. Added a second paragraph `\paragraph{Earlier simulator framing.}` that explicitly disclaims the earlier-draft "simulations of this paper" framing as targeting a superseded simulator codebase, with §Results runs flagged as pending rerun against the new reference.

#### Hierarchical Scale Structure (line 2114)

Replaced "We set $s_{\max} = 25$" with "The reference implementation in `MAgent_Model/gauge_agent/ouroboros.py::OuroborosTower` adopts $s_{\max} = 25$ as the default."

#### Item 4 — IB Lagrangian framing (lines 2131–2138)

Removed the closed-form Chechik-Tishby attribution at line 2138 since the Gaussian-IB Theorem 3.1 requires jointly-Gaussian-vector scope that the parent state $T = (q_I, s_I, U_I)$ does not satisfy. Reformulated as: the Gaussian-IB closed form is withdrawn as a wrong-domain invocation; three non-Gaussian variants are listed as research targets: deterministic IB [StrouseSchwab2017], agglomerative IB [SlonimTishby2000], and a group-IB variant for the gauge-frame component. The three open ingredients (transition kernel, gauge-frame encoder noise, empirical comparison) are retained explicitly as research-direction items.

#### Item 6 — Dispersion-term truncation at line 2179

Quantified the coherence regime: the dropped quadratic dispersion term is bounded by $\varepsilon := \max_{i,j} \mathrm{KL}(\Omega_{ij}q_i\|q_j)$ at second order, with the bound uniformly small inside the threshold-detector pass region $\Gamma > \Gamma_{\min}=0.5$ where the leading $C_q = \exp(-\overline{\mathrm{KL}}/\tau_q)$ factor enforces $\overline{\mathrm{KL}} < \tau_q\log 2$. The truncation matches the standard mean-field lower-bound decomposition of [Beal2003 §2.2.2]. The full moment-matched form remains available as the natural refinement once a continuous-time evaluation of Eq. 2123 is in hand.

#### Item 10 — Karcher non-compact $\mathrm{GL}^+(K)$ (line 2160)

Reframed the unadjudicated GL+(K) choice. Compact $\mathrm{SO}(N)$ is now stated as the operative case in §Implementation, with Karcher-mean existence-uniqueness directly applicable [Karcher1977, Pennec2009]. For the $\mathrm{GL}^+(K)$ generalization, [Milnor1976] is cited for non-existence of a bi-invariant metric, with two principled substitutes named: polar-decomposition + affine-invariant SPD [PennecFillardAyache2006] and left-invariant Riemannian metric on quotient manifolds [BonnabelSepulchre2009]. The unadjudicated choice is explicitly scoped out of §Implementation and flagged as a `\textbf{TODO}` in §Open Problems.

#### Threshold-detector subsection (lines 2167–2174)

Replaced "The simulations of this paper use $\Gamma_{\min}=0.5$, $N_{\min}=2$" with the reference-implementation citation `meta_agents.py::ConsensusDetector`. Added a new `\paragraph{Reference-implementation status: detector form (\textbf{TODO}).}` paragraph documenting the two known divergences:
- `belief_coherence`, `model_coherence` at `meta_agents.py:55–80` compute $C = 1 - \overline{\mathrm{KL}}$ rather than the canonical Gibbs $\exp[-V/\tau]$ form.
- `consensus_score` at `meta_agents.py:82–91` returns the two-factor product $C_q \cdot C_s$ without the presence factor $P$.
Realignment to the detector form specified in the canonical paragraph above is the next implementation pass; the gap is now visible on the page.

#### Gauge-covariant belief construction (lines 2179–2192)

Replaced "in the simulations the previous-iteration estimate ..." with a code-faithful description: "The reference implementation `MAgent_Model/gauge_agent/meta_agents.py::MetaAgentFormation.form_meta_agent` realizes this fixed point with a $\chi$-weighted cold start and an explicit convergence check." Added a `\paragraph{Reference-implementation status: frame averaging (\textbf{TODO}).}` documenting the extrinsic-Euclidean-vs-log-Euclidean divergence at `meta_agents.py` and the log-Euclidean realignment via `lie_groups.matrix_log_principal` as the next pass.

#### Item 7 — RG language (lines 2197–2213)

Tightened the line-2197 RG disclaimer: clarified that the construction has not exhibited an analytic $\beta$-function from a closed-form exact pushforward of the Gibbs measure, and that the reference implementation `renormalization.py` provides a numerical RG pipeline (KL-proximity blocking, per-scale coupling extraction, discrete-derivative $\beta$-function, linearized fixed-point analysis) that we treat as exploratory empirical machinery distinct from an analytic derivation. The line-2213 claim "important direction for future work we are currently engaged in" was replaced with a precise statement: form-preservation is necessary but not sufficient for a Wilson-sense RG fixed-point analysis; the analytic identification of fixed points and the critical-exponent computation remain open and are flagged in §Open Problems.

#### Item 9 — Emergent-properties single-seed disclosure (lines 2226–2230)

Rewrote the "We expect, though do not directly measure" paragraph as structural predictions of the construction, not measured outcomes. The substantive existential claim "whole becomes qualitatively different from sum of its parts" was removed. The paragraph now lists four testable structural predictions (coherence-length scaling, fluctuation averaging, parent-covariance shrinkage via information pooling, alignment-coupling-mediated coordination) and explicitly defers any substantive emergence claim to a `\textbf{TODO}` multi-seed measurement pending the §Results rerun.

#### Emergent Timescale Hierarchy (lines 2232–2241)

Rewrote the slowing-mechanism paragraph in framework-internal terms: integration via Eq. 2181 averages high-frequency components, consensus barriers via the gauge-covariant barycenter resist rapid updates, and parent stiffness $[M_{\mu\mu}]_{II}$ scales with cluster size $|I|$ through the incoming-relational and outgoing-recoil mass contributions of Section §Mass. Added a `\paragraph{Reference-implementation status: per-scale timescale (\textbf{TODO}).}` documenting that `dynamics.py` exposes only the global belief-vs-model timescale separation $\epsilon$ (`model_lr_ratio`); per-scale slowdown $\tau^{(s+1)} > \tau^{(s)}$ is not yet exposed at `ouroboros.py::evolve_scale` and is tracked as the next implementation pass.

#### Item 8 — West-Harrison citation (line 2275)

Refined the West-Harrison citation from a general book cite to `[WestHarrison1997 §10.7]` (the discounted-likelihoods construction) and added [Smith1979] as the infinite-horizon geometric-discount anchor. The book section is the canonical home for the multiplicative discount factor identification used in Eq. 2270.

#### Item 3 — Implementation Note at line 2284

Replaced the self-disclosed verification gap with a code-faithful description: the reference implementation `meta_agents.py::TopDownFeedback.propagate_prior` realizes the full $\Omega_{i,I} = U_i \cdot U_I^{-1}$ transport on both belief and model fibers via transposed-solve algebra at `meta_agents.py:226–227`; multi-generation ancestral propagation lives in `ouroboros.py::OuroborosTower._propagate_hyperpriors` with geometric weights $\rho^k$ per Eq. 2270. The reference implementation therefore closes the realization gap that earlier drafts deferred. Reworded "In our experiments with direct assignment" at line 2293 to "Under the direct-assignment convention adopted above."

#### Non-Equilibrium Dynamics (lines 2295–2303)

Added a `\paragraph{Reference-implementation status: non-equilibrium indicators (\textbf{TODO}).}` paragraph documenting three proxy-form divergences between the canonical $\Phi_E, \Phi_I, V_\nabla, E_{\mathrm{score}}, E_{\min}$ specification and the reference implementation at `ouroboros.py::compute_non_equilibrium_score` (lines 425–531): $\Phi_E$ is a discrete cross-scale finite difference; $\Phi_I$ uses only $H[q_i]$ omitting the $H[p_i]$ contribution; $V_\nabla$ is a cross-scale energy variance not the per-agent gradient-norm variance; and $E_{\min}$ is computed but not enforced as an early-stop. The epistemic-death detector at `meta_agents.py::check_epistemic_death` currently triggers on max-pairwise-KL collapse rather than on $E_{\mathrm{score}} \to 0$ and likewise awaits realignment.

### Bibliography additions to `Attention/references.bib`

Appended ten new entries to support the §Implementation revision: `Smith1979` (JRSSB 41:375–387, infinite-horizon discount), `SlonimTishby2000` (NIPS 1999, agglomerative IB), `StrouseSchwab2017` (Neural Computation 29(6), deterministic IB), `Milnor1976` (Adv. Math. 21, curvatures of left-invariant metrics), `Karcher1977` (CPAM 30, Riemannian center of mass), `Pennec2009` (LNCS 5416, statistical computing on manifolds), `PennecFillardAyache2006` (IJCV 66(1), Riemannian framework for tensor computing), `BonnabelSepulchre2009` (SIAM J. Matrix Anal. Appl. 31(3), Riemannian metric on PSD), `Beal2003` (UCL Gatsby thesis, variational algorithms). `Hall2015` was already present and cited from the new frame-averaging defense paragraph.

### Verification

After this pass, §Implementation of `Attention/Participatory_it_from_bit.tex` (lines 2101–2304, now extended by the reference-implementation preface and per-subsection `\textbf{TODO}` status paragraphs):

- Contains no remaining "the simulations of this paper" / "in our experiments" / "in our simulations" prose hooks except the explicit disclosure paragraph at the top of §Implementation that names the supersession.
- Names the new `MAgent_Model/gauge_agent/` reference implementation at each subsection where the new code realizes a construction, with specific module references (`meta_agents.py`, `ouroboros.py`, `free_energy.py`, `dynamics.py`, `renormalization.py`, `manifolds.py`).
- Carries five `\textbf{TODO}` status paragraphs documenting known divergences: detector form, frame averaging, multi-generation Ouroboros weighting (implicit in the bibliographic-anchor paragraph already), per-scale timescale, non-equilibrium indicators.
- Withdraws the wrong-domain Chechik-Tishby closed-form IB attribution and replaces it with three non-Gaussian-IB research-direction targets.
- Quantifies the dispersion-term truncation regime against the Beal 2003 mean-field lower-bound decomposition.
- Scopes the Karcher frame barycenter to compact $\mathrm{SO}(N)$ for §Implementation and flags the $\mathrm{GL}^+(K)$ extension as a follow-up `\textbf{TODO}`.
- Tightens the RG language so that line 2210 form-invariance is positioned as a necessary-but-not-sufficient condition for a Wilson-sense RG analysis, and line 2213 "future work" is replaced with a precise statement of what remains open.
- Rewrites the line-2228 emergent-properties existential as structural predictions, not measured outcomes, with the substantive "whole becomes qualitatively different from sum of its parts" claim removed pending multi-seed measurement.
- Cites [WestHarrison1997 §10.7] and [Smith1979] for the discount-factor identification at Eq. 2270.

All cross-references resolve: `sec:open_problems` (line 3539) and `sec:methods_metagent` (line 3613) exist and are referenced by the new `\textbf{TODO}` paragraphs. No banned phrases (per `style_constraints.md`) were introduced. Item 5 (cross-scale "transport" → "frame-change" relabeling) is deferred per user instruction.

## §Speculative Extensions revisions — three strike-required items from `docs/debates/2026-05-21-pifb-speculative-extensions-rock-solid/04_verdict.md`

Verdict: REMAND (chief reconciliation under Rule 4, mode-weighted tiebreak for `theory` mode → canon-strict, whose outcome is REMAND). Three strike-required revisions to `Attention/Participatory_it_from_bit.tex` applied; two editorial-discretion items not applied per the verdict (`04_verdict.md:39–43`).

### §sec:phenomenological_interpretation line 3084 — asymmetric falsification replaced with symmetric statement

Replaced the sentence cluster "Success would strongly support the phenomenological interpretation. Failure would not falsify the framework (our mathematical development might be incomplete) but would limit its explanatory power" with a symmetric falsification statement: success substantively supports the interpretation by exhibiting an instance of the derivation it commits to, and failure under a fully developed information-geometric construction (statistical manifolds, comparison structure, and Cencov scale fixings all specified) substantively counts against the interpretation. Failure under an incomplete construction is non-diagnostic, and the burden is on the program to develop the construction to the point where the test is symmetric, not to indefinitely defer the test by appeal to incompleteness. Discharges the Popper §6 paragraph-level immunization-manoeuvre strike, which blue conceded verbatim at `03_blue_rebuttal.md:5`.

### §sec:summary_implications postulate ledger (lines 3127–3145) — added postulate (iv) for 4D extension

Added "(iv) under the 4D extension of the worked signature example (Section~\ref{sec:worked_signature}), the three spatial generators $T_x, T_y, T_z \in \mathfrak{sl}(K, \mathbb{R})$ are chosen mutually trace-orthogonal, $\mathrm{tr}(T_a T_b) = 0$ for $a \neq b$, so that the spatial block of the frame-twist metric $G_{ab}^{\mathrm{tw}} = \mathrm{tr}(A_a A_b)$ is diagonal in the coordinate basis without a further unstated stipulation." Updated "None of (i)--(iii) is derived..." to "None of (i)--(iv) is derived...". The line 2854 "is open" qualifier on the 4D global metric is retained as-is. Closes the un-enumerated 4D-postulate gap identified by red (`02_red_opening.md:15`, Vector B) and conceded by blue (`03_blue_rebuttal.md:5`).

### §sec:sustained_nonequilibrium line 3155 — Hofstadter strange-loop sentence removed

Deleted "This pattern is structurally adjacent to Hofstadter's 'strange loop'~\cite{Hofstadter1979}." The load-bearing canon at this paragraph is the Wheeler self-excited-circuit reference cited immediately before; the Hofstadter sentence was decorative and pulled the section into popular-science register. The `Hofstadter1979` entry in `references.bib` is no longer cited from any `.tex` file in `Attention/` (verified by Grep); leaving the .bib entry in place is harmless under standard BibTeX (uncited entries are not rendered).

### Editorial-discretion items not applied (per `04_verdict.md:39–43`)

- Line 2941 modal-qualifier placement on the gauge-invariance-as-consensus inversion: not applied. The manuscript's existing disclosure at line 2943 ("the hypothesis may not be falsifiable from within the framework") satisfies Popper §6 by direct admission per the verdict's reasoning at line 41.
- Lines 2841/2846 real-part-projection allowability against Kontsevich-Segal 2021 / Witten 2021: not applied. The manuscript already discloses "no Wick counterpart" at line 2846 and "structural existence proof, not a dynamical derivation" at line 2856 per the verdict's reasoning at line 43.

These two items remain available for the publication-version pass at the user's editorial discretion. Follow-up debate (`pifb-speculative-extensions-rock-solid-v2`) re-tests the seven conjunctive sub-claims at `00_claim.md:35–51` against the revised section.

## §Speculative Extensions — four-debate sweep (PM, follow-up to v2)

Four parallel red-blue debates (`pifb-spec-1-pullback`, `pifb-spec-2-signature`, `pifb-spec-3-consensus-dim`, `pifb-spec-4-phys-quant`) partitioned §Speculative Extensions (lines 2579–3158) into four topical sub-claims. All four returned RED_WINS. Eleven sentence-level edits applied. Artifacts at `docs/debates/2026-05-21-pifb-spec-*/`. The reframed operational reading — math correct, canon correctly invoked, disclaimers complete and honest, no speculative claim dressed as derivation — now holds across the section after these edits.

### D1 Edit 1 — BCH order error at line 2649 (Eq. eq:bch_chart_step new label)

The chart-coordinate truncation error was previously stated as $O(\|\eta_\phi \tilde\nabla_{\phi_i}\mathcal{F}\|^2)$. The leading Baker-Campbell-Hausdorff correction $\tfrac{1}{2}[\phi_i^t, \eta_\phi \tilde\nabla_{\phi_i}\mathcal{F}]$ is bilinear in $\phi_i^t$ and $\eta_\phi \tilde\nabla_{\phi_i}\mathcal{F}$; at fixed $\phi_i^t = O(1)$ this is first-order in the gradient step, not second-order. Replaced the equation with a discrete-step BCH expansion making the leading commutator explicit, and tagged with new label `eq:bch_chart_step`. The accompanying paragraph rewritten to state explicitly: BCH correction first-order, abelian-sector exact, group-level retraction $U_i^{t+1} = U_i^t \exp(-\eta_\phi \tilde\nabla_{\phi_i}\mathcal{F})$ preserves group structure exactly. Sympy-verified by red team in `02_red_opening.md`: residual decreases linearly with eta (10x per decade), not quadratically (would be 100x per decade). Blue conceded in `03_blue_rebuttal.md`.

### D1 Edit 2 — Hoffman 2019 over-attribution at line 2751

Previous text attributed to Hoffman's Interface Theory of Perception the architectural claim "the carrier of that interface is precisely the slow-timescale generative model rather than the moment-to-moment posterior." Hoffman's actual argument is fitness-beats-truth at the species-evolutionary level; the slow-parameter vs fast-state distinction is from Friston 2017 hierarchical active inference, not Hoffman. Rewrote the paragraph to (a) retain Hoffman for the species-genome-level interface claim, (b) attribute the within-lifetime slow-parameter vs fast-state architectural separation as an internal framework commitment supported by Friston 2017, (c) explicitly disavow attributing the within-lifetime Bayesian-architectural claim to Hoffman.

### D2 Edit 1 — Single-generator postulate named at line 2826 (was unstated)

The frame decomposition at the previous Eq. 2824 introduced two generators $T_\tau, T_x$; the next equation silently collapsed to a single shared $T$. Red sympy-verified that the Lorentzian conclusion depends on this collapse: swapping $T_x$ for an admissible compact-subalgebra element $[[0,1],[-1,0]] \in \mathfrak{gl}(2,\mathbb{C})$ yields signature (-,-) not (-,+). Inserted an explicit postulate-naming sentence between the two-generator decomposition and the imaginary-postulate equation: "We further postulate that the temporal and spatial generators collapse to a single shared $T = \mathrm{diag}(1, -1)$ for concreteness, $T_\tau = T_x = T$; the case of distinct $T_\tau, T_x$ requires separate treatment and is not adopted here. The signature conclusion of this worked example depends on this single-generator choice..."

### D2 Edit 2 — Real-part projection reframed as rank-changing at line 2842

Previous prose said the real-part projection "sets $G^{\mathrm{Lor}}_{\tau x} = 0$" — phrased as if discarding an off-diagonal imaginary piece. Under the single-generator collapse the unprojected complex bilinear form has rank one and vanishing determinant; the real-part projection changes the rank from one to two. Added the determinant calculation $G_{\tau\tau} G_{xx} - G_{\tau x}^2 = -4(\partial_\tau\psi_\tau)^2(\partial_x\psi_x)^2 + 4(\partial_\tau\psi_\tau)^2(\partial_x\psi_x)^2 = 0$ and the explicit phrase "The real-part projection is therefore a rank-changing operation on the bilinear form (rank one complex to rank two real), not merely a discard of an off-diagonal imaginary piece."

### D2 Edit 3 — "Three features" extended to "Four features" at line 2857

Added a fourth feature to the post-worked-example summary: "Fourth, the worked example collapses the two generators $T_\tau, T_x$ introduced at Eq.~\eqref{eq:complex_gauge_frame} into a single shared $T = \mathrm{diag}(1, -1)$; the case of distinct generators requires separate treatment and is not adopted here, and the signature conclusion is sensitive to this collapse..." Brings the post-derivation summary into alignment with the new explicit postulate at the front of the worked example.

### D2 Edit 4 — Cross-section regulator obstruction paragraph added at line 2859

Inserted a new `\paragraph{Cross-section regulator obstruction.}` paragraph between the four-features summary and the "central open question" paragraph. The Lorentzian signature lives on the frame-twist quadratic form $\mathrm{tr}(A_\mu A_\nu)$, which §sec:consensus_metric already establishes is gauge-noninvariant under local $g(c)$ and admits no regulator-free gauge-orbit average under non-compact $\mathrm{SO}(1,3)$. The signature conclusion is therefore an observable feature of the within-an-agent's-gauge-fixing pullback, not yet a gauge-invariant assertion at the multi-agent or consensus level. Closes the cross-section incoherence that red identified as the "framework places its Lorentzian signature on an object the same chapter says has no extractable gauge-invariant content" wound.

### D3 Edit 1 — Signature postulate inheritance cross-referenced at line 2980

Previous text "For human agents, we conjecture this comprises approximately 4 dimensions (1 temporal + 3 spatial)" silently inherited the indefinite-signature postulates from §sec:signature_resolution — but the spectrum of the positive semi-definite induced metric on $T_c\mathcal{C}$ cannot host a temporal direction without those postulates being in force. Rewrote the sentence to make the inheritance explicit: "For human agents, we conjecture this sector comprises approximately 4 base-direction dimensions; under the indefinite-signature postulates of Section~\ref{sec:signature_resolution} (imaginary frame component along one base direction with real-part projection, single non-compact generator, $+\mathrm{tr}(AB)$ bilinear-form sign convention), the 4-dimensional sector admits a $(1\text{ temporal} + 3\text{ spatial})$ Lorentzian decomposition. The temporal sign is not intrinsic to the positive semi-definite spectral decomposition of Eq.~\eqref{eq:induced_metric_full} alone; it is imported from the postulate set of Section~\ref{sec:signature_resolution} and inherits the open-problem status of that section."

### D3 Edit 2 — K-symbol conflation at line 3024 disentangled

Previous text "Only a tiny sliver (e.g. 4 dimensions out of K) becomes phenomenal spacetime" used $K$ (the Gaussian fiber parameter, $K = 768$ in the working implementation, $\dim(\mathcal{B}) \approx 296{,}064$) as if it were the observable-dimension bound. Per the rank theorem already stated at line 2970, the bound is $n = \dim(\mathcal{C})$, not $K = \dim$-of-fiber-parameter-space. Rewrote to disentangle the base-dimension reduction and the fiber-direction unsampled layer: "...there are two distinct senses in which most of the manifold structure remains invisible, and they should not be conflated. At the base-manifold level, only $|\mathcal{D}_{\text{obs}}|$ eigen-directions of $G_i(c)$ on $T_c\mathcal{C}$ (e.g., 4 for a human cognitive agent) appear as phenomenal spacetime; the remaining $\dim(\mathcal{C}) - |\mathcal{D}_{\text{obs}}|$ base directions carry subthreshold or internal eigenvalues. At the fiber level, the differential $d\sigma_i|_c: T_c\mathcal{C} \to T_{q_i(c)}\mathcal{B}$ samples at most $\dim(\mathcal{C})$ directions out of $\dim(\mathcal{B})$ (for the $K = 768$ Gaussian fiber, $\dim(\mathcal{B}) \approx 296{,}064$)..."

### D3 Edit 3 — "All perspectives are valid" at line 3045 narrowed to within-frame validity

Previous text concluded "...all perspectives are valid" unqualified, which is in tension with the within-species consensus framing at §sec:consensus_metric (line 2937 commits the structural-tier construction as the framework's candidate for a gauge-invariant shared geometry). Narrowed to "Each agent's perspective is internally consistent within its own gauge frame, and the structural-tier consensus construction of Section~\ref{sec:consensus_metric} (conditional on a regulator that the present manuscript does not supply) is the framework's candidate for a gauge-invariant shared geometry, to which individual agent geometries are reconciled within a cognitive species rather than absolutely. The position is therefore one of within-frame validity and within-species consensus, not unconditional relativism."

### D4 Edit 1 — Euler-Lagrange identity at line 3076 replaced with form-vs-derivation distinction

Previous text "The Euler-Lagrange equations from $\delta\mathcal{F} = 0$ become our natural-gradient flow equations" used the verb "become" — asserting identity between two systems that have different order (first-order natural-gradient flow vs second-order Euler-Lagrange) and different ontological readings (descent on a statistical manifold vs conservative dynamics on configuration manifold). Per [Amari1998 §2] natural gradient is steepest descent; per Goldstein-Poole-Safko §2.3 Euler-Lagrange from Hamilton's principle is second-order conservative dynamics. Manuscript's own §sec:mass line 1906 and §sec:gravity line 3172 confirm 3076 was the outlier. Rewrote to: "The variational principle for the free-energy functional $\mathcal{F}[q,p,\phi] = ...$ defines its critical points by $\delta\mathcal{F} = 0$. The dynamics in our framework are not the second-order Euler-Lagrange equations of Hamilton's principle, but rather the first-order Riemannian natural-gradient flow of Amari~\cite{Amari1998}: Fisher-Rao natural gradient on the Gaussian sector $(\mu, \Sigma)$ and Lie-group natural gradient on the gauge-frame sector $G$... The mathematical analogy with Hamilton's principle is one of form (both are variational principles with an extremized scalar functional) rather than of derivation (the resulting equations of motion have different order and a different ontological reading: first-order descent on a statistical manifold versus second-order conservative dynamics on a configuration manifold)."

### D4 Edit 2 — Structural-realism agnosticism at line 3092 replaced with OSR commitment

Previous text claimed agnosticism among Cassirer's neo-Kantian variant, Worrall's ESR, and Ladyman-Ross's OSR. ESR retains unobservable individuals whose intrinsic natures are inaccessible; eliminative OSR denies that there are unobservable individuals beneath structure. These positions are mutually exclusive on ontological commitments. The manuscript's own line 3090 ("information geometry... constitutes the noumenal realm") is OSR-flavored, so the "agnosticism" was undercut. Rewrote to commit explicitly: "The framework recasts this in the ontic-structural-realism family of Ladyman \& Ross~\cite{Ladyman2007} — information geometry is the structure that constitutes the noumenal realm, with no further individuals beneath it — with Cassirer's neo-Kantian variant and Worrall's epistemic structural realism~\cite{Worrall1989} as related precursors. We explicitly disavow Kant's stronger constitutive-a-priori commitment, which Section~\ref{sec:scope_limitations} addresses directly."
