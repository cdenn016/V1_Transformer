# 2026-05-18 edits

## Per-step live status on the /vfe training console

User requested that the `/vfe` training loop show real-time iterations
per second every step, not only on the `log_interval`-gated verbose
line. The verbose every-200-step format

```
Step 200/2000 | Loss: 1.4183 | CE: 6.3428 | β: 0.0939 | PPL: 568.4
```

stays as-is for high-level captures; a per-step indicator was added
on the tqdm bar so the step counter ticks live with the metrics and
rate.

### Surgery — `transformer/vfe/trainer.py::VFETrainer.train`

Three localized edits inside the `train()` method.

**1. tqdm bar configuration (lines 899-906).** Renamed
`desc="Training"` to `desc="Step"` so the bar prefix matches the user's
preferred `Step N/total` wording, and dropped `mininterval` from
`0.5` to `0.1` so the bar redraws at ~10 Hz instead of every half
second — that change is what makes the per-step ticks feel real-time.

**2. Per-step postfix (lines 938-953, newly inserted).** Immediately
after `metrics = self.train_step(batch)` and the CSV/pub-tracker
hooks, the trainer now derives `it_per_sec = 1.0 / metrics['step_time']`
(guarded against zero) and, when a tqdm bar is active, calls
`pbar.set_postfix({'loss', 'ce', 'ppl', 'it/s'}, refresh=False)`. The
postfix update is cheap — it touches only fields already in the
`metrics` dict, which `train_step` populated synchronously, so no
extra GPU→CPU sync is triggered. `refresh=False` lets tqdm's own
`mininterval=0.1` rate-limit the redraw rather than forcing one per
step. `β` is deliberately excluded from the postfix because
`_attention_summary()` does a per-block `.tolist()` GPU sync (see
`trainer.py:230-`); calling it every step would add 2-3 `cudaSync`
points per iteration, defeating the purpose of a real-time rate
display.

**3. Verbose log line (lines 958-969).** Appended `| it/s: {it/s:.2f}`
to the every-`log_interval` message so the captured high-level lines
also carry the rate. β stays on this line — that single
`_attention_summary()` call every 200 steps was already paid for.

### Runtime trace under the user's config

User's active config is `transformer/vfe/train_vfe.py` line 124,
`'log_interval': 200`. Trace:

- `train_vfe.py:175` — `cfg = VFEConfig(**config)` (log_interval=200).
- `train_vfe.py:192` — `trainer.train(num_steps=cfg.max_steps)` with no
  `log_interval` kwarg, so the resolution at `trainer.py:861`
  (`log_interval = log_interval or self.cfg.log_interval`) picks up
  200.
- `trainer.py:899` — `if _HAS_TQDM:` is True (import smoke confirms
  `tqdm` is importable in the project's env), so `pbar` is a real
  tqdm bar.
- `trainer.py:914` — `for step in _iter:` iterates the tqdm bar,
  which ticks per step.
- `trainer.py:944` — `if pbar is not None:` is True, so
  `pbar.set_postfix` fires every step. The bar redraws at
  `mininterval=0.1` cadence.
- `trainer.py:958` — every 200 steps still emits the full
  `Step N/T | Loss: ... | CE: ... | β: ... | PPL: ... | it/s: ...`
  line via `_write` (tqdm.write), printed above the bar so the bar
  redraws cleanly underneath.

### Verification

- `python -c "import ast; ast.parse(open(...).read())"` — syntax OK.
- `python -c "import transformer.vfe.trainer"` — imports cleanly,
  `_HAS_TQDM = True`.
- No new dependencies introduced; uses only fields already populated
  by `train_step`'s metrics dict.

### Non-tqdm fallback

When tqdm is unavailable, `pbar is None`, so the `set_postfix` block
is skipped entirely (no fallback per-step print added — flooding the
console at 2000+ lines per run would defeat the purpose). The
augmented verbose every-`log_interval` line still fires in both
paths.

---

## Follow-up: full formatted line on the bar every step

User clarification: the per-step `set_postfix` (loss/ce/ppl/it/s only)
was still considered "only updating every log interval" because the
full `Step N/T | Loss | CE | β | PPL | it/s` line — the format the
user actually reads — was gated by `log_interval`. The request was to
have that exact line update live every step in the same manner
`experiment_runner.py` updates its bar (the second loop at
`experiment_runner.py:2861` uses `pbar.set_description(log_msg)`,
which surfaces the formatted line directly inside the bar prefix).

### Surgery — `transformer/vfe/trainer.py::VFETrainer.train` (lines 938-967)

The previous postfix-only block was replaced. The trainer now builds
the full `Step | Loss | CE | β | PPL | it/s` message every iteration
and pushes it onto the bar via `pbar.set_description_str(msg, refresh=False)`.
`set_description_str` (not `set_description`) suppresses the trailing
`": "` that tqdm would otherwise append, so the bar reads
`... it/s: 50.00 12%|...` instead of `... it/s: 50.00:  12%|...`.
`refresh=False` keeps the redraw rate-limited by tqdm's existing
`mininterval=0.1` (~10 Hz) instead of forcing a screen redraw on every
step. The `_write(msg)` at the `log_interval` boundary is preserved
unchanged for scrollback — it now prints exactly the same string that
the bar is showing, giving a permanent record every 200 steps without
duplicating the live status.

### Cost trade

The previous postfix path deliberately excluded β because
`_attention_summary()` does a `.tolist()` sync, and the original aim
was zero per-step extra syncs. Showing the full line live requires β
every step, so this trade is now accepted: one `.tolist()` per step on
the per-block stacked stats tensor (see `trainer.py:208-251`). For
n_layers=1 this is a single sync; the trainer already issues several
host-side reads per `train_step` (grad-norm `.tolist()`, CSV writes,
publication tracker), so the marginal cost is small relative to the
existing per-step work.

### Why `set_description` and not `tqdm.write`

`tqdm.write(msg)` would scroll a new line above the bar every step —
2000 lines for a 2000-step run, defeating tqdm. `set_description` puts
the message inline as the bar's prefix, so the user sees one line that
updates in place — the live status the user asked for. The
`log_interval`-gated `_write(msg)` then converts that live status into
a scrollback record at coarse intervals.

### Runtime trace (vfe_ablation_suite.py active config)

User's working tree has both `train_vfe.py` and `vfe_ablation_suite.py`
modified. The ablation suite path:

- `vfe_ablation_suite.py:181` — `BASELINE_CONFIG['log_interval']=200`.
- `vfe_ablation_suite.py:484` — `trainer.train(num_steps=vcfg.max_steps)`
  with no `log_interval` override, so `trainer.py:861` resolves
  `log_interval = self.cfg.log_interval = 200`.
- `trainer.py:899` — `_HAS_TQDM` is True, `pbar` is real, `_write =
  pbar.write`.
- `trainer.py:914` — `for step in _iter:` ticks the bar per step.
- `trainer.py:946` (new) — `_attention_summary()` runs every step; one
  `.tolist()` sync; populates `beta_kl`.
- `trainer.py:957` (new) — `pbar.set_description(msg, refresh=False)`
  fires every step; bar's natural mininterval refreshes the display.
- `trainer.py:966` — every 200 steps `_write(msg)` still prints the
  identical line above the bar for scrollback.

### Verification

- `python -c "import ast; ast.parse(...)"` — syntax OK.
- Confirmed `_attention_summary()` is `@torch.no_grad()` and reads
  pre-populated `_last_attention` / `_last_kl_matrix` from each
  block's e_step (written unconditionally on the final E-step
  iteration, `e_step.py:599-605` and `:1294`), so calling it per step
  doesn't trigger any extra forward passes.

---

## Orphaned e_sigma_lr / e_phi_lr sweeps in vfe_ablation_suite.py

User observation: sweeping `e_sigma_lr` and `e_phi_lr` via
`vfe_ablation_suite.py` produced identical Val PPL across every value,
while `e_mu_lr` sweeps did vary as expected. The orphan warning added
in commit `39f19299` fires for the user's `BASELINE_CONFIG`, but the
sweep machinery did not act on it.

### Root cause (asymmetry between mu vs. sigma/phi)

Inside the E-step iteration body (`vfe/e_step.py:666-696`):
```
1. Compute beta, nat_grad_mu, nat_grad_sigma  (reads INPUT mu, sigma, phi)
2. mu  <- mu - e_mu_lr * nat_grad_mu          (retract mu)
3. sigma <- retract via e_sigma_lr             (retract sigma)
4. phi   <- retract via e_phi_lr               (retract phi)
```

After the loop exits with `n_e_steps=1`, the per-output downstream
consumers under BASELINE_CONFIG (`n_layers=1`, `use_prior_bank=False`,
`norm_type='layernorm'`) are:

| Output | Reaches loss via | Live? |
|---|---|---|
| mu | CE decoder: `logits = output_proj(mu)` | YES, always |
| sigma | next-iter `nat_grad_mu` / next-layer / `MahalanobisNorm` / `prior_bank.decode` | NO under all four gates |
| phi | next-iter attention / next-layer | NO under both gates |

So `e_mu_lr` changes a value the decoder reads (loss changes);
`e_sigma_lr` and `e_phi_lr` change values the decoder never reads
(autograd computes their effect on a discarded tensor; identical loss).
This is exactly the condition the `VFEConfig.__post_init__` orphan
warning detects (`config.py:368-393`).

### Fix — per-sweep `'requires'` prerequisite overrides

Two-part edit to `transformer/vfe/vfe_ablation_suite.py`.

**1. SWEEPS registry entries (~lines 211-230).** Added a `'requires'`
dict to the orphaned entries:
```python
'e_sigma_lr': {
    ...
    'requires': {'n_e_steps': 2},
},
'e_phi_lr': {
    ...
    'requires': {'n_e_steps': 2},
},
```
`n_e_steps=2` is the smallest config change that lifts both: iteration
#2's `nat_grad_mu` reads the retracted sigma, and iteration #2's
attention/transport reads the retracted phi. Other lift conditions
(`n_layers>=2`, `use_prior_bank=True`, `norm_type='mahalnorm'`) would
be larger architectural deviations from baseline.

**2. `make_run_configs` (~line 400-430).** Read the optional `requires`
key and merge it into each generated config BEFORE the swept param is
set, so the swept param always takes precedence over the prerequisite
override:
```python
requires = sweep.get('requires', {})
...
for val in _sweep_values(sweep):
    cfg = copy.deepcopy(base_config)
    cfg.update(requires)   # prerequisites first
    cfg[param] = val       # then the swept param
    ...
```
Sweeps without `'requires'` keep the BASELINE values unchanged (no
extra compute cost for sweeps that already work).

### Cost trade

`n_e_steps=2` doubles the E-step work for the two affected sweeps
(e_sigma_lr: 8 runs, e_phi_lr: 6 runs = 14 runs total). All other
sweeps remain at `n_e_steps=1`.

### Verification

Inline smoke test (no actual training run; covers the wiring only):

- `make_run_configs('e_sigma_lr', base)` -> every run cfg has
  `n_e_steps=2` and the swept `e_sigma_lr` value preserved. PASS.
- `make_run_configs('e_phi_lr', base)` -> every run cfg has
  `n_e_steps=2` and the swept `e_phi_lr` value preserved. PASS.
- `make_run_configs('learning_rate', base)` -> `n_e_steps=1` unchanged.
  PASS.
- `VFEConfig(**lifted_cfg)` -> orphan warning silenced under
  `n_e_steps=2`. PASS.
- `VFEConfig(**original_cfg)` -> orphan warning fires under
  `n_e_steps=1`, confirming the warning is the correct signal we are
  acting on. PASS.

End-to-end verification (Val PPL actually varying across the sweep)
requires re-running the sweep; user-driven, not part of this edit.

## Per-group M-step learning rates (replace `learning_rate`)

User asked for separate M-step learning rates, named in the same style
as the E-step (`e_mu_lr`, `e_sigma_lr`, `e_phi_lr`). The previous
`VFEConfig.learning_rate` was a single master scale that
`trainer.py::_build_optimizer` spread across five param groups with
hard-coded multipliers; the multipliers are now removed and each group
has its own field.

### Decisions

1. `learning_rate` removed entirely from `VFEConfig`. Five independent
   fields take its place — no master scale, no back-compat shim.
2. Naming for the catch-all bucket that holds the E-step's *learnable
   hyperparameters* (`raw_c0`, `raw_b0`, `log_kappa`,
   `_phi_preconditioner`): `m_hyper_lr`. These are M-step-trained, but
   they belong to the E-step modules — `m_hyper` is more accurate
   than `m_estep` (which would conflict with the inner-loop `e_*_lr`
   names) and broader than `m_alpha` (which doesn't cover `log_kappa`).
3. Defaults are clean values, not arithmetic preservation of the old
   `3e-4 × multiplier` products:

   | Field        | Default | Targets                                                                    |
   |--------------|---------|----------------------------------------------------------------------------|
   | `m_mu_lr`    | 1e-3    | `PriorBank.base_mu`                                                        |
   | `m_sigma_lr` | 5e-5    | `PriorBank.base_log_sigma`, `PriorBank.decode_log_scale`                   |
   | `m_phi_lr`   | 1e-4    | `PriorBank.phi_embed`, `Positional.pos_phi`                                |
   | `m_hyper_lr` | 1e-5    | `raw_c0`, `raw_b0`, `log_kappa`, `_phi_preconditioner` (when registered)   |
   | `m_other_lr` | 3e-4    | catch-all: `head_mixer.mixer_delta`, `non_flat.W_raw`, output projection   |

   The previous effective defaults (3e-4 × {3.0, 0.15, 0.3, 0.03, 1.0}
   = {9e-4, 4.5e-5, 9e-5, 9e-6, 3e-4}) are abandoned. Existing user
   configs that set `learning_rate=0.02` or `=0.05` are migrated to
   the new clean defaults — their old tuned scales are gone.

### Edits

**`transformer/vfe/config.py:189-204`.** Replaced the single field
`learning_rate: float = 3e-4` with the five fields above, each with a
trailing comment naming the parameters it touches. No `__post_init__`
changes; the optimizer-driver consumes them directly.

**`transformer/vfe/trainer.py::_build_optimizer` (lines 104-152).**
Rewrote the body. The matching rules (`'base_mu' in name`,
`'base_log_sigma' in name or 'decode_log_scale' in name`,
`'phi_embed' in name or 'pos_phi' in name`, `'e_step' in name or
'_phi_preconditioner' in name`, else) are identical to the previous
implementation — only the LR sourcing changed (`cfg.learning_rate * k`
→ `cfg.m_*_lr`). Group names changed from `prior_mu`/`prior_sigma`/
`prior_phi`/`e_step`/`other` to `m_mu`/`m_sigma`/`m_phi`/`m_hyper`/
`m_other` to match the config field names. The `lr=cfg.learning_rate`
AdamW default kwarg (which was dead code — every group sets its own
`lr`) is now `lr=cfg.m_other_lr` for the same reason: still dead, but
visually consistent with the catch-all group.

**`transformer/vfe/trainer.py::_log_to_csv` (lines 688-701).** Updated
the `lr_map` dict so the CSV column names stay backward-compatible
with downstream analysis tooling that reads `mu_embed`, `sigma_embed`,
`phi_embed`, `ffn`, `other`:

```
'm_mu'    -> 'mu_embed'
'm_sigma' -> 'sigma_embed'
'm_phi'   -> 'phi_embed'
'm_hyper' -> 'ffn'        (legacy mapping for what was 'e_step' -> 'ffn')
'm_other' -> 'other'
```

The single composite `metrics['lr']` value passed to the publication
tracker at `trainer.py:544, :739` is unchanged — it comes from
`scheduler.get_last_lr()[0]`, the first group's post-schedule LR
(now `m_mu`).

**`transformer/vfe/train_vfe.py:110`.** Replaced `'learning_rate': 0.05`
in the entry-point CONFIG dict with the five new keys at the clean
defaults listed above. User's previous `0.05` base is gone — they
re-tune from `m_*_lr` directly if desired.

**`transformer/vfe/vfe_ablation_suite.py:24, 167, 308-313, 330`.**
Four updates:
- Header comment (line 24): swept M-step LR list updated to enumerate
  `m_mu_lr, m_sigma_lr, m_phi_lr, m_hyper_lr, m_other_lr`.
- `EM_CONFIG` (line 167): `'learning_rate': 0.02` replaced with the
  five `m_*_lr` keys at clean defaults. User's tuned `0.02` master
  scale is gone (per the same migration choice as `train_vfe.py`).
- Sweep definitions (lines 308-): single `learning_rate` sweep
  (values `[0.02, ..., 0.08]`) replaced with five sweep entries
  (`m_mu_lr`, `m_sigma_lr`, `m_phi_lr`, `m_hyper_lr`, `m_other_lr`),
  each with its own value list spanning ~10x around the new
  baseline.
- `SWEEP_ORDER` (line 330): `'learning_rate'` entry replaced with the
  five new sweep names in order.

### Verification

Smoke tests covering the new wiring:

- `VFEConfig()` defaults → `m_mu_lr=1e-3, m_sigma_lr=5e-5,
  m_phi_lr=1e-4, m_hyper_lr=1e-5, m_other_lr=3e-4`; `hasattr(cfg,
  'learning_rate') == False`. PASS.
- `VFETrainer._build_optimizer()` on a minimal config — four groups
  registered (`m_hyper` filtered out because `learnable_kappa=False`
  and `E_learnable_alpha=False`), each with the expected LR after
  warmup. PASS.
- Same on a `learnable_kappa=True` config — all five groups present;
  `log_kappa` lands in `m_hyper` with `lr=1e-5`. PASS.
- `_get_learning_rates()` returns the new group names. PASS.
- `grep -r learning_rate transformer/vfe/` → only the unrelated
  `_get_learning_rates` method name remains. PASS.

Pytest run:

```
pytest tests/transformer/test_vfe_package.py
       tests/transformer/test_vfe_new_features.py
       tests/transformer/test_phi_traceless_under_projection.py
       tests/smoke_test_entropy.py
```

64 passed, 2 failed. Both failures are pre-existing assertion drift on
`VFEConfig` default values (`n_layers == 4` and
`exact_diagonal_transport is True`) and have no relation to the
learning-rate refactor — both tests construct `VFEConfig()` with no
arguments and assert on fields untouched by these edits.

## VFE theory-auditor agents — new project agents and knowledge base

User asked for a custom agent that is a resident domain expert in VFE,
active inference, the gauge VFE, information geometry, and gauge theory,
to be used both for theoretical-purity audits of the codebase and for
peer-reviewing the manuscripts in `Attention/`. Designed and built as
**two** sibling agents sharing a knowledge directory (Option B from the
brainstorming pass), rather than one agent with two modes, so that each
agent's `description` triggers cleanly under the Agent tool's routing
and the two output contracts (severity-tagged code audit vs structured
peer review with `MR-N` math-reviewer items) stay distinct.

### New files

```
.claude/agents/vfe-codebase-auditor.md        # the code-purity agent
.claude/agents/vfe-manuscript-reviewer.md     # the peer-review agent
.claude/agents/vfe-knowledge/                 # shared knowledge base
  README.md                                   # index + hard constraints from CLAUDE.md
  canonical_math.md                           # F, β, Ω, sandwich, τ=κ√K, KL forms
  em_modes.md                                 # 5-mode gradient table + vfe/ profile
  e_step_constraints.md                       # E-step hygiene, LRs, SPD retraction, IFT vs amortized
  codebase_map.md                             # where each construct lives, core/ vs vfe/ differences
  manuscript_index.md                         # per-.tex synopsis + load-bearing claims
  audit_methodology.md                        # ordered audit checklist + report format
  review_methodology.md                       # ordered review checklist + review format
  style_constraints.md                        # banned phrases, banned LaTeX, pushback rule
docs/superpowers/specs/2026-05-18-vfe-theory-auditor-agents-design.md
```

### Design choices

- **Two agents not one.** Their deliverables differ (audit report vs
  peer review) and their tool surfaces differ (the manuscript reviewer
  needs `WebFetch` / `WebSearch` for citation verification; the
  auditor needs `Bash` for running targeted pytest invocations). One
  agent doing both forced an awkward branching prompt; splitting
  cleaned that up.
- **Sidecar knowledge files instead of inline.** Keeps each agent body
  small (persona + workflow + output contract), lets both agents share
  the same canonical math, and lets the user edit knowledge without
  redeploying the agent prompts.
- **Model `opus`** on both. Math verification and manuscript review
  benefit from the strongest model; 1M-context Opus lets the
  manuscript reviewer hold the canonical math + the relevant code +
  the manuscript section without summarization loss.
- **`Skill` listed in both agents' tools** so they can call `sympy`,
  `peer-review`, `literature-review`, `arxiv-database`,
  `math-skills/symbolic-computation-guide`, and
  `topology-geometry-guide` when warranted.
- **The CLAUDE.md pre-fix protocol is baked into the auditor workflow.**
  Step 2 of every audit is "open the user's actual entry point, trace
  every relevant config key through the loader and any override
  logic, state the resolved values in the report preamble" — failing
  to do this is what produces wrong findings.
- **Pushback rule encoded in both agents.** From CLAUDE.md:
  "Maintain position under pushback — ask 'What am I missing?'
  rather than capitulating." Both agent prompts explicitly tell the
  agent not to retract a finding backed by `canonical_math.md`
  evidence just because the user expresses doubt.
- **Banned Claude-isms apply to the agents' own output** too — not
  just to the manuscripts they review.

### Invocation

```
Agent(
  description="Audit free-energy assembly in vfe/block.py",
  subagent_type="vfe-codebase-auditor",
  prompt="Audit transformer/vfe/block.py for free-energy assembly. "
         "My active config is the dict at the top of "
         "transformer/vfe/train_vfe.py. Focus on whether the "
         "tau*beta*log(beta/pi) entropy term is present and whether "
         "covariance transport uses the sandwich product. Report under "
         "300 words."
)
```

```
Agent(
  description="Peer-review GL(K)_attention.tex",
  subagent_type="vfe-manuscript-reviewer",
  prompt="Peer-review Attention/GL(K)_attention.tex end to end. "
         "Apply the equation-by-equation check against canonical math, "
         "the manuscript-vs-code consistency check, and the style scan. "
         "Use MR-N numbering for math-reviewer items to match the commit "
         "history convention."
)
```

### What this does not do

- Does not auto-commit fixes. Both agents emit reports; the user
  decides what to do.
- Does not duplicate `code-reviewer`, `debugger`,
  `refactoring-specialist`, or `performance-engineer`. The auditor
  defers to those for non-theoretical concerns.
- Does not honor `em_mode` switching in the `transformer/vfe/`
  package — the agent knows about the hardwired profile and flags
  divergence rather than papering over it.

### Known limitations to monitor

- Knowledge files are a snapshot of the codebase as of 2026-05-18.
  When `vfe/e_step.py` or `core/block_config.py` change in ways that
  affect the canonical math, `canonical_math.md` and `em_modes.md`
  need updating. The agents are explicitly told to read the *current*
  code, not just the knowledge files, so they should detect drift —
  but the knowledge files themselves can become stale.
- The `Skill` tool may not be available in the agent's tool surface
  on every platform; the auditor falls back to in-prompt math
  derivation if `sympy` is unavailable.

## VFE theory-auditor agents — source-of-truth orientation flipped

User clarified the design after the initial build: the agents must not
treat the user's own CLAUDE.md, codebase, or manuscripts as sources of
truth. They must treat the **standard tools and math of the relevant
fields** as canon — Friston's free energy principle, Amari & Nagaoka's
information geometry, Nakahara / Kobayashi-Nomizu / Frankel for
differential geometry and gauge theory, Vaswani for transformer
attention, Bai-Kolter-Koltun for IFT gradients, Amari for natural
gradient, Absil-Mahony-Sepulchre for manifold optimization — and
evaluate the user's work against that canon. This is what makes them
peer reviewers rather than internal-consistency checkers.

### New files

```
.claude/agents/vfe-knowledge/external_bibliography.md
.claude/agents/vfe-knowledge/external_canon_math.md
.claude/agents/vfe-knowledge/external_canon_inference.md
.claude/agents/vfe-knowledge/external_canon_transformers.md
```

The `external_canon_math.md` file covers information geometry (Fisher
metric, Cencov uniqueness, KL closed form for Gaussians, dual
connections, natural gradient), differential geometry (smooth
manifolds, fiber bundles, connections, parallel transport), and gauge
theory (principal bundles, the **standard sandwich identity**
`T → ρ(g)ᵀ T ρ(g)` for bilinear forms under change of frame from
Nakahara §10.3 and Kobayashi-Nomizu Vol. I §III, Lie algebra
exponential and BCH non-commutativity, holonomy, gauge invariance vs
equivariance distinction, standard pitfalls).

The `external_canon_inference.md` file covers the three equivalent
forms of standard variational free energy from Friston 2010, the
standard expected free energy decomposition (epistemic + pragmatic)
from Parr-Pezzulo-Friston 2022, the ELBO from Blei et al. 2017,
mean-field vs structured vs amortized q-families, the standard EM
algorithm vs Bai-Kolter-Koltun 2019 IFT vs amortized inference, KL
asymmetry and forward-vs-reverse mode-seeking distinction, predictive
coding correspondence under Gaussian/linear assumptions only, and the
contested status of NESS/Markov-blanket FEP.

The `external_canon_transformers.md` file covers the standard scaled
dot-product attention from Vaswani 2017 (with the variance argument
for `1/√d_k` from §3.2.1), multi-head structure, the standard
positional encodings (sinusoidal, RoPE from Su 2024), causal masking
as additive bias, layer normalization (Ba-Kiros-Hinton 2016),
geometric deep learning (Bronstein 2021, Cohen-Geiger-Kohler-Welling
2018, group-equivariant networks), alternative interpretations of
attention (kernel from Tsai 2019, Hopfield from Ramsauer 2021,
predictive coding from Millidge 2021), retractions and SPD geometry
from Absil-Mahony-Sepulchre 2008, log-Cholesky and Bures-Wasserstein
metrics, natural gradient on Lie groups, and **the explicit warning
that the Killing form is sign-indefinite on non-compact gl(K)**, so
that any use of Killing preconditioning on the user's gauge frames
needs separate justification.

### Rewrites

- `README.md` — rewrote to flip the source-of-truth principle. Now
  explicitly distinguishes external-canon files (sources of truth)
  from user-claim files (the thing being evaluated). Project policies
  (no NN, no CLI, click-to-run pattern, code-focus rule) are
  preserved but framed as *project policy*, not as theoretical canon.
- `audit_methodology.md` — added a new "source-of-truth rule" preamble
  and a new finding-classification scheme (standard-consistent,
  standard-with-implementation-drift, novel-correctly-implemented,
  novel-incorrectly-implemented, claimed-standard-actually-novel).
  Reports now require an explicit "Standards against which the audit
  was performed" section citing from `external_bibliography.md`, and
  every finding has a "Standard treatment" line with `[Source]` tag.
- `review_methodology.md` — added the (S)/(R)/(N)/(I) claim
  classification scheme. Reviews now require a "Standards against
  which the manuscript was reviewed" section and a "Novel-construction
  inventory" section. Every finding is grounded in a cited external
  source.
- `vfe-codebase-auditor.md` — rewrote the persona to emphasize the
  expertise areas (with named external references for each), made
  the source-of-truth rule the first substantive section. New "Hard
  rules" section lists standard facts from the external canon (BCH
  non-commutativity, sandwich identity, IFT requires implicit linear
  system, etc.) rather than internal CLAUDE.md rules.
- `vfe-manuscript-reviewer.md` — parallel rewrite for the reviewer.
- The five user-claim files (`canonical_math.md`, `em_modes.md`,
  `e_step_constraints.md`, `codebase_map.md`, `manuscript_index.md`)
  got new opening paragraphs flagging them as descriptions of user
  claims to be evaluated, NOT as canon. `canonical_math.md` has a
  misleading filename (kept for continuity with the prior build) but
  is explicitly relabeled in its header as "summary of user claims."

### Why the flip matters

The first build had the agents check internal consistency between the
user's code and the user's manuscripts. That is useful but it is not
peer review — a manuscript can be internally consistent with its
codebase and still wrong against the standard literature. The flipped
design lets the agents produce findings like "the user's free energy
extends Friston's standard F with a multi-agent coupling structure
that is not in the standard FEP literature; the manuscript should
label this as a novel construction and justify it independently,
rather than presenting it as derived from FEP." That kind of finding
is the load-bearing content of peer review.

The flip does not change the project-policy constraints (sandwich
product, E-step blindness, no NN, etc.) — those remain. But the
agents now cite *Nakahara* for the sandwich, *Friston* for the E/M
separation, *Vaswani* for the √d_k scaling, etc., rather than citing
CLAUDE.md.

### Spec note

The original design spec at
`docs/superpowers/specs/2026-05-18-vfe-theory-auditor-agents-design.md`
predates the source-of-truth flip and is now partially out of date in
its description of the knowledge base. The agents themselves and the
README inside the knowledge directory are the authoritative current
description.

---

## Deep audit follow-through — fixes for all 34 confirmed findings

Report at `docs/audits/audit-2026-05-18-vfe.md` produced 34 CONFIRMED
findings (1 REFUTED-partial, the `is_identity` test-coverage claim).
This pass applies every CONFIRMED fix.

### Skipped, with reason

- **4B `_attention_summary()` every step.** Intentional — the live-tqdm
  β display in the earlier section of this same dated doc deliberately
  pays one `.tolist()` per step to surface β on the bar.
- **4C `_compute_gradient_norms()` every step.** Load-bearing for
  `_log_to_pub_tracker` — gating it would silently emit zero norms to
  the publication figure tracker on non-log steps.
- **4D `block_exp_pairs` recomputed inside `_update_phi`.** Structural
  autograd-graph isolation requirement (the phi leaf must be a fresh
  detached node for `torch.autograd.grad`). Documented at the call
  site.
- **4E non-flat Omega computed twice.** Same structural reason as 4D —
  the detached monitor pass and the autograd pass operate on different
  leaves.
- **3D `triangle_holonomy_norm` test-only utility.** Used by
  `test_vfe_new_features.py`; not orphan. No change.
- **3E `is_identity()`.** Already REFUTED-partial: tests call it.

### Applied

**`transformer/vfe/config.py`** —
- 5E: `__post_init__` rejects negative `m_*_lr` (zero allowed, for
  deliberate group freezing).
- 1C: removed dead `non_flat_tile_size` field, then *restored* it as a
  no-op-reserved field after the user's intentional revert of
  `train_vfe.py` kept the key in the click-config (avoiding TypeError
  on construction).

**`transformer/vfe/e_step.py`** —
- 3I: hoisted `import torch.nn.functional as F` to module top; removed
  three inline `import torch.nn.functional as F_fn`.
- 3F: extracted `_get_alpha_c0(self) -> Optional[Tensor]` helper used
  by both the phi-mode `forward()` and `_forward_omega_direct()` hoist
  blocks (was duplicated four-line pattern).
- 1A/2B/4A: gated `_f_monotone_step` on
  `(self.monitor_monotonicity or self.track_layer_diagnostics)` at
  both call sites (phi-mode `:569` and omega-direct `:1263`).
  `monitor_monotonicity=False` now actually disables the .item()
  syncs; `track_layer_diagnostics=True` still pulls f_history into
  `_last_diagnostics` for the regression test.
- 2A: detach `sigma_p = priors.sigma.detach()` in
  `_forward_omega_direct` (was the only path missing the defensive
  detach; phi-mode already had it).
- 3C/5F: removed unused `alpha_c0_full` parameter from `_iter_nonflat`
  signature + caller.
- 5C: `_update_phi` `kappa` annotation widened to
  `Optional[torch.Tensor | float]`.
- 5D: `_iter_nonflat` return type uses
  `List[Tuple[Tensor, Tensor]]` for the omega_pairs element (was bare
  `list`); `block_exp_pairs` param annotated as
  `List[Tuple[Tensor, Optional[Tensor]]]` (was bare `list`).
- 4G: single-shot `phi.norm(dim=-1)` shared across the three
  `phi_norm_{mean,std,max}` diagnostics (was three separate norms).

**`transformer/vfe/trainer.py`** —
- 2C: removed unreachable `or '_phi_preconditioner' in name` clause
  from the optimizer routing (the buffer never appears in
  `named_parameters()`).
- 3A: deleted dead `_get_learning_rates()` method. Wired up
  `_log_vfe_dynamics_to_csv` at the `train_step` log site so the
  pre-collected VFE-dynamics columns (sigma_q stats, prior-belief KL,
  phi norms, alpha stats, kappa stats) actually reach the CSV
  instead of being silently dropped.

**`transformer/vfe/non_flat.py`** —
- 2D/5H: declare `_W_cache_key: int = -1` and `_W_cache:
  Optional[Tensor] = None` in `__init__`; `W()` cache check now also
  verifies `cached.device == self.W_raw.device` so a `.to(device)`
  call invalidates correctly.
- 5B: `compute_kl_attention_pairwise` `kappa` annotated as
  `"float | torch.Tensor"` (was bare).
- 4H: folded `Omega_h ** 2` into the einsum operand list
  (`'bijkl,bijkl,bjl->bijk'`) so the squared-Omega temporary is no
  longer materialized.

**`transformer/vfe/prior_bank.py`** —
- 1B: removed the `cfg.mu_init_std > 0` auto-rescale at `:109`.
  `mu_init_std=0` now produces a literal zero-variance prior — the
  ablation sweep's `mu_init_std=0` data point is no longer mislabeled.
- 2E: removed all three `torch.amp.autocast('cuda', enabled=False)`
  guards. The downstream explicit `.float()` casts already override
  AMP; the hardcoded `'cuda'` device-type string was the fragile part.
- 4I: pre-built `arange(V)` as `_all_token_ids` buffer in `__init__`
  so `decode()` no longer rebuilds the index tensor on every cold
  cache hit.

**`transformer/vfe/attention.py`** —
- 5A: `compute_kl_attention` `kappa` annotation widened to
  `"float | torch.Tensor"` (matches actual callers which pass
  `effective_kappa: Tensor | float`).

**`transformer/vfe/head_mixer.py`** —
- 5G: `type_to_slices` annotated as
  `OrderedDict[str, List[Tuple[int, int, int]]]` to match the
  three-element tuple actually appended at `:97`; `_type_slices`
  attribute annotated as `Dict[str, List[Tuple[int, int, int]]]`
  (was bare `dict`).

**`transformer/vfe/positional.py`** —
- 3G: resolved `lie_compose_bch_general_torch` at `__init__` and
  raise `ImportError` immediately when `bch_order > 1` but
  `math_utils` is unavailable. Removed the runtime silent-fallback
  path that would have trained on first-order additive composition
  for the entire run while only warning once.

**`transformer/vfe/vfe_ablation_suite.py`** —
- 3B: removed the duplicate trailing
  `use_autograd_mu_sigma`/`use_equivariant_head_mixer`/
  `gauge_covariant_ridge` triple (was set in two locations in the
  same dict literal — silent last-write-wins).
- 1C: removed the `'non_flat_tile_size': 0` line.
- 3H: added explicit `'include_attention_entropy': True` adjacent
  to `lambda_soft` so the relationship (entropy-on means
  `lambda_soft` is unused) is visible.
- 3J: `'alpha_divergence': 1.0` (was int `1`).

**`transformer/vfe/train_vfe.py`** — initial cleanup applied; user
intentionally reverted to keep their own values (custom `m_mu_lr=0.2`,
the duplicate triple at bottom, `'alpha_divergence': 1` int,
`'non_flat_tile_size': 0`, no `include_attention_entropy` key). Not
re-applying — user choice per the post-edit notification.

### Verification

```
pytest tests/transformer/test_vfe_package.py
       tests/transformer/test_vfe_new_features.py
       tests/transformer/test_phi_traceless_under_projection.py
       tests/smoke_test_entropy.py --no-header -q --tb=line
```

64 passed, 2 failed. Same pre-existing pair as before any audit fix:
`TestVFEConfig.test_construction_defaults` (asserts `n_layers == 4`,
default is `1`) and `TestExactDiagonalTransport.test_config_default_true`
(asserts `True`, default is `False`). Neither failure relates to any
audit finding — assertion drift on `VFEConfig` defaults that predates
this session.

Additional smoke:
- `VFEConfig()` default works; the new `m_*_lr` positivity check
  raises on negative inputs and accepts zero.
- `VFEConfig(learnable_kappa=True)` registers `log_kappa` in the
  `m_hyper` optimizer group as expected.
- Schedule advances all five groups together via cosine+warmup; no
  group is dropped.

## VFE theory-auditor agents — two more added (analyst + cross-manuscript consistency)

User asked which other bespoke agents would be worth building. The
recommendation was three: an experiment analyst, a cross-manuscript
consistency checker, and a reproducibility tracer. User picked the
first two; this section documents the build.

### New files

```
.claude/agents/vfe-experiment-analyst.md
.claude/agents/vfe-cross-manuscript-consistency.md
.claude/agents/vfe-knowledge/analysis_methodology.md
.claude/agents/vfe-knowledge/diagnostics_glossary.md
.claude/agents/vfe-knowledge/consistency_methodology.md
.claude/agents/vfe-knowledge/notation_dictionary.md
```

Knowledge base went from 13 files to 17. `README.md` index updated to
list the four methodology files (one per agent) and to add the new
"when to read what" rows.

### `vfe-experiment-analyst`

Reads training output (CSV from `MetricsTracker`, sibling
system-info JSON with git commit, scaling-fit JSONs, `PowerLawFit`
outputs from `transformer/analysis/scaling_stats.py`). Identifies
anomalies and classifies them as **expected dynamics** (cite the
standard reference for the expectation), **implementation suspect**
(defer to `vfe-codebase-auditor`), **theoretical drift** (defer to
`vfe-manuscript-reviewer`), or **genuine result** (recommend next
experiment). Recommends experiments with explicit falsification
criteria and seed counts. Never declares significance from a single
seed.

The agent's knowledge base entry `diagnostics_glossary.md` was
written from the actual code in `transformer/analysis/` and
`transformer/training/metrics_tracking.py` — `HolonomySnapshot`
fields, `PowerLawFit` structure, `PublicationMetrics::record_*`
methods, etc. — not from invented metrics. The agent is told to
confirm each diagnostic still exists (via Grep) before citing it.

Standard interpretive priors are calibrated against the external
canon: β-entropy collapse cites Vaswani 2017's saturated-softmax
discussion, holonomy non-decrease cites Nakahara's flatness ⇔ zero
curvature, convergence-plateau cites Bai-Kolter-Koltun 2019 as
invalidating IFT-claimed gradients, geodesic deviation cites Amari
1998 natural gradient.

### `vfe-cross-manuscript-consistency`

Walks all `.tex` in `Attention/` simultaneously. Six-phase scan:
**notation drift** (`\newcommand` / `\providecommand` definitions
across manuscripts), **equation drift** (sandwich `Σ → Ω Σ Ωᵀ`,
transport factorization, attention form, F with entropy term, `τ =
κ√K`), **citation drift** (same paper with different bibkeys),
**`MR-N` status** (math-reviewer items), **style drift**
(per-manuscript counts of banned Claude-isms and forbidden LaTeX
macros), and **narrative consistency** (claim/scope/limitation
alignment across the set).

The `notation_dictionary.md` knowledge file already documents three
real drifts found via grep at build time:
- `\KL` is `\operatorname{KL}` in the GL(K) pair but `\mathrm{KL}`
  in `belief_inertia_unified.tex` and `Participatory_it_from_bit.tex`.
- `\softmax` has the same split.
- `\Tr` vs `\tr` (capitalization) differs between the GL(K) papers
  and `belief_inertia_unified.tex`.

These three drifts are listed in the dictionary as starting evidence
for the agent — when invoked, the agent will detect them again
(and any others that have appeared) and emit a structured drift
report with recommended unifications.

### Design notes shared with the first two agents

Same source-of-truth principle: external canon (Friston, Amari,
Nakahara, Vaswani, Bai-Kolter-Koltun, Kaplan) is canon; the user's
prior runs and prior manuscripts are *data*, not canon. Same citation
hygiene rule: tag at source level, do not append unverified
section/equation numbers. Same Glob-then-Read pattern for knowledge
discovery. Same opus model. Same `Skill` listed in tools so the
agents can invoke `hypothesis-generation`, `pymc`,
`scientific-visualization`, `sympy`, `scientific-writing` when
warranted.

### Memory entry updated

`~/.claude/projects/.../memory/project_vfe_theory_auditor_agents.md`
expanded from listing two agents to four. Total knowledge-file count
updated from 13 to 17.


## Multi-agent peer review of `GL(K)_attention.tex` + `GL(K)_supplementary.tex`

User requested an `ultrathink` multi-agent review of the two GL(K)
manuscripts and then, after the verifier report came back, issued
"fix all". Six parallel `vfe-manuscript-reviewer` agents (A–F)
covered the main paper §1–3, §4, §5–7 and the supplementary
appendices A–B, C–D, E–H respectively; a verifier pass spot-checked
the 15 most load-bearing findings with independent sympy/numpy and
WebSearch. Outputs in `Attention/REVIEW_GLK_2026-05-18/`:
six reviewer reports, two verification scripts, the verifier
synthesis, and a bulk-style strip script.

### Math corrections

**Supp §B.1 Hessian (line ~373).** The Σ-Hessian of the Gaussian KL
divergence was previously written as `Σ_1^{-1}⊗Σ_1^{-1} + Σ_2^{-1}⊗Σ_2^{-1}`.
Sympy verification (`verifier_hessian_check.py`) shows the Hessian
depends ONLY on Σ_1 (the trace term is linear in Σ_1, so its second
derivative w.r.t. Σ_1 vanishes). Corrected to
`½ Σ_1^{-1} ⊠_sym Σ_1^{-1}` and rephrased the conclusion to claim
*consistency* with the variational dynamics rather than asserting
emergence-by-contraction.

**Supp App C Eqs. 530, 534.** Right-trivialised `dexp_φ` was being
used as if it were the Fréchet derivative D(exp). Numerical check
(`verifier_dexp_check.py`) showed the RHS as written was factor-of-2
off in the simplest non-commuting case. Rewrote the derivatives in
terms of `Q_a Ω_{ij}` (consistent right-trivialised form) and added
an explanatory remark identifying the identity
`D(exp)[T_a] = Q_a exp(φ)` and the equivalent Fréchet form.

**App H Conditional Uniqueness Theorem.** Theorem statement was
missing the real-analyticity hypothesis on `f`. Added
"real-analytic on (0,∞)" to the hypothesis list and replaced the
hand-wave "ranges over all of R^+" step in the proof with an
explicit appeal to analytic continuation from an open subinterval.

**App F RG conjecture.** Clauses (i) and (iv) of the universality
conjecture contradicted each other: (i) claimed `g_1^* = 0` is a
fixed point while (iv) asserted `Σ_A = σ²I + Var_A(μ)` is anisotropic
even at `g_1 = 0`. Split the anisotropy into intrinsic `g_1^(orig)`
and emergent `g_1^(emer)` channels; clause (i) now ranges over the
intrinsic-channel coordinates while clause (iv) describes the
non-zero floor in the total channel. Also resolved the `y_3` ambiguity
(`−1` for the linear holonomy norm vs `−2` for the squared action),
and propagated the same split to the main manuscript §6.4.

### Citation hygiene

- Added `xiao2024efficient` (Xiao et al., ICLR 2024) for the
  attention-sink claim at line 2094.
- Added `rossmann2002lie` (Rossmann, Oxford 2002).
- Renamed citation keys `gallier2020differential` →
  `Gallier2020`, `higham2008functions` → `Higham2008` to match the
  existing bib entries.
- Replaced fabricated `shen2008coarse` (real authors and journal
  did not match — verified via WebSearch and philsci-archive) with
  the standard coarse-graining citation `kadanoff1966scaling`
  alongside the already-cited `wilson1974renormalization`.
- Replaced fabricated `garciaMillan2024network` (the real
  García-Millán & Pruessner paper is the 2023 PRL on
  Gell-Mann–Low criticality, not a Nature Physics network-renorm
  paper) with `garciaperez2018multiscale` (García-Pérez,
  Boguñá, Serrano, Nature Physics 14:583, 2018).

### Reproducibility and honesty

- Table 1: `GL(10) K=90` row updated from `74.9` (single seed) to
  `76.4 ± 1.05 (n=2)` with footnote pointing at the CSV.
- Main §5.3 prose updated to report the `76.4 ± 1.05`/`75.5 ± 0.44`
  pair, and the `74.9 → 71.6` improvement narrative replaced with
  `76.4 → 71.6`.
- The `1.66×` headline now carries a `17.7×` parameter-overhead
  disclosure plus the param-equalized comparison (`1.87×` and
  `1.91×` against the 9.1M/9.2M-param ablation rows).
- BERT validation `bar r = 0.804` is now presented alongside the
  hierarchical posterior mean `0.867` and HDI `[0.808, 0.915]` so
  the slight HDI-exclusion of the raw mean is explicit.
- App E.9 Bayesian section now discloses the `keynorm` chain's
  weaker mixing on `σ_β` (`r-hat = 1.09, ESS_bulk = 18`) and its
  propagation to Cohen's d (`r-hat = 1.02, ESS_bulk = 44`); the
  `halflife_doublings` posterior is now reported with its
  posterior sd of 164 doublings.

### Theorem hygiene and disclosures

- Theorem `thm:vanishing_holonomy` downgraded to Lemma "Cocycle
  Identity for Vertex-Frame Transport" and the surrounding prose
  reframed as a defining property of the parameterization, not a
  derived dynamical statement.
- Main §4.3 line 1632 now commits to the Cartan-involution-modified
  bilinear form for the natural-gradient metric on `gl(K)`, noting
  that the Killing form is degenerate on the centre `R·I`.
- §4.4 RoPE subsection now discloses the abelianness of
  `SO(2)^{d_k/2}` and the resulting sign-convention compatibility.
- Supp App A.1 now opens with an explicit "Bundle triviality"
  paragraph stating that the manuscript's `Ω_{ij} = exp(φ_i)exp(-φ_j)`
  encodes a globally trivial principal G-bundle, and the
  Čech-cocycle apparatus is retained only because the edge-relaxed
  extension promotes it to a non-trivial bundle.
- Supp App G now reads "six overlapping agents" everywhere (was
  "8 overlapping agents" in the body while figure captions said
  "six").
- Main §6.5 Limitations now lists: (a) the prior-bank decode that
  subsumes the residual linear output projection, (b) the opt-in
  `connection.py` MLP transport variant, (c) the
  diagonal-σ/RoPE/MahalanobisNorm gap, (d) the single-seed `GL(15)`
  and `n=2` `GL(10)` seed disclosure.
- Supp §B.1 prose now explicitly notes the derivation is conducted
  in the entropy-suppressed surrogate (canonical F differs by the
  `τβ log(β/π)` term whose Σ-gradient vanishes).
- Table 1 status column gained `D^♯` for the
  `W_Q W_K^⊤ = σ^{-2} Ω^{-⊤}` row (derived up to non-uniqueness),
  and the `μ-Euler step ↔ residual connection` row was reclassified
  `S` (structural — any Euler discretization has residual form).

### Missing figure files

Copied two K=90 figures from the headline checkpoint
`transformer/checkpoints_publication/137-72_no_resid-phi=0.0025/`
into `Attention/figs/`:
- `training_curvesk=90.png` (504 KB)
- `train_val_gapk=90.png` (151 KB)

(The latex `\includegraphics{attention/figs/training_curvesk=90.png}`
references at line ~2087 of the main file now resolve.)

### Style cleanup

`strip_spacing.py` removed 441 occurrences of banned `\;`, `\,`, `\!`
spacing macros (277 main + 164 supp). Verified zero remain in either
file. Multi-line `\\` row breaks (table/array rows) untouched.

### CLAUDE.md update

The stale pointer "Manuscript line 1261 explicitly distinguishes the
canonical F from the 'entropy-suppressed surrogate'" was incorrect —
line 1261 of `GL(K)_attention.tex` is a `Q_i K_j^⊤` identity.
Updated to point at lines 766, 855, and 1354 (where the canonical
vs surrogate distinction is actually made) and added a pointer to
the supplementary §B.1 disclosure.

## Participatory_it_from_bit.tex — Phase 1 audit fixes (citation hygiene)

A four-reviewer + one-verifier deep audit of `Participatory_it_from_bit.tex`
was conducted on 2026-05-18 (reports in
`REVIEW_PARTICIPATORY_IFB_2026-05-18/review_*.md` and
`verifier_report_2.md`). Phase 1 addresses the four lowest-risk,
verifier-confirmed citation/attribution issues with no math or
structural impact.

### Phase 1.1 + 1.2 — Lloyd2002 / Wheeler / Jacobson1995 / VanRaamsdonk2010

`Participatory_it_from_bit.tex` §2534 (in §Time as Information Flow).

The aphorism "time is what prevents everything from happening at once"
was attributed to Lloyd2002. The verifier (using arXiv abstract +
Quote Investigator) confirmed the aphorism is Wheeler-popularised
(origin Cummings 1922); Lloyd's actual 2002 PRL is about the
universe's computational capacity. The manuscript's own §3020 already
attributes the aphorism to Wheeler, so §2534 was internally
inconsistent.

Jacobson1995 was characterised as deriving spacetime structure
"from entanglement entropy". Jacobson1995 actually derives the
Einstein field equations from horizon-area entropy plus the local
Clausius relation. The entanglement-spacetime correspondence is later
work (Van Raamsdonk 2010, Jacobson 2016). VanRaamsdonk2010 was
already in `references.bib`.

The line-2534 paragraph was rewritten to:

- Attribute the Wheeler aphorism correctly (via `Wheeler1990`).
- Restate Lloyd's actual content (computational-capacity bound on
  temporal evolution).
- State the Jacobson1995 mechanism correctly (horizon-area entropy
  plus Clausius).
- Add Van Raamsdonk 2010 as the entanglement-spacetime reference.

### Phase 1.3 — Page-Wootters 1983 + Connes-Rovelli 1994 added

Both reviewers and the verifier flagged Page-Wootters 1983 and
Connes-Rovelli 1994 as the canonical emergent-time constructions in
the external literature; their absence was a major related-work gap
for a manuscript with an emergent-time thesis. Added two `@article`
entries to `references.bib` under the existing
`COMPUTATIONAL & RELATIONAL TIME` section (between `Lloyd2002` and
`Rovelli2004`):

- `PageWootters1983` — Phys. Rev. D 27, 2885 (1983),
  "Evolution without evolution".
- `ConnesRovelli1994` — Class. Quant. Grav. 11, 2899 (1994), the
  von Neumann modular flow = thermal time construction.

Both are now cited in the rewritten §2534 paragraph, with one
sentence distinguishing the present construction (temporal parameter
on the agent's belief trajectory) from each external proposal (clock
subsystem, modular flow, horizon thermodynamic state).

### Phase 1.4 — Lahav-Neemeh framing softened

`Participatory_it_from_bit.tex` §3417 (in
§Gauge-Theoretic Realisation of the Cognitive-Frame Transformation
Law).

The manuscript described Lahav-Neemeh's two perspectives as related
by "an isomorphism". Per primary-source verification by the
Discussion/Appendices reviewer (fetched
`pmc.ncbi.nlm.nih.gov/articles/PMC9255957/`), Lahav and Neemeh do
not supply an explicit transformation law: their formalization
indicates a correspondence between the two perspectives but uses a
placeholder, not a formal isomorphism. The verifier confirmed the
primary source uses both Alice/Bob and Alice/ALICE (human/zombie AI)
notations.

Rewrote §3417 to:
- Note the published paper presents the thought experiment with both
  Alice/Bob and an Alice/ALICE variant.
- State that the explicit transformation law is "indicated rather
  than supplied" in the published formalization (correspondence
  asserted, but no explicit map given).

Aligned downstream wording in the §3422 ("Recovering Alice/Bob as a
derived special case") and post-equation paragraph: "the structural
map Lahav and Neemeh assert" → "the structural map ... that Lahav
and Neemeh leave as a placeholder in the published formalization";
"single-scale Alice/Bob isomorphism" → "single-scale Alice/Bob
correspondence"; "posited isomorphism" → "posited correspondence".
This brings §3414-3438 into a single consistent reading where L&N
posit a correspondence whose explicit transformation law is
unsupplied, and the present paper supplies that law as a
gauge-transport composition.

### Compile verification

`pdflatex` followed by `bibtex` resolved all three new citation keys
without warnings (`PageWootters1983`, `ConnesRovelli1994`,
`VanRaamsdonk2010` confirmed in `Participatory_it_from_bit.bbl`).
Pre-existing unicode-ε error at `references.bib:227`
("The renormalization group and the ε expansion") is unrelated to
Phase 1 edits.

## Participatory_it_from_bit.tex — Phase 2 audit fixes (disclosure / caveat propagation)

Phase 2 addresses the four single-sentence-insert disclosure gaps
flagged by the four reviewers and confirmed by the verifier
(`verifier_report_2.md`). Each is a propagation of a caveat that
already appears elsewhere in the manuscript but had been dropped at
the locus where the affected claim is operationally invoked.

### Phase 2.1 — (S)→(N) disclosure at §1019 (Theory M1)

`Participatory_it_from_bit.tex` §1019 (Multi-Agent Extension: From
Single Inference to Collective Dynamics).

The opening of §1019 read as if the multi-agent free energy
functional is a natural extension of Friston's single-agent F. The
disclosure that the inter-agent KL coupling
$\sum_{ij}\beta_{ij}\mathrm{KL}(q_i\|\Omega_{ij}q_j)$, the
attention-entropy term $\tau\beta\log(\beta/\pi)$, and the
model-channel analogue with $\gamma$ are not in the standard FEP
literature was buried in the §1029 Mixture-of-Sources subsection
(at §1034).

Promoted a one-sentence (S)$\to$(N) disclosure to the head of §1019
naming the three novel additions, citing `Friston2010`/`Parr2022` as
backbone, and cross-referencing the §1034 status note. The FEP
citations are retained as the conceptual scaffolding (variational
inference, ELBO bound, complexity/accuracy decomposition) but the
multi-agent functional is now explicitly identified as an engineered
consensus energy.

### Phase 2.2 — κ disclosure at canonical reduction §1797-1808 (Theory M4)

`Participatory_it_from_bit.tex` §1799-1810 (Temperature scaling and
Complete Attention Formula in the transformer reduction) and the
manuscript abstract.

The §1242 statement of the pointwise free energy already factorises
$\tau = \kappa\sqrt{K}$ with $\kappa$ a learnable scalar. At the
canonical transformer reduction, however, §1804 read
$\tau = \sqrt{d_k}$ unconditionally and the boxed formula at §1808
reproduced Vaswani's $\mathrm{softmax}(QK^\top/\sqrt{d_k})V$ as if
the recovery were exact.

Three coordinated edits restore the $\kappa$ disclosure at the
canonical reduction:

1. §1799-1810 Temperature scaling now factorises
   $\tau = \kappa\sqrt{d_k}$, with $\kappa = 1$ explicitly noted
   as the case that recovers `Vaswani2017`. The boxed equation is
   rewritten as
   $\beta_{ij} = \mathrm{softmax}_j(Q_i K_j^\top/(\kappa\sqrt{d_k}))$.
2. §1830-1838 Complete Attention Formula boxed equation rewritten
   with $\kappa$ in the denominator and a trailing sentence
   "with $\kappa = 1$ recovering the standard scaled dot-product
   attention of `Vaswani2017`".
3. Abstract (line 54) extended: "up to a separately introduced
   learned bilinear compatibility $M$, the standard normalisation
   and bias assumptions, \emph{and a learnable temperature scalar
   $\kappa$ on top of the dimensional $\sqrt{d_k}$ scaling (with
   $\kappa = 1$ recovering `Vaswani2017`)}".

These three are coordinated so the reader cannot read the abstract,
the temperature paragraph, or the boxed final formula without
seeing the $\kappa$ qualifier.

### Phase 2.3 — Regulator caveat at §3175 and §3345 (Disc M3, partial)

`Participatory_it_from_bit.tex` §3175 and §3345.

The consensus-metric regulator caveat is honestly flagged at §2880,
§3083, and §3169 but was dropped at §3175, §3324, and §3345 where
the consensus-equilibrium argument is invoked operationally.

Added a sentence to the end of the §3175 within-species paragraph
explicitly noting that the within-species alignment recovered there
is contingent on the same regulator condition as the consensus
pullback metric of `sec:consensus_metric`, and that the within-species
reading inherits the conditional status.

Added a sentence to the end of the §3345 "physics is objective in
the sense that it achieves robust consensus" paragraph cross-
referencing `sec:consensus_metric` and noting that the
cognitive-shareability reading of objectivity inherits the
regulator-conditional status.

The §3325 paragraph also needs the regulator caveat, but is held to
Phase 3 because the same paragraph requires the Disc M2 declarative-
to-interpretive wording revision; doing both edits in one pass
avoids a half-revised paragraph.

### Phase 2.4 — Tighten four declarative (I) sentences (Spec M7)

`Participatory_it_from_bit.tex` §2700, §3001, §3024, §3046.

Four sentences in the Speculative Extensions section dropped the
conditional qualifier that the surrounding paragraphs already use,
making them read as factual identifications rather than
interpretive claims. Per the Spec reviewer's explicit recommended
edits (review_speculative_extensions.md §M-Spec7) each sentence
received the smallest prefix needed to restore the conditional
voice:

- §2700 (formerly line 2701): "there is no agent-independent
  metric on $\mathcal{C}$" $\to$ "within the manuscript's
  interpretive frame and conditional on the regulator caveat of
  Section~\ref{sec:consensus_metric}, there is no agent-independent
  metric on $\mathcal{C}$" (the in-tension-with-§2880 finding).
- §3001 (formerly line 3003): prefix "Under this framework's
  interpretive commitment," added to "what we call 'physical
  reality' is always phenomenal reality...".
- §3024 (formerly line 3026): prefix "Within the framework, where
  information is primal," added; trailing parenthetical added
  ("outside this framework's interpretive commitment, the
  identification is structural rather than derived") with a
  cross-reference to `sec:phenomenological_interpretation`.
- §3046: prefix "Under this framework's interpretive frame,"
  added to the "What we call 'mass'/'distance'/'time'" triple;
  the three sentences are now joined into one sentence under
  that single qualifier so the conditional voice carries through.

### Compile verification

Full `pdflatex; bibtex; pdflatex; pdflatex` cycle completes with
zero "Citation undefined" or "Reference undefined" warnings. The
pre-existing unicode-$\varepsilon$ at `references.bib:227` and the
missing-figure warnings for `Fig_*.png` are unchanged (pre-existing,
not introduced by Phase 2).

## Participatory_it_from_bit.tex — Phase 3 audit fixes (wording / labelling)

Phase 3 addresses the five wording-level findings flagged across the
four reviewers and verifier-confirmed (T-M2, T-M5, T-M6, D-M2, D-M4).
These are not single-sentence inserts; each requires rewriting one
or more sentences in place to bring the local prose in line with
disclosures the manuscript already makes elsewhere.

### Phase 3.1 — Cross-scale shadow "theorem" → "structural commitment" (Theory M2)

`Participatory_it_from_bit.tex` §541 and §910.

Eq.~\eqref{eq:cross_scale_shadow} at §535-540 *defines* the prior
$p_i^{(s)}$ as the gauge transport of the meta-agent's posterior
$q_I^{(s+1)}$. The matched-bundle property that beliefs and priors
share the latent-state manifold is then trivially true by
construction, but the manuscript was repeatedly calling this a
"theorem" (at §541: "matched-bundle property of
Section~\ref{sec:working_framework} is a theorem rather than a
simplifying assumption"; at §910 "both as theorems following from
the cross-scale shadow relation").

Reworded §541 to: "Because the prior $p_i^{(s)}$ is defined as the
gauge transport of the meta-agent's posterior $q_I^{(s+1)}$, beliefs
and priors share the latent-state manifold $\mathcal{B}_{\mathrm{state}}$
by construction... This is a structural commitment of the framework
rather than a theorem of standard hierarchical variational inference:
in the standard scheme [Friston2017,Parr2022] the level-$\ell$ prior
is derived from a generative-model conditional $p(s_\ell\mid s_{\ell+1})$,
not posited as a transported posterior, and we do not display the
reduction (or approximation) of the standard hierarchical scheme to
the present cross-scale shadow construction."

Reworded §910 to: "both \emph{by construction} from the cross-scale
shadow relation ... the shared-manifold property is therefore a
structural commitment of the framework, not a derivation."

Theory reviewer's M2 prescription (verbatim wording) was followed,
including the explicit contrast with the standard hierarchical
scheme.

### Phase 3.2 — Mass analogy stiffness/inertia duality at §2011 (Theory M5)

`Participatory_it_from_bit.tex` §2011 (Within-Framework
Interpretation: Stiffness as Precision).

The mass analogy was already flagged at §1847 and §2027 with the
correct stiffness-postulate framing, but the local §2011-2022 reading
used "effective mass", "bare mass", "inertia" matter-of-factly,
making the local reading sound like a derivation of inertial mass.

Per Theory reviewer's M5 prescription, added one sentence to §2011:
"We reuse $M_i$ in Eq.~\eqref{eq:effective_mass} as both the
stiffness (Hessian of $\mathcal{F}$ at the consensus point) and the
inertia (kinetic-metric coefficient, postulated separately in
Section~\ref{sec:velocity_quadratic}); without that additional
kinetic-metric postulate, $M_i$ is only a stiffness on belief
configuration space, and the 'mass' terminology in the equation
labels and surrounding prose should be read as a shorthand for the
dual stiffness/inertia reading rather than as a derivation of
inertial mass."

The equation label `eq:effective_mass` was left unchanged (changing
the label would have broken downstream cross-references at minimal
informational benefit; the sentence-level disclosure carries the
substance).

### Phase 3.3 — Killing form restriction at line 2030 (Theory M6)

`Participatory_it_from_bit.tex` line 2030.

The line at the end of the kinetic-form construction wrote
$\langle\dot\phi,\dot\phi\rangle_{\mathfrak{g}} = -\mathrm{tr}(\dot\phi^2)$
without flagging that this is positive definite only for compact
$G$; the manuscript discloses the non-compact case correctly at
§2099 and Eq.~\eqref{eq:gauge_natural_gradient_def} (the
position-dependent right-invariant form of
Eq.~\eqref{eq:pullback_metric}).

Inserted parenthetical: "(valid as a positive-definite bi-invariant
inner product for compact $G = \mathrm{SO}(K)$; for the general
$\mathrm{GL}(K)$ case the Killing form on $\mathfrak{gl}(K)$ is
indefinite and the kinetic-metric is supplied instead by the
position-dependent right-invariant form of
Eq.~\eqref{eq:pullback_metric}, as stated at
Eq.~\eqref{eq:gauge_natural_gradient_def})."

### Phase 3.4 — §3325, §3343, §3501 declarative → interpretive (Disc M2 + bundled M3 at §3324 and §3501)

`Participatory_it_from_bit.tex` §3325, §3343, §3501, and the
addition of `\label{sec:knowledge_collective_fem}` at §3454.

The §3288 "metaphysical interpretation, not a derivation"
disclaimer was contradicted at §3325 ("revolutionary
reinterpretation that gauge invariance is not a property of the
noumenal substrate...") and at §3343 ("physics is a theory of
language and informational compatibility rather than a description
of external substance"); both sentences read as theses asserted,
not as readings consistent with the §3288 disclaimer.

Per Disc M2 prescription:

- §3325 first sentence rewritten to: "This reading is consistent
  with --- but does not establish --- the interpretation that
  observed gauge invariance reflects the structure of human
  collective cognition rather than the structure of the noumenal
  substrate $\mathcal{C}$." The remaining downstream sentences of
  §3325 reframed under "On this reading,..." and bundled into a
  single qualified sentence. The Disc M3 regulator caveat was
  added to the end of the paragraph cross-referencing
  Section~\ref{sec:consensus_metric}.
- §3343 first sentence rewritten to: "On this reading, physics
  functions as a constitutive constraint on shareable description
  in roughly Friedman's sense of a relativized a priori
  (Section~\ref{sec:knowledge_collective_fem} below); whether it
  also constitutes a theory of external substance is not
  adjudicated here." The remaining two sentences of §3343
  reframed to "Physical laws read this way as..." and "Gauge
  invariance, ... then appear in this reading as..."
- §3501 (Disc M3 regulator caveat): cross-reference to
  Section~\ref{sec:consensus_metric} added at "scientific
  consensus" with the regulator-conditional note.
- Added `\label{sec:knowledge_collective_fem}` at the
  "Philosophy of Science: Knowledge as Collective Free Energy
  Minimization" subsection header (§3454) to support the new
  cross-reference from §3343.

### Phase 3.5 — Transformer identification softened at §3245 / §3251 (Disc M4)

`Participatory_it_from_bit.tex` §3245 and §3251.

The verbs "reveals" (§3245) and "represent" (§3251) presented an
interpretive (I) identification as if it were a derivation. Disc
M4 specifically called out the parallel with the previous round's
M3 on §1597 / §3271 and noted that multiple equally rigorous
readings of attention exist (kernel-method, modern-Hopfield,
predictive-coding).

- §3245: "The transformer derivation reveals that standard
  transformers are degenerate gauge-theoretic systems" $\to$
  "The transformer derivation supports a framework-internal
  reading in which standard transformers can be interpreted as
  degenerate gauge-theoretic systems ...; the identification is
  one interpretive lens among several equally rigorous readings
  of attention (kernel-method, modern-Hopfield, and
  predictive-coding interpretations are alternative readings of
  the same architecture)." Added citations
  `tsai2019transformer`, `ramsauer2021hopfield`,
  `millidge2021predictive` (lowercase bibkeys as they appear in
  `references.bib`).
- §3251: "Standard transformers represent the zero-dimensional,
  gauge-fixed, single-scale limit" $\to$ "Under this reading,
  standard transformers can be interpreted as the
  zero-dimensional, gauge-fixed, single-scale limit ...; the
  identification is interpretive rather than a derivation that
  selects the present framework over alternative readings of
  attention." Subsequent sentences reframed under "on this
  reading,...".

### Compile verification

Full `pdflatex; bibtex; pdflatex; pdflatex` cycle: zero
"Citation undefined" / "Reference undefined" warnings after the
two-pass bibtex+pdflatex needed for the new
`sec:knowledge_collective_fem` label. Pre-existing
unicode-$\varepsilon$ and missing-figure warnings unchanged.

## Participatory_it_from_bit.tex — Phase 4 audit fixes (structural / numerical)

Phase 4 addresses the four structural/numerical findings flagged by
the Speculative-Extensions and Implementation/Results reviewers and
verifier-confirmed (Spec M4, Spec M6, Impl M4, Impl M8 + M3).

### Phase 4.1 — GL(K,C) "necessity" softened (Spec M4)

`Participatory_it_from_bit.tex` line 2851 (Temporal Structure and
the Signature Problem).

Manuscript claimed "GL(K,$\mathbb{C}$) extension is necessary but
not sufficient" for Lorentzian signature. The Spec reviewer's
sympy verification confirmed that mixed compact + non-compact real
generators in $\mathfrak{gl}(K, \mathbb{R})$ with a real frame can
also produce indefinite signature under the $+\mathrm{tr}(AB)$
convention; the necessity claim therefore depends on the
single-generator assumption of the worked example and is too strong
in general.

Rewrote the paragraph to: "under the single-generator assumption of
Section~\ref{sec:worked_signature} ... $\mathrm{GL}(K, \mathbb{C})$
extension is sufficient ... We do not claim $\mathrm{GL}(K,
\mathbb{C})$ is necessary in any stronger sense: an alternative
route in which a frame contains both compact and non-compact real
generators in $\mathfrak{gl}(K, \mathbb{R})$ also produces
indefinite signature under the $+\mathrm{tr}(AB)$ convention ... ".

### Phase 4.2 — ±2 → ±1 normalization disclosed (Spec M6)

`Participatory_it_from_bit.tex` line 2795 (just after boxed
Lorentzian metric `eq:lorentzian_metric`).

The boxed metric coefficients $\pm 2$ are artefacts of the
unnormalised generator $T = \mathrm{diag}(1, -1)$ with
$\mathrm{tr}(T^2) = 2$. Under the standard normalisation
$\mathrm{tr}(T^2) = 1$ the coefficients become $\mp 1$. The
construction therefore only fixes a conformal class (signature and
relative sign are intrinsic; overall scale tracks the normalisation
choice).

Added one sentence stating this explicitly and flagging the
conformal-class qualifier.

### Phase 4.3 — F-test result reported alongside bootstrap CI (Impl M4)

`Participatory_it_from_bit.tex` §2508 (Multi-Seed Scaling
Validation body text), figure caption at §2513, and abstract.

The Impl reviewer's M4 (sympy-independent F-test on the released
CSV) gave $F(1, 8) = 9.73$, $p = 0.014$, which rejects $b = -1$ at
$\alpha = 0.05$ on the eleven per-$K$ seed means. The bootstrap
percentile CI brackets $-1$ at the upper edge (a near-miss), so the
two natural testing procedures give different verdicts on the same
data. The manuscript previously reported only the convenient CI
criterion ("$b$ is statistically indistinguishable from $-1$").

Three coordinated edits:

1. §2508 body: replaced "$b$ is statistically indistinguishable
   from $-1$ within the present sweep; a restricted two-parameter
   fit ... We therefore report $b \approx -1$ as a within-architecture
   empirical observation rather than as a statistically distinct
   exponent" with explicit disclosure of both criteria
   (bootstrap CI vs nested $F$-test) and their disagreement; report
   $b \approx -1.05$ (95\% CI $[-1.10, -1.00]$) as the point
   estimate; note that the restricted $b = -1$ model also fits well
   but is rejected under nested-$F$.
2. Figure 1 caption: replaced "so $b$ is statistically
   indistinguishable from $-1$ within this sweep" with the
   bootstrap-CI-vs-$F$-test disagreement.
3. Abstract (line 54): replaced "so the exponent is statistically
   indistinguishable from $-1$ within the present sweep" with
   "the two natural tests of $b = -1$ disagree at this sample size
   of eleven $K$ points ... rejecting $b = -1$ at $\alpha = 0.05$
   ... we report $b \approx -1.05$ as the point estimate".

The three loci now consistently flag the criterion disagreement
rather than selectively reporting only the convenient framing.

### Phase 4.4 — $\Gamma$-coherence overclaim + simulator transport claim (Impl M8 + M3)

`Participatory_it_from_bit.tex` §2113 (Meta-Agent Detection) and
§2216 (Top-Down Participation Implementation Note).

Impl M8: §2113 claimed "$\Gamma$ tracks the same coherence factor
that controls the savings" — overstating the heuristic relation
between the discrete detector $\Gamma = P \cdot C_q \cdot C_s$ and
the continuous variational criterion. Rewrote the sentence to
explicitly call the relation "heuristic and partial rather than a
derivation"; clarified that the variational savings contain
coherence and size factors separately whereas the detector bundles
them in a single product; explicitly stated that the detector form
is not derived from the variational criterion even in the
high-coherence limit.

Impl M3: §2216 Implementation Note asserted "Our implementation
uses direct prior assignment $p_i \leftarrow \Omega_{i,I}[q_I]$";
the released transformer code at `transformer/vfe/stack.py:75-110`
implements a cross-layer prior handoff that is an identity-copy
with damping (not a multi-scale $\Omega_{i,I}$ transport), and the
multi-agent simulator code that the §2216 description applies to
is not released. Rewrote the Implementation Note to: (1) describe
the construction as the intended (rather than asserted-realised)
behaviour, (2) explicitly state that simulator-realised vs
frame-trivial-substitute is not independently verified in the
manuscript with the simulator code release deferred
(cross-reference to Section~\ref{sec:methods_metagent}), and (3)
warn the reader that the transformer codebase referenced in the
abstract is a separate code path with its own cross-layer handoff
and is not the simulator implementation of §2216.

### Compile verification

Full `pdflatex; bibtex; pdflatex; pdflatex` cycle: zero
"Citation undefined" / "Reference undefined" warnings. The new
cross-reference `sec:methods_metagent` resolves cleanly (label
existed at §3603). Pre-existing unicode-$\varepsilon$ and
missing-figure warnings unchanged.

---

## Fix: `std(): degrees of freedom is <= 0` warning in /vfe E-step diagnostics

### Reported symptom

During a /vfe training run with `use_prior_bank=True`, the console
emits three PyTorch UserWarnings on every validation block:

```
e_step.py:837: UserWarning: std(): degrees of freedom is <= 0.
e_step.py:859: UserWarning: std(): degrees of freedom is <= 0.
e_step.py:868: UserWarning: std(): degrees of freedom is <= 0.
```

### Root cause

PyTorch's default `torch.std()` uses Bessel correction
(`correction=1`). When the input has `numel == 1`, the reduction
factor (`input.numel() / output.numel()`) is 1, the correction
matches it, the variance denominator is zero, and PyTorch emits
the warning above (this was added in PyTorch 2.x; pre-2.x silently
returned NaN).

The three call sites all live in the `track_layer_diagnostics`-gated
block in `VFEEStep.forward` (`transformer/vfe/e_step.py:827-868`):

| Line | Expression                       | Tensor shape         |
|------|----------------------------------|----------------------|
| 837  | `beta.std().item()`              | `(B, N, N)`          |
| 859  | `phi.norm(dim=-1).std().item()`  | `(B, N)`             |
| 868  | `kl_qp.sum(-1).std().item()`     | `(B, N)`             |

A second omega-direct E-step path
(`e_step.py:1552`, `beta_det.std().item()`) has the same shape and
the same vulnerability.

These three diagnostics collapse to `numel == 1` precisely when
`B == 1 and N == 1`. That happens at exactly one place in the
trainer:

- `trainer.py:1099` — `_generate_sample()` calls
  `model.generate(prompt_ids=torch.tensor([[prompt_id]], …))`.
- `model.py:292-305` — the first iteration of the generate loop
  feeds `ids_cond` with `shape (1, 1)` into `self.forward(ids_cond)`.
- `block.py:140` — `e_step(beliefs, priors, mask, …)` runs the
  iterative E-step on those `(1, 1, K)` beliefs.
- `e_step.py:827` — the final-iter diagnostics dict is populated
  because `track_layer_diagnostics=True` in the user's config
  (`train_vfe.py:130`).
- Lines 837 / 859 / 868 / 1552 see `numel == 1` tensors.

The warning fires regardless of `use_prior_bank` — both the
prior-bank decode and the legacy embedding decode reach the same
E-step. The user's framing reflects their current config; the bug
is not prior-bank specific.

### Surgery — `transformer/vfe/e_step.py`

Four `.std()` calls switched to `.std(unbiased=False)` (equivalent
to `correction=0`, population std):

1. Line 841 (was 837) — `beta.std()` in the main E-step diagnostic
   dict.
2. Line 862 (was 859) — `phi.norm(dim=-1).std()` in the same dict.
3. Line 873 (was 868) — `kl_qp.sum(-1).std()` for the per-dim KL
   prior-belief gap.
4. Line 1556 (was 1552) — `beta_det.std()` in the omega-direct
   E-step diagnostic dict.

`unbiased=False` is not just a warning-silencer. It is the
*semantically correct* choice for these diagnostics: they describe
the population of the current batch×seq grid, not a sample from a
hypothetical larger population. For `numel == 1` it returns `0.0`,
which is the correct population standard deviation of a single
observation.

`sigma.std().item()` at `e_step.py:858` is **not** changed — the
covariance tensor has shape `(B, N, K)` with `K = embed_dim = 20`,
so the reduction can never collapse to `numel == 1`. Same reasoning
for `c0.std()`, `b0.std()`, `alpha_at_zero.std()` in
`trainer.py:340-343` (all shape `(K,)`).

### Surgery — `transformer/vfe/trainer.py`

A secondary bug surfaced during this investigation. The
`_generate_sample()` call at the validation block overwrites every
block's `e_step._last_attention` and `_last_diagnostics` with the
single-token-generate state. The β heatmap save at
`_plot_attention_patterns(step+1)` was reading those overwritten
tensors, not the val-batch β.

Fix: reorder so `self._plot_attention_patterns(step + 1)` runs
*before* `self._generate_sample()`. The console-output ordering is
preserved (the val log block prints between
`_plot_attention_patterns` and the post-block `_write("")`).
The previous comment block claiming "β is fresh because evaluate()
just ran the model forward" was correct in intent but wrong about
the actual execution order; replaced with a comment explaining the
constraint:

```python
# Attention summary AND heatmap save MUST happen before
# _generate_sample(): model.generate() runs the model with
# (B=1, N=1) for each new token, overwriting every block's
# _last_attention / _last_diagnostics with single-token state.
# Reading them after generate yields stale tensors, not the
# val-batch β the heatmap is meant to show.
```

`_last_diagnostics` pollution from `_generate_sample` is structurally
benign for the metrics CSV — the next training step's
`train_step()` always re-runs the model forward (overwriting
`_last_diagnostics`) before `_collect_e_step_diagnostics` reads it,
so no stale value lands in the CSV. The only consumer that read
after `_generate_sample` was the heatmap, which this fix moves.

### Verification

`python -c "import ast; ast.parse(open('transformer/vfe/e_step.py').read()); ast.parse(open('transformer/vfe/trainer.py').read())"` — both files
parse clean.

Targeted repro under `python -W error::UserWarning`:

```
OLD repro confirms warning: std(): degrees of freedom is <= 0.
FIX (unbiased=False): no warning, value = 0.0
```

i.e. the user's exact warning fires on the pre-fix path and is
silenced on the post-fix path, with `std(0)` returning the
mathematically defined population std for `numel == 1`.

### Files touched

- `transformer/vfe/e_step.py` — 4 `.std()` calls switched to
  `.std(unbiased=False)`, with explanatory comments at the first
  call site.
- `transformer/vfe/trainer.py` — reordered
  `_plot_attention_patterns(step + 1)` to run before
  `_generate_sample()` inside the validation block; replaced the
  outdated "β is fresh" comment with an accurate explanation.

## Φ/Φ̃ bundle-morphism disclosure pass — Participatory_it_from_bit.tex

Followup audit (parallel investigators + verifier per CLAUDE.md
"deploy parallel investigation agents first and then a separate
'verifier'") on whether `Participatory_it_from_bit.tex` covers the
user's framework commitment to cross-bundle morphisms
`Φ: E_model → E_state` and `Φ̃: E_state → E_model` for the
separate-associated-bundle case, and to cross-scale connections.

### Findings (verifier-confirmed)

**Cross-scale connections — substantive and adequately disclosed.**
Downward map `eq:cross_scale_shadow` (L535); upward `Λ^{s→s+1}` named
L883 and constructed as a gauge-covariant variational barycenter at
L2078–2090; unification `Ω_{i,I} = exp(φ_i^{(s)}) exp(-φ_I^{(s+1)})`
at L885. The 2026-05-18 first-pass downgrade from "theorem" to
"structural commitment" (L541) was the right call. Three honest gaps
remain — all already flagged in-text: no reduction to standard
hierarchical VI displayed (L541), Yang-Mills `F^{cross}_{μν}` not
developed (L887), BCH-approximation issue for non-compact `GL+(K)`
(L2130).

**Φ/Φ̃ morphisms — sketched at PDF depth, not deeper.** Three
mentions: speculative passage L889, summary L999, one-clause caveat
L4276. The canonical free-energy functional `eq:free_energy_functional_final`
(L1250–1261) contains **no Φ-coupled KL term**. Belief and model
channels couple only through (i) the shared structure group `G`
(with `Ω_{ij}` and `\tilde\Omega_{ij}` acting as the same group
element in two representations), and (ii) within-channel cross-scale
shadows. The early-formulation `cognitive physics.pdf` is no more
substantive on this point — it lists Φ/Φ̃ as items 7,8 in a numbered
list of 8 morphisms, gives a degenerate commutativity diagram
(`π_q ∘ Φ = π_p ∘ Φ̃` reduces to `π_C = π_C`), and explicitly sets
Φ = Φ̃ = identity in all simulations.

**Verdict.** The manuscript is under-claiming, not over-claiming.
One small honesty gap: the Matched-Bundles subsection at L916–920
never explicitly states that `eq:free_energy_functional_final` contains
no cross-bundle morphism term, so a casual reader could infer that
Φ enters somewhere downstream. It does not.

### Edits applied (disclosure-only, "Option A")

**1. L889 (Hierarchy of Transport Operators).** Sharpened the
speculative caveat from "mathematically natural but not yet essential
for our dynamics" to "mathematically natural but do not enter the
canonical free-energy functional `\eqref{eq:free_energy_functional_final}`
of this paper" — removes ambiguity about whether Φ appears in the
working dynamics.

**2. L920 (Matched Bundles).** Appended one sentence to the
matched-bundles paragraph stating that
`eq:free_energy_functional_final` contains no cross-bundle morphism
`Φ` or `Φ̃` coupling model and belief channels; the two channels
couple only through (i) the shared structure group `G` (with
`Ω_{ij}` and `\tilde\Omega_{ij}` acting as the same group element in
two representations, cross-referencing `sec:gauge_group_choice`) and
(ii) the within-channel cross-scale shadows
of `eq:cross_scale_shadow`. The cross-bundle morphisms anticipated at
`sec:transport_hierarchy` are reserved for future development.

**3. L877 (Hierarchy of Transport Operators subsection).** Added
`\label{sec:transport_hierarchy}` to support the cross-reference
introduced in edit 2.

### Why "Option B" (add a Φ-coupled KL term as labelled future-work
expression) was rejected

A `KL(s_i ‖ \tilde\Phi[q_i])` belief→model term is mechanically an
EM-style "memory consolidation" feedback. In the present functional,
`q` already informs `s` through the existing M-step gradient on `F`
(the chain `KL(q ‖ p)` with `p` derived from `s` upstream lets
gradients flow back to `s`). Adding `KL(s ‖ \tilde\Phi[q])` would be
a second, more direct `q → s` channel that partially double-counts
the existing EM coupling unless `λ_{\tilde\Phi}` is tuned to subtract
what the implicit coupling already does. That is presumably why
neither the PDF nor the manuscript actually instantiates the term in
their F — both stop at naming Φ. Disclosure (Option A) closes the
honesty gap without committing to an under-justified extension term.

### Build verification

`bibtex` + two `pdflatex` passes report zero new undefined-reference
or undefined-citation warnings introduced by these edits. The
pre-existing `Fig_*.png` "file not found" warnings and the
pre-existing unicode-ε in `references.bib:227` are unchanged and
documented elsewhere as carry-over issues.

### Files touched

- `Attention/Participatory_it_from_bit.tex` — three edits
  (L877 add label, L889 wording, L920 append disclosure sentence).
