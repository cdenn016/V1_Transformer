# 2026-05-19 — Overnight multi-agent deep audit and fix pass

## Scope and method

Per user request: deploy multiple VFE agents to deeply audit
`GL(K)_attention.tex`, `GL(K)_supplementary.tex`, and
`Participatory_it_from_bit.tex`, verify findings with adversarial verifier,
then address concerns. Per CLAUDE.md agent policy: parallel investigators
first, then verifier.

Four investigators ran in parallel:
- `vfe-manuscript-reviewer` on `GL(K)_attention.tex` + supplementary
- `vfe-manuscript-reviewer` on `Participatory_it_from_bit.tex`
- `vfe-cross-manuscript-consistency` on the GL(K) ↔ Participatory pair
- `vfe-codebase-auditor` on four manuscript-vs-code claims

Reports landed at:
- `Attention/REVIEW_GLK_2026-05-18/reviewer_FRESH_2026-05-18-night.md`
- `Attention/REVIEW_PARTICIPATORY_IFB_2026-05-18/reviewer_FRESH_2026-05-18-night.md`
- `Attention/REVIEW_PARTICIPATORY_IFB_2026-05-18/cross_consistency_FRESH_2026-05-18-night.md`
- `Attention/REVIEW_PARTICIPATORY_IFB_2026-05-18/code_audit_FRESH_2026-05-18-night.md`

Then independent verifier (orchestrator + numpy/sympy) checked the top
new math-load-bearing claims (Killing-form indefiniteness on sl(3); RoPE
abelian-collapse claim; Q_a left/right trivialisation; broken `\ref`).
All four verified.

## Triage outcome

Of the 17 prior Participatory verifier findings, 14 already FIXED by today's
earlier Phase 1-4 + Φ/Φ̃ passes; 2 still-present (I-M1 detector form,
I-M7 ablation absence) need author judgment; 1 minor still-present (S-M5
Wick comparison) is mechanically fixed here.

Of the 15 prior GL(K) verifier findings, 13 FIXED; 1 partial regression
(M-B-3 → N-1 supplementary Killing-form), 1 partial author-judgment
(M-F-2 graph-RG sign framing). The N-1 regression is fixed here.

7 new GL(K) findings (N-1 through N-7), 10 new Participatory findings
(F1-F10), 5 new cross-consistency findings (E8 Critical, N11, NC5, NC6,
E9). All verified findings addressed below, except where flagged as
author decision.

## Mechanical fixes applied this pass

### `Attention/GL(K)_attention.tex`

- **L677**: Replaced broken `Appendix~\ref{app:model_channel}` with
  `Supplementary Appendix~\ref{app:model_channel_supp}`, AND appended
  the canonical-vs-reduced-F bridge paragraph identifying the present
  form as the envelope/reduced specialisation of the companion paper's
  symmetric two-channel form, AND added the Φ/Φ̃ no-coupling disclosure
  matching `Participatory_it_from_bit.tex:920`. Fixes Critical E8,
  Major NC5, Major N10/NC6 in one block.
- **L972**: Replaced second broken
  `Appendix~\ref{app:model_channel}` → `Supplementary
  Appendix~\ref{app:model_channel_supp}`. Fixes Critical E8 second site.
- **L512, L1730, L2215, L2271**: Four stale
  `Theorem~\ref{thm:vanishing_holonomy}` → `Lemma~\ref{thm:vanishing_holonomy}`
  cross-references (the labelled object is `\begin{lemma}` at L640 after
  the prior downgrade, but the prefix word stayed "Theorem"). Fixes
  N-3.
- **L855**: Inserted `\tau = \kappa\sqrt{K}` factorisation sentence after
  `eq:free_energy_final` to match the canonical CLAUDE.md form and the
  companion paper's `Participatory:1244`. Fixes E4/N10.
- **L1847**: Rewrote the false abelianness-collapse claim
  "`abelianness collapses both forms to exp(φ_j − φ_i)`" with the
  correct transpose-equivalence statement and the
  `Q^T M K = K^T M^T Q` absorption argument. Verified numerically:
  `exp(φ_i)exp(−φ_j)` and `exp(−φ_i)exp(φ_j)` are transposes/inverses,
  not equal. Fixes N-4 (regression introduced by today's M-B-4 patch).
- **L2058**: Polar-decomposition surjectivity prose tightened to
  existential-claim form with explicit witness `(φ_i ∈ Sym, φ_j ∈ so)`
  and non-injectivity remark; cited `\citep{Higham2008}` for matrix-log
  of SPD. Fixes N-7.

### `Attention/GL(K)_supplementary.tex`

- **L658**: Internal-consistency follow-up of N-1 rewrite. Item (i) of
  the natural-gradient enumeration was renamed from "the *Killing
  form* natural gradient" (which contradicted the corrected App.~C.3
  text two pages earlier) to "the *Cartan-involution-modified*
  natural gradient", with a cross-reference back to App.~C.3 for the
  positive-definiteness argument.
- **L580-588 (App C §3)**: Full rewrite. The prior text falsely claimed
  the actual Killing form of `gl(K)` is "positive definite on sl(K)".
  Numpy verification: `B(skew, skew) = -12` (negative) and
  `B(sym, sym) = +12` (positive) on `sl(3)`; the Killing form is
  indefinite on `sl(K)` for K≥2, NEGATIVE on `so(K)` and POSITIVE on
  `Sym_0(K)`. New text distinguishes (i) the genuine Killing form
  `B(X,Y) = 2K tr(XY) − 2 tr(X)tr(Y)` (indefinite) from (ii) the
  Cartan-involution-modified form `g(X,Y) = −½ B(X, θ(Y))` with
  `θ(X) = −X^⊤` (positive-definite on `sl(K)`, the one actually used
  in this paper). Citation: `\citep{Hall2015,Gallier2020}`. Fixes N-1
  (regression vs main-paper L1632 which had the correct treatment).
- **L597**: "Ψ is the inverse Bernoulli function" → "Ψ(z) = (e^z-1)/z
  is the generating series for dexp; its multiplicative inverse
  z/(e^z-1) is the Bernoulli generating function". The original name
  was misleading: Ψ does not generate Bernoulli numbers; `z/(e^z-1)`
  does, and appears in `dexp^{-1}`/Magnus. Citation `\citep{Hall2015}`.
  Fixes N-5.
- **L648-656 (App D dexp closed form)**: Renamed `Q_a` → `Q_a^L`
  throughout to disambiguate from `Q_a^{(i)}` (right-trivialised) in
  App C. Added one explicit sentence relating the two via the Ad
  identity `R · exp(φ) = exp(φ) · L` with `L = Ad_{exp(−φ)}(R)`.
  Numpy-verified both right- and left-trivialised closed forms
  reproduce the numerical Fréchet derivative to 1.4e−9. Fixes N-2.

### `Attention/Participatory_it_from_bit.tex`

- **L185 (notation paragraph)**: Rewrote the bare-κ paragraph to
  acknowledge the deliberate overload: in the body and working
  implementation κ is the learnable scalar of `τ = κ√K`; in the
  pullback construction κ(·,·) with two Lie-algebra arguments denotes
  the Killing form. The prior text said "bare κ denotes the Killing
  form" while every body usage (L187, L1244, L1803, L1805, L1834) used
  bare κ as a temperature scalar. Within-paper consistency restored.
  Fixes N11.
- **L189, L2067**: Stripped two "the notational collision the earlier
  draft had" self-referential phrases per
  `feedback_no_self_referential_history.md`. Replaced with clean
  "the symbols are reserved to their respective roles" / "the two
  symbols are reserved to their respective roles". Fixes F3.
- **L114, L1641, L3163, L3189, L3320**: Five "in particular" instances
  replaced with "specifically" or removed per CLAUDE.md style ban.
  Fixes F4.
- **L2129**: "critical property" → "defining property". Fixes F5.
- **L699, L707**: Stripped four math-mode `\,` instances (CLAUDE.md
  bans `\,`, `\;`, `\!` in equations). Borderline cases at L1462
  (SI-unit text formatting) and L1805/L1834 (`softmax\!(` typographic)
  left for author decision. Fixes F1 partial.
- **L886**: Appended BCH-bound caveat to the cross-scale gauge
  transformation paragraph stating the additive frame average is
  BCH-accurate to `O(‖φ‖²)` for compact `G` on the convexity ball;
  for non-compact `GL+(K)` the additive average is a heuristic, not
  a Karcher barycenter. Propagation of the §2131 caveat to the
  cross-scale construction. Fixes F8.
- **L2796 (Wick-rotation comparison)**: After "We flag this as a
  derivation gap." inserted three sentences distinguishing the
  construction from standard Wick rotation (continues a coordinate
  vs. continues a Lie-algebra component; standard Wick produces a
  real metric without needing a real-part projection; the
  construction here is Wick-like in the algebra plus an extra
  real-projection step with no Wick counterpart). Fixes S-M5.
- **L2802 (tetrad-rescaling clarification)**: Added one sentence
  identifying `η = diag(-1, +1)` as the orthonormal-frame metric, with
  the rescaling `e₀^τ ∝ 1/|∂_τψ_τ|`, `e₁^x ∝ 1/|∂_xψ_x|` absorbing
  the `|∂ψ|` factors of the boxed metric. Closes the presentational
  gap between the tetrad-Lorentz check at L2802 and the boxed metric
  at L2794. Fixes F7.
- **L2489-2498 (Participatory loop summary)**: Honesty-only
  reframing. Header `Demonstrated → Realised`; items 2 and 3 of the
  enumeration explicitly labelled `(structural consequence)` /
  `(structural consequence of the update rule)` with the construction
  stated inline (positivity of cross-scale KL flow is non-negative
  by definition; `Δp_i` tracking `Δq_I` is a re-statement of the
  direct-assignment update rule). Added explicit closing sentence
  that an ablation isolating the empirical contribution of the
  participatory loop is left to future work. Fixes F6, partially
  addresses I-M7 (without an actual ablation experiment).
- **L3657 (SO(3) appendix bridge)**: Inserted one paragraph
  identifying the appendix's `κ_β, κ_γ` with the body's bare `τ`:
  `κ_β = κ√K` on the belief fibre, `κ_γ = κ√M` on the model fibre,
  with `κ > 0` the single learnable scalar shared across channels.
  Fixes N5/E3 cross-manuscript drift without renaming the appendix.
- **Bibkey unification, 23 sites**: Replaced
  CamelCase citation keys `Friston2010`, `Vaswani2017`, `Parr2022`,
  `Amari2016`, `Nakahara2003`, `Frankel2011` with lowercase
  `friston2010free`, `vaswani2017attention`, `parr2022active`,
  `amari2016information`, `nakahara2003geometry`,
  `frankel2011geometry` (matching the main JMLR paper's convention).
  Script: `Attention/REVIEW_PARTICIPATORY_IFB_2026-05-18/bibkey_unify.py`.
  Fixes C1, C2, C4, C5, C6, C7 cross-manuscript drift on the
  Participatory side. The duplicate `@article{Friston2010,...}`,
  `@inproceedings{Vaswani2017,...}` etc. bib entries are NOT deleted
  in this pass — `belief_inertia_unified.tex` still cites the
  CamelCase keys and that manuscript is out of scope this round.
  Cosmetic duplication in the printed bibliography is now invisible
  to readers of GL(K)_attention.tex and
  Participatory_it_from_bit.tex (both use lowercase) but persists
  for any manuscript that compiles both keys. Cleanup deferred to
  belief_inertia review pass.

## Pre-existing undefined references in `GL(K)_attention.tex` (NOT introduced by this pass)

The following four undefined references are present in the current
file state but were not introduced by tonight's edits. They will
ship as `??` in the JMLR PDF if not fixed, but are unrelated to
the audit scope:

- `eq:beta_grad_phi` — referenced on page 40 (input line in supp linkage)
- `sec:rope_gauge` — referenced on page 42
- `fig:glk_pca_frames` — referenced on pages 48 and 49

These should be tracked separately as a non-blocking follow-up
before any actual JMLR submission. They are distinct from the
author-decision items below.

## Items still requiring author decision (not applied)

The mechanical fixes above are safe to merge. The following are
substantive choices that should not be silently made overnight:

- **M-F-2 (GL(K) supplementary, graph-RG `y_3 = +0.17` sign)**: the
  empirical exponent has the wrong sign relative to the linear
  prediction `y_3 = -1`. Current prose at supp:1031 attributes the
  gap to "finite-size effects" honestly but the sign mismatch itself
  is not framed; either weaken "deviate from" → "do not match" or
  defer the graph-based table to the companion paper. See GLK
  reviewer N-1 author-question 1.
- **N-6 (GL(K) supplementary, 30 vs 20 passages in
  `tab:temp_dispersion_supp`)**: caption says 30, on-disk JSON has 20
  for all five models. Either commit the 30-passage analysis output
  or update the caption to 20. Reviewer offered both options at
  reviewer_FRESH_2026-05-18-night.md author-question 3.
- **I-M1 (Participatory, detector form `§2114` vs `§3613`)**: two
  mathematically distinct rules described (`Γ = P · C_q · C_s ≥
  0.5` vs raw KL hard threshold `< 0.05`). The simulator code is not
  in the repo (Audit 1 above CANNOT-VERIFY) so the manuscript must
  pick which rule was actually run. Author decision; rewrite of
  §3613 prescribed at Participatory reviewer reviewer_FRESH §I-M1.
- **I-M7 (Participatory, top-down ablation)**: §2491-2498 reframed
  here as structural consequences (honesty-only fix F6 above). The
  substantive fix — actually running and reporting an ablation with
  the top-down channel suppressed — was not done because it requires
  a simulator that is not in the repo. Author decision; substantive
  experiment vs honesty-only labelling.
- **Bib hygiene: full duplicate-key deletion**: deferred until
  `belief_inertia_unified.tex` can be passed to lowercase as well.
- **F1 borderline cases**: SI-unit `\,` at Participatory:1462 and
  `softmax\!(` at L1805/L1834 are typographic conventions; project
  policy reads as "in equations" so both could go either way.
  Left as author decision.

## Verifier scripts

Independent math-claim verification of N-1 / N-2 / N-4 used inline
numpy/scipy:
- N-1: `Killing(X,Y) = 2K tr(XY) - 2 tr(X) tr(Y)` on `sl(3)`:
  `B(skew, skew) = -12.0`, `B(sym, sym) = +12.0` → indefinite.
- N-4: `exp(0.3J) exp(-0.7J)` vs `exp(-0.3J) exp(0.7J)` on so(2):
  equal? `False`. Transposes? `True`. Inverses? `True`.
- N-2: numerical Fréchet derivative `D_phi(exp)[T]` on random
  `phi ∈ so(3)`: matches both `R · exp(phi)` (R = right form) and
  `exp(phi) · L` (L = left form) to `1.4e-9`. Both individually
  correct under their conventions; the manuscript symbol was
  overloaded.

E8 verified by grep: `\label{app:model_channel_supp}` exists at
supp:1063; `\ref{app:model_channel}` (without `_supp`) appears in
main paper at L677 and L972 only → would compile as `??`.

## Compile verification

`pdflatex + bibtex + pdflatex × 2` over both manuscripts after the
fixes. Both compile cleanly to PDF:

- `Participatory_it_from_bit.pdf` (181 pages, 1.22 MB): **zero**
  undefined references or undefined citations.
- `GL(K)_attention.pdf` (67 pages, 658 KB): zero NEW undefined refs
  or citations introduced by this pass. Four pre-existing undefined
  references remain (`eq:beta_grad_phi` page 40, `sec:rope_gauge`
  page 42, `fig:glk_pca_frames` pages 48 and 49); these were
  already broken before today's pass and are unrelated to the
  current fixes. They should be tracked separately as a follow-up.

The two `app:model_channel_supp` cross-document `\ref` warnings that
my initial edit introduced were replaced with the descriptive
"Supplementary Appendix~G, Model-Channel Formalism" wording, which
does not require cross-document label resolution; both sites now
compile clean.

The `bibtex` log shows zero unresolved citations for both manuscripts
on the final pass (only the citation-style warnings persist, none
of which were introduced by today's edits).

## Phase-by-phase result

- **Phase 1 (parallel investigators):** 4 agents ran in parallel, 4
  fresh reports produced.
- **Phase 2 (adversarial verifier):** independent numpy/sympy/grep
  verification of the four highest-stakes new claims. All four
  verified: N-1 Killing form indefinite on sl(3), N-4 RoPE forms are
  transposes not equal, N-2 both Q_a conventions are correct under
  their own signs, E8 cross-reference label confirmed missing.
- **Phase 3 (apply fixes):** all mechanical fixes applied, both
  manuscripts compile clean, changelog written.

---

# Cross-head coupling ported to `/vfe`

User asked: "investigate whether cross_coupling feature is available in /vfe
implementation … build it out plug any metrics and visualizations". Investigation
confirmed the feature was legacy-only (`transformer/core/model.py` +
`transformer/core/block_equivariant_mixer.py`); shared math kernels lived in
`math_utils/generators.py` (`generate_glK_cross_head_generators`,
`merge_coupled_heads`, `reorder_cross_head_generators`, `_dedup_cross_couplings`).
This pass ports it into the `/vfe` package end-to-end, covering the full +
non-flat + full-cov scope per the user's selection.

## What `cross_couplings` is

Each `(a, b)` entry adds `d_head²` off-diagonal generators spanning head-a's
subspace into head-b's. The resulting `Ω = exp(φ·G)` becomes non-block-diagonal
across the coupled heads, so gauge transport itself moves information between
them — a different mechanism from the value-level `VFEHeadMixer`. Coupled heads
are merged via union-find into super-blocks; the K-axis is permuted so super-
blocks are contiguous.

## Files changed

`transformer/vfe/config.py`

- `cross_couplings: List[Tuple[int,int]]`, `auto_close_cross_head_basis: bool`,
  `validate_cross_head_closure: bool` fields added.
- `__post_init__` validates: GLK-only, single-type multihead, `n_heads >= 2`,
  rejects self-coupling, out-of-range head indices, malformed pairs; silently
  dedups exact duplicates (preserves directional `(a,b)` vs `(b,a)`).
- `n_gen` updated to add `|cross_couplings| · d_head²` to the diagonal count.
- New properties: `is_cross_coupled`, `super_block_dims`,
  `super_block_head_groups`, `effective_block_dims` (returns
  `super_block_dims` when cross-coupled, else `irrep_dims`). Lazily computed
  via `merge_coupled_heads`; results cached on the instance.

`transformer/vfe/model.py`

- `_build_generators` dispatches to `generate_glK_cross_head_generators` →
  `merge_coupled_heads` → `reorder_cross_head_generators` when
  `cross_couplings` is non-empty. Optional Lie-closure handling honored via
  `auto_close_cross_head_basis` / `validate_cross_head_closure`. Stashes
  `_cross_head_perm`, `_super_block_dims_cache`,
  `_super_block_head_groups_cache` on `cfg` for downstream consumers.
- Threading: `VFEPositionalEncoding` and `init_omega_from_phi` now receive
  `cfg.effective_block_dims` (super-block partition under cross-coupling,
  identity otherwise).

`transformer/vfe/e_step.py`

- `self.irrep_dims = cfg.effective_block_dims` so every block-iteration site
  (per-block softmax, per-block KL, per-block Ω, kappa_per_head dispatch,
  killing/cartan preconditioner, F-monitor diagnostic state) walks the
  correct gauge-block partition. `self._original_irrep_dims = cfg.irrep_dims`
  retained for diagnostics that need the original per-head granularity.
- `kappa_per_head` parameter sized to `len(cfg.effective_block_dims)`:
  one learnable κ per super-block, not per original head.
- `VFENonFlatConnection` constructor receives `head_sub_dims` so diagonal
  generators inside a merged super-block get a tight `d_head × d_head`
  mask while cross generators (which span multiple heads inside the super-
  block) fall back to the full super-block mask.

`transformer/vfe/prior_bank.py`

- `self.irrep_dims = cfg.effective_block_dims`. `_phi_trace_vec`,
  `phi_project_slk`, and per-block sandwich-transform iteration all walk
  the super-block partition; the trace constraint targets one determinant
  per super-block GL.

`transformer/vfe/head_mixer.py`

- `VFEHeadMixer.__init__` accepts `super_block_head_groups`. Heads inside a
  merged super-block are *excluded* from the mixer (each merged super-block
  has full GL(d_super) gauge with scalar Schur commutant — any additional
  value-level mixing of constituent heads breaks equivariance). Singleton
  super-blocks of the same type still get the standard free `n × n` mixer.

`transformer/vfe/block.py`

- Passes `cfg.super_block_head_groups` into `VFEHeadMixer`.

`transformer/vfe/non_flat.py`

- `VFENonFlatConnection.__init__` accepts `head_sub_dims` and builds finer
  block candidates so the auto-detection picks the tightest containing block
  per generator under cross-coupling.

`transformer/vfe/positional.py`, `transformer/vfe/omega_direct.py`

- No internal changes — both already iterate `irrep_dims` as the gauge-block
  partition; correctness under cross-coupling follows from passing
  `effective_block_dims` at the call sites above.

`transformer/vfe/cross_coupling_metrics.py` (new)

- `phi_energy_partition`: splits `||φ||²` between diagonal and cross
  generator groups.
- `omega_block_strength`: mean Frobenius mass of each (head_a, head_b)
  sub-block of `Ω`, addressed in the *original* head order via the inverse
  K-axis permutation.
- `cross_block_kl_share`: fraction of total per-super-block KL inside
  merged super-blocks vs singletons.
- `per_super_block_effective_rank`: exp-entropy effective rank of diagonal σ.
- `per_super_block_attention_entropy`: row entropy of `β_ij` per super-block.
- `holonomy_norm_cross_blocks`: Frobenius norm of length-3 holonomies on
  each super-block, broken out by merged vs singleton.
- `collect_cross_coupling_metrics`: convenience aggregator.

`transformer/vfe/cross_coupling_viz.py` (new)

- `plot_generator_sparsity`: `(K, K)` heatmap of `Σ_a |G_a|` with super-block
  boundaries (white dashed) and per-head sub-boundaries inside merged super-
  blocks (gray dotted).
- `plot_omega_block_strength`: head-pair heatmap with merged super-block
  outlines highlighted in dashed Wong-palette red.
- `plot_super_block_graph`: node-link diagram of cross-coupling pattern;
  nodes colored by super-block ID, edges drawn as directed arcs.
- `plot_phi_energy_partition`: stacked bar of diag vs cross φ-energy per
  layer.
- `plot_kl_share`: stacked bar of singleton vs merged super-block KL per
  layer.
- `plot_super_block_diagnostics`: side-by-side bar charts of per-super-
  block effective rank and attention entropy.

All figures use matplotlib only (no plotly/seaborn), Wong-2011 colorblind-safe
palette, tight layout, 300 dpi-ready per CLAUDE.md publication-quality default.

`tests/transformer/test_vfe_cross_coupling.py` (new, 22 tests passing)

- Config validation (dedup, GLK-only, single-type, n_heads ≥ 2, self-coupling
  rejected, out-of-range pair rejected).
- Super-block partition correctness for one pair and transitive patterns.
- Generator builder n_gen, shape, fallback to multihead when empty, super-
  block-contiguous K layout for non-adjacent coupled heads.
- Forward pass shape, backward gradient reaches cross-generator entries of
  `phi_embed`.
- Metrics shape/type correctness, identity-Ω round-trip on
  `omega_block_strength`.
- Visualization smoke tests for every plot function.

`transformer/vfe/train_vfe.py`, `transformer/vfe/vfe_ablation_suite.py`

- `cross_couplings`, `auto_close_cross_head_basis`,
  `validate_cross_head_closure` added to CONFIG / BASE with empty defaults
  (zero behavioral change).

## Regression

`pytest tests/transformer/test_vfe_package.py` — 48/48 pass.
`pytest tests/transformer/test_vfe_cross_coupling.py` — 22/22 pass.

---

# Cross-head coupling: metrics + viz wired into both trainers

Follow-up pass. The cross-coupling metrics / visualizations now run automatically
at every periodic evaluation in both training paths. No-op when
`cross_couplings` is empty so unconditional wiring is safe.

## `/vfe trainer.py`

- `transformer/vfe/e_step.py`: `_last_attention_state` now also caches `phi`
  (post-E-step). This is the one new field needed for `phi_energy_partition`
  on the trainer side; everything else (`mu_q`, `sigma_q`,
  `block_exp_pairs`, RoPE state, kappa) was already cached.
- `VFETrainer._collect_cross_coupling_artifacts`: pulls per-block phi /
  sigma / mu_q / block_exp_pairs from each block's
  `e_step._last_attention_state`, assembles a (B, N, K, K) `omega_full` by
  block-diag of `block_exp_pairs`, and assembles the pairwise per-block
  Ω_ij = exp_h(i) · exp_h_inv(j) for the holonomy metric.
- `VFETrainer._save_cross_coupling_diagnostics(step)`: computes the full
  metrics suite via `cross_coupling_metrics.collect_cross_coupling_metrics`
  per layer, serializes scalar+array results to
  `output_dir/cross_coupling/cross_coupling_step_{step:08d}.json`, and
  saves five figures via `cross_coupling_viz`:
  generator sparsity, super-block adjacency graph, omega block strength,
  phi-energy partition (across layers), per-super-block diagnostics
  (effective rank + attention entropy).
- Call site: inserted into the eval branch of `train()` right after
  `_plot_attention_patterns(step + 1)`. The scalar `cc_phi_energy_*`
  summaries are merged into the `_metrics_tracker.log_val` payload so
  they show up in `metrics.csv` alongside `val_loss` / `val_ppl`.

## Legacy `experiment_runner.py`

- `transformer/core/model.py`: also stashes `_super_block_head_groups` and
  `_cross_couplings` (the deduped list) on `GaugeTransformerLM` so the
  diagnostics adapter does not need to re-run `merge_coupled_heads` or
  parse the raw config dict. Defaults to `None` / `[]` on every
  non-cross-coupled code path so the adapter never AttributeErrors.
- `ExperimentRunner._build_cross_coupling_adapter()`: produces a thin
  `types.SimpleNamespace` exposing the VFEConfig-shaped surface
  (`cross_couplings`, `is_cross_coupled`, `irrep_spec`,
  `effective_block_dims`, `super_block_dims`, `super_block_head_groups`,
  `_cross_head_perm`, `embed_dim`, `irrep_dims`) sourced from the legacy
  model. Returns `None` when cross-coupling is inactive.
- `ExperimentRunner.save_cross_coupling_visualization(step, batch)`:
  captures encode-time φ from `gauge_token_embedding.phi_embed`, runs one
  `forward_with_attention` for β, then saves the generator sparsity,
  super-block graph, phi-energy partition, and per-super-block diagnostics
  figures plus a JSON of scalar / per-super-block-array metrics under
  `checkpoint_dir/cross_coupling/`.
- Call site: appended to the existing `save_attention_visualization`
  block in `train()` (right after the legacy attention plots fire).

## Tests (25 total in `test_vfe_cross_coupling.py`, all passing)

Two new smoke tests for the trainer wiring:
- `TestVFETrainerWiring::test_save_cross_coupling_diagnostics_smoke` —
  builds a minimal `VFEModel` with `cross_couplings=[(0, 1)]`, runs one
  forward with attention-state capture enabled, calls
  `_save_cross_coupling_diagnostics`, asserts that exactly one JSON and
  ≥ 2 PNG files land under `cross_coupling/`.
- `TestVFETrainerWiring::test_save_cross_coupling_diagnostics_noop_when_empty` —
  with `cross_couplings=[]`, the method returns `{}` and writes no files.

## Regression

`pytest tests/transformer/ --ignore=tests/transformer/test_attention_diagnostics.py`
— 913/913 pass (up from 910 — three new tests added across both passes).

---

## AIF Build-Out — Compute Feasibility Analysis (Investigation Agent 4)

New planning document written at:
`docs/plans/2026-05-19-aif-transformer-buildout/04_compute_feasibility.md`

This is a read-only analysis document; no source code was modified.

Content summary:

- §1 establishes that the dominant cost per forward is the `(B,N,2K) × (V,2K)^T` decode matmul at `transformer/vfe/prior_bank.py:498-500`, costing 33 GFLOPs and 1.65 GB logit memory at training shape (B=64, N=128, V=50257, K=20). The E-step attention grid costs approximately 2–3 GFLOPs — about 15× cheaper than decode.

- §2 analyzes the depth-1 EFE candidate scoring loop (`vfe/efe.py:164-201`) and identifies a structural double-encode: the model forward at `efe.py:174` and the BALD encode at `efe.py:191` each perform a separate embedding lookup on the same token sequence. The beliefs from the first forward are not surfaced. This is a latent integration point for the build-out.

- §3 establishes the tree-search FLOP and wall-clock frontier. At B=1 generation shape, (D=2, b=8) requires 128 forwards per token (~0.5–2 s on A100) and is the practical demo target. (D=4, b≥16) and (D=5, b≥8) are demo-prohibitive. Full-covariance Σ at depth > 1 is structurally intractable.

- §4 shows the belief cache at (D=3, b=8) totals approximately 103 MB, well within 24 GB GPU. φ dominates the per-node cache at n_gen=200 vs K=20 (factor 10×). The shared decode prior cache (4 MB) must be preserved across tree siblings; the current `invalidate_cache()` call per forward would create 585 redundant recomputations at D=3 b=8.

- §5 concludes S=4 is acceptable for generation-time scoring with epistemic_weight=0.5. The streaming BALD pattern at `core/active_inference.py:277-296` must be carried through any depth-D recursion to avoid S×(B,N,V) logit stacking.

- §6 recommends demo defaults: D=2, b=4, S=4, top_k=50, B=1, diagonal Σ. Expected 0.5–3 s per token on 24 GB GPU under the current sequential forward loop.

- §7 lists OOM failure modes: training-shape tree search (1.65 GB × b² logits), full-cov at depth > 1 (128× E-step cost), non-streaming BALD in recursion, per-node decode cache recomputation, and gradient-tracked tree search (200–500× memory overhead).

- §8 compares beam, top-k, and nucleus sampling at expansion; sophisticated inference vs flat leaf scoring; and depth-1 vs depth-D EFE quality tradeoffs. Sophisticated inference (recursive EFE at every node) adds only a 2× cost at D=2 and <1.1× overhead at D=3.

- §9 covers training-time EFE cost. Regime A (observed trajectory as policy, no expansion): less than 1% overhead over standard CE. Regime B (tree expansion at training time): approximately 200–500× overhead, estimated 940 A100-hours for 15,000 steps — off-by-default, research-only.

---

## AIF Build-Out — Verifier Report + Final Plan + Phase 0 + Phase 1 (Execution)

New planning documents:
- `docs/plans/2026-05-19-aif-transformer-buildout/05_verifier_report.md` — verifier (general-purpose agent) cross-checked all four investigators against canon, code references, and CLAUDE.md hard constraints. Verdict: YELLOW (math canonical, code references verified bitwise, no hard-constraint violations; merge-clean §8 patches integrated into the final plan).
- `docs/plans/2026-05-19-aif-transformer-buildout/06_plan.md` — consolidated final plan with five phased milestones (Phase 0 verdict-rename, Phase 1 canonical depth-1 anchor, Phase 2 beam tree search, Phase 3 Friston-2021 sophisticated recursion, Phase 4 optional EFE-augmented training).

### Phase 0 — Surrogate path DELETED (per user request)

User opted to remove the surrogate entirely rather than rename (verdict had proposed `self_evidencing_regularizer` rename; user simplified to deletion). Reasoning: the new `transformer/aif/` module provides the canonical EFE machinery and supersedes the surrogate's role as a low-entropy preference at the policy level.

**Files removed:**
- `transformer/vfe/active_inference.py` — VFEActiveInference class.

**Files modified:**
- `transformer/vfe/__init__.py` — removed `VFEActiveInference` import and `__all__` entry.
- `transformer/vfe/config.py` — removed `active_inference: bool` and `pragmatic_weight: float` fields. Kept `epistemic_weight`, `epistemic_samples`, `decode_tau` (still used by `transformer/vfe/efe.py:VFEExpectedFreeEnergy` for canonical generation-time EFE). Section header relabeled "Decode and generation-time EFE (canonical path)".
- `transformer/vfe/model.py` — removed the `if cfg.active_inference: ...` construction block and the `active_inference_fn` argument from the `self.stack(...)` call.
- `transformer/vfe/stack.py` — removed `ActiveInferenceFn` type alias, `Callable`/`Tuple` imports, and the `active_inference_fn` parameter from `VFEStack.forward`. Cleaned per-block call.
- `transformer/vfe/block.py` — removed the `TYPE_CHECKING` import of `ActiveInferenceFn` and the parameter from `VFEBlock.forward`.
- `transformer/vfe/e_step.py` — removed `ActiveInferenceFn` TYPE_CHECKING import; removed the parameter and surrogate-application block from `VFEEStep.forward` (φ-mode) AND from `_forward_omega_direct` (ω-direct mode). Two application blocks deleted (lines 1165-1170 and lines 2208-2213 in pre-edit indexing).
- `transformer/vfe/train_vfe.py` — removed `active_inference: True` and `pragmatic_weight: 1.0` from the click-to-run config dict. Renamed section header to "Decode and generation-time EFE (canonical path; consumed by vfe/efe.py)".
- `transformer/vfe/vfe_ablation_suite.py` — same field removal as train_vfe.py.
- `tests/transformer/test_vfe_package.py` — removed `from transformer.vfe.active_inference import VFEActiveInference` and the entire `TestVFEActiveInference` class. Renumbered section header (8. VFEActiveInference → 8. VFEExpectedFreeEnergy).

The `transformer/core/active_inference.py` kernel was left in place because the legacy `transformer/core/` path (separate trainer) still uses it. Touching `/core` is out of scope for this build-out.

`pytest tests/transformer/test_vfe_package.py` after the deletions: **47/47 passed**. No regression.

### Phase 1 — Canonical /aif module shipped

**New file: `transformer/vfe/model.py:VFEModel.forward_with_beliefs`** — accessor that returns `(logits, beliefs)` so the new AIF path does not re-encode (closes the double-encode pattern at `vfe/efe.py:191-196` per verifier §8.8). Refactor extracts the shared `_encode_step_decode` helper used by both `forward` (loss-bearing path) and `forward_with_beliefs` (AIF path).

**New package: `transformer/aif/`** with eight files:
- `__init__.py` — public surface: AIFConfig, three Preference subclasses, PolicyNode, EFEComponents, BeliefStateCache, compute_G_at_node, AIFGenerator.
- `config.py` — `AIFConfig` dataclass with full Phase-1 validation (rejects D < 1, gamma ≤ 0, missing preference path for empirical_marginal / task_conditioned, sophisticated branching, efe_augmented training). `validate_against_model` enforces the full-covariance guard at D > 1 (verifier §8.7).
- `preferences.py` — `Preference` ABC plus `EmpiricalMarginalPreference` (canonical default per `01_canonical_efe_for_lm.md` §4: pragmatic value = cross-entropy of model predictive against corpus marginal), `LowEntropyPreference` (strict self-evidencing, the surrogate's policy-level role), `TaskConditionedPreference` (RLHF-style external preference). `build_preference` factory.
- `policy.py` — frozen `PolicyNode` dataclass (action_seq, depth, parent, cumulative G), `EFEComponents` (pragmatic, ambiguity, epistemic, G_local).
- `belief_cache.py` — `BeliefStateCache` with LRU eviction at `belief_cache_max_entries` and `evict_below_depth` for per-commit pruning. Stores detached tensor snapshots so the cache holds graph-independent state.
- `efe_score.py` — `compute_G_at_node` computes canonical Form-3 BALD decomposition. Reuses `VFEModel.forward_with_beliefs` to avoid the double-encode bug. `_bald_mi_and_ambiguity` returns MI and mean predictive entropy from one sampling pass.
- `tree_search.py` — `pick_top_candidates` (top-k from a predictive). `sophisticated_expand` is a Phase-3 stub that raises NotImplementedError.
- `generator.py` — `AIFGenerator` wraps a trained `VFEModel` by composition, runs the depth-1 reduction anchor (Phase 1), validates the AIF config against the wrapped model's config at construction.
- `train_aif.py` — click-to-run entry point with `AIF_CONFIG` dict at top of file; loads a trained checkpoint, builds the generator, emits a sequence. Prints belief-cache hit/miss diagnostics.

**New test file: `tests/transformer/test_aif_package.py`** with 29 tests covering:
- AIFConfig validation (7 tests): defaults, rejects D < 1, rejects negative gamma, rejects efe_augmented, rejects sophisticated branching, requires preference path for empirical_marginal, full-cov rejection at D > 1.
- Preference distributions (4 tests): empirical-marginal pragmatic equals cross-entropy, prefers higher-freq tokens, low-entropy prefers peaked distributions, build_preference dispatch.
- PolicyNode (3 tests): root is empty, child extension, components breakdown.
- BeliefStateCache (5 tests): put/get with hit counter, miss counter, LRU eviction, evict_below_depth, stored tensors detached.
- compute_G_at_node (3 tests): returns components and beliefs, decomposition is well-formed, G_local = pragmatic + ambiguity - epistemic_weight * epistemic.
- AIFGenerator (4 tests): generate extends prompt correctly, argmin sampling is deterministic, horizon_D > 1 raises NotImplementedError, cache records entries.
- Law-1 regression (3 tests): inference paths have no `targets` parameter.

`pytest tests/transformer/test_aif_package.py tests/transformer/test_vfe_package.py`: **76/76 passed** (29 new AIF tests + 47 existing VFE tests, no regression).

### Phases deferred (per the final plan)

- **Phase 2** — beam tree search at horizon_D > 1. `AIFGenerator.generate` raises NotImplementedError when called with D > 1; the validator guard in `AIFConfig.validate_against_model` is already in place.
- **Phase 3** — `[Friston2021SophisticatedInference]` recursive form. `sophisticated_expand` stub + config rejection are in place.
- **Phase 4** — optional EFE-augmented training. `AIFConfig.training_objective='efe_augmented'` raises NotImplementedError. Gated by user approval per `06_plan.md` §6.

---

## AIF Build-Out — Phase 2: beam tree search at horizon_D > 1

Per-branch beam tree search shipped. `AIFGenerator` now expands a depth-D
tree at every commit step and back-propagates leaf G_cum to the root
children via mean aggregation. The depth-1 reduction anchor still passes
all Phase-1 tests (zero behavioral drift on the existing path).

**Files modified:**

- `transformer/aif/efe_score.py` — added `score_components_from_beliefs(beliefs, model, preference, cfg)` for cached-belief scoring (no fresh forward). Refactored `compute_G_at_node` to re-decode at AIFConfig.decode_tau independently of VFEConfig.decode_tau (fixes a double-tau application that had crept in at Phase 1; corrected as part of Phase 2 wiring).
- `transformer/aif/belief_cache.py` — added `BeliefStateCache.commit_action(action)` that re-keys cache entries `(action, *rest) -> (*rest,)` and evicts orphaned subtrees. Returns the kept-entry count for the cache-hit-rate diagnostic. Truncation safety derives from the fact that `[old_prompt, action, rest]` and `[new_prompt, rest]` (with `new_prompt = old_prompt ++ (action,)`) truncate to the same trailing `max_seq_len` tokens.
- `transformer/aif/tree_search.py` — implemented `beam_expand` end-to-end. Algorithm: BFS by level; at each parent, take the top-`beam_width` candidates from the parent's predictive (decoded from cached belief); score each via `_score_child_with_cache` (cache hit → `score_components_from_beliefs`, miss → `compute_G_at_node`). `G_cum` along each path is the geometric-discounted sum `Σ_d γ^d G_local_d`. Back-propagation walks from depth `D` up to depth 1 taking MEAN over children's V values; root children receive the mean leaf G_cum reachable through them. Added `_build_extended_context` truncation helper.
- `transformer/aif/generator.py` — rewrote `AIFGenerator.generate` to call `beam_expand` for every horizon_D (depth-1 and depth-D unified through one path). Removed the Phase-1 `_score_root_children` and the `NotImplementedError` on D > 1. After each commit, calls `self.cache.commit_action(action)` to re-key for cross-commit prefix sharing.
- `tests/transformer/test_aif_package.py` — added 15 new tests covering Phase-2 paths:
  - `TestScoreComponentsFromBeliefs` (2 tests): cached scoring returns EFEComponents; pragmatic agrees deterministically between the two scoring entry points.
  - `TestCommitAction` (3 tests): re-keying keeps the committed-action subtree, evicts orphans, returns kept count.
  - `TestBeamExpand` (5 tests): D=1 reduction (G_cum = G_local), D=2 b=1 single-path, D=2 b=2 mean aggregation, discount finite-value sanity, cache populated to 1+b+b² entries after one beam_expand.
  - `TestCrossCommitCacheReuse` (2 tests): hits observed on the second commit step, commit_action is called from generate.
  - `TestBuildExtendedContext` (3 tests): empty action_seq, append, truncation to max_seq_len.
  Also updated `test_depth_greater_than_one_not_implemented` → `test_depth_two_runs_end_to_end` (Phase 2 supports D > 1).

### Math design choices (Phase 2)

- **Per-branch beam (not global beam).** Each node has exactly `beam_width` children; total leaves = `beam_width^D`. The full-cov runtime guard at D > 1 keeps the diagonal-Σ practical envelope reasonable on RTX 5090 (32 GB). The recommended demo preset (D=2, b=4) explores 16 leaves per commit step.
- **Mean back-propagation.** Root child V = mean over reachable leaf G_cum. This is the uniform-policy expectation of the canonical Friston-2021 recursion `E_{q(a'|s)}[G_{d+1}]`; the softmax-weighted form (canonical) is Phase 3. Mean-over-beam is a defensible Phase-2 default because (a) it has no extra hyperparameter and (b) it preserves the property that argmin over root V is the argmin path's G_cum at b=1.
- **Geometric discount across depth.** Hyperparameter `discount` (default 1.0, undiscounted). Each depth-d local G contributes `discount^d * G_local`. Discount is an engineering hyperparameter, not a canonical EFE quantity (per verifier §7.5).
- **AIF decode_tau independent of VFE decode_tau.** The AIF readout re-decodes the last position at `AIFConfig.decode_tau` so generation-time scoring can be tuned without retraining. Costs one extra (V, K) matmul per node — small fraction of the per-node E-step cost.

### Test results

91/91 pass across both suites (47 existing VFE + 44 AIF). The 15 new Phase-2 tests plus the 29 retained Phase-1 tests cover the full depth-1 reduction invariant, the depth-2 tree expansion, and the cross-commit cache re-keying.

### Phases remaining

- **Phase 4** — optional EFE-augmented training (gated by user approval).

---

## AIF Build-Out — Phase 3: sophisticated-inference recursion

Canonical [Friston2021SophisticatedInference] recursion shipped. The
Phase 2 mean back-propagation was the uniform-child-posterior limit of
the canonical recursive form; Phase 3 replaces it with the softmax-
weighted expectation under :math:`q(a' \mid s_d) \propto \exp(-\gamma\, V(a'))`.

### Math

The canonical recursion per `01_canonical_efe_for_lm.md` §3:

.. math::
    V(\text{leaf}) = G_{\text{local}}(\text{leaf})

.. math::
    V(\text{internal}, d) = G_{\text{local}}(d) + \gamma_{\text{disc}}
        \sum_{a' \in \text{beam}(d+1)} q(a' \mid s_d) \, V(a').

The child posterior is dispatched on ``cfg.branching_strategy``:

- ``'beam'`` / ``'top_k'``: uniform ``q(a') = 1 / |beam|``, equivalent to
  the Phase 2 mean aggregation. This is the :math:`\gamma \to 0` limit
  of sophisticated inference.
- ``'sophisticated'``: ``q(a') ∝ exp(-gamma · V(a'))``. At
  :math:`\gamma \to 0` converges to uniform (Phase 2 beam); at
  :math:`\gamma \to \infty` collapses to argmin over the beam.

### Files modified

- ``transformer/aif/tree_search.py``: replaced the Phase 2 mean back-
  propagation with a parametrized recursive V computation. New helpers
  ``_make_aggregator(strategy, gamma)`` dispatches uniform vs softmax-
  weighted aggregation; ``_backprop_V(level_nodes, horizon_D, discount,
  aggregator)`` walks the tree bottom-up. ``V(leaf) = components.G_local``;
  ``V(internal_d) = components.G_local + discount · aggregator(child V's)``.
  Under uniform aggregation the recursion unrolls to mean-over-leaves of
  the geometric-discounted path-sum (matching Phase 2 bitwise modulo
  floating-point order). The ``sophisticated_expand`` Phase-1 stub is
  removed.
- ``transformer/aif/config.py``: removed the ``__post_init__`` rejection
  of ``branching_strategy='sophisticated'``. Updated the docstring on
  ``branching_strategy`` to document the three-way dispatch and the
  ``gamma → 0`` / ``gamma → ∞`` limits. Updated the ``gamma`` docstring
  to note its dual role: root-level action posterior AND recursive
  child-action posterior under sophisticated inference.
- ``tests/transformer/test_aif_package.py``: converted the Phase-1
  ``test_rejects_sophisticated_branching`` guard into a
  ``test_accepts_sophisticated_branching`` acceptance test. Added 14
  new Phase-3 tests:
  - ``TestMakeAggregator`` (7): beam is mean, top_k is mean, sophisticated
    at small gamma matches mean, sophisticated at large gamma matches
    argmin, intermediate gamma is between min and mean, empty input
    returns 0, unknown strategy raises.
  - ``TestSophisticatedExpand`` (4): D=1 matches beam at D=1 (no
    aggregation), small-gamma D=2 ≈ beam D=2, large-gamma D=2 runs
    end-to-end without numerical issues, AIFGenerator with
    sophisticated branching generates tokens.
  - ``TestBackpropV`` (3): hand-computed verification on a synthetic
    2-level tree — uniform recursion, discount halving, sophisticated
    argmin limit at large gamma.

### Design choices

- ``cfg.gamma`` is shared between the recursion's child posterior and
  the root-level action sampling. This is the canonical Friston 2021
  interpretation: one policy precision controls all softmax-weighted
  posteriors. Separating them is a research extension.
- ``_backprop_V`` keys nodes by ``id(node)`` (not by ``action_seq``)
  because two distinct nodes can share an action_seq under aggressive
  truncation; ``id()`` is safe by construction.
- Aggregator builds use fp64 internally for the softmax to preserve
  precision in the ``gamma → ∞`` argmin limit (the
  ``test_sophisticated_at_large_gamma_matches_argmin`` test would fail
  in fp32 at ``gamma=1e6``).

### Test results

105/105 pass across both suites (47 VFE + 58 AIF — 14 new Phase-3
tests on top of the 44 from Phases 1+2). The new tests cover the
aggregator limits, the recursion's end-to-end behaviour, and direct
unit tests of ``_backprop_V`` on hand-computed trees.

### Phases remaining

- **Phase 4** — optional EFE-augmented training (NOW SHIPPED, see next section).

---

## AIF Build-Out — Phase 4: trajectory-as-policy training augmentation

Phase 4 (Regime A) ships the canonical training-time EFE augmentation.
The observed training sequence is treated as the agent's chosen policy;
per-position EFE is computed from the model's own forward outputs and
folded into the M-step objective as :math:`L_{\text{total}} =
L_{\text{CE}} + \lambda_{\text{AIF}}\, L_{\text{AIF}}`. No tree
expansion at training time (Regime B is structurally infeasible at
~200-500× CE cost per ``04_compute_feasibility.md`` §9).

### Math

For each batch position :math:`(b, t)`:

.. math::
    G_t = \text{pragmatic}_t + \text{ambiguity}_t
        - \lambda_{\text{epi}}\, \text{epistemic}_t,

with

- **Pragmatic**: :math:`\text{prag}_t = E_{q(o \mid a)}[-\log p^*(o)]`
  computed as cross-entropy of the predictive against the configured
  preference distribution. Cheap — no extra forward passes.
- **Ambiguity**: :math:`\text{amb}_t = E_{q(s_t)}[H[p(o \mid s_t)]]` via
  BALD MC sampling at every batch position. Gradient through the
  reparameterized :math:`z_s = \mu + \sqrt{\Sigma}\, \varepsilon`
  carries into both :math:`\mu` and :math:`\Sigma`.
- **Epistemic**: :math:`\text{epi}_t = I_{q(s_t, o)}(s_t; o) = H[\bar p]
  - E_s[H[p_s]]`. Shares the same MC sampling pass with ambiguity (no
  extra forward passes when both are enabled).

The augmentation is gated per term: ``train_include_pragmatic``,
``train_include_ambiguity``, ``train_include_epistemic`` flags let the
user drop the expensive BALD sampling (e.g. pragmatic-only ≈ <1%
overhead over standard CE per the verifier's Regime A estimate).

### Files added / modified

- **NEW** ``transformer/aif/training_loss.py``: standalone gradient-
  tracked loss function ``compute_training_efe_loss(logits, beliefs,
  preference, prior_bank, cfg) -> scalar tensor``. Helpers
  ``_pragmatic_per_position`` and
  ``_bald_ambiguity_and_epistemic_per_position`` keep the three terms
  modular. The reparameterized BALD sampling is the only stochastic
  component; both fields of beliefs (mu and sigma diagonal) receive
  gradients via the reparameterization.
- ``transformer/aif/config.py``:
  - Removed the Phase-1 NotImplementedError on
    ``training_objective='efe_augmented'``.
  - Added four new fields with documentation: ``aif_loss_weight``
    (multiplier, default 0.1), ``train_include_pragmatic``,
    ``train_include_ambiguity``, ``train_include_epistemic`` (all
    default True).
  - ``__post_init__`` now raises ValueError if
    ``training_objective='efe_augmented'`` is set with all three term
    flags False (the augmentation would be a no-op).
- ``transformer/aif/__init__.py``: exported ``compute_training_efe_loss``.
- ``tests/transformer/test_aif_package.py``: converted the Phase-1
  ``test_rejects_efe_augmented_training`` guard into a
  ``test_accepts_efe_augmented_training`` acceptance test plus a
  ``test_efe_augmented_with_no_terms_raises`` ValueError check. Added
  ``TestTrainingEFELoss`` with 6 new tests:
  - Returns a scalar tensor with finite value.
  - Pragmatic-only mode works (no BALD pass).
  - Gradient flows to at least one PriorBank parameter on backward.
  - Pragmatic value is lower when the preference is concentrated on the
    model's predicted argmax token (sign correctness).
  - Raises RuntimeError when called with ``training_objective != 'efe_augmented'``.
  - Composes correctly with cross-entropy in a representative training
    step.

### Integration pattern for the user's training loop

The function is a drop-in addition to any training script — four lines
of integration:

```python
from transformer.aif import AIFConfig, build_preference, compute_training_efe_loss

aif_cfg = AIFConfig(training_objective='efe_augmented', preference_path=...)
preference = build_preference(aif_cfg.preference_type, aif_cfg.preference_path)
# ... inside the training step:
logits, beliefs = model.forward_with_beliefs(input_ids)
ce_loss = F.cross_entropy(logits.view(-1, V), targets.view(-1), ignore_index=-100)
aif_loss = compute_training_efe_loss(logits, beliefs, preference, model.prior_bank, aif_cfg)
total_loss = ce_loss + aif_cfg.aif_loss_weight * aif_loss
total_loss.backward()
```

Replaces the existing ``model.forward(input_ids, targets)`` call with
``forward_with_beliefs`` plus explicit CE; the AIF loss is added as a
single line. The user's existing ``VFETrainer`` can be extended this
way without subclassing.

### Test results

112/112 pass across both suites (47 VFE + 65 AIF — 7 new Phase-4 tests
on top of the 58 from Phases 1-3).

### Phase 4 ablation

Per ``06_plan.md`` Phase 4: the ablation comparing
``training_objective='standard_vfe'`` vs ``'efe_augmented'`` at a fixed
step budget is the user's call. The code is ready; the empirical
validation is gated by user review.

### Build-out status

All four phases of the build-out are complete:

- **Phase 0**: surrogate path deleted ✓
- **Phase 1**: canonical depth-1 EFE anchor ✓
- **Phase 2**: per-branch beam tree search at horizon_D > 1 ✓
- **Phase 3**: Friston-2021 sophisticated inference recursion ✓
- **Phase 4**: trajectory-as-policy training augmentation ✓

Total surface: 8 new files under ``transformer/aif/``, 1 modified
``transformer/vfe/model.py`` (adding ``forward_with_beliefs``), and 1
new ``tests/transformer/test_aif_package.py`` with 65 tests. 112/112
tests pass.

---

## AIF Build-Out — Phase 4 follow-up: click-to-run training experiment

Per user request "i just want to run an experiment" (not a sweep), ships
a single click-to-run entry point that fires off one EFE-augmented
training run from a config dict.

### Files added

- **NEW** ``transformer/aif/trainer.py``: ``AIFAugmentedTrainer``
  subclasses ``VFETrainer`` and overrides ``train_step`` only. The
  override reproduces ``VFEModel.forward``'s loss composition exactly
  (CE + ``loss_scale`` + ``mass_phi`` + per-block aux hyper-loss) and
  then adds ``self._loss_scale * aif_cfg.aif_loss_weight * L_AIF`` where
  ``L_AIF`` comes from
  :func:`transformer.aif.training_loss.compute_training_efe_loss`. The
  returned metrics dict carries a new ``aif_loss`` field for logging.
  Every other piece of training infrastructure — optimizer, schedulers,
  gradient clipping, CSV metrics, attention plots, eval loop — is
  inherited unchanged.
- **NEW** ``transformer/aif/train_aif_augmented.py``: click-to-run
  entry. Two config dicts at the top — ``vfe_config`` (mirrors
  ``train_vfe.py`` defaults) and ``aif_config`` (Phase-4 augmentation
  parameters). On first run it builds the empirical-marginal preference
  distribution from the training corpus and caches it to
  ``aif_runs/preferences/{DATASET}_empirical_marginal.pt`` (Laplace add-
  one smoothing for finite log-probs on unseen tokens). Subsequent runs
  reuse the cache. Output directory tag includes
  ``aif{aif_loss_weight}`` so multiple runs at different strengths
  don't collide.

### Tests added

``TestAIFAugmentedTrainer`` in ``tests/transformer/test_aif_package.py``
adds 4 smoke tests:

- ``test_construction``: builds an AIFAugmentedTrainer end-to-end with
  a tiny in-memory ``DataLoader``.
- ``test_rejects_standard_vfe_objective``: ``ValueError`` if the user
  passes an ``AIFConfig`` with
  ``training_objective='standard_vfe'`` — they should use
  ``VFETrainer`` directly in that case.
- ``test_train_step_returns_aif_loss_metric``: a single train_step
  returns a metrics dict including a finite ``aif_loss`` field plus
  the standard ``loss`` and ``ce`` keys.
- ``test_step_advances_global_step_and_optimizer``: the optimizer
  actually steps; at least one parameter receives a non-zero gradient.

### Test results

116/116 pass across both suites (47 VFE + 69 AIF). The four new trainer
smoke tests are on top of the 65 Phase 1-4 tests already in place.

### Usage

To run an EFE-augmented training experiment:

1. Edit ``transformer/aif/train_aif_augmented.py``: adjust
   ``vfe_config`` (model spec) and ``aif_config`` (augmentation
   strength + sampling) at the top of the file.
2. Press Run.
3. First run builds + caches the corpus marginal preference; subsequent
   runs reuse the cache.
4. Outputs land in ``aif_runs/{DATASET}_aif{lambda}_{timestamp}/``
   alongside CSV metrics, attention plots, and checkpoints from the
   standard ``VFETrainer`` infrastructure.

To recover standard VFE training from the same entry point: set
``aif_config['aif_loss_weight'] = 0.0`` (the AIF terms still compute
but contribute zero to backward), or set all three
``train_include_*`` flags to ``False`` to skip the augmentation
entirely.

## Late pass: mass-analogy empirical-anchor cleanup (2026-05-19 PM)

Triggered by the red-blue-debate verdict at
`docs/debates/2026-05-19-mass-analogy-precision-stiffness/04_verdict.md`
(RED_WINS on the compound claim that §`sec:mass` of
`Participatory_it_from_bit.tex` is both algebraically correct AND
appropriately motivated).

C1 (algebra) verified clean by both teams under sympy and
finite-difference on all six boxed identities, including the GL(d)
precision-transport law and the at-consensus cross-block vanishing.
C2 (motivation) failed on three independent points:

1. **Missing empirical anchor.** Lines 1933, 1981, and 2983 of
   `Participatory_it_from_bit.tex` referenced "the empirical
   mass-precision validation of Section sec:mass," but §`sec:mass`
   (1843–2040) displays no figure, no R², no measured ω², and no
   precision sweep. The integrator description at line 3547
   ("velocity-Verlet symplectic … for the mass-precision experiments")
   confirmed machinery exists but no output is reported in the
   manuscript.
2. **R² conflation.** Line 2983 attached "R² = 0.9998" to "the
   empirical mass-precision study," but the only R² = 0.9998 in the
   manuscript is at line 2483 — the WikiText-103 perplexity-vs-K
   scaling fit for the transformer model, a different experiment.
3. **Kinetic-postulate circularity.** Line 2028 postulated the
   kinetic-metric coefficient equal to the potential Hessian
   $M_{\mu\mu}$, then licensed $\omega^2 \propto m_{\text{eff}}^{-1}$
   from the harmonic relation $\omega^2 = k/m$. When $k$ and $m$ are
   both equal to the same matrix by postulate, $\omega^2$ collapses to
   a per-direction unit relation; the harmonic scaling is a
   definitional consequence of the postulate, not an independent
   dynamical scaling. Standard Lagrangian mechanics
   [arnold1989mathematical] requires the inertia tensor and the
   potential Hessian to be operationally independent (0,2)-tensors at
   the equilibrium for a non-degenerate harmonic identification.

Per user instruction: experiments will eventually be done properly;
for now delete the unsupported empirical claims and leave TODO
placeholders.

### Edits applied to `Attention/Participatory_it_from_bit.tex`

- **Line 1846.** Replaced "empirically observable mass-like scaling"
  with "postulate-dependent mass-like scaling," and appended a
  `\textbf{TODO}` flag stating that an empirical test in which
  $\omega$ and $m_{\text{eff}}$ are measured as operationally
  independent quantities is deferred to future work and not reported
  in the manuscript.
- **Line 1848.** Replaced "the empirical scaling
  $\omega^2 \propto 1/m_{\text{eff}}$ (Section sec:mass) reflects the
  postulated kinetic structure" with a statement that the relation is
  a definitional consequence of identifying the kinetic-metric
  coefficient with the potential Hessian, with citation to
  ~\cite{arnold1989mathematical}. Replaced "the Newtonian-shaped
  equation of motion $M\ddot\mu = -\nabla V$ is obtained empirically
  once the kinetic-metric postulate is in place" with "is a
  structural consequence of the kinetic-metric postulate rather than
  a derived dynamical law."
- **Line 1930 (was 1933).** Replaced "The empirical mass-precision
  validation of Section sec:mass operates in the isolated-agent limit
  $\beta_{ij} = 0$" with "The boxed forms below are stated in the
  isolated-agent limit $\beta_{ij} = 0$."
- **Line 1981.** Same edit as line 1930.
- **Line 1960.** Replaced "the symmetric-attention or isolated-agent
  limits where the empirical validation of Section sec:mass operates"
  with "the symmetric-attention or isolated-agent limits where the
  boxed forms of Section sec:mass apply."
- **Line 2028.** Sharpened the kinetic-postulate hedge to acknowledge
  the circularity directly: $\omega^2 \propto m_{\text{eff}}^{-1}$ is
  a definitional consequence of the postulate when $k$ and $m$ are
  both equal to $M_{\mu\mu}$. Added the Arnold citation and stated
  that a non-trivial test would require an operationally independent
  measurement of $\omega$, deferred to future work.
- **Line 2983.** Removed the sentence "This identification is
  computationally validated in the empirical mass-precision study of
  Section sec:mass, which confirms $M_{\text{eff}} \propto
  \Sigma_p^{-1}$ with $R^2 = 0.9998$ and the harmonic-oscillator
  frequency scaling $\omega^2 \propto 1/M$." Replaced the surrounding
  framing ("the empirical regime where … is validated") with "on this
  slice as formally distinct objects … that agree by construction in
  the limit." Inserted a `\textbf{TODO}` for the future independent
  computational test.
- **Line 3547.** Removed the sentence "Hamiltonian belief dynamics
  for the mass-precision experiments use a velocity-Verlet symplectic
  integrator with energy drift $<0.001\%$ over $25$ time units." (No
  results from that integrator are reported in the manuscript; the
  description without results was misleading.)

### What survives

The boxed second-variation equations, the at-consensus cross-block
vanishing identity, the GL(d) precision-transport law, the asymmetric
attention caveat at §`sec:mass_block_caveats`, and the rock illustration
at line 2023 (read under its within-framework meaning of "dynamics" as
natural-gradient flow on $\mathcal{F}$) all stand without modification.
The section title "A Mass Analogy" remains a labeled novel
construction.

### Compile status

`pdflatex` on the post-edit file reports no new errors near any of the
edited lines (1846, 1848, 1930, 1960, 1981, 2028, 2983, 3540).
Pre-existing failures — missing `Fig_1.png` … `Fig_8.png`, a `\path{}`
inside the line-2488 figure caption that LaTeX treats as a moving
argument, and a Unicode ε at line 582 of `references.bib` — are
unchanged by this pass and are not caused by these edits.

### Bibliography touch

Added one in-text citation to `arnold1989mathematical` (Arnold 1989,
*Mathematical methods of classical mechanics*) at the kinetic-postulate
paragraphs (lines 1848 and 2028). The entry is already in
`references.bib:276`; no new bibliography entry was created. The
Marsden & Ratiu citation suggested by the verdict was not added because
the bibliography does not yet contain it; the Arnold citation alone
carries the standard-mechanics canon for the kinetic-vs-potential
independence requirement.

### Follow-up

The kinetic-metric circularity remains a real theoretical issue under
the section's current postulate; the edit acknowledges it explicitly
rather than papering over it. A future revision may want to either (a)
supply an operationally distinct kinetic metric (e.g., a Fisher-Rao
flow whose preconditioner differs from the second variation of
$\mathcal{F}$) and report $\omega^2$ measurements from that flow, or
(b) reframe the section as a stiffness-only result and drop the
"Mass" terminology from the title.

## Ouroboros tower: textbook identification edits (debate-driven)

Driven by the verdict at
`docs/debates/2026-05-19-ouroboros-ancestral-weights-bayesian-id/04_verdict.md`
(BLUE_WINS on the disjunctive identification claim through
Identification III broadened to the discount-factor DLM family).
The verdict required four concrete manuscript revisions to convert
the Ouroboros tower from a free-form posit into a named, citable
construction. All four applied.

### Edit 1 — Disambiguate "non-Markovian" at line 2196

The phrase "In this non-Markovian extension" was textually ambiguous
between two readings: (a) a long-memory generative process on the
scale index (which would require fractional-Brownian-motion machinery
per Beran 2010), or (b) a multi-source agent-level prior under a
Markovian-in-construction scale-process. The verdict's adjudication
(question 4) found the multi-source-prior reading the cleanest match
to the manuscript's structural form (which stores each ancestral
shadow as a separate field $h_i^{(s,k)}$ rather than as a marginal of
an AR(1) chain). The edit commits explicitly to that reading and
points forward to Eq.~\eqref{eq:ouroboros_F} for the operational form.

### Edit 2 — Explicit free-energy fragment at line 2208

Replaced the prose-only "exponentially decaying weights" sentence with
an explicit equation
$$ \mathcal{F}_{\mathrm{ouro}} = \sum_{k\ge 0}\lambda_0\rho^k\big[\mathrm{KL}(q_i\|\Omega q_{I_k}) + \mathrm{KL}(s_i\|\tilde\Omega s_{I_k})\big] $$
labelled `eq:ouroboros_F`, showing both state-fiber and model-fiber
contributions. The verdict's Action item (1) required this; red's
attack point (3) that the role of $\rho^k$ was underspecified in the
manuscript was correct on the textual reading, and is now resolved.

### Edit 3 — Textbook citation block at line 2208

Identified the construction as a discount-factor dynamic linear
model in the sense of West \& Harrison 1997 (primary), equivalently
a Gaussian log-linear pool / product-of-experts prior in the sense
of Genest \& Zidek 1986 and Hinton 2002 (adjacent). Cited the
standard correspondence between additive KL contributions and
product priors via Friston 2010 and Blei et al. 2017. The verdict
selected the discount-factor DLM as the primary identification
because that reference was named on the claim's own reference list
under Identification III (per `00_claim.md:26` of the Ouroboros
debate); the log-linear-pool identification is the algebraically
cleanest form for the Gaussian case and is cited alongside.

### Edit 4 — Normalization commit

Made the normalization choice explicit at line 2208. The
unrestricted default $\lambda_0, \rho > 0$ does not impose
$\sum_{k\ge 0}\lambda_k = 1$; the construction is therefore a
*tempered* free energy in the generalized-Bayesian sense of
Bissiri, Holmes \& Walker 2016, which is itself a standard
hierarchical-Bayesian object. The strictly-normalized special case
$\lambda_0 = 1 - \rho$ recovers the externally-Bayesian log-linear
pool of Genest \& Zidek 1986 and is registered as a special case.

This addresses red's tempering objection in
`03_red_rebuttal.md` of the Ouroboros debate; the ambiguity that
red flagged is now resolved by an explicit commit to the broader
tempered/generalized-Bayesian reading as default with the
normalized case named as a special.

### Bibliography additions

Four new entries added to `Attention/references.bib`:

- `WestHarrison1997` — *Bayesian Forecasting and Dynamic Models*,
  2nd ed., Springer. The discount-factor DLM canon.
- `GenestZidek1986` — "Combining probability distributions"
  (*Statistical Science*). The log-linear-pool axiomatic treatment
  and external-Bayesianity result.
- `HintonPoE2002` — "Training products of experts by minimizing
  contrastive divergence" (*Neural Computation*). The
  product-of-experts canon in the machine-learning literature.
- `BissiriHolmesWalker2016` — "A general framework for updating
  belief distributions" (*JRSS-B*). The generalized-Bayesian
  framework for tempered / power-weighted posterior updates.

### Verdict supersession

The earlier debate verdict at
`docs/debates/2026-05-19-agent-meta-agent-hierarchy-theory/03_blue_rebuttal.md`
conceded the Ouroboros tower as a "free-form posit ... out-of-scope
for theory mode." With these four revisions, that concession is
superseded: the Ouroboros tower is now a named hierarchical-Bayesian
construction (discount-factor DLM / log-linear pool with geometric
forgetting) with explicit free-energy form, citations, normalization
commit, and disambiguated Markovianity status.

## Late pass: eliminating-external-observations subsection cleanup (2026-05-19 PM)

Triggered by the red-blue-debate verdict at
`docs/debates/2026-05-19-eliminating-external-observations/04_verdict.md`
(RED_WINS on the compound correctness+motivation claim).

C1 (algebra) verified clean by both teams under sympy: Gaussian KL
closed form, mean-gradient match $\Lambda_o(\mu_i - c_k)$, covariance
gradient $\tfrac12(\Lambda_o - \Sigma_i^{-1})$ vs. negative
log-likelihood $\tfrac12\Lambda_o$ giving discrepancy
$-\tfrac12\Sigma_i^{-1}$, and the cross-entropy identity
$-\mathbb{E}_q[\log p] = \mathrm{KL}(q\|p) + H(q)$ from
[CoverThomas2006 §2.5].

C2 (motivation) failed on three independent grounds:

1. **Agent-definition deficit.** Environmental agents at lines
   1426–1431 supply only $q_{e_k}$ and an implicit $\phi_{e_k}$ via
   $\Omega = I$. The required primitive sections ($s_{e_k}$), derived
   sections ($r_{e_k}$), scale label, and meta-agent index of
   §`sec:agent_definition` (lines 617–626) are not supplied, and
   $p_{e_k} = q_{e_k}$ is stipulated by hand rather than derived as a
   cross-scale shadow per Eq. `eq:cross_scale_shadow`. The
   bottom-scale boundary clause at line 548 licenses absence of
   downward propagation, not hand-setting $p = q$.
2. **Cross-entropy substitution recovers the standard FEP operator.**
   The canonical free energy at lines 1252–1263 already contains
   term 5 (line 1260) as a cross-entropy
   $-\mathbb{E}_{q_i}[\log p(o(c)|\cdot)]$. The cross-entropy
   substitution at line 1442 is identification with that existing
   slot, which IS the standard FEP observation operator
   [Friston2010 §3; parr2022active §4.3]. The reformulation re-routes
   the source of the observation term (from data $o_k$ to env-agent
   density $q_{e_k}$) without eliminating it.
3. **Unconditional bookends vs. conditional body.** Title at 1394 and
   parsimony sentence at 1447 were unconditional; the proposition at
   1415–1423 and the body at 1398, 1423, 1445 are conditional. The
   cross-referenced §`sec:symmetry_breaking` at 1484 explicitly names
   env agents as external source fields (Zeeman analogy).

Per user instruction, A1–A5 implemented mechanically; A6 (optional
equation-level cross-scale construction) deferred.

### Edits applied to `Attention/Participatory_it_from_bit.tex`

- **A1, line 1394.** Title replaced with "Recasting External
  Observations as Environmental-Agent Couplings: Mean-Gradient
  Equivalence and a Cross-Entropy Resolution." The new title names
  what the section actually delivers rather than advertising an
  elimination the body conditions on.
- **A2, line 1409.** Replaced "internal to the framework, subject to
  the same information-geometric dynamics as all other agents" with
  an explicit boundary-class description: env agents have fast-channel
  and slow-channel dynamics fixed by construction
  ($q_{e_k} = p_{e_k}$, $\beta_{i,e_k} = 1$); they are carriers of
  sensory information, not autonomously F-minimizing agents. Added a
  sentence stating the agent-environment asymmetry is "relocated to
  the choice of which agents are dynamic and which are boundary,
  rather than removed."
- **A3, line 1428.** Added an explicit boundary-condition
  stipulation immediately after the $p_{e_k} = q_{e_k}$ definition:
  the equality is stipulated rather than derived through the
  cross-scale shadow relation, env agents do not inherit a prior from
  a scale-$(s+1)$ meta-agent, and primitive generative-model /
  hyper-prior sections ($s_{e_k}$, $r_{e_k}$), scale label, and
  meta-agent index are not specified. The env-agent sub-class is
  named as a degenerate special case of §`sec:agent_definition`.
- **A4, line 1442.** Labeled the cross-entropy substitution equation
  as `\label{eq:xent_resolution}` (referenced by A5). Added a
  paragraph after the cross-entropy identity stating that
  $-\mathbb{E}_{q_i}[\log q_{e_k}]$ is structurally identical to term
  5 of the canonical free energy at Eq. `eq:free_energy_functional_final`:
  the standard active-inference observation operator
  [Friston2010, parr2022active]. The substitution is identification
  with the existing observation slot; full variational equivalence
  re-routes the source of the observation term without eliminating
  it.
- **A5, line 1447.** Replaced the unconditional "observation-free
  formulation is more parsimonious ontologically — it requires only
  agents and their couplings, with no external reality providing
  special inputs" with a notational-parsimony framing under Eq.
  `eq:xent_resolution`. Explicit acknowledgment that the dependence
  on external information is not eliminated, that
  §`sec:symmetry_breaking` identifies env agents as external source
  fields, and that the Markov-blanket-of-sensors picture is a
  heuristic composition rather than an equation-level cross-scale
  construction.

### What was NOT changed

- All boxed equations and the explicit mean/covariance gradients
  (lines 1431–1439).
- The Dirac caveat at line 1425.
- The proposition statement at lines 1415–1423 (already
  conditional).
- The gauge-fixing $\Omega_{i,e_k} = I$ paragraph (now embedded
  after the A3 disclosure).
- The structural-analogy table at lines 1452–1464.

A6 (optional equation-level cross-scale construction realizing the
user's intended "observations as cross-scale agent communication"
reading) was not added; the verdict marked this as optional and
flagged it as the only path that delivers the user's stated intended
reading. A follow-up debate has been pre-flagged.

### Compile status

`pdflatex` reports no new errors near any of the edited lines
(1394, 1409, 1428–1431, 1442–1446, 1448). Pre-existing failures
(missing `Fig_*.png`, `\path` in moving argument at line 2488,
Unicode $\varepsilon$ in `references.bib`) are unchanged by this
pass.

### Bibliography touch

The A4 citation `~\cite{Friston2010, parr2022active}` reuses entries
already present in `references.bib` and used elsewhere in the same
section (line 1396 has the identical `~\cite{Friston2010, parr2022active}`
citation in the setup paragraph). No new bibliography entry added.

## RG-construction section: calibrating edits (debate-driven)

Driven by the verdict at
`docs/debates/2026-05-19-rg-construction-meta-agent/04_verdict.md`
(BLUE_WINS on the integrated claim with three calibrating manuscript
revisions). The verdict accepted all four sub-claims (A: pushforward
RG, B: closure ansatz, C: retention rules, D: RG framing) and
identified three labeling edits that sharpen rather than invalidate
the section.

### Edit 1 — Theorem 4 rename and post-proof remark

Renamed Theorem~\ref{thm:rg_exact_closure} from "Gaussian closure
with local-potential correction" to "Augmented-class closure with
explicit log-determinant correction" at line 4508. The previous
name placed strict Gaussian-KL closure in the foreground, but the
strict case ($A_{\xi\xi}$ independent of $Y$) is empty for any
Gaussian belief network with parent-dependent precisions — the
substantive content is the augmented-class form with the explicit
log-determinant potential $V(Y) = \tfrac{\tau}{2}\log\det'A_{\xi\xi}(Y)$.

Inserted citations to `[Wilson1974]` and `[Cardy1996]` in the
theorem statement to identify this augmentation as the canonical
Wilsonian effective-action correction generated by integrating out
fast modes (a standard step in renormalization-group calculations,
not an escape from the parametric class).

Added a `\begin{remark}...\end{remark}` block after the proof at
line 4523 stating explicitly that the strict-closure clause is
empty in the operational regime and that the augmented-class form
is the substantive deliverable. The remark also notes that the
log-determinant augmentation is itself iterable in the Wilsonian
sense.

This resolves the verdict's calibration on Theorem 4: the theorem
is now labeled to advertise what it actually delivers rather than a
sub-case that is empty in practice.

### Edit 2 — Proposition 1 paragraph heading

Changed the paragraph heading at line 4541 from "Controlled
approximate closure" to "Structural decomposition of the closure
residual." The previous heading promised quantitative control of
the closure residual, but the proposition delivers a structural
decomposition into named residual sources (dispersion, holonomy,
edge-marginal mismatch, anharmonic, non-Gaussian) without pinning
the constants $C_1, \ldots, C_6$, the closure norm $\|\cdot\|_\mathcal{B}$,
or the regularity class of the parent ansatz.

Added a one-sentence prefatory note that multiplicative constants
are left smoothness-class-dependent in the standard form of
schematic asymptotic bounds (cite `[Wong2001]`), and that a
quantitatively tight version is the natural follow-up. This makes
the labeling match what the proposition actually provides: a
catalog of residual sources, not a numerical bound.

The proposition body itself (line 4554) already registers this
limitation explicitly; the edit aligns the paragraph heading with
the body's own self-disclosure.

### Edit 3 — Theorem 5 local-derivability remark

Inserted a new `\begin{remark}[Local derivation of the Lipschitz
bound]...\end{remark}` block immediately before
Theorem~\ref{thm:rg_detector_retention} at line 4560. The remark
establishes that the local Lipschitz bound used as the theorem's
hypothesis is derivable under the closure-ansatz conditions
(i)--(v) by first-order Taylor expansion of the parent free energy
around the high-coherence configuration, with the constants
identifiable as $L_q \le \lambda_{\max}(F(q_I)) \cdot \max_i\|w_i^I\|$,
$L_p$ analogously, and $A_I = \tau \cdot m_I$ from
Eq.~\eqref{eq:rg_constrained_gap}.

Citation added: `[Amari2016]` for the operator-norm bound on the
Fisher information that anchors the Lipschitz constants.

This converts Theorem 5 from a free-standing conditional theorem
("Suppose $L$-Lipschitz...") to a conditional theorem with the
hypothesis explicitly grounded in the preceding closure-ansatz
conditions. The theorem statement itself is unchanged; the remark
supplies the missing derivation context.

### Bibliography touch (RG section)

No new bibliography entries required. All three edits use existing
keys: `Wilson1974` and `Cardy1996` are already present in
`references.bib` and used in the surrounding RG section; `Amari2016`
is already present (line 609 of `references.bib`) under
*Information Geometry and Its Applications*, Springer, the modern
textbook canon for the Fisher-information operator-norm bound that
anchors the Lipschitz constants.

### Verdict scope

The verdict explicitly noted that the section is not invalidated by
any of these edits; all three are calibrations of labeling rather
than substantive corrections. An optional follow-up math-mode debate
on a tighter quantitative version of Proposition 1 (pinning the
closure norm, regularity class, and Laplace-derived values of at
least $C_1$ and $C_5$) was flagged as user discretion in the
verdict's Action section. The user is now initiating that follow-up
debate.

