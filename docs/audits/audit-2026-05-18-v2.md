# Deep Audit — 2026-05-18 (v2, post-Session-6)

Triggered by user request: "deploy many vfe agents to deeply audit the codebase ... then deploy verifier agents." This is the second deep audit on 2026-05-18 — the first (`audit-2026-05-18.md`) closed 39 findings via batches A–F. Between that report and this one, six post-audit sessions landed in `post_edit_2026-05-18.md` (orphan warning, cleanup, CSV remap, run_test_evaluation, gauge_fixed_priors toggle, console + per-head plot). This audit re-sweeps for what those sessions introduced or missed.

## Scope

- `transformer/vfe/` — primary target (block.py, stack.py, e_step.py, attention.py, prior_bank.py, model.py, trainer.py, config.py, train_vfe.py, active_inference.py, efe.py, non_flat.py, head_mixer.py, positional.py, omega_direct.py, vfe_ablation_suite.py).
- `transformer/core/` — variational_ffn.py, vfe_gradients.py, transport_ops.py, kl_computation.py, gauge_utils.py, attention.py, blocks.py, prior_bank.py, em_modes.py, active_inference.py, expected_free_energy.py, vfe_utils.py, vfe_deq.py, vfe_closed_form.py, connection.py, embeddings.py, model.py.
- `transformer/training/`, `transformer/analysis/`, `transformer/utils/`, `transformer/visualization/`, `tests/`.

## Active config (resolved from `transformer/vfe/train_vfe.py`)

`gauge_fixed_priors=False, diagonal_covariance=True, use_rope=True, rope_full_gauge='off', prior_handoff_sigma=0.0, prior_handoff_rho=1.0, n_layers=1, n_e_steps=1, use_prior_bank=False, norm_type='layernorm', include_attention_entropy=True, lambda_align=4, lambda_soft=0.0, mass_phi=0.0, E_learnable_alpha=True, learnable_kappa=False, kappa=1.0, normalize_ce_by_dim=True, embed_dim=20, irrep_spec=[('fund', 2, 10)], e_mu_lr=0.4, e_sigma_lr=0.015, e_phi_lr=0.05, e_sigma_q_trust=5.0, track_layer_diagnostics=False, active_inference=False, use_non_flat_transport=False, gauge_parameterization='phi'`.

Under this config the orphan warning at `vfe/config.py:399-405` fires for both `e_sigma_lr` and `e_phi_lr` (`n_e_steps=1 ∧ n_layers=1 ∧ ¬use_prior_bank ∧ norm_type='layernorm'`). The fused-kernel branch is active. The full-cov σ-handoff branch is inactive (`prior_handoff_sigma=0`). MLP-mode connection is inactive (non-flat off).

## Methodology

Ten investigators in parallel (single message, ten `Agent` calls): five `vfe-codebase-auditor` invocations with disjoint focus (gauge equivariance, E-step purity, free-energy functional, prior-bank/decode, recent-changes) plus the standard five (`code-reviewer`, `debugger`, `refactoring-specialist`, `performance-engineer`, `python-pro`). Each returned ≤10 structured findings. Three `general-purpose` verifier agents then split the ~90 findings and re-read every cited source line — emitting CONFIRMED / REFUTED / INCONCLUSIVE with a `file.py:line` citation. The verifier rule "comment/docstring-only evidence ⇒ REFUTED" was applied strictly. Some refutations are technical (the underlying claim holds; the cited evidence was poor) — those are noted inline.

## Investigator Findings (consolidated)

### Gauge equivariance & transport (vfe-codebase-auditor #1)
- **G1 (low):** `vfe/stack.py:105,139` — cross-layer mean handoff carries μ posterior but resets φ to embedding (mixed-frame triple).
- **G2 (low, inactive):** `vfe/stack.py:114,127-131` — full-cov σ blend is a linear convex combination across gauge frames followed by an eigh floor; latent under `prior_handoff_sigma=0`.
- **G3 (info):** `core/connection.py:114-117` — MLP-mode contains `nn.Linear/nn.GELU/nn.Linear` (CLAUDE.md documented exception, opt-in).

All transport sandwich identities (`vfe/trainer.py::_compute_per_head_beta`, `core/transport_ops.py::_apply_rope_to_covariance`, `core/gauge_utils.fused_block_diagonal_kl_diag`, `vfe/prior_bank.py::_apply_gauge_transform`, `vfe/omega_direct.py`, `vfe/non_flat.py`) verified numerically by the investigator.

### E-step purity & EM boundary (vfe-codebase-auditor #2)
- **E1 (high):** `core/variational_ffn.py:1183,1315` + `core/em_modes.py:9-19` — default `em_mode='ift_phi'` does a single `phi_current.clone().requires_grad_(True)` + one `autograd.grad`. True IFT (Neumann series) lives only behind `use_deq=True` in `vfe_deq.py:17-25`. The label `ift_phi` is amortized-inference [BaiKolterKoltun2019] mislabelled.
- **E2 (low):** `vfe/e_step.py:924,930`; `core/vfe_utils.py:553-607` — CLAUDE.md spec describes a `decay_t` cosine factor on `e_*_lr`; code uses constants. Moot at `n_e_steps=1`.
- **E3 (low):** `vfe/e_step.py:1036-1039` — `_update_phi` retraction uses `create_graph=False`, so under `n_e_steps>1` only entry-time φ contributes a differentiable backward.
- **Confirmations:** orphan warning correct under active config (E4); E-step target-blind (E5); `sigma_p.detach()` at entry (E6); trust-region decoupled from LR (E7); fused/unfused gate consistent (E8); cross-layer cascade `mu_p` attached (E9).

### Free-energy functional (vfe-codebase-auditor #3)
- **F1 (high):** `vfe/efe.py:199`; `core/expected_free_energy.py:96-98, 132-133` — `risk + ambiguity = log V` constant in `preference_mode='uniform'` (the only mode used by `select_action`). EFE candidate scoring is driven entirely by `-epistemic_weight·epistemic`. The Parr/Pezzulo/Friston (2022) decomposition uses `ambiguity = E_q(z)[H[p(o|z)]]` (BALD-style), not `H[q_marginal]`.
- **F2 (medium):** `core/active_inference.py:197-201` — E-step "pragmatic" term is `-Σ p log p` of the model's own predictive (self-confidence), not preference cross-entropy. Mislabelled relative to standard EFE.
- **F3 (medium):** `vfe/efe.py:169-174` (preference-mode risk) vs `core/active_inference.py:197-201` (self-entropy) — two paths labelled "pragmatic" use different functionals.
- **F4 (medium):** `vfe/model.py:210-244`; `vfe/trainer.py:608` — outer M-step minimises `CE + 0.5·mass_phi·‖φ‖² + Σ aux_hyperparam`, not converged F. This is amortised inference [BaiKolterKoltun2019], not classical EM — documented in `e_step.py:39-49` but worth flagging if the manuscript labels it EM without qualification.
- **F5 (medium, citation refuted):** Realised F is a strict subset of canonical F (γ-coupled model coupling and `λ_h·KL(s‖h)` absent). Investigator cited the e_step.py docstring; verifier marked REFUTED on the "comment-only evidence" rule. The behavioural claim is independently verifiable: a Grep for `gamma_ij`, `lambda_h`, or any γ/λ_h symbol finds zero executable references. Treat as CONFIRMED in substance.
- **F6 / P3 (low):** `vfe/prior_bank.py:169,458-459` — `decode_log_scale: nn.Parameter(torch.zeros(1))`, `scale = exp(clamp(-3,3))`, init `c=1`. Adds learnable softmax temperature on top of canonical `−KL/τ`.
- **F7 (low):** `vfe/e_step.py:172-176` vs `499-502` — F-monotone check drops `+τ·log(N)·Σβ` constant. Harmless additive-constant asymmetry between diagnostic and aux paths.
- **F8 / R4 / B8 (medium):** `vfe/trainer.py:470` (`τ_h = κ·√d_h`) vs `core/vfe_gradients.py:1324-1325` (`κ·√K`) — per-head β diagnostic uses different temperature than training kernel. Diagnostic-only but mismatches manuscript plots against training.
- **F9 (info):** confirmed entropy κ never enters M-step under `learnable_kappa=False`.
- **F10 (medium):** `vfe/train_vfe.py:65`; `vfe/e_step.py:1026`; `core/vfe_gradients.py:1350` — `lambda_align=4` rescales `(Σβ·KL + entropy)` block; ratio preserved (softmax stationarity intact) but overall scale not in canonical manuscript form.

### PriorBank & decode (vfe-codebase-auditor #4)
- **P1 (major):** `vfe/prior_bank.py:187` (`clamp(0.01, sigma_max)`) vs `:200` (`clamp(self.eps=1e-6, sigma_max)`) — σ-floor four orders of magnitude tighter in gauge-fixed mode than direct mode. Direct mode (active user config) can drive σ_p arbitrarily small relative to the gauge-fixed claim of "same Gaussian manifold."
- **P2 (note):** `vfe/prior_bank.py:212-214` — direct mode's `_diag_to_full_block` produces strictly diagonal Σ; the `diagonal_covariance=False` toggle is silently a no-op for the prior in direct mode.
- **P4 (minor):** `vfe/trainer.py:135-140` — `decode_log_scale` bucketed under `m_sigma_params`; dead-parameter under `use_prior_bank=False`.
- **P5 (note):** `vfe/prior_bank.py:371-381` — φ_v decoupled from prior in direct mode; orbit interpretation `μ_v = A_v·μ_0` does not hold.
- **P6 (note):** `vfe/prior_bank.py:146-147` vs `:151-155` — direct mode init carries V·K independent random scalars; gauge-fixed mode carries K. `mu_init_std` has mode-dependent semantics.

### Recent-changes sweep (vfe-codebase-auditor #5)
- **R1 (high):** `vfe/train_vfe.py:137` (`'track_layer_diagnostics': False`) + `vfe/e_step.py:856` (diagnostics gated on flag) — user's config flips off the very flag Session 3 set to `True`, defeating the namespaced-CSV remap that depends on `_last_diagnostics` being populated.
- **R2 (medium):** `vfe/e_step.py:553`, `vfe/prior_bank.py:153` — orphan-warning lift table at `config.py:399-405` does not differentiate gauge-fixed vs direct mode; warning text under direct mode is misleading (the parameter does learn via attached `beliefs.sigma`, just not via the path the warning describes).
- **R3 / B8 (low):** `vfe/e_step.py:702` — `_can_use_fused` gate's `self.irrep_dims is not None` clause is vacuously true; `irrep_dims` is `List[int]` per `:252`.
- **R5 (low):** `vfe/trainer.py:516-521` — per-head plot fallback "single panel" branch is unreachable under any supported config.
- **R6 (low):** `vfe/trainer.py:749` — `run_test_evaluation` calls `model.eval()` but never restores `model.train()`. Defensive cleanup pattern in `_generate_sample` at `:300-309` not followed.
- **R7 (medium):** `vfe/prior_bank.py:198-201` — `_per_token_priors` returns `mu` in native dtype but force-promotes `sigma_diag` to float32. Silent dtype split under autocast/half-precision.
- **R8 (low):** `vfe/trainer.py:765-769` — test-eval CE accumulator does not guard `if n_tokens > 0`; NaN propagates if a batch is all padding. Latent risk; pre-existing pattern in `evaluate()` at `:709-711`.
- **R9 (low):** `vfe/e_step.py:840` — `_last_attention_state['kappa']` forces `.cpu().item()` on every final E-step iteration (every forward), not just when plotting.
- **R10 (low):** `vfe/trainer.py:912` + `vfe/model.py:223-224` — `train_loss_total` CSV column equals `ce/√K` under `normalize_ce_by_dim=True`. Reader sees `loss_total ≠ loss_ce` and likely interprets the gap as regulariser contribution.

### Debugger
- **B1 (CRITICAL/high):** `vfe/model.py:223-224` divides `ce_loss` by `√K` then adds raw `aux_hyperparam_loss` (lines 232-244) without the same scaling. Under the active config (`E_learnable_alpha=True, normalize_ce_by_dim=True, K=20`), aux gradient to `raw_c0/raw_b0` is `√K ≈ 4.47×` larger than the M-step CE gradient it should match.
- **B2 (high):** `vfe/e_step.py:843-855` stores `_last_attention_state['block_exp_pairs']` from the iteration-start `block_exp_pairs` (`:595`) but the φ update at `:954` runs after. The per-head β diagnostic is rendered from the *pre-update* φ, not the converged φ.
- **B3 (medium):** `vfe/e_step.py:887` — `sigma_q_std` uses default `unbiased=True` while every adjacent stat (`:870, :894, :904`) explicitly passes `unbiased=False`. Returns NaN for `numel=1` (e.g., single-token generation).
- **B4 (medium):** `vfe/e_step.py:692` — `kl_matrix = beta` in the `rope_full_gauge != 'off'` branch. Downstream `_last_kl_matrix` and `kl_mean/kl_max` diagnostics report attention weights, not KLs (off by 1-2 orders of magnitude in dashboards).
- **B5 (medium):** `vfe/e_step.py:789-804, 911, 924-954` — F-monitor records `F` at the *start* of each iteration. `f_final` is `F` pre the last update, not the converged value. Iteration-to-iteration descent is still monotone-checked correctly.
- **B6 / E2:** decay_t spec drift.
- **B7 (medium):** `vfe/trainer.py:403` — bare `except Exception: # noqa: BLE001` returns `None` silently in tokenizer-decode path; broken decoder silently produces blank attention-plot axis labels.
- **B9 (REFUTED, doc drift):** `core/gauge_utils.py:54,85` — signature `max_norm=10.0`, docstring "Default 20.0". Verifier refused on comment-only-evidence rule; the doc drift itself is real.
- **B10 (low):** `core/vfe_utils.py:1099-1113` — full-cov `eigvalsh` failure silently skips the condition-clamping with no logger emit.

### Code review
- **Q1, Q2 (REFUTED on rule technicality):** `core/attention.py:15`, `core/embeddings.py:14`, `utils/testing.py:13` ("Key Insight"); `training/train_fast.py:331`, `core/blocks.py:830` ("notably"). Verifier refused on "docstring evidence" rule, BUT CLAUDE.md explicitly bans these phrases in writing, and `analysis/scaling_report.py:30,33` lints for them. These are style-rule violations whose locus IS the docstring/comment — the verifier rule is misapplied here. Treat as CONFIRMED style violations.
- **Q3 (low):** `tests/transformer/test_evaluate_test_set.py:62, 74, 86`, `tests/transformer/test_train_fast.py:147` — tests still hardcode `weights_only=False`. Risk is low (tempdir self-saved); pattern teaches the unsafe form.
- **Q4 / T3 (high-type, low-impact):** `vfe/e_step.py:135-136` — `_f_monotone_step` `alpha_eff`, `kappa` lack annotations; every sibling helper in the file uses `'torch.Tensor | float'`.
- **Q5 (low):** `vfe/vfe_ablation_suite.py:709` — bare except prints to stdout (rest of module uses `logger`).
- **Q6 (low):** `vfe/trainer.py:313` — `_generate_sample` swallows all exceptions, returns sentinel string, no logger emit.
- **Q7 (low):** `vfe/e_step.py:172, 483, 881, 1024, 1331` (`1e-30`) vs `vfe/trainer.py:256` (`1e-10`) — two different β-log floors for the same conceptual operation.
- **Q8, Q9, Q10 (CONFIRMED negative):** no `nn.Linear/GELU` outside documented exceptions in `transformer/vfe/`; all production `torch.load` flows through `weights_only=not trusted`; no `pickle`/`shell=True`/`os.system`/`eval`-on-input.

### Refactoring / dead code
- **D1, D2 (high):** `transformer/training/config.py:217` (`omega_trust_region`), `:272` (`propagate_kl_nonfinite`) — two **un-annotated** mirror-only fields beyond the eight already tagged `(mirror-only)`. BlockConfig consumes the dict-side keys; TrainingConfig instance reads never happen.
- **D3 (medium):** `transformer/visualization/publication_gap_figures.py` — zero external references; not in `test_visualization_smoke.py:32-40` import list.
- **D4 (medium):** `transformer/utils/testing.py::diagnose` — only callsite is the module's own `__main__`. Ad-hoc dev script in a library path.
- **D5 (low):** `transformer/core/variational_ffn.py:75, 80, 81, 99, 100` — five dead re-imports (`_aggregate_multihead_vfe_debug`, `retract_spd_torch`, `retract_spd_diagonal_torch`, `_compute_vfe_gradients_block_diagonal`, `_compute_vfe_gradients_block_diagonal_diag`).
- **D6 (low):** `transformer/core/model.py:49-50` — `generate_so3_generators`, `generate_soN_generators` imported, never called.
- **D7 (low):** `transformer/core/vfe_gradients.py:39` — `SIGMA_EPS` imported, never referenced in body.
- **D8 (low):** `transformer/core/kl_computation.py:36` — `fused_block_matrix_exp_pairs` imported, never called.
- **D9 (low):** `transformer/core/vfe_utils.py:65-66` — `GRAD_CLIP_THRESHOLD`, `KAPPA_CLAMP_RANGE` defined module-locally, never read.
- **D10 (low):** `transformer/core/embeddings.py:38` — `import numpy as np` only used in a comment.
- **D11 (low):** `transformer/vfe/stack.py:39` — `List` imported, never referenced.

### Performance
- **PF1 (high):** `transformer/core/vfe_gradients.py:357, 388, 396, 403` — four unconditional `torch.isnan(X).any()` host syncs inside the full-cov per-block kernel. Not gated by `_kl_diagnostics_enabled()`.
- **PF2 (high):** `transformer/core/vfe_gradients.py:1876` — `compute_natural_gradient_gpu` unconditional `.any()` sync on the full-cov non-block-diagonal path.
- **PF3 (high):** `transformer/core/vfe_gradients.py:356, 1178, 1874, 1879, 1909, 2176, 2275` — `torch.eye(d, device, dtype)` allocated inside per-block loops; `_cached_eye` exists at `core/gauge_utils.py:35` but is used only by `newton_schulz_orthogonalise:155`.
- **PF4 (high):** `transformer/core/vfe_gradients.py:453, 486, 1898, 1917, 1946` — `.clone()` after `.expand(-1, -1, N, -1, -1)` materialises O(B·N²·d²) tensors; downstream einsums accept the non-contiguous view directly.
- **PF5 (medium):** `transformer/vfe/e_step.py:1001-1010` — `_update_phi` runs a separate `compute_kl_attention` for the `phi_for_grad` leaf; reduction from 3→2 Omega builds was the realised Batch-E win, but the third can be avoided entirely when `e_phi_lr=0`.
- **PF6 (medium):** `transformer/vfe/trainer.py:709-710, 766-767` — 2 `.item()` syncs per validation batch (4000 syncs per 2000-batch eval). Hoist into one tensor accumulator.
- **PF7 (medium):** `transformer/vfe/trainer.py:355-362` — `_collect_bayesian_alpha_stats` does 8 `.item()` syncs per log_interval; the batch-`.tolist()` pattern already used elsewhere applies.
- **PF8 (low):** `transformer/vfe/trainer.py:473` — `_compute_per_head_beta` allocates the `(N,N)` causal mask inside the per-head loop. Hoist out.
- **PF9 (low):** `transformer/vfe/train_vfe.py:172-178` — `create_dataloaders` called without `num_workers`; default `0` blocks GPU on every batch.
- **PF10 (info):** `transformer/vfe/model.py:151-152` — `invalidate_cache` correctly guarded by `if self.use_prior_bank`.

### Python / typing
- **T1 (high-type):** `transformer/core/vfe_utils.py:632-636` — `_retract_phi` four `T = None` defaults missing `Optional[T]`.
- **T2 (high-type):** `transformer/core/transport_ops.py:66-67` — `_build_rope_freqs` `torch.device = None` / `torch.dtype = None` missing Optional.
- **T3 / Q4:** `vfe/e_step.py:135-136` untyped `alpha_eff, kappa`.
- **T4 (high-type):** `transformer/vfe/trainer.py:406` — `_compute_per_head_beta -> Optional[Any]`. Returns `numpy.ndarray` on the success path.
- **T5 (medium):** `transformer/core/transport_ops.py:277, 363` — `compute_transport_operators(**kwargs)` documented sole key is `generators_are_skew`. Typo silently degrades the path.
- **T6 (medium):** `transformer/core/transport_ops.py:27-37` — `TransportDict(total=False)` contradicts the "always present" docstring claim for three of four fields. PEP 655 `NotRequired` or the two-class required/optional split is the right structure.
- **T7 (medium):** `transformer/core/vfe_utils.py:899-900` — `RecorderProtocol.record_step(*args, **kwargs)` erases the structural contract; conformers have concrete signatures the Protocol does not enforce.
- **T8 (medium):** `transformer/vfe/vfe_ablation_suite.py:404, 434, 446` — `_expand_range`, `_sweep_values` untyped param + bare `-> list`.
- **T9 (low):** `transformer/vfe/trainer.py:105-106` — `_metrics_tracker, _pub_tracker` initialised to `None` with no class-level `Optional[...]` annotation. Mypy infers `None` for the entire class scope.
- **T10 (REFUTED):** `vfe/prior_bank.py:203-215` — a diagonal matrix IS trivially block-diagonal with 1×1 blocks; docstring not strictly wrong.

## Verifier Verdicts (summary)

| Verifier | Findings | CONFIRMED | REFUTED | INCONCLUSIVE |
|---|---|---|---|---|
| A — gauge / E-step / functional / prior-bank | 28 | 27 | 1 (F5) | 0 |
| B — recent / debugger / code-review | 30 | 27 | 3 (B9, Q1, Q2) | 0 |
| C — refactor / perf / types | 31 | 30 | 1 (T10) | 0 |
| **Total** | **89** | **84** | **5** | **0** |

**Refutation analysis.**
- F5 (canonical-F subset): substantive claim CORRECT; investigator's evidence pointer was the docstring. A Grep for `gamma_ij`/`lambda_h` confirms absence. Promote to CONFIRMED.
- B9 (`max_norm` 10.0 vs 20.0): code says 10.0, docstring says 20.0. This *is* a doc-drift bug; verifier refused on the comment-only-evidence rule. Promote to CONFIRMED (low).
- Q1, Q2 (Claude-ism docstrings): violations of CLAUDE.md style rules whose locus is documentation. The verifier rule "comments are intent, not behaviour" applies to bug claims; style rules ARE about documentation text. The repo's own linter at `analysis/scaling_report.py:30,33` catches these. Promote to CONFIRMED (style violations, low severity).
- T10 (diagonal IS block-diagonal): genuine refutation; keep.

**Effective verdict counts after promotion:** CONFIRMED 88 / REFUTED 1 / INCONCLUSIVE 0.

## Confirmed Punch List (critical → high → medium)

### Critical
1. **[critical] B1 — aux-loss scale mismatch under `normalize_ce_by_dim=True`.** `vfe/model.py:223-244`, `vfe/e_step.py:443`. Under the user's active config, the `raw_c0/raw_b0` gradient is `√K ≈ 4.47×` larger than the CE gradient. Fix: divide `_aux_hyperparam_loss` by `√K` when `normalize_ce_by_dim=True`, or move the `/√K` rescaling above the `loss = loss + aux` accumulation.

### High
2. **[high] B2 — `_last_attention_state` cached from pre-φ-update exp pairs.** `vfe/e_step.py:843-855` stores from `:595` build site; φ update at `:954` runs *after*. Per-head β plots render from stale φ. Fix: move the cache assignment to after the φ update (and rebuild `block_exp_pairs` from the updated φ, or snapshot phi pre-update).
3. **[high] E1 — `em_mode='ift_phi'` is amortised inference, not IFT.** `core/variational_ffn.py:1183, 1315` + `core/em_modes.py:9-19`. Rename to `amortized_phi` OR document explicitly that `ift_phi` is the *name* of the default while true IFT requires `use_deq=True ∧ deq_include_phi=True`.
4. **[high] F1 — EFE `risk + ambiguity` cancels in `'uniform'` mode.** `vfe/efe.py:199`; `core/expected_free_energy.py:96-98, 132-133`. Sum equals `log V` constant; candidate scoring reduces to `-epistemic_weight·epistemic`. Fix: compute ambiguity as `E_q(z)[H[p(o|z)]]` via the BALD pass-1 mean entropy (the `mean_H` already available in `_compute_epistemic_value`), not as `H[q_marginal]`.
5. **[high] P1 — σ-floor asymmetry between gauge-fixed and direct prior modes.** `vfe/prior_bank.py:187` (0.01) vs `:200` (1e-6). Apply `clamp(0.01, sigma_max)` in `_per_token_priors` or document explicitly that direct mode allows sharper priors by design and is not a Law-3-equivalent reparameterisation.
6. **[high] R1 — `track_layer_diagnostics=False` defeats Session-3 namespaced CSV remap.** `vfe/train_vfe.py:137`; `vfe/e_step.py:856`. The user later flipped the flag that Session 3 explicitly turned on. Fix: flip to `True` (the Session-3 baseline) OR split diagnostics into a cheap subset that fires unconditionally.
7. **[high] D1 / D2 — two un-annotated mirror-only fields in `TrainingConfig`.** `training/config.py:217` (`omega_trust_region`), `:272` (`propagate_kl_nonfinite`). Match the existing `(mirror-only)` tag pattern at `:205-212`.
8. **[high] PF1 / PF2 — 5 unconditional `.any()` host syncs on the full-cov path.** `core/vfe_gradients.py:357, 388, 396, 403, 1876`. Replace with `torch.where(isnan(X), fallback, X)` mirroring the fused-path treatment at `:1180, 1209, 1217`; gate any `_nr(...)` counter on `_kl_diagnostics_enabled()`.
9. **[high] PF3 — `torch.eye(d, ...)` allocated in per-block hot loops; `_cached_eye` exists.** `core/vfe_gradients.py:356, 1178, 1874, 1879, 1909, 2176, 2275`. Import `_cached_eye` from `gauge_utils.py:35` and replace.
10. **[high] PF4 — `.clone()` after `.expand()` materialises O(B·N²·d²) tensors.** `core/vfe_gradients.py:453, 486, 1898, 1917, 1946`. Einsums accept non-contiguous views; drop the `.clone()`.

### Medium
11. **[medium] B3 — `sigma_q_std` uses `unbiased=True`, NaN on `numel=1`.** `vfe/e_step.py:887`. Change to `sigma.std(unbiased=False).item()` matching `:870, :894, :904`.
12. **[medium] B4 — `kl_matrix = beta` aliasing in `rope_full_gauge` branch.** `vfe/e_step.py:692`. `_last_kl_matrix` and `kl_mean/kl_max` diagnostics report attention weights, not KLs.
13. **[medium] B5 — `_f_monotone_step` records pre-update F as `f_final`.** `vfe/e_step.py:789-804, 911, 924-954`. Compute one additional post-update F on the final iteration, or rename to `f_pre_final_update`.
14. **[medium] B7 — bare `except Exception` in `_attention_summary`.** `vfe/trainer.py:403`. Narrow + add `logger.debug`.
15. **[medium] F2 — E-step "pragmatic" term is readout-entropy minimisation, not preference CE.** `core/active_inference.py:197-201`. Rename `pragmatic_weight` → `confidence_weight` OR accept a preference distribution and compute the canonical cross-entropy.
16. **[medium] F3 — two EFE paths label different formulas as "pragmatic".** `vfe/efe.py:169-174` vs `core/active_inference.py:197-201`. Unify through one helper that exposes preference-mode, ambiguity formula, and epistemic weight.
17. **[medium] F4 — M-step labelled EM but is amortised inference.** Docstring already discloses; ensure the manuscript labels the loop accordingly.
18. **[medium] F5 — realised F is a strict subset of canonical F.** Already disclosed in `vfe/e_step.py:13-49`. Ensure manuscript labels the absence of γ-coupling and `λ_h·KL(s‖h)`.
19. **[medium] F8 / R4 — per-head β diagnostic uses `τ_h = κ·√d_h`, training uses `τ = κ·√K` over summed-block KL.** `vfe/trainer.py:470` vs `core/vfe_gradients.py:1324-1325`. Either use `κ·√K` for the plot or annotate the figure caption.
20. **[medium] F10 — `lambda_align = 4` is an undocumented overall scale on `(Σβ·KL + entropy)`.** Disclose in manuscript.
21. **[medium] R2 — orphan-warning lift table ignores `gauge_fixed_priors` mode.** `vfe/config.py:399-405`. Either suppress under direct mode or rewrite text to clarify the path `sigma_log_embed` actually learns through.
22. **[medium] R7 — `_per_token_priors` dtype-inconsistent μ vs σ.** `vfe/prior_bank.py:198-201`. Promote `mu` consistently or drop the `.float()` on σ.
23. **[medium] PF5 — `_update_phi` runs a third Omega build.** `vfe/e_step.py:1001-1010`. Skip `_update_phi` entirely when `e_phi_lr=0` (which the active config approaches via the orphan condition).
24. **[medium] PF6 — 2 `.item()` syncs per validation batch (4000 per 2000-batch eval).** `vfe/trainer.py:709-710, 766-767`. Accumulate on GPU and call `.item()` once after the loop.
25. **[medium] PF7 — 8 `.item()` syncs per log interval in `_collect_bayesian_alpha_stats`.** `vfe/trainer.py:355-362`. Stack into a single tensor and `.tolist()` once.
26. **[medium] T5 — `compute_transport_operators(**kwargs)` silently swallows typos of `generators_are_skew`.** `core/transport_ops.py:277, 363`. Promote to explicit keyword parameter.
27. **[medium] T6 — `TransportDict(total=False)` contradicts the "always present" docstring claim.** `core/transport_ops.py:27-37`. Use PEP 655 `NotRequired` or two-class required/optional split.
28. **[medium] T7 — `RecorderProtocol` signatures erased by `*args/**kwargs`.** `core/vfe_utils.py:899-900`. Tighten to the minimal shared signature.
29. **[medium] T8 — `_expand_range`, `_sweep_values` untyped param + bare `-> list`.** `vfe/vfe_ablation_suite.py:404, 434, 446`.
30. **[medium] D3 — `transformer/visualization/publication_gap_figures.py` orphan.** Delete.
31. **[medium] D4 — `transformer/utils/testing.py::diagnose` is a dev script in a library path.** Move to `scripts/` or delete.

### Low (38 findings)

Group A — Notes on novel constructions / config-locality:
- G1, G2 (vfe/stack.py cross-layer frame-mixing notes; inactive at `prior_handoff_sigma=0`).
- G3, P2, P4, P5, P6 (gauge-fixed vs direct mode semantics, novel-construction disclosures).
- E2, E3, B6 (CLAUDE.md `decay_t` spec drift vs code).
- F6, F7, F9 (decode-temperature, F-monitor additive constant, fixed-κ).

Group B — Diagnostic / dashboard correctness:
- R3, B8 (dead `_can_use_fused` clause).
- R5, R6, R8, R9, R10 (per-head plot fallback unreachable; eval-mode not restored; NaN on all-pad; kappa sync per forward; `train_loss_total` includes /√K).
- B9 (`stable_matrix_exp_pair` max_norm doc drift, promoted from REFUTED).
- B10 (full-cov eigvalsh failure silently skips condition clamp).

Group C — Style / convention:
- Q1, Q2 (Claude-isms in docstrings/comments, promoted from REFUTED).
- Q3 (tests hardcode `weights_only=False`).
- Q5, Q6 (bare except patterns in vfe_ablation_suite.py:709, trainer.py:313).
- Q7 (1e-30 vs 1e-10 β-log-floor inconsistency).

Group D — Dead code:
- D5–D11 (5 dead re-imports in variational_ffn.py; 2 in core/model.py; 1 each in vfe_gradients.py, kl_computation.py, embeddings.py, stack.py; 2 dead constants in vfe_utils.py).

Group E — Type cleanups:
- T1, T2, T3/Q4, T4, T9 (`Optional[T]` corrections, untyped helper params, `Optional[Any]` leaks, missing class-level annotations).

Group F — Performance (low):
- PF8 (causal mask per-head allocation), PF9 (`num_workers=0`).

### Informational (negative results)
- Q8: no neural network components in `transformer/vfe/` beyond the documented decode-head exception.
- Q9: no unsafe `torch.load` paths remain in production code; Batch-D security flip is complete inside `transformer/`.
- Q10: no `pickle.load` / `shell=True` / `os.system` / `eval`-on-input anywhere.
- PF10: `invalidate_cache` correctly guarded.
- All sandwich-identity transport sites verified numerically by the gauge-equivariance investigator.

## Test Suite

- Command: `python -m pytest -x --tb=short -q`
- Result: **940 passed, 3 skipped, 46 warnings in 48.47s** — same count as the post-Batch-F baseline from this morning's audit. No regressions introduced by post-audit Sessions 1–6.
- Failures: none.

## What was deliberately NOT done

- **No fixes applied.** Per the deep-audit skill's "Audit + fix in one shot" red flag and CLAUDE.md's "ALWAYS PLAN MODE FIRST", this report presents the verified punch list; the user authorises any subsequent fix batch.
- **Manuscript files in `Attention/`** were not touched — F4/F5/F10/F2/F3 will need cross-references when the manuscript is revised, but those revisions are out of this audit's code-only scope.

## Cross-references

- Previous audit (same day): `docs/audits/audit-2026-05-18.md` (39 findings closed via batches A–F).
- Post-audit session log: `post_edit_2026-05-18.md` at repo root (Sessions 1–6).
- Investigator agent IDs (for SendMessage continuation): see `audit-2026-05-18-v2.log` if needed.
