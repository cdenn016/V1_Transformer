# Deep Audit — 2026-05-17

Branch: `fix/holonomy-numerics-2026-05-05`. Five parallel investigators
swept the repo, one independent verifier re-read every cited line.

## Scope

Full Python source tree, with priority on:

- `transformer/core/` — legacy variational FFN + attention path.
- `transformer/vfe/` — newer pure-VFE package.
- `transformer/baselines/`, `transformer/data/datasets.py` (+600 lines
  uncommitted), `transformer/utils/checkpoint.py`,
  `transformer/train_publication.py`.
- `scripts/`, `math_utils/`.

Out of scope: documentation, figures, the LaTeX manuscript, and the
`Attention/` write-ups.

## Investigators

| # | Subagent | Domain |
|---|---|---|
| 1 | `code-reviewer` | Quality, security, idioms |
| 2 | `debugger` | Bugs, gradient flow, theory-invariant violations |
| 3 | `refactoring-specialist` | Dead code, orphans, post-`29ac9b55` residue |
| 4 | `performance-engineer` | Hot-path allocations, redundant compute |
| 5 | `python-pro` | Type safety, public contracts |

Verifier: `general-purpose`, re-read every cited source location.

## Verifier Verdicts

| ID | Investigator | Finding | Verdict | Source |
|---|---|---|---|---|
| 1.1 | code-reviewer | Stale `.tmp` files in `transformer/core/` | CONFIRMED | `transformer/core/model.py.tmp.22376.6bb3b64bc3ac`, `variational_ffn.py.tmp.22376.868f54876483` |
| 1.2 | code-reviewer | `_LayerNormSigmaAdapter` uses `nn.LayerNorm`; current default in `train_vfe.py` | CONFIRMED | `transformer/vfe/block.py:46` `self.norm = nn.LayerNorm(dim)`; `transformer/vfe/train_vfe.py:103` `'norm_type': 'layernorm'` |
| 1.3 | code-reviewer | `forward` returns 3-tuple but typed as 2-tuple union | CONFIRMED | `transformer/vfe/model.py:87` annotation; line 168 `return logits, loss, ce_for_log` |
| 1.4 | code-reviewer | `pure_vfe/model.py` `torch.load(weights_only=False)` no `trusted=` | CONFIRMED | `transformer/pure_vfe/model.py:245` |
| 1.5 | code-reviewer | `save_checkpoint(optimizer, ...)` missing type hint | CONFIRMED | `transformer/utils/checkpoint.py:22` |
| 1.6 | code-reviewer | Invalid `Optional` defaults | CONFIRMED | `embeddings.py:90-91`, `vfe/block.py:48` (`= None` without `Optional[...]`) |
| 1.7 | code-reviewer | Bare `except Exception: pass` in debug path | CONFIRMED | `transformer/core/active_inference.py:407-408` |
| 1.8 | code-reviewer | `nn.Linear` in hybrid-baseline print line | REFUTED | Line 426 is a string literal; actual `nn.Linear` at line 415 is documented baseline path |
| 1.9 | code-reviewer | `torch.load(weights_only=False)` at 5 sites with no `trusted=` | CONFIRMED | `utils/evaluation.py:40`, `utils/test_query_variation.py:220,267`, `scripts/fiber_trajectory_analysis.py:111`, `scripts/gauge_geometry_analysis.py:114` |
| 1.10 | code-reviewer | Entry-point CLI parsers violate no-CLI policy | CONFIRMED | `run_ablation_suite.py:1102`, `kn5_baseline.py:419`, `generate_publication_figures.py:251`, `gauge_frame_spectral_analysis.py:1185` |
| 2.1 | debugger | `sigma_trust=self.e_sigma_lr` — trust region collapsed onto step LR | CONFIRMED | `transformer/vfe/e_step.py:493` |
| 2.2 | debugger | `f_history` monitor omits attention-entropy term | CONFIRMED | `transformer/vfe/e_step.py:377-380` (no `kappa·sqrt(K)·Σ β log(β/π)` term) |
| 2.3 | debugger | Closed-form diagonal transport ignores `exact_diagonal_transport` | CONFIRMED | `transformer/core/vfe_closed_form.py:181-183`; flag never referenced |
| 2.4 | debugger | `VFEConfig` missing `e_sigma_q_trust` field | CONFIRMED | `transformer/vfe/config.py:38-50`; `BlockConfig` has `E_sigma_q_trust: float = 5.0` at `block_config.py:109` |
| 2.5 | debugger | `log_kappa` not in `_update_phi` autograd inputs | CONFIRMED (not a bug) | `transformer/vfe/e_step.py:571-574`; kappa still gets gradient via outer `loss.backward()` |
| 2.6 | debugger | `skip_attention` warning dead for VFEBlock path | CONFIRMED | `transformer/core/block_config.py:256`; `VFEConfig.__post_init__` lacks equivalent |
| 2.7 | debugger | RoPE `exact` → `bch2` silent fallback for GL(K) | CONFIRMED | `transformer/core/embeddings.py:993-995` |
| 2.8 | debugger | Diagonal-Σ transport via `Omega**2 @ sigma` | CONFIRMED (allowed) | `transformer/core/embeddings.py:511`; matches CLAUDE.md diagonal-σ approximation |
| 2.9 | debugger | `isotropic_covariance` wiring is correct | CONFIRMED (no issue) | `transformer/vfe/e_step.py:494-496` |
| 2.10 | debugger | `VFEStack` in-place diagonal clamp on blended sigma | CONFIRMED (not a bug) | `transformer/vfe/stack.py:108-113`; target tensor is freshly allocated by `(1-rho)*init + rho*post` |
| 3.1 | refactoring-specialist | Stale `.tmp` artifacts (dup of 1.1) | CONFIRMED | Duplicate of 1.1 |
| 3.2 | refactoring-specialist | Dead imports in `variational_ffn.py` | CONFIRMED | `variational_ffn.py:90,93`: `compute_transport_operators`, `_nr` never invoked in executable body |
| 3.3 | refactoring-specialist | Dead store of legacy EM flags on `GaugeTransformerStack` | CONFIRMED | `transformer/core/blocks.py:1095-1096` |
| 3.4 | refactoring-specialist | `BlockConfig.cross_head_perm` never consumed by any block | CONFIRMED | `block_config.py:441,574`; permutation applied at `model.py` level instead |
| 3.5 | refactoring-specialist | Old 5-flag EM properties still public on `BlockConfig` | CONFIRMED | `block_config.py:319-332` |
| 3.6 | refactoring-specialist | Duplicate KL implementation in `train.py` vs `kl_computation.py` | CONFIRMED | `train.py:37-260+` vs `kl_computation.py:111+` |
| 3.7 | refactoring-specialist | `argparse` imports in 5 scripts | CONFIRMED | (overlap with 1.10) — adds `utils/evaluation.py:14`, `training/experiment_runner.py:27` |
| 3.8 | refactoring-specialist | `gauge_frame_spectral_analysis.py` imports `click` inside `__main__` | CONFIRMED | `scripts/gauge_frame_spectral_analysis.py:1183` |
| 3.9 | refactoring-specialist | `VFEConfig.E_learnable_alpha` never consumed | REFUTED | Read at `vfe/e_step.py:74-75,223,256` and `vfe/trainer.py:218-219` |
| 3.10 | refactoring-specialist | `BlockConfig.amortize_sigma` property has zero readers | CONFIRMED | `block_config.py:323-324`; only `_flags['amortize_sigma']` is consumed |
| 4.1 | performance-engineer | Full `(B,N,N,d_h,d_h)` Omega tensor in closed-form per head | CONFIRMED | `vfe_closed_form.py:180-187`; repeats at 413-418 |
| 4.2 | performance-engineer | `_update_phi` runs second full forward (KL + transport) per E-iter | CONFIRMED | `vfe/e_step.py:499, 527-545` |
| 4.3 | performance-engineer | `torch.eye` allocated inside hot per-head loops | CONFIRMED | `variational_ffn.py:965, 1111, 1132`; `blocks.py:838` |
| 4.4 | performance-engineer | ~15 `.item()` CUDA syncs in final-iteration diagnostics | CONFIRMED | `vfe/e_step.py:413-480` |
| 4.5 | performance-engineer | `optimizer.zero_grad()` missing `set_to_none=True` | CONFIRMED (minor) | `vfe/trainer.py:391`; PyTorch ≥2.0 already defaults to True |
| 4.6 | performance-engineer | Redundant `fused_block_matrix_exp_pairs` in CF phi branch | CONFIRMED | `vfe_closed_form.py:638-642` |
| 4.7 | performance-engineer | Unconditional `.stack().cpu().tolist()` in final iter | CONFIRMED | `variational_ffn.py:2235` |
| 4.8 | performance-engineer | `phi.clone()` instead of `phi.detach()` for grad leaf | CONFIRMED | `vfe/e_step.py:528` |
| 4.9 | performance-engineer | Full pairwise Omega in `aggregate_beliefs` on diagonal path | CONFIRMED | `vfe/attention.py:217-219` |
| 4.10 | performance-engineer | `_update_phi` recomputes transport ignoring cache | CONFIRMED | `vfe/e_step.py:535` does not forward `cached_block_exp_pairs` |
| 5.1 | python-pro | `em_mode: str` should be `Literal[...]` | CONFIRMED | `block_config.py:129` |
| 5.2 | python-pro | `save_checkpoint(optimizer, ...)` untyped (dup of 1.5) | CONFIRMED | `utils/checkpoint.py:22` |
| 5.3 | python-pro | `from_config(cross_head_perm=None)` missing annotation | CONFIRMED | `block_config.py:446` |
| 5.4 | python-pro | `cross_head_perm: Optional[object]` effectively `Any` | CONFIRMED | `block_config.py:441` |
| 5.5 | python-pro | `get_tokenizer` missing return annotation | CONFIRMED | `utils/checkpoint.py:189` |
| 5.6 | python-pro | Dataset `decode` methods untyped on input | CONFIRMED | `data/datasets.py:941, 1554, 1775` |
| 5.7 | python-pro | `Tuple | Tuple` union without `from __future__ import annotations` | CONFIRMED | `data/datasets.py:2202`; SyntaxError on Python 3.9 |
| 5.8 | python-pro | `training_mode`, `optimizer_type` as bare `str` | CONFIRMED | `training/config.py:42, 71` |
| 5.9 | python-pro | `VFEConfig` missing `Literal` import | CONFIRMED | `vfe/config.py:9` |
| 5.10 | python-pro | `_download_wikitext*_fallback` returns bare `dict` | CONFIRMED | `data/datasets.py:264, 320` |

Totals: 50 findings · 48 CONFIRMED · 2 REFUTED · 0 INCONCLUSIVE.

## Conflicts

None of substance. Three pairs overlap on the same location with the
same direction (1.1↔3.1 stale `.tmp` files, 1.5↔5.2 untyped optimizer,
1.10↔3.7 argparse violations). No two investigators contradict each
other on the same line.

## Severity Adjustments (from verifier)

- **1.2** stays high — `nn.LayerNorm` is the *current default* of the
  local `train_vfe.py` config, so production runs are using a
  learnable-affine norm. Per CLAUDE.md "LOCALLY DEFINED CONFIGS"
  reminder, the user may already be in this regime.
- **1.4** lowered to medium — only one site (`pure_vfe/model.py:245`)
  lacks `trusted=`; the canonical `checkpoint.py:53` already exposes
  the flag.
- **1.8** REFUTED outright — cited line is a string literal.
- **2.1** lowered critical → high — bug is real, but with
  `e_sigma_lr=0.001` the clamp would not bind in practice; effect is
  "trust region effectively absent" not "σ frozen".
- **2.5**, **2.10** lowered to low — not bugs; gradient still flows
  to kappa via outer backward, and the in-place clamp targets a
  freshly allocated tensor.
- **3.6** lowered to low — duplicate KL is a refactor opportunity,
  not a correctness issue.
- **4.3**, **4.5** lowered — `torch.eye` cost is small at K=64;
  `set_to_none=True` is the PyTorch ≥2.0 default.

## Confirmed Punch List (critical → high)

Ranked, executable items the user can authorise. None are auto-fixed
by this audit.

1. **[high]** `_LayerNormSigmaAdapter` is the active default and uses
   `nn.LayerNorm` (learnable affine) — `transformer/vfe/block.py:46` +
   `transformer/vfe/train_vfe.py:103`. Either remove the adapter and
   default to `mahalnorm`, or extend the documented exception list in
   CLAUDE.md to cover learnable-affine LayerNorm.
2. **[high]** `VFEConfig` is missing the `e_sigma_q_trust` field that
   CLAUDE.md documents as default 5.0 —
   `transformer/vfe/config.py:38-50`. As a result,
   `transformer/vfe/e_step.py:493` passes `sigma_trust=self.e_sigma_lr`,
   collapsing the trust region onto the step LR.
3. **[high]** Closed-form diagonal transport silently uses the
   approximation regardless of `exact_diagonal_transport=True` —
   `transformer/core/vfe_closed_form.py:181-183`. Closed-form users
   asking for the exact path get the diagonal one with no warning.
4. **[high]** `f_history` monotone monitor in the VFE E-step omits
   the `tau·β·log(β/π)` attention-entropy term when
   `include_attention_entropy=True` —
   `transformer/vfe/e_step.py:377-380`. Monitor measures a different
   objective than the one being descended.
5. **[high]** Closed-form path materialises the full `(B,N,N,d_h,d_h)`
   Omega tensor twice per head per E-step —
   `transformer/core/vfe_closed_form.py:180-187` and 413-418.
   Dominant memory term at large `N`.
6. **[high]** `_update_phi` does a second full `compute_kl_attention`
   pass per E-step iteration without forwarding the cache —
   `transformer/vfe/e_step.py:499, 527-545`.
7. **[high]** Final-iteration diagnostics block fires ~15 `.item()`
   syncs on every forward — `transformer/vfe/e_step.py:413-480`. Same
   pattern in `variational_ffn.py:2235`. Gate behind a flag.
8. **[high]** Two stale `.tmp` editor crash artifacts in source dir,
   not gitignored — `transformer/core/model.py.tmp.22376.*`,
   `transformer/core/variational_ffn.py.tmp.22376.*`.
9. **[high]** `em_mode: str` rather than `Literal[...]` —
   `transformer/core/block_config.py:129`. CLAUDE.md memory rule
   "no lenient config aliases" indicates the user expects strict
   typing here.
10. **[high]** `create_dataloaders` uses Python 3.10+ `Tuple|Tuple`
    union in annotation position without `from __future__ import
    annotations` — `transformer/data/datasets.py:2202`. SyntaxError on
    3.9 at import time.

## Test Suite

Command: `pytest` (against the repo root, after dataset-test
deletion; full run, no `-x`).

Result: **889 passed · 5 failed · 3 skipped** of 897 collected
(43.5 s wall, Python 3.14.4, pytest 9.0.2).

Failures empirically corroborate four audit findings:

| Failing Test | Corroborates Finding |
|---|---|
| `test_phi_traceless_under_projection.py::test_pos_phi_free_grad_does_not_populate_trace` — `ValueError: too many values to unpack (expected 2, got 3)` at `_, loss = model(...)` | **1.3** — `vfe/model.py:87` annotation says 2-tuple but body returns 3-tuple `(logits, loss, ce_for_log)`. |
| `test_vfe_package.py::TestVFEModel::test_forward_with_targets` | Same 2-tuple-vs-3-tuple regression; finding **1.3**. |
| `test_vfe_package.py::TestVFEModel::test_gradient_flow_to_prior_bank` — raises `RuntimeWarning: E-step iter 1: F increased … Monotone descent violated; check e_*_lr or conditioning.` | **2.1** (`sigma_trust=self.e_sigma_lr` collapses trust region) AND **2.2** (`f_history` monitor omits attention-entropy term, so the "violation" the monitor sees is measuring the wrong objective). |
| `test_vfe_package.py::TestLearnableKappa::test_gradient_flows_to_log_kappa` | **2.5** — `_update_phi` excludes `log_kappa` from its `autograd.grad` inputs; though the verifier flagged this as not-a-bug in isolation, the test asserts kappa receives a gradient and is empirically failing. |
| `test_vfe_gradients.py::TestComputeVFEGradientsGPUDiagonal::test_exact_diagonal_transport_fd_mu` | **2.3** — closed-form diagonal transport ignores the `exact_diagonal_transport` flag; the FD check on `exact_diagonal_transport=True` is the natural place this regresses. |

The five failing tests cluster on the `transformer/vfe/` package — the
newer "pure VFE" path that is still being stabilised on this branch.
None of the legacy `transformer/core/` tests fail.

## Notes for the User

- The two dataset test files were deleted as part of this audit pass
  per explicit instruction: `tests/transformer/test_data.py` and
  `tests/transformer/test_dataset_stride.py`.
- After the punch list was reviewed and the user authorised "begin
  fixing all", the audit findings were worked in six batches (see
  `Attention/2026-05-17_edits.md` for per-file detail). Final state:
  894 passed, 0 failed.
- Several investigators flagged the same items from different angles
  (the stale `.tmp` files, the untyped `optimizer` parameter, the
  argparse violations). These are deduplicated in the punch list.

## Fix Log (audit-pass closure)

| ID | Status | Note |
|---|---|---|
| 1.1 | FIXED | `transformer/core/*.tmp.22376.*` deleted. |
| 1.2 | KEPT | LayerNorm default retained per user; the construction-time `UserWarning` remains. |
| 1.3 | FIXED | `vfe/model.py:87` 3-tuple annotation + docstring; three stale test sites updated. |
| 1.4 | FIXED | `pure_vfe/model.py:245` — `trusted=True` parameter added. |
| 1.5 / 5.2 | FIXED | `checkpoint.py:21` — `optimizer: Optional[torch.optim.Optimizer]`. |
| 1.6 | FIXED | Naked `= None` defaults wrapped in `Optional[...]` in `embeddings.py` and `vfe/block.py`. |
| 1.7 | FIXED | `active_inference.py:407` — narrowed to `(ImportError, AttributeError)`. |
| 1.8 | REFUTED | Pre-existing verifier verdict (cited line was a string literal). |
| 1.9 | FIXED | Five sites now thread `trusted=`/`trusted_checkpoint=` matching `checkpoint.py`. |
| 1.10 / 3.7 / 3.8 | FIXED | All six argparse/click violations converted to click-to-run CONFIG dicts. |
| 2.1 | FIXED | `vfe/e_step.py:493` — `sigma_trust=self.e_sigma_q_trust`. |
| 2.2 | FIXED | `vfe/e_step.py:377-396` — `f_history` monitor adds the entropy term. |
| 2.3 | FIXED | `vfe_closed_form.py:run_closed_form_e_step` now lifts diagonal σ to a full matrix when `exact_diagonal_transport=True` and routes through the Cholesky-inverse branch; diagonal extracted at exit. |
| 2.4 | FIXED | `vfe/config.py` — `e_sigma_q_trust: float = 5.0` added and wired. |
| 2.5 | RESOLVED | Test passes; verifier-confirmed not-a-bug (kappa gradient flows via outer backward). |
| 2.6 | RESOLVED | Existing `BlockConfig` guard already correct for its scope; VFEConfig has no detaching em_modes. |
| 2.7 | FIXED | `embeddings.py:993-1015` — RoPE `exact→bch2` fallback now emits `RuntimeWarning`. |
| 2.8 | KEPT | Documented diagonal-σ approximation per CLAUDE.md (no change). |
| 2.9 | NO-OP | Wiring was already correct. |
| 2.10 | KEPT | Diagonal clamp targets a freshly allocated tensor (verifier-confirmed not-a-bug). |
| 3.1 | FIXED | Duplicate of 1.1. |
| 3.2 | FIXED | `variational_ffn.py:90,93` dead imports removed. |
| 3.3 | FIXED | `blocks.py:1095-1096` dead `amortized_inference`/`em_phi_mode` stores removed. |
| 3.4 | FIXED | `block_config.py` — `cross_head_perm` field and `from_config` kwarg removed; `model.py:338` call site cleaned. |
| 3.5 / 3.10 | FIXED | Four public `@property` wrappers on `BlockConfig` removed (no readers). |
| 3.6 | NOTED | Cross-reference comment added at `transformer/train.py:33` pointing to `transformer.core.kl_computation` as canonical. Two parallel implementations remain; future numerical drift should be resolved in favour of `kl_computation`. |
| 3.9 | REFUTED | Pre-existing verifier verdict (`E_learnable_alpha` is consumed by `vfe/e_step.py` and `vfe/trainer.py`). |
| 4.1 | FIXED | Closed-form diagonal path rewritten in `vfe_closed_form.py:180-194, 425-446` to compute `mu_j_t` and `sigma_j_t_diag` via two-step contractions; full `(B,N,N,d_h,d_h)` Omega tensor no longer materialised. Algebraic identity verified to 1e-12 at float64 by `tests/transformer/test_vfe_closed_form.py`. |
| 4.2 | WONTFIX | `_update_phi` second forward pass is required by autograd (cached pairs were built from `phi`, not `phi_for_grad`; reusing them would zero the phi gradient). Documenting comment at `vfe/e_step.py:549`. |
| 4.3 | FIXED | `VariationalFFNDynamic` now owns an `_eye_cache: Dict[(d, device, dtype), Tensor]` via helper `_get_eye(...)`; the three hot-path sites (`variational_ffn.py:961, 1107, 1128`) and `blocks.py:838` use the cache instead of allocating fresh identity matrices per call. |
| 4.4 | FIXED | `vfe/e_step.py:413` — diagnostics block gated behind `track_layer_diagnostics`. |
| 4.5 | FIXED | `vfe/trainer.py:391` — `set_to_none=True`. |
| 4.6 | FIXED | `vfe_closed_form.py:638` — redundant `fused_block_matrix_exp_pairs` removed. |
| 4.7 | FIXED | `track_iteration_diagnostics: bool = False` kwarg added to `VariationalFFNDynamic.__init__`, plumbed from `blocks.py:520`; the final-iter `.cpu()`/`.item()` block at `variational_ffn.py:2220-2247` is gated behind the flag. Trainers read `_e_step_grad_norms.get(key, 0.0)` so unpopulated state is a safe no-op. |
| 4.8 | FIXED | `vfe/e_step.py:528` — `phi.detach().requires_grad_(True)`. |
| 4.9 | FIXED | Same Omega-free contraction pattern applied to the `is_diagonal=True` branch of `transformer/vfe/attention.py::aggregate_beliefs:217-232`. Algebraic identity verified to 1e-12 at float64 (both `mixture` and `precision` modes) by `tests/transformer/test_aggregate_beliefs.py`. |
| 4.10 | REFUTED at fix time | Passing cached pairs would detach Omega from `phi_for_grad`'s autograd; comment added at `e_step.py:535`. |
| 5.1 | FIXED | `block_config.py:129` — `em_mode: Literal[...]`. |
| 5.3 / 5.4 | RESOLVED via 3.4 | Field removed entirely. |
| 5.5 | FIXED | `checkpoint.py:189` — `-> Optional[Any]`. |
| 5.6 | FIXED | Three dataset `decode(...)` methods typed `Union[List[int], torch.Tensor]`. |
| 5.7 | FIXED | `datasets.py` — added `from __future__ import annotations`. |
| 5.8 | FIXED | `training/config.py` — `training_mode`, `optimizer_type` retyped to `Literal[...]`. |
| 5.9 | FIXED | `vfe/config.py:9` — `Literal` imported. |
| 5.10 | FIXED | Two fallback download functions return `Dict[str, str]`. |

**Summary (initial pass, 2026-05-17 evening)**: 38 FIXED · 4 DEFERRED · 2 REFUTED · 6 RESOLVED-or-KEPT.

**Summary (deferred-item closure pass, 2026-05-17 late evening)**: 4 of 5 remaining deferred items moved to FIXED (4.1, 4.3, 4.7, 4.9, 2.3); 4.2 stays WONTFIX (autograd-required); 3.6 NOTED (cross-ref comment, no code change). Two new test files (`test_vfe_closed_form.py`, `test_aggregate_beliefs.py`) provide the previously-missing FD oracle coverage at float64 1e-12.

**Test gate (final)**: `pytest --no-header -p no:randomly` →
**908 passed · 3 skipped · 0 failed** (+14 tests from the new oracle files).

## Monotone-Descent Investigation (post-closure)

After the closure pass the `RuntimeWarning: E-step iter ...: F
increased ... Monotone descent violated` line still surfaced from
`transformer/vfe/e_step.py:407-413`. User asked for an investigation.

**Findings**:

1. **Confirmed mismatch (FIXED)**: the (mu, sigma) gradient passed to
   `compute_vfe_gradients_gpu` and
   `_compute_rope_full_gauge_gradient_per_head` used
   `lambda_softmax=self.lambda_soft` (typically 1.0) regardless of
   `include_attention_entropy`. With the entropy term in F, the
   envelope theorem at the softmax stationary point of beta gives
   `dF/dtheta = sum_j beta * dKL/dtheta` — the softmax-coupling
   term `sum_j KL * dbeta/dtheta` cancels exactly against the
   entropy-gradient term `tau * sum_j log(beta) * dbeta/dtheta`.
   The original code descended the entropy-suppressed surrogate
   `F_surr = sum_j beta * KL` while the monitor measured the
   manuscript F.

   **Fix**: when `include_attention_entropy=True`, pass
   `lambda_softmax=0.0`. Applied to:
   - `transformer/vfe/e_step.py:288` (rope_full_gauge per-head loop)
   - `transformer/vfe/e_step.py:344` (standard E-step gradient)
   - `transformer/core/variational_ffn.py:1947` (legacy rope path)
   - `transformer/core/variational_ffn.py:1964` (legacy fused path)
   - `transformer/core/variational_ffn.py:2006` (legacy standard path)

   The closed-form path (`vfe_closed_form.py`) already gates
   `lambda_softmax > 0` internally and is unaffected.

   Regression tests added under
   `tests/transformer/test_vfe_package.py::TestFMonitorEnvelopeRouting`
   verify the routing by spying on the gradient kernel kwarg.

2. **Residual non-monotonicity (DOCUMENTED, not fixed)**: even
   with envelope-correct gradient routing the F monitor continues
   to flag iteration-over-iteration increases. Root cause traced
   to a design-level partiality:
   `compute_vfe_gradients_gpu` returns the QUERY-side partial
   gradient only (`partial F / partial mu_k | mu_{j!=k} fixed`),
   per its FD oracle at
   `tests/transformer/test_vfe_gradients.py::test_exact_diagonal_transport_fd_mu`.
   The KEY-side contribution — how F changes when `mu_k` enters
   `KL_ik` for `i != k` via the transport `Omega_ik * mu_k` — is
   not summed in.

   For descent of the manuscript F (which is symmetric in
   i/j summation), the total gradient at `mu_k` should include
   both roles. The query-side-only step therefore does not
   strictly descend F; the monitor surfaces this.

   This is a long-standing design choice that pre-dates the audit
   (standard self-attention BP-style update). The transformer
   nonetheless trains successfully because (a) gradient
   accumulation over batches averages the query/key role
   asymmetry, (b) AdamW momentum + adaptive step compensates,
   and (c) the actual training target is the cross-entropy loss
   on logits, not the manuscript F. The F monitor is informational
   — its violation reflects "the natural-gradient step direction
   is the query-side partial, not the total" rather than
   "training is broken".

   **Status**: not fixed in this session. Closing it would require
   either (a) extending `compute_vfe_gradients_gpu` to compute the
   key-side contribution (substantial refactor; new FD oracle
   needed), (b) routing (mu, sigma) updates through autograd like
   `_update_phi` does (slower; loses the analytic fast-path), or
   (c) softening the monitor's tolerance / reframing its semantics.

**Test gate (after monotone-descent fix)**: `pytest --no-header
-p no:randomly` → **911 passed · 3 skipped · 0 failed** (+3 routing
regression tests). The lambda_softmax-routing fix did not eliminate
the warnings (deeper issue above), but it ensures the descent
direction matches the manuscript F at the softmax stationary point
of beta — a correctness improvement even if monotonicity remains
imperfect.
