# Red Rebuttal — wilson-observable-scope

## Concession

I grant blue's narrow code-state observation that `non_flat.py` is bitwise Regime~I at initialization. The strength gate is `self.raw_strength = nn.Parameter(torch.zeros(()))` at `transformer/vfe/non_flat.py:234`, the bilinear forms are `self.W_raw = nn.Parameter(torch.zeros(n_gen, K, K))` at `transformer/vfe/non_flat.py:228`, and `s = s_max · tanh(0) = 0` at line 259 so `δ_ij ≡ 0` for every edge at step 0. Under PIFB:874 (`default α = 0 together with δ_ij ≡ 0 recovers the vanishing-holonomy theorem`), a freshly instantiated `/vfe` model with `use_non_flat_transport=True` is indistinguishable from the flag-off path at step 0. The init-is-Regime-I half of blue's argument is factually correct.

I also grant that the identifiers `cocycle_relaxation` and `holonomy_penalty` literally exist in `transformer/core/connection.py` and `transformer/analysis/holonomy_metrics.py` and that the PIFB:868 and PIFB:874 implementation sentences are *true relative to that wider tree*.

What I do not grant is that init-state + wider-tree existence rescues the manuscript-vs-`/vfe` consistency gap.

## Core attack

Blue's load-bearing move is two-pronged: (i) the Wilson observable is Regime~II content per PIFB:824, PIFB:826, PIFB:880 and therefore out of the language-modeling scope, and (ii) causal attention makes the Wilson observable structurally trivial per PIFB:876 so the absence does not matter empirically. Both prongs collapse against PIFB:876 itself.

PIFB:876 reads: "The clearest experimental test of the gauge-curvature linguistic conjecture (Section~\ref{sec:gauge_curvature_conjecture}) therefore lives in bidirectional architectures where the holonomy is not a purely formal index-space invariant." This sentence is doing two things blue's defense cannot survive simultaneously.

First, it labels the Wilson observable as the *clearest experimental test of the gauge-curvature linguistic conjecture* — a language-modeling claim by the manuscript's own framing. Section~\ref{sec:gauge_curvature_conjecture} is a linguistic-conjecture section, not a gravitational-extension section. Blue cannot simultaneously cite PIFB:824 / PIFB:826 to push Wilson into the gravitational regime *and* leave PIFB:876 standing, because PIFB:876 explicitly relocates the Wilson observable back into the linguistic-conjecture program. The Regime~II label at PIFB:824 is a label about the *connection's algebraic form* (pure-gauge versus independent connection variable), not a label removing the construction from the language-modeling agenda; PIFB:876 is the disambiguation.

Second, PIFB:876 names *bidirectional architectures* as the experimental venue. The `/vfe` module has no bidirectional mode: `transformer/vfe/train_vfe.py:38` sets `'causal_lower_triangle': True`, `transformer/vfe/vfe_ablation_suite.py:96` sets the same, every persisted run in `transformer/vfe/vfe_runs/*/system_info.json` records `causal_lower_triangle: true`, and `transformer/vfe/config.py:449` defines the field with no `bidirectional` companion. So blue's argument actually *strengthens* the red attack: the manuscript locates Wilson's experimental test in bidirectional attention, and `/vfe` provides neither the bidirectional mode nor the Wilson observable to measure under it. The absence is doubled, not excused.

Blue's second defense — that `non_flat.py` is bitwise Regime~I at init — addresses only step 0. PIFB:874 exposes α as `cocycle_relaxation` precisely so the user can interpolate between Regime~I (α=0) and Regime~II (α=1) under training. The `/vfe` package has neither α (no `cocycle_relaxation` field per the grep at evidence pack line 8) nor the `holonomy_penalty` regularizer (PIFB:868) that would penalize unwanted Regime~II drift back toward the manuscript's stated default. A `/vfe` user who enables `use_non_flat_transport=True` and trains has no toggle to enforce α=0 and no regularizer to monitor the drift the manuscript guarantees a control surface for. Wilson 1974 §3 and Creutz 1983 ch.~5 define the Wilson loop as the canonical observable of a lattice gauge theory with link variables — PIFB:838 cites both as the source for Eq.~\ref{eq:edge_relaxed_omega} — so the absence of any observable means the `/vfe` non-flat path is running a lattice gauge construction without the canonical-source observable that defines it as such.

## Defense

Blue's wider-tree defense ("the identifiers exist in `transformer/core/`, `transformer/analysis/`, `transformer/pure_vfe/`") interprets PIFB:868 and PIFB:874 as referring to the broader codebase rather than to the user-nominated canonical pure path. The text at PIFB:868 reads "the form actually implemented as the optional `holonomy_penalty` regularizer" and PIFB:874 reads "The implementation exposes α as the `cocycle_relaxation` configuration parameter." Both sentences use the definite article — *the* implementation, *the* form actually implemented — without qualification. They do not say "in `transformer/core/`" or "in the broader codebase." If PIFB intends the wider tree, PIFB needs an explicit clarification; absent that clarification, the natural reading is that *the* implementation referred to by PIFB is the implementation the user has nominated as canonical, and that implementation is `transformer/vfe/` per the parent debate's scoping (`docs/debates/2026-05-20-vfe-module-purity-for-pifb/04_verdict.md`).

This is the manuscript-vs-code consistency gap I claimed in the opening: PIFB:868 and PIFB:874 commit "the implementation" to features that are missing from the `/vfe` subtree. Blue's defense resolves the gap by *moving the referent* of PIFB's implementation sentences outside `/vfe`. That is a legitimate move only if PIFB says so, and PIFB does not say so. The CLAUDE.md hard constraint "There should ALWAYS exist a theoretically/mathematically 'pure' path under appropriate toggles" makes the existence of the pure path mandatory; the parent debate identified `/vfe` as that path. PIFB then names two implementation referents that are *not* in the pure path. Either the manuscript needs amendment (clarify that `holonomy_penalty` and `cocycle_relaxation` live outside `/vfe` and explain why the pure path lacks them) or `/vfe` needs the referents — but the present asymmetry between PIFB's text and `/vfe`'s code is what the claim names a falsifying gap, and blue has not closed it.

The Wilson observable is the canonical observable of lattice gauge theory per `[Wilson 1974 §3]`, `[Kogut-Susskind 1975]`, `[Creutz 1983 ch.~5]`, all three of which PIFB:838 cites as the canonical source for Eq.~\ref{eq:edge_relaxed_omega} — the very construction `non_flat.py` realizes. The canonical-source rule of `debate_methodology.md` puts these three above either side's preferred reading: the implementation `/vfe` ships under `use_non_flat_transport=True` is a lattice gauge construction by PIFB's own citation, and the canonical observable of that construction is absent from `/vfe`. That is the falsifying gap, and blue's Regime-II-scoping and DAG-triviality moves do not patch it because PIFB:876 itself returns the Wilson observable to the linguistic-conjecture program and points to a bidirectional venue `/vfe` does not provide.
