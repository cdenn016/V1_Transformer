# Verdict — pifb-intro-epistemic-status

## Outcome

RED_WINS

## Decisive evidence

`Attention/Participatory_it_from_bit.tex:2735, :2774, :2783` enumerate six body-flagged postulates that drive the worked Lorentzian construction — (a) Regime I / $F_{\mu\nu} \equiv 0$ pure-gauge restriction (line 2735: "The construction in this subsection stays inside Regime~I … and the field strength $F_{\mu\nu}$ remains identically zero throughout"), (b) the $+\mathrm{tr}(AB)$ sign convention on $\mathfrak{g}$ (line 2774: "The choice of generator and the choice of bilinear form on $\mathfrak{g}$ are therefore part of the postulate set that drives the result"), (c) the non-compact generator choice $T = \mathrm{diag}(1,-1) \in \mathfrak{sl}(2,\mathbb{R})$ rather than compact $\mathfrak{so}(2)$ (line 2774: "this non-compact choice is what makes $\mathrm{tr}(T^2) > 0$ available"), (d) the separable ansatz $\psi_\tau = \psi_\tau(\tau), \psi_x = \psi_x(x)$ (line 2783: "We further restrict to the separable ansatz … Without separability, the connection would carry cross-derivative terms … that the displayed result suppresses"), (e) the imaginary temporal gauge-frame postulate (Eq. eq:complex_gauge_frame), and (f) the real-part projection $G^{\mathrm{Lor}}_{\mu\nu} := \mathrm{Re}(G_{\mu\nu})$. §1.7 line 142 lists three ("constant generator," "imaginary temporal gauge component," "real-part projection"); §sec:open_problems line 3499 lists a different three ("one base direction is designated temporal," "the corresponding gauge-frame component is multiplied by~$i$," "the imaginary off-diagonal piece … is discarded by a real-part projection"). Neither the §1.7 enumeration nor the §sec:open_problems enumeration carries the Regime I / $F_{\mu\nu}=0$ restriction, the $+\mathrm{tr}(AB)$ vs $-\mathrm{tr}(AB)$ sign convention, or the separability ansatz. Blue's rebuttal explicitly concedes this count mismatch ("The §1.7 entry names three of the six body-flagged postulates and omits three"). The claim's own parenthetical fixes the bound as "an imposed imaginary frame component, a real-part projection, and a 2D linearized setup" — exactly three — which the body's own line-by-line audit shows is undercounted.

A second decisive citation is the verb-choice slippage between §1.6 and §sec:transformers. `Attention/Participatory_it_from_bit.tex:125` reads "Standard scaled dot-product attention is recovered"; `Attention/Participatory_it_from_bit.tex:1601` reads "we therefore frame the result not as a derivation of $QK^\top$ attention from the gauge framework, but as a statement that standard attention is consistent with a broader gauge-covariant KL attention mechanism under these limits." The body uses "consistent with"; the §1.6 headline uses "recovered". The two are different epistemic claims. Line 1609 then states: "The empirical experiments in Section~\ref{sec:scaling_validation} implement the general gauge model without these limits." The trained architecture is the general gauge model; the trivial-frame standard-attention reduction is an analytic limit the sweep does not occupy.

A third citation is `Attention/Participatory_it_from_bit.tex:3509`, which records that "The structural target is the gauge-orbit / Haar-averaged consensus metric of Section~\ref{sec:consensus_metric}, which itself remains regulator-dependent and is presented there as a heuristic rather than a finite gauge-invariant observable." §1.7 contains no entry for this status. The §1.7 line "No quantitative physics predictions" (line 144) covers numerical-prediction absence; it does not cover the structural-observable-not-finitely-defined status of the framework's only candidate gauge-invariant geometric observable. The reader who stops at §1.7 carries away a strictly weaker scope than the body warrants.

## Reasoning

The claim is compound: it asserts §1.6 / §1.7 "accurately classify" the manuscript's deliverables across four sub-claims. Sub-claim (ii) — the Lorentzian-signature placement is "correctly bounded given §sec:worked_signature's content (an imposed imaginary frame component, a real-part projection, and a 2D linearized setup)" — is falsified on the body's own postulate-naming pass. The body identifies six postulates; the claim's parenthetical identifies three. Blue concedes the count and defends only the epistemic bucket ("Worked example only"). The claim as written is about whether §1.7's framing is correctly bounded against the body's content, not about whether the bucket label is approximately the right tier. The body's audit at lines 2735, 2774, and 2783 sets the bound at six body-flagged postulates plus a regime restriction, and §1.7's three-element enumeration undercounts that bound by a factor of two. Sub-claim (i) is further weakened by the §1.6 "recovered" vs §sec:transformers line 1601 "consistent with" verb-choice slippage and by line 1609's explicit statement that the sweep trains the general gauge model. The §1.7 completeness side — whether the catalogue is complete against the body's load-bearing scope items — is broken by the absence of a consensus-metric regulator-gap entry, which §sec:open_problems line 3509 itself flags as load-bearing for the within-species pullback agreement program and which is the framework's only candidate gauge-invariant geometric observable. Sub-claims (iii) and (iv) survive in form, but they cannot rescue the compound claim once (i), (ii), and the §1.7 completeness side carry red's evidence. Blue's defense rests on a re-reading of conjunct (ii) as bucket-correctness rather than bound-correctness; the claim text fixes the bound explicitly in its own parenthetical, so the re-reading does not survive.

## Action

Apply three textual edits to the introduction. First, expand the §1.7 line 142 enumeration to surface the three body-flagged postulates that §sec:worked_signature line 2735, 2774, and 2783 name and that line 3499 in §sec:open_problems also omits: the Regime I / $F_{\mu\nu} \equiv 0$ pure-gauge restriction, the $+\mathrm{tr}(AB)$ bilinear-form sign convention with the non-compact generator choice, and the separable-ansatz restriction. Bring §sec:open_problems line 3499 into postulate-count alignment with the resulting §1.7 list. Second, replace "Standard scaled dot-product attention is recovered" in §1.6 line 125 with the §sec:transformers line 1601 phrasing "Standard scaled dot-product attention is consistent with a zero-dimensional isotropic-Gaussian limit of the KL-consensus construction under three explicit reductions (isotropic covariances, trivial-frame transport, learned bilinear $M$)," and add an explicit sentence separating "analytic limit derived in §sec:transformers" from "empirical sweep on §sec:scaling_validation, which trains the general gauge model rather than the trivial-frame reduction." Third, add a §1.7 entry recording that the consensus metric is the framework's only candidate gauge-invariant geometric observable and is regulator-dependent / not finitely defined per §sec:consensus_metric line 2880 and §sec:open_problems line 3509. After these edits, re-spawn the debate with the same evidence pack to confirm the §1.6 / §1.7 framing now matches the body audit.
