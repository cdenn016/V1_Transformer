# Blue panel choice — Phase 3 rebuttal

Reuse of the Phase 2 panel. Red attacks three vectors covering exactly these lenses; no swap improves coverage.

| Tag | Justification |
|---|---|
| `philosophy-of-science` | Mandatory. Polices Lakatosian "degenerating programme" attack vs honest-disclosure reading; adjudicates the literal-vs-intent-faithful operationalization question that ultimately decides the verdict. |
| `gauge-theorist` | Holds the Vector 2 counter — $\Omega_{i,I}$ at `meta_agents.py:226-227` is a non-trivial frame-change, not identity-copy; separates "frame-change in $U_I U_i^{-1}$ canonical form" from "parallel transport of a specified connection" so blue claims only what the code supports. |
| `variational` | Holds Vector 3a — variational FE-improvement criterion vs threshold-detector surrogate. The manuscript at line 2174 explicitly flags the surrogate-vs-principled-object gap; tests whether honest flagging is sufficient under Bissiri-Holmes-Walker / Blei et al. standards. |
| `info-geometer` | Holds Vector 3b — IB closed form. Tests whether manuscript line 2138's explicit acknowledgement that Chechik-Tishby does not apply to group-valued $X$ is "honest enumeration of unsupplied ingredients" or "hand-wave-with-citation." |
| `implementation-engineer` | Holds Vector 1 reading — verifies the consensus detector at `meta_agents.py:55-91` against manuscript line 2174 line-by-line; verifies the χ collapse at line 256-263 is in `form_meta_agent` and NOT in `consensus_score`. Concedes the literal sub-claim 6 mismatch precisely. |

Selection is 5, includes `philosophy-of-science`, and continuous with Phase 2 panel.
