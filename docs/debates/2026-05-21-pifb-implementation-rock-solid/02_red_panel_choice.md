# Red Panel Choice — 2026-05-21-pifb-implementation-rock-solid (Phase 2 opening)

Mode: theory. Roster size: 10. Panel size: 5. `philosophy-of-science` mandatory.

| Tag | Justification (one sentence) |
|---|---|
| `philosophy-of-science` | Mandatory; needed to frame-check "rock solid and publication ready" against the section's own scope hedges (Wheeler "toy model demonstrating possibility", RG-inspired status, "we expect though do not directly measure"), and to decide whether the self-disclosed wounds at lines 2174 / 2197 / 2228 / 2284 are honest scope statements or load-bearing escape hatches that erase the demonstration claim. |
| `implementation-engineer` | Sub-claim 6 (manuscript-vs-code consistency) is half the §Implementation surface, and the operative simulator lives at `MAgent_Model-main/gauge_agent/meta_agents.py`; the consensus-detector form mismatch (`1 - KL` two-factor in code vs `exp(-V/τ) · P · C_q · C_s` three-factor in manuscript) and the line-2284 self-disclosed gap on $\Omega_{i,I}$ realization vs frame-trivial substitute can only be adjudicated by reading the simulator code, not the manuscript prose. |
| `info-geometer` | The IB Lagrangian at Eq. 2133 cites Tishby 1999 / Chechik-Tishby 2005 / Bialek 2001 with three unsupplied ingredients; the Karcher mean at Eq. 2156 on non-compact $\mathrm{GL}^+(K)$ at line 2160 sits on a Riemannian-geometry result with a known existence-uniqueness obstruction; the BCH first-order frame average at line 2191; the Gibbs $C_q = \exp(-V/\tau_q)$ vs `1-KL` form question is an information-geometric one (KL is unbounded above on a non-compact statistical manifold). |
| `gauge-theorist` | The cross-scale transport $\Omega_{i,I}$ at lines 2247 (top-down shadow priors) and 2270 (ouroboros free energy) is the load-bearing gauge object; the manuscript at line 2284 itself says it is "not independently verified" whether the simulator realizes the full transport or a frame-trivial substitute; the Karcher frame barycenter at line 2156 on non-compact $\mathrm{GL}^+(K)$ with the explicit no-bi-invariant-metric caveat at line 2160 is a gauge-theoretic obstruction. |
| `variational` | The free-energy improvement criterion at Eq. 2123 is the conjunctive top of the section ("a meta-agent forms when its post-aggregation FE plus a complexity cost is strictly less than the pre-aggregation FE"); the variational barycenter at Eqs. 2142 / $\mu_I^*$ / $\Sigma_I^*$ is a standard mean-field minimization with closed-form solution that needs verification against Amari / Bishop; the top-down shadow priors at Eq. 2247 are a cross-scale variational coupling whose Lagrangian closure is unstated; the ouroboros fragment at Eq. 2270 is a tempered geometric series of KL terms whose convergence in $\rho < 1$ needs canonical backing. |

Dropped from the theory default and from claim-keyword overrides:

- `geometer` — subsumed by `gauge-theorist` (Karcher / transport / sandwich) and `info-geometer` (Riemannian mean on statistical manifold). Adding `geometer` would duplicate the Karcher critique without opening a new vector.
- `transformer-ml` — the simulator (`gauge_agent/`) is an agent-based model, not a transformer; no attention / RoPE / multi-head content in §Implementation.
- `ml-engineer` — the "single-seed run" empirical-disclosure critique (line 2228) and the non-equilibrium aggregate indicator $E_{\mathrm{score}}$ critique (line 2301) are covered by `philosophy-of-science` (scope) and `info-geometer` (canonical non-equilibrium measures vs engineered indicator) respectively.
- `numerical-analyst` — fixed-point iteration on $(\mu, \Sigma, w)$ and BCH first-order approximation have numerical aspects but the load-bearing critique is gauge-theoretic / variational, not finite-precision; saving the slot for higher-yield lenses.
- `code-quality` — `implementation-engineer` already covers the simulator audit; code-quality is a weaker red vector for a manuscript-section claim.

This panel is biased toward attacking the manuscript-vs-code mismatch (sub-claim 6), the canonical-fidelity claims on IB / Karcher / BCH / pooling (sub-claim 2), the scope honesty of the RG-inspired and "we expect" disclosures (sub-claim 4), the self-disclosed gap on $\Omega_{i,I}$ (sub-claim 7), and the variational closure of the FE-improvement criterion and ouroboros fragment (sub-claim 1). Adversarial panel selection is permitted by the methodology.
