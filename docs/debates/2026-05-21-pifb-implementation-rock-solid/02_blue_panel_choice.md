# Blue Panel Choice — pifb-implementation-rock-solid (Phase 2, opening)

Mode: theory. Default theory panel is `geometer, info-geometer, variational, gauge-theorist, philosophy-of-science`. The blue side deviates from the default by swapping `geometer` for `implementation-engineer` because the strongest red attack runs through sub-claim 6 (manuscript-vs-simulator consensus-detector mismatch), which a pure-theory panel cannot adjudicate without reading `MAgent_Model-main/gauge_agent/meta_agents.py:55-91`. `gauge-theorist` already covers Karcher / non-compact $\mathrm{GL}^+(K)$ / BCH topology, so the redundancy with `geometer` is acceptable to displace.

| Tag | Justification |
|---|---|
| `philosophy-of-science` | Mandatory. Polices "RG-inspired" labeling, "heuristic and partial" disclaimer, the IB "research direction" framing, and circular-defense risk (defending the manuscript by citing the manuscript). Adjudicates whether §Implementation's epistemic disclaimers are reviewer-grade or evasive. |
| `gauge-theorist` | Karcher mean on non-compact $\mathrm{GL}^+(K)$ (line 2160), bi-invariant Riemannian metric existence, BCH first-order approximation for $\phi_I = \sum w_i \phi_i$ (line 2191), gauge-covariant transport in barycenter (lines 2181-2186). Verifies the Karcher caveat is real and discharged by compact-$\mathrm{SO}(N)$ scoping. |
| `variational` | Variational FE-improvement criterion (Eq. 2123-2125), IB Lagrangian sign convention and Gaussian-IB closed form (line 2133-2138), forward-KL barycenter (Eq. 2141-2152), variational vs IFT distinction. Owns the math correctness of sub-claim 1. |
| `info-geometer` | The four pooling-anchor citations at line 2275 (West-Harrison 1997, Hinton 2002, Genest-Zidek 1986, Bissiri-Holmes-Walker 2016) and the additive-KL ↔ product-of-experts / log-linear pool correspondence. Verifies each citation is the right paper for the right claim under sub-claim 2. |
| `implementation-engineer` | Reads `MAgent_Model-main/gauge_agent/meta_agents.py:35-149` directly. Determines whether the `1 - E` form and the two-factor consensus score are a calibration choice, a structural divergence, or load-bearing for §Implementation's claims. Adjudicates sub-claim 6 with `path:line` evidence rather than hand-waving. |
