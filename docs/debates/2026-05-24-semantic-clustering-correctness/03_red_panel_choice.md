# Red Panel Choice — Phase 3 rebuttal — semantic-clustering-correctness

The Agent sub-tool is unavailable in this environment (blue noted the same at `02_blue_opening.md:3`), so the five memos are authored in-role from the canon and the read code, written to disk for the judges, and synthesized in `03_red_rebuttal.md`. Exactly five experts, philosophy-of-science included.

| Tag | Justification |
|---|---|
| `philosophy-of-science` (mandatory) | Carries the load-bearing attack: "correct-by-design" rebuckets every present accuracy defect into a "completeness" bin and is therefore unfalsifiable in the Popper sense. Frame-checks the claim's own equivocation. |
| `gauge-theorist` | The φ-panel (whitened Euclidean on Lie-algebra coordinates) and the Ω-panel (left-invariant Frobenius distance on GL⁺(K) group elements) are different geometric objects. Needed to state precisely what the geodesic at `geometry.py:349-352` is and is not, and to concede the per-head quadrature cleanly. |
| `info-geometer` | Amari 1998 / Cencov 1972 reparameterization-invariance supplies the canon for why PCA-whitening is not a principled metric, sharpening blue's own concession into the panel-comparability attack. |
| `implementation-engineer` | Owns the `path:line` reachability for the vocab decode (130/200 unrenderable), the per-occurrence flatten (`extract.py:98-106`), and the unguarded block-restriction (`geometry.py:337-340`). Anchors the figure-correctness attack in executed code, not docstrings. |
| `code-quality` | A scientific-visualization figure is a deliverable; the bar for "correct" is whether a competent reader draws the right conclusion. Frames the byte-fallback vocab view and undisclosed duplicates as a deliverable-quality defect that the label "correct" papers over. |

`numerical-analyst` dropped: the only numerical questions (Ω-path agreement 4.4e-16, block-diagonality exact 0.0) are settled and conceded; no live numerical-stability vector remains.
