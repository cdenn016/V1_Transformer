# Memo — philosophy-of-science (blue, Phase 2, mandatory)

Authored in-role by the coordinator (dispatch tool unavailable); synthesized into `02_blue_opening.md`.

## Frame check
The claim separates two predicates: "correct-by-design" (geometry math + index alignment) and "complete/well-communicated" (disclosure, vocab-view informativeness, label salience, runtime guard). This is not an equivocation: the claim asserts the first and lists the second as genuine defects. The danger "correct misleads a viewer" is real only if the two predicates are conflated; the claim does not conflate them. So the framing is honest, not a dodge.

## Circularity policing
The defense must not cite `Attention/*.tex`, `CLAUDE.md`, or `user_theory_summary.md` as authority for canonical correctness. The opening derives every correctness vector from external canon (Nakahara2003, Hall2015, Lee2013, McInnes2018, Rousseeuw1987) or executed `path:line` evidence, and concedes whitening against Cencov/Bishop. No manuscript-as-authority. The "correct-by-design" label is grounded in the code's structural guarantees (flatten → per-occurrence; block-diagonal bank → exact restriction), not in the module's own docstrings.

## The live question
Whether shipping a byte-fallback-dominated vocab view (72/200 unique, 53% junk) as a publication-quality figure is a scientific-integrity defect — i.e., presenting a near-meaningless figure as if meaningful — is a falsifiability/communication judgment the judges must weigh. This is the single point on which the debate most plausibly turns, and it is correctly framed as the load-bearing concession, not hidden.

## Primary-source citation
[Cencov1972] (via the whitening concession) — the canonical invariance standard the defense holds itself to rather than appealing to the manuscript.
