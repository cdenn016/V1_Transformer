# Blue Panel Choice — pifb-signature-problem (Phase 2, opening)

Mode: theory + formal math. Defending a narrowly-scoped existence-demonstration claim. The five lenses below are the ones most likely to *defend* the claim on external-canon grounds, with `philosophy-of-science` mandatory.

| Expert | One-sentence justification |
|---|---|
| `philosophy-of-science` (mandatory) | The whole claim turns on a scope/falsifiability question — "existence demonstration, not derivation" — and on whether the hedges are complete; this lens polices both the frame and any manuscript-as-authority circularity in the defense. |
| `gauge-theorist` | The mechanism lives entirely in the Lie-algebra bilinear form `tr(A_μ A_ν)` on the connection sector, the choice of non-compact generator `T∈sl(2,ℝ)`, the SL(2,C)≅Spin⁺(1,3) and SO⁺(1,3) facts, and the gauge-noninvariance disclosure — all gauge-theory canon. |
| `geometer` | Sylvester's law of inertia (causal-cone signature count and the "GL(K,ℝ) preserves positive-definiteness" no-go), the rank-1→rank-2 real-part projection, and the local O(1,1) frame group are differential-geometry / linear-algebra facts that must be verified against standard texts. |
| `info-geometer` | The sector-split claim rests on Fisher-Rao positive-semidefiniteness, Gaussian KL ≥ 0 on the real fiber, and the assertion that complex Ω makes the Gaussian KL complex/negative — Cencov, Amari, the closed-form Gaussian KL are the canon here. |
| `numerical-analyst` | The first-order-parabolic vs hyperbolic signal-speed tension in the causal-cone route is a PDE-conditioning fact, and the manuscript's "numerically checked at K=2,3" sector-split claim is a finite-precision verification claim the panel can reproduce. |

Discounted from the roster: `variational` (the EM/ELBO machinery is not load-bearing for the signature claim — the claim explicitly disclaims dynamical derivation, so variational selection is the *open problem*, not the defended content), `transformer-ml`, `ml-engineer`, `implementation-engineer`, `code-quality` (no code implements this section — code-truth is N/A per the claim header).
