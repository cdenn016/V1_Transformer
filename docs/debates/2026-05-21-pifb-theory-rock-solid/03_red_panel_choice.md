# Red Panel Choice — 03 Rebuttal

Mode: theory. Round: rebuttal (Phase 3). Side: red.

## Selected panel (5 of 10) — same as Phase 2

| Tag | One-sentence justification |
|---|---|
| `philosophy-of-science` | Mandatory under the methodology, and load-bearing here: blue's opening explicitly concedes the conjunctive operationalization fails under literal reading and invites the chief judge to choose a reading. The frame-check on that move is the core of the rebuttal. |
| `gauge-theorist` | Blue's evidence item 5 rests on the residual-subgroup discipline and the edge-mode / QRF citation chain. Needs a continuity check for whether Role B language past line 571 actually attaches the residual restriction at every load-bearing occurrence, and whether the lattice-gauge edge-mode license transfers. |
| `variational` | Blue's evidence item 4 puts decisive weight on the cross-scale shadow's $\sigma^2\to 0$ rigid-link reduction "closing" the standard-FEP gap via a ladder-VAE generative model. The standard hierarchical-VI corpus uses finite-variance conditionals, not Dirac singularities; the manuscript itself flags the Dirac case as ill-defined at line 1458. Owns this strike. |
| `info-geometer` | Blue's evidence item 3 rests on the envelope-theorem reduction at line 1404 holding uniformly past that line, and on the Gaussian KL matching the textbook closed form. Needs the canonical-fidelity check on the per-coordinate $\alpha$ Bregman-Lagrangian and the autograd-vs-reduced-free-energy distinction downstream. |
| `geometer` | Blue's evidence items 1–2 grant the sandwich rule at line 614 and the GL-correct precision transport at line 1894. Concession territory. Geometer's remaining role is to check whether the asymmetric-attention Lyapunov fallback at line 1994 and the Hessian-as-mass identification at line 2064 actually survive the kinetic-metric postulate that blue's defense leaves untouched. |

## Rationale for no swap

The blue opening engages on six fronts (sandwich, precision transport, KL closed form, envelope theorem, mixture-of-sources ansatz, ladder-VAE cross-scale reduction, dual-role gauge frame, Goldstone analogy, notation/analogy labeling) and concedes the conjunctive operationalization fails on sub-claims 5 and 6 under literal reading. None of the engagement points has shifted the claim toward `numerical-analyst` (no fp16 / condition-number content) or `transformer-ml` (multi-head deferral is a self-containedness attack, not a transformer-architecture attack) territory. The Phase-2 panel covers every blue front and the new operationalization-as-binding-text strike sits inside `philosophy-of-science`.
