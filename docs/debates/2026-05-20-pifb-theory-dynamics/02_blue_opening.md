# Blue Opening — pifb-theory-dynamics

## Steelman (opposing position)

The strongest red-team attack is that the §Theory dynamics block silently slides between (a) the *full* variational free energy whose stationarity in the row-Lagrangian gives softmax β, (b) the *reduced* free energy `−τ log Z_i` obtained by substituting the optimal β back in, and (c) the *entropy-suppressed surrogate* `Σ_ij β* KL(q_i ‖ Ω_ij q_j)` that autograd implementations actually differentiate; that the Mass Analogy presents `ω² ∝ 1/m_eff` as Newtonian without making the kinetic postulate operationally testable; and that the transformer-recovery section claims more than the gauge-fixing limits earn.

## Position

The seven sub-claims of `00_claim.md` survive on the manuscript text as written, because the four load-bearing distinctions red would attack are stated *in the manuscript itself*, at the lines the evidence pack cites, with the surrogate-vs-canonical gradient gap given as an exact covariance and the Newtonian reading explicitly restricted to the symmetric / isolated-agent limit. The §Theory dynamics block is defensible as a derivation of softmax attention from F-stationarity, a postulate-flagged stiffness/mass analogy, and a gauge-fixed transformer reduction labeled as such — not as a derivation of inertial mass from precision or of dot-product attention from gauge theory.

## Evidence

### B1. Softmax attention is derived as the row-Lagrangian stationarity condition of the *full* F that includes the attention-entropy term, and the entropy-suppressed surrogate is explicitly labeled non-equivalent.

The pointwise free energy at line 1238 includes the term `τ Σ_ij β_ij(c) log(β_ij(c)/π_ij(c))` *explicitly inside* F. The boxed complete functional at line 1258 carries the same term `τ β_ij(c) log(β_ij(c)/π̃_ij(c))` under the integral. The row-Lagrangian is written out in full at line 1266: `L_i = ∫ Σ_j [β_ij KL(q_i ‖ Ω_ij q_j) + τ β_ij log(β_ij/π̃_ij)] − λ_i(c)(Σ_j β_ij − 1)`. Setting `∂L/∂β_ij = 0` is carried out at lines 1086–1110 of `sec:mixture_derivation` and again at 1266–1280 of `sec:complete_free_energy`, yielding the boxed softmax (eq. `beta_optimal`, line 1268) and the prior-free softmax `β_ik = softmax_k(−E_ik)` (eq. `mixture_softmax`, line 1108). The derivation is the canonical Lagrange-multiplier solution to an entropy-regularized energy minimization — the same construction that produces the softmax mean-field solution in the Gibbs / Boltzmann form `[BleiKuckelbirgJordan2017 Eq. 11]` and the Helmholtz-free-energy formula `F = ⟨E⟩ − T·H` in statistical mechanics.

The CLAUDE.md "entropy-suppressed surrogate" warning is addressed directly inside the manuscript, not as an external caveat. Line 1245 reads: "The entropy-suppressed surrogate `Σ_ij β*_ij KL(q_i ‖ Ω_ij q_j)` that is sometimes used in autograd implementations has the same value at β = β* as the full functional minus a constant, but its x-gradient differs from `∇F_red` by `−τ⁻¹ Cov_β*(E, ∇_x E)` (which need not vanish), so the surrogate and the reduced free energy do not share stationary points in general." This is repeated at line 1282 inside the boxed complete-F discussion, and again at line 1371 under "Autograd versus reduced-free-energy gradients" with the explicit identity `∇⟨E⟩_β − ∇F_red = −τ⁻¹ Cov_β*(E, ∇_x E)`. The manuscript names which of the three objects is canonical (the full functional, with the reduced form as its envelope) at line 1245 and 1282. This is the same identity that `CLAUDE.md` flags; it is *in* the manuscript.

### B2. The Mass Analogy registers the kinetic-metric postulate as separate from F, incorporates the Pullback Gravity forward-reference, and concedes the operational-independence gap.

Line 1847 opens `sec:mass` by stating that the construction yields "a postulate-dependent mass-like scaling `ω² ∝ m_eff^{-1}` in the isolated-agent harmonic limit, once the kinetic-metric postulate of Section sec:velocity_quadratic is in place." The very next paragraph (line 1849) is titled "What the Hessian gives, and what is added" and writes "the Hessian of `F` that we derive in this section is therefore, in the first instance, a *stiffness* on belief configuration space, not a mass. The identification of `Σ_p^{-1}` with an effective mass requires a separate kinetic-metric postulate." It explicitly cites `[arnold1989mathematical]` for the standard Lagrangian-mechanics requirement that the inertia tensor and the potential Hessian be operationally independent (0,2)-tensors at the equilibrium configuration, and concedes that "the present construction reuses the same matrix `M_μμ` for both roles and therefore does not supply such an independent test." Lines 2013–2024 form the "Within-Framework Interpretation: Stiffness as Precision" paragraph, which reiterates that "the identification of the Hessian sector `[M_μμ]` with an 'effective mass' is interpretive within the framework rather than a derivation of physical inertial mass."

The Pullback Gravity forward-reference is at line 2026, exactly where the evidence pack places it: "Under the structural-tier reading developed in Section sec:gravity_pullback, perceived geometry is moved to the model-fiber generative-model precision `Σ_{s,i}^{-1}`, and inertial mass on the state fiber and any pullback-curvature reading of gravitational coupling on the model fiber decouple." Line 2031 opens `sec:velocity_quadratic` with the paragraph title "This is a postulate, not a consequence of `F`" — verbatim the conclusion from the Kinetic Term Discussion debate at commit a0d4a53f. The postulate is registered three times in `sec:mass` (1847, 1849, 2031) and once in the structural-tier paragraph (2026); this is not a buried caveat.

### B3. Transformer recovery is explicitly framed as a gauge-fixed limit, with the boxed `Q_i = U_i^{-1}μ_i`, `K_j = U_j^⊤ Σ_j^{-1}μ_j` carving derived from the untied per-token-frame route, and the constant-gauge presentation corrected as structurally inconsistent.

Line 1574 opens `sec:transformers`: "We now show that standard scaled dot-product attention is recovered as a gauge-fixed and isotropic-Gaussian limit of the construction, **not derived uniquely from the gauge-theoretic framework**." The section labels the recovery as a "statement that standard attention is consistent with a broader gauge-covariant KL attention mechanism under these limits." Two parallel routes are developed: the per-token-frame route (`sec:untied_qk`, 1675–1715) and the trivial-frame route (`sec:dot_product_derivation`, 1717–1810). The boxed untied carving `Q_i = U_i^{-1}μ_i`, `K_j = U_j^⊤ Σ_j^{-1}μ_j` appears at line 1699 (eq. `gauge_qk`), derived from the exact transported-Gaussian KL decomposition of eq. `full_kl_general` (line 1681) — verified "symbolically against the direct Gaussian KL to machine precision" (line 1690). The implicit pair-dependent bilinear `M_ij = Ω_ij^{-⊤} Σ_j^{-1}` of line 1706 is shown to range over all of `GL(d_k)` via polar decomposition, so the gauge-derived bilinear has the same expressive power as an independently learned `W_Q W_K^⊤`.

The "constant gauge limit" presentation that earlier drafts and the wider literature sometimes use is corrected inside the section. Line 1666 reads: "We want to flag that this presentation is structurally inconsistent with the vertex-frame parameterization of this manuscript and replace it with the correct construction. Under `Ω_ij = U_i U_j^{-1}` the self-pair gives `Ω_ii = I`, and the cocycle `Ω_ij Ω_jk = Ω_ik` at `i = k` forces `Ω_ij Ω_ji = I`; pair-independence `Ω_ij = Ω` then yields `Ω² = Ω`, and invertibility forces `Ω = I`." This forces the separation of pairwise transport from the shared bilinear compatibility `M`, which the rest of the subsection executes. The complete attention formula at line 1837 is boxed and labeled with the explicit qualifier "with `κ = 1` recovering the standard scaled dot-product attention of `[vaswani2017attention]`" — i.e., the standard form `[Vaswani2017 §3.2.1]` is recovered as a one-parameter limit, not asserted as identical.

The framing is consistent with the Discussion subsection's "one interpretive lens" language: line 1842 of `sec:transformers` writes "The limits are deliberately aggressive: they collapse the statistical manifold into a basic Euclidean space and absorb the gauge parameters into the learned matrices. Each limit discards a specific geometric degree of freedom and each can be independently relaxed." The gauge-fixed reading is explicit in the manuscript, and the Gauge-VFE Transformer debate edit at commit 3191deae added the matching "(under gauge-fixed reading)" qualifier at line 3190 of the Discussion section.

### B4. The asymmetric-attention caveat at sec:mass_block_caveats is internally cross-referenced from both the mean-sector and the covariance-sector off-diagonal blocks, and is restated in the Velocity-Quadratic Metric Form section.

Lines 1958–1961 are the explicit caveat block. Line 1958 introduces `sec:mass_block_caveats` and clarifies that block-transpose symmetry of the Hessian is automatic by Schwarz's theorem; line 1961 then states the substantive content: "What can fail under asymmetric attention is not block-transpose symmetry of the Hessian but the existence of a global potential whose second variation matches the running update rule: if attention weights `β_ij` are treated as instantaneous and frozen-asymmetric (`β_ik ≠ β_ki`) within the dynamics, the resulting flow is no longer the gradient flow of `F` alone, and the bilinear form `M_μμ` does not function as the inertia tensor of a conservative Hamiltonian. The reciprocal-attention conditions `β_ik = β_ki` and `Ω_ik Ω_ki = I` are sufficient for the dynamics to remain gradient-like and for the kinetic-energy reading to be Newtonian. In the asymmetric regime the second variation is best read as a Lyapunov / dissipative metric on configuration space; the Newtonian reading is recovered cleanly in the symmetric-attention or isolated-agent limits where the boxed forms of Section sec:mass apply."

This caveat is referenced from the covariance-sector off-diagonal block at line 1992: "The asymmetric-attention caveat from §sec:mass_block_caveats applies symmetrically to the covariance sector." It is also referenced from `sec:velocity_quadratic` at line 1961's parent claim, and the postulate-flagging paragraph at line 1849 explicitly defers to it: "The asymmetric-attention case explicitly lacks a conservative-Hamiltonian reading (Section sec:mass_block_caveats below)." The cross-referencing is multi-directional, not one-shot.

### B5. The autograd-vs-reduced-F convention is registered as a deliberate switch, not silently elided.

Line 1934 of `sec:mass` opens with the paragraph "Differentiation convention" and states: "The second-variation expressions below treat the attention weights `β_ij` as fixed at their softmax-equilibrium values, equivalent to the envelope-theorem convention for analyzing the local geometry of equilibria. Off-equilibrium, full autograd through the softmax produces additional cross terms involving `∂β_ij/∂μ` and `∂β_ij/∂Σ`; these are subdominant in regimes of soft attention and consensus alignment, and dominant in regimes of sharp attention or rapid reorganization. The boxed forms below are stated in the isolated-agent limit `β_ij = 0` where the distinction does not appear. The first-order gradients of the preceding subsection 'First Variations' are written in autograd convention (see the 'Autograd versus reduced-free-energy gradients' paragraph); the convention switch here is deliberate and is what licenses the clean expressions for the boxed mass-matrix blocks below."

The two conventions are named (autograd vs envelope), the regime of validity for each is stated, and the switch between them is flagged at the line where it occurs. This is the same discipline `CLAUDE.md` requires when the canonical and surrogate-gradient paths differ.

### B6. The Mean-Gradient Equivalence section honestly registers that env-agent KL coupling matches the standard observation operator only on the mean and only up to a covariance-sector entropy discrepancy.

Lines 1415–1446 of `sec:formal_equivalence` carry the Proposition with the explicit qualifier "mean-gradient equivalence; conditional full equivalence." The derivation at lines 1431–1439 computes the env-agent KL and *its* `Σ_i`-gradient explicitly, and shows the discrepancy with the negative log-likelihood is `−½ Σ_i^{-1}`. Line 1441 then proposes the cross-entropy substitution `F^xent_agent = −E_{q_i}[log q_{e_k}] = KL(q_i ‖ q_{e_k}) + H(q_i)`, which is identified with the canonical observation slot of eq. `free_energy_functional_final`. The section does not claim full variational equivalence without flagging the substitution; the "Proposition" itself names what equivalence it grants.

### B7. The Timescale Hierarchy is consistent with the project's CLAUDE.md and with the Born–Oppenheimer / center-manifold standard reductions.

`CLAUDE.md` `## Mathematical Reference: Timescales` reads "Fast E-step (belief q inference per forward pass) / Slow M-step (prior/model s,p parameter learning via backprop) / Static hyper-prior h (frozen at init, never learned)." Manuscript line 1547 (Timescale Hierarchy and Adiabatic Approximation) writes "characteristic ratio `η_q : η_s : η_φ ~ 1 : ε : ε²`" and identifies the fast / slow / very-slow tiers with `q_i` / `s_i` / `φ_i` respectively, with hyper-priors `r_i` as fixed boundary conditions inherited from the parent meta-agent via the cross-scale shadow relation. Line 1551 cites `[Born1927]` (Born–Oppenheimer) and `[Carr1981]` (center manifold theory) as the standard mathematical analogs of the separation. The hierarchy matches the project's E-step / M-step / frozen-hyper-prior structure literally.

## Falsification conditions

The position above is wrong if any of the following holds:

1. **F1.** A manuscript line in [1005, 2043] that derives or uses `softmax(β)` from a *different* objective than the row-Lagrangian of the full F at line 1266 — for example, an undocumented direct postulate of `β_ij ∝ exp(−E_ij/τ)` that bypasses the entropy term and is not labeled as the entropy-suppressed surrogate. (The corresponding manuscript text at lines 1245 and 1282 says the canonical form includes the entropy term; a contradicting derivation would falsify B1.)

2. **F2.** A boxed equation in `sec:mass` (1844–2043) that claims `M_i` *is* an inertial mass without the kinetic-metric postulate, contradicting the explicit "This is a postulate, not a consequence of `F`" of line 2031 and the "in the first instance, a *stiffness*, not a mass" of line 1849. (The corresponding text at 1849, 2013–2024, 2026, 2031 says the identification is postulate-dependent; a contradicting boxed claim would falsify B2.)

3. **F3.** A line in `sec:transformers` that asserts standard scaled dot-product attention is *derived uniquely* from the gauge-theoretic framework, contradicting line 1574 ("not derived uniquely") and line 1842 ("the limits are deliberately aggressive"). (B3 falsifies if the section claims more than gauge-fixed-limit equivalence.)

4. **F4.** A reading of `sec:mass_block_caveats` in which the Newtonian/Hamiltonian interpretation survives the asymmetric-attention regime (`β_ik ≠ β_ki`) without restriction. The text at line 1961 explicitly forbids this reading. (B4 falsifies if red can show the Newtonian reading is asserted off the symmetric/isolated-agent limits.)

5. **F5.** A numerical or symbolic verification showing the autograd-surrogate gradient and the reduced-F gradient *do* share stationary points off equilibrium — i.e., that the covariance `Cov_β*(E, ∇_x E)` of line 1371 vanishes generically. (B5 falsifies if the gradient gap is empirically zero, since then the surrogate-canonical distinction collapses and the manuscript is over-cautious; this would be a softer falsification because the manuscript would be conservative rather than wrong.)

6. **F6.** A line in `sec:formal_equivalence` (1411–1448) that asserts full variational equivalence without either the cross-entropy substitution of line 1441 or the fixed-covariance restriction stated in the Proposition. (B3-adjacent sub-claim 3 falsifies if the equivalence is over-claimed.)

7. **F7.** A discrepancy between the timescale hierarchy at line 1547 and the project's E-step / M-step / static-hyper-prior structure in `CLAUDE.md` — e.g., if `r_i` is described as learned at any timescale, or `q_i` as written via backprop alongside `s_i`. (B7 falsifies if the manuscript reverses the project's fast/slow/static assignment.)

If red cites a manuscript line that satisfies any of F1–F7, blue concedes that sub-claim. Otherwise, the seven sub-claims of `00_claim.md` stand on the text as written.
