# Red Opening — pifb-theory-dynamics

## Steelman (opposing position)

Lines 1005-2043 of `Attention/Participatory_it_from_bit.tex` constitute a mathematically rigorous dynamical core: the row-Lagrangian with the explicit `τ β log(β/π)` entropy term yields `β_ij = softmax(-KL/τ)` as a strict stationarity condition (not as a surrogate or limit), the envelope-theorem reduction is correctly registered against the autograd surrogate, the transformer recovery is presented as one interpretive lens with explicit gauge-fixing conditions and disclosed closure requirements, and the Mass Analogy section explicitly labels the kinetic-metric form as a postulate while cross-referencing the structural-tier reading that partially withdraws the prior-precision-as-mass identification.

## Position

**I cannot falsify sub-claims 1-7 under the evidence at lines 1005-2043.** The manuscript carries the disclosures and qualifiers required by `external_canon_math.md` and `external_canon_transformers.md`. I attempted three lines of attack (entropy-suppressed surrogate, mass cross-reference, gauge-fixing conditions in transformer recovery) and each was already pre-empted by the manuscript text. I record the search and bound the concession with concrete falsification conditions that would change the verdict if discharged outside this debate.

## Evidence

The three pre-empted attacks:

**R1 (entropy-suppressed surrogate vs canonical F).** `external_canon_math.md` and the project canon (CLAUDE.md "Mathematical Reference") require that the row-Lagrangian carry the `τ β log(β/π)` entropy term so that softmax `β` is a true stationary point of `F`, not a delta. The boxed canonical functional at line 1238 carries this term explicitly: `+ τ Σ_{ij} β_ij log(β_ij/π_ij)`. The reduced-vs-surrogate distinction is made at line 1245 in the exact form CLAUDE.md flags as required: the surrogate gradient differs from `∇F_red` by `-τ^{-1} Cov_{β*}(E, ∇E)`, which need not vanish. The canonical form is again stated at line 1238 (in §VFE Functional) and line 1266 (in §Complete F) with the row-Lagrangian derivation `L_i = Σ_j[β_{ij} KL + τ β_{ij} log(β_{ij}/π̃_{ij})] - λ_i(Σ_j β_{ij} - 1)`, yielding the softmax closed form at Eq. (eq:beta_optimal), line 1268. Standard variational stationarity with entropic regularization [Wainwright-Jordan 2008 §3.4] is the reference for this construction; the manuscript matches it. Sub-claim 1 holds.

**R2 (mass-analogy cross-references).** Sub-claim 7 requires the structural-tier forward reference at line 2026 to point at sections that actually develop the decoupling. Resolved by `grep`: `sec:gravity_pullback` resolves to line 3102; `sec:three_tiers` resolves to line 2666; `eq:gauge_natural_gradient_def` to 2556; `eq:pullback_metric` to 2563. The §Three Tiers subsection at 2666-2678 develops the epistemic/expectational/structural separation in the form line 2026 invokes, and explicitly relocates "perceived ambient geometry" from `G_i^{(p)}` (state-fiber prior pullback) to `G_i^{(s)}` (model-fiber generative-model pullback). The kinetic-metric postulate text at line 1849 ("identifying $\Sigma_p^{-1}$ with an effective mass requires a separate kinetic-metric postulate") and line 2031 ("This is a postulate, not a consequence of $\mathcal{F}$") matches the Pullback-Gravity-debate convention. The Arnold (1989) reference at 1849 / 2031 (non-degenerate harmonic identification requires operationally independent inertia and stiffness tensors) is the canonical reference [Arnold 1989 *Mathematical Methods of Classical Mechanics* Ch. 4]; the manuscript correctly states that the present construction does not supply such independence. Sub-claim 7 holds.

**R3 (transformer recovery and gauge-fixing).** Sub-claim 6 requires explicit gauge-fixing conditions and a disclosed "one interpretive lens" framing. Two routes are introduced at line 1604: the trivial-frame route (1602-1673) takes `U_i = U` so `Ω_ij = I` and inserts a learned bilinear `M = W_Q W_K^T`; the untied-QK route (1675-1715) retains `Ω_ij = U_i U_j^{-1}` and carves `Q_i = U_i^{-1} μ_i, K_j = U_j^T Σ_j^{-1} μ_j` (Eq. eq:gauge_qk, line 1699). The closure required to collapse `H_j → C^{-1}` is stated explicitly at line 1712: `Σ_j = U_j C U_j^T` for shared SPD `C`. The rotary-frame instantiation at line 1714 satisfies this closure automatically when `C` commutes with the rotation block structure (`U_i ∈ O(d_k)` implies `U_i^{-1} = U_i^T`, so the closure reduces to a commutation condition). The key-bias cancellation is disclosed at lines 1742-1758 in three sufficient regimes (high-dimensional concentration, pre-projection norm + isotropic embeddings, post-projection key normalization), with the explicit caveat that "outside these regimes, the KL limit gives a distance/RBF-style attention with a residual key-dependent bias rather than pure dot-product attention" (line 1758). The asymmetric-attention caveat at 1958-1961 (`β_ik ≠ β_ki` and `Ω_ik Ω_ki ≠ I` defeat the conservative-Hamiltonian reading) is the disclosure the project canon requires; the "one interpretive lens" framing is at 1574 ("not derived uniquely from the gauge-theoretic framework"). The √d_k vs κ √d_k distinction is correct: line 1806 identifies `κ = 1` as the [Vaswani 2017] standard case, treating κ as a learnable scalar on top of the standard scaling — matching `external_canon_transformers.md §2`. Sub-claim 6 holds.

The advisor and pre-fix-protocol check both confirm: every load-bearing disclosure the project canon requires is present at the cited line numbers.

## Falsification conditions

This concession is wrong — and the verdict flips to RED_WINS — if any of the following hold:

1. **(In-scope.) The closure `Σ_j = U_j C U_j^T` at line 1712 fails to hold under the rotary-frame instantiation at line 1714 for any concrete `C`.** This is the load-bearing structural condition under which the untied-QK route at 1675 collapses to standard symmetric `Q^T K / √d_k` attention. If the commutation condition `[U_i, C] = 0` (with `U_i` the per-position rotation block) fails for every nontrivial `C ≠ I`, the per-token-frame route does not in fact recover standard attention except at `C = I`, and the manuscript's claim that "the two routes coincide on standard scaled dot-product attention" (line 1673) is true only in the trivial closure. Verifiable by direct computation; the manuscript should either supply the explicit `C` or restrict the closure claim to `C = α I` for scalar `α`.

2. **(In-scope.) The forward-reference paragraph at line 2026 fails to identify a *dynamical* decoupling between state-fiber `Σ_{p,i}^{-1}` and model-fiber `Σ_{s,i}^{-1}`.** Line 2026 states "an equivalence-principle-style identification of the two would require an additional dynamical link that the present construction does not derive." If the §Pullback Gravity section at 3102 (out of strict 1005-2043 scope but referenced from inside it) does not in fact instantiate this decoupling — e.g., if it secretly tethers `Σ_{s,i}^{-1}` to `Σ_{p,i}^{-1}` through the cross-scale shadow relation in a way that re-couples the two tiers — the partial-withdrawal claim at 2026 is misadvertised. The cross-reference is in scope; the destination is outside the strict line range but in scope by reference.

3. **(Out-of-scope but flagged.) The autograd-vs-reduced-F gradient discrepancy at line 1371 (`∇⟨E⟩_β - ∇F_red = -τ^{-1} Cov_{β*}(E, ∇E)`) is shown nonvanishing in the user's actual training loop.** The manuscript correctly states the principle (a presentation matter for `theory` mode); whether the running code uses the canonical reduced gradient or the autograd surrogate is a `code`-mode question. The two displays of the same Cov-discrepancy at 1245 and 1371 use consistent sign conventions on second inspection, so the presentation is not a falsifier; the implementation-mode test would be.

If none of (1)-(3) discharge against the manuscript, the claim stands. On the evidence at 1005-2043 as written, I cannot falsify sub-claims 1-7.

## Out-of-scope observation

The Lagrangian derivation at lines 1086-1108 sets the entropy weight to 1 and obtains `β = π exp(-E)`, then introduces `τ` at line 1112 by rescaling `E → E/τ`. The full functional at 1238 carries `τ` on the entropy term explicitly, and the row-Lagrangian at 1266 properly carries `τ`. The two derivations are equivalent under the rescaling and yield the same argmin (`β ∝ π exp(-E/τ)`); the two-stage presentation is a standard notational convention in entropic-regularization derivations [Wainwright-Jordan 2008 §3.4]. This is a presentation observation, not a falsifier.
