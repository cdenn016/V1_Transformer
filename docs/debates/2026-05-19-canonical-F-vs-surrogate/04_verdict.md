# Verdict — canonical-F-vs-surrogate

## Outcome

RED_WINS

## Decisive evidence

`03_blue_rebuttal.md:7` — Blue's own concession: "Sub-claim ε is granted: the implementation's E-step does not execute descent on `F_red` between iterations; it executes descent on `⟨E⟩_{β*}`." Combined with `03_blue_rebuttal.md:11`: "The manuscript's prose at `Attention/GL(K)_attention.tex:967–972` ... and `:2008–2014` ... needs reconciliation with `:874`. As written, those passages name `F` as the descent target without flagging that the gradient computed in the algorithm is the autograd gradient of `⟨E⟩_{β*}`, which is not `∇F_red`. This is an editorial gap and should be reconciled in revision." Verified at manuscript line 967 ("The global free energy is minimized by gradient descent on the beliefs"), line 2008 ("E-step (Belief Inference): ... natural gradient descent on the free energy: `q_i^{(t+1)} ← q_i^{(t)} − η_E · ∇̃_{q_i} F`"), and line 874 ("The gradient expressions below are therefore gradients of the attention-weighted energy `Σ_j β_{ij}* E_{ij}` ... not of the reduced free energy `F_red`; the two differ by [eq:autograd_envelope_gap]"). Sub-claim α is verified by sympy in both opening files (residual = 0); sub-claims β, γ, δ are conceded by red in `03_red_rebuttal.md:5–11`.

## Reasoning

The load-bearing sub-claim ε states the framework's E-step gradient flow descends on the surrogate `⟨E⟩_{β*}`, not on the canonical `F_red`. Blue's rebuttal grants this in three separate places. First, the trajectory under autograd does not descend on `F_red` off-equilibrium. Second, line 874's "we adopt the same convention" imports computational practice rather than deriving the surrogate as the correct variational target. Third, the prose at lines 967 and 2008 declares descent on `F` while line 874 declares the gradient is not of `F_red` — Blue concedes these "need reconciliation" and calls the situation an "editorial gap." That set of admissions concedes the substantive content of sub-claim ε and the framing the claim attaches to it: the framework's analytical narrative ("minimize F") is not what the algorithm executes ("autograd of `⟨E⟩_{β*}`"), and the manuscript currently contains the unreconciled contradiction between the two prose statements. Blue's structural defense — that the GLU/FFN correspondence at line 1937 requires the autograd-retained term — establishes only that the additional `∂β/∂x` terms have a recognizable functional identity inside the framework's taxonomy. It does not establish that gradient descent on `⟨E⟩_{β*}` is gradient descent on `F_red`. Red's rebuttal point that the §5.4 GLU identification is a labeling of the gap's functional form (not a derivation that the surrogate is the correct variational descent target) is correct as a reading of lines 1925–1946: those lines describe family membership in the GLU class and identify the centered softmax-gradient as a "higher-order non-linearity beyond the first-order GLU gate ... unique to the VFE setting," which is structural analogy, not first-principles derivation of the surrogate as the variational objective. Blue's "variational principle vs variational algorithm" reading is a reasonable revision path, but it is not what the manuscript currently says — the manuscript currently says both "minimize F" (lines 967, 2008) and "the gradient is not of F_red" (line 874), and Blue concedes the reconciliation is editorial work yet to be done. The previous "softmax-beta-stationary-point" verdict (RED_WINS, `F_align^(τ)` engineered rather than ELBO-derived) compounds the position: `F_align^(τ)` is a posited soft-assignment Lagrangian whose stationary-point motivates the softmax rule, `F_red` is its reduction at `β = β*`, and the implementation then descends on `⟨E⟩_{β*}` rather than on `F_red`. The stack of interpretive translations is real, disclosed in the manuscript text, and acknowledged by Blue as requiring revision. The claim under debate states this stack obtains. The evidence supports the claim.

## Action

Reconcile the descent-target prose at `Attention/GL(K)_attention.tex:967–972` and `:2005–2014` with the autograd-convention adoption at `:874`. The simplest fix is to rewrite the belief-dynamics passage at line 967 and the E-step passage at line 2008 to state that the implementation descends on `⟨E⟩_{β*}` (the autograd objective at `β = β*(x)`), which inherits `F_red`'s stationary points at joint equilibrium and retains the softmax-gradient nonlinearity identified at `:1937–1946`. The narrative "we minimize `F`" as currently written at lines 967 and 2008 is not what the algorithm executes; either revise those lines to match line 874's disclosure, or supply a first-principles derivation that the surrogate is the correct variational descent target (red's falsification condition 1 in `02_red_opening.md:53`). The Σ_i case in supplementary §B.1 line 183 is mathematically correct under "β held fixed" but should note that under the autograd convention adopted in the main text, `β` is tracked through `E_{ij}(Σ_i)` and the gap re-enters on the Σ_i channel as well; the per-channel gap structure is already correctly described at `:1946`. No further sub-debate is required on the math; the action item is editorial reconciliation in the manuscript text and an explicit statement of which functional the E-step descends on.
