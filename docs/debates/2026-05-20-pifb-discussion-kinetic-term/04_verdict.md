# Verdict — pifb-discussion-kinetic-term

## Outcome

RED_WINS

## Decisive evidence

The "fundamental-physics reading" the manuscript invokes at `Participatory_it_from_bit.tex:3095` is anchored, by blue's own selection, in three FEP-as-action sources: Friston (2008) DEM *NeuroImage* 41(3), Friston et al. (2022) path integrals (arXiv:2210.12761), and Friston, Da Costa, Sajid, Heins, Ueltzhoffer, Pavliotis, Parr (2023) *Physics Reports* (arXiv:2201.06387). None of these constructs the kinetic-metric identification the manuscript's hedge inherits. Friston 2008 DEM builds second-order behavior by *embedding* the state in generalized coordinates of motion and running a first-order variational filter on the embedded state; inertia enters through a generalized precision on serial derivatives, not through identifying the Hessian of free energy with a kinetic metric. Friston et al. 2023 *Physics Reports* decomposes a first-order stochastic flow by the Helmholtz construction `x_dot = (Q - Gamma) grad log p + div(Q - Gamma)` with antisymmetric solenoidal `Q` and symmetric dissipative `Gamma`; no velocity-quadratic Lagrangian `(1/2) x_dot^T M x_dot` with `M` = potential Hessian appears. Amari (1998) *Neural Computation* 10(2) §2 gives the natural-gradient flow as an intrinsic first-order Riemannian gradient flow `theta_dot = -G(theta)^{-1} grad L`, with `G` a Fisher metric, not a kinetic-metric coefficient. The four body registrations blue cites (`1849`, `2013-2015`, `2026`, `2029-2030`) all register the postulate against the Arnold (1989) §22 small-oscillation canon, not against the FEP-as-action canon. The Discussion at `3095` is therefore the only point in the manuscript where the FEP-as-action canon is connected to the kinetic-metric postulate, and it connects them in a way that suggests the postulate is downstream of that canon when in fact it is an additional postulate on top of it.

Blue's rebuttal does not produce a primary source in which the FEP-as-action line of work contains the manuscript's specific `M_{mu mu} = Sigma_p^{-1}` kinetic-metric identification, and explicitly grants that the equation at `3089-3091` would be tighter as a labelled textbook illustration or as a Langevin form with `gamma` defined per Friston et al. 2023 §3. Blue also concedes strike (ii) on `3093` in full against Friston (2010) §"Learning and Memory", Friston et al. (2017) §"Learning", and Parr, Pezzulo, Friston (2022) Ch. 3, all of which contain canonical model-parameter learning.

## Reasoning

The composite claim asks whether the hedge at `3095` is correctly calibrated. Three independent strikes were brought; two are conceded by blue in the rebuttal, and the third — the FEP-as-action anchor strike — survives without a counter-citation. Conceded strike (ii) at `3093` is independently dispositive on its own: blue explicitly grants that the sentence "in traditional approaches predictive models are considered fixed" is factually wrong against the active-inference canon, which puts a factual error on the page directly adjacent to the hedge under debate. Strike (i) is partially conceded: blue grants that the natural-gradient flow is not derived as the `m -> 0` overdamped limit of an inertial SDE in Amari (1998), and grants the mechanical-analogy equation at `3089-3091` would be cleaner labelled as a textbook stand-in or recast in Langevin form with `gamma` given a referent. Strike (iii) is the one blue defends most squarely on the merits, and on the editorial-completeness question (whether `\ref{sec:mass}` reaches `sec:mass_block_caveats`) blue is correct that the label is intact at `1958` and reachable via the existing cross-reference. The presentation gap blue grants — a reader of `3085-3095` in isolation is not alerted to the symmetric-attention regime restriction — is real but does not by itself defeat the hedge sentence.

The load-bearing question is strike (i)'s deeper form, surfaced in red's rebuttal: the hedge at `3095` reads the kinetic-metric postulate as something that the "fundamental-physics reading" of FEP already legitimates, when the canon blue itself cites does not contain that identification. Friston 2008 builds inertia through generalized coordinates; Friston 2023 *Phys Reports* builds conservative-dissipative splits through Helmholtz decomposition of first-order flows; Amari 1998 builds Riemannian gradient flow without a kinetic term. The manuscript's `(1/2) dot{mu}^T M_{mu mu} dot{mu}` with `M_{mu mu} = Sigma_p^{-1}` (at `2031` and `2029-2030`) is a Lagrangian-mechanics construction in the sense of Arnold (1989) §22.2 — and the body registers it against Arnold, not against Friston. The Discussion's invocation of the FEP-as-action canon at `3095` therefore borrows legitimacy that the canon does not in fact extend to the specific postulate being inherited. Per the source-of-truth precedence binding this judgment, the external canon governs; blue cited the canon but could not produce the specific construction the hedge implicitly attributes to it.

A judge ruling under the "err toward RED_WINS where the FEP-as-action canon does not contain the manuscript's specific `M = ∇²F` postulate" directive cannot rule otherwise. The hedge is under-calibrated on exactly that axis.

## Action

Three small edits at `Attention/Participatory_it_from_bit.tex:3085-3095`, none touching the load-bearing clause "it is not a derivation of inertia from the variational principle":

1. **Fix the factual error at line 3093.** Replace the two sentences "Furthermore, the framework allows agent priors to evolve dynamically.  In traditional approaches predictive models are considered fixed." with a corrected statement that names the actual divergence from standard active inference. Proposed text:

   > Furthermore, the framework treats the generative-model parameters as evolving on the same gauge-covariant message-passing graph that carries belief inference rather than as separately-clocked slow-timescale plasticity; standard active inference does include model-parameter learning at slow timescales (e.g., Friston 2010 \S\,Learning and Memory; Friston et al.\ 2017 \S\,Learning; Parr, Pezzulo, Friston 2022 Ch.\ 3), but it does so by a distinct learning rule rather than by extending the same coupled flow used for belief inference.

2. **Label or recast the mechanical-analogy equation at lines 3089-3091.** Either label it explicitly as a textbook illustration of the regime distinction, or restate in Langevin form with `gamma` given a defined referent. Minimum-edit version: add a leading clause to the sentence at line 3087 ending so that the equation is unambiguously framed as illustrative. Proposed insertion before the equation:

   > In the textbook mechanical analogy (with `m` and `gamma` standing for inertia and friction coefficients against thermal noise rather than for any defined object in the present construction), this corresponds to

   and immediately after the equation at line 3091, replace "where the mass-to-damping ratio vanishes and acceleration becomes instantaneously yoked to the force." with:

   > where the mass-to-damping ratio vanishes and acceleration becomes instantaneously yoked to the force. The analogy carries the regime distinction; it is not the derivation route by which standard active inference arrives at first-order natural-gradient descent, which is an intrinsic Riemannian gradient flow on the statistical manifold in the sense of Amari (1998) \S\,2, not the overdamped limit of a Newtonian SDE.

3. **Add an inline cross-reference and FEP-as-action qualifier at line 3095.** The `\ref{sec:mass}` cross-reference reaches `sec:mass_block_caveats` editorially, but two distinct calibration gaps remain in the hedge sentence itself: the asymmetric-attention regime restriction is not surfaced inline, and the FEP-as-action canon is invoked in a way that suggests the kinetic-metric postulate is downstream of it. Replace the sentence beginning "This reintroduction is contingent on the fundamental-physics reading and inherits the postulate that the stiffness matrix $\Sigma_p^{-1}$ doubles as the inertia tensor;" with:

   > This reintroduction is contingent on the fundamental-physics reading and on a kinetic-metric postulate that is additional to the FEP-as-action canon rather than implied by it: Friston (2008) DEM constructs second-order behavior via embedding into generalized coordinates of motion and a first-order variational filter on the embedded state, and Friston et al.\ (2023) \emph{Physics Reports} decomposes the flow into solenoidal and dissipative parts of a first-order stochastic vector field, neither of which writes a velocity-quadratic kinetic energy with the potential Hessian as inertia tensor. The reading further presupposes the symmetric-attention or isolated-agent regime identified in Section~\ref{sec:mass_block_caveats}, outside of which the bilinear form $M_{\mu\mu}$ does not function as the inertia tensor of a conservative Hamiltonian. It is not a derivation of inertia from the variational principle, and the biological-timescale omission remains entirely correct for its intended target.

After these three edits the hedge at `3095` correctly registers (a) that the kinetic-metric postulate is additional to the FEP-as-action canon rather than downstream of it, (b) that the conservative-Hamiltonian reading depends on the symmetric-attention regime restriction recorded at `1958-1961`, and (c) that the adjacent mechanical-analogy equation is illustrative rather than a derivation route. The factual error at `3093` is resolved independently. None of the body-level disclosures at `1849`, `2013-2015`, `2026-2034` need to change.
