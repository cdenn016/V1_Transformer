# Red Panel Choice — pifb-consensus-gauge-invariance (Phase 2 opening)

Mode: theory + formal math. Panel=full; coordinator picks 5-of-10. No `Agent` tool in
this environment, so lenses are embodied directly: one `memo_red_<tag>.md` per lens,
canon harvested via WebFetch/WebSearch, then synthesized into `02_red_opening.md`.

| Tag | One-sentence justification |
|---|---|
| `philosophy-of-science` | **Mandatory.** Carries the primary attack: the "gauge invariance from consensus" thesis is a redescription of a built-in invariance, not a derivation, and the self-flag names "unfalsifiable" without naming the circularity. |
| `info-geometer` | The linchpin of the circularity attack: KL on Gaussian belief fibers is GL(K)-invariant by Cencov/sufficient-statistic uniqueness — the framework selected KL *because* it is invariant, so the consensus constraint is auto-satisfied. |
| `gauge-theorist` | Carries the second attack: the enumeration U(1), SU(2), SU(3), SO(1,3) "as available subgroups" (:2992) is suggestive of a Standard-Model derivation it cannot deliver; also adjudicates the Maurer-Cartan and Faddeev-Popov concessions. |
| `geometer` | Adjudicates the Haar-measure obstruction (non-compact ⇒ infinite Haar) and the manifold-of-maps functional-integral claim — verifies the concession floor is stated correctly. |
| `variational` | Frames "what is the consensus metric *for*" and connects the "objective reality / closest analog" prose to the FEP intersubjectivity reading; confirms whether the ontological language is discharged at its own locations. |

Dropped: `numerical-analyst` (the regulator question is conceptual, not finite-precision);
`transformer-ml`, `ml-engineer`, `implementation-engineer`, `code-quality` (code-truth N/A for
this theory-only span).
