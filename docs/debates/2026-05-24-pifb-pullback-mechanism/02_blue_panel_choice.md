# Blue Panel Choice — pifb-pullback-mechanism (Phase 2 opening)

Theory mode, differential-geometry / information-geometry content. Five experts selected to DEFEND the claim by deriving support from external canon.

| Expert | One-sentence justification |
|--------|----------------------------|
| `geometer` | The core object is a bundle metric with a horizontal⊕vertical split and a pullback by a section; the canonical lineage (Sasaki metric, Kaluza–Klein metric) is differential-geometry territory and supplies the standard form $g_E$ must be checked against. |
| `gauge-theorist` | The horizontal block $\kappa(A,A)$ is built from a connection one-form $A=U^{-1}dU$ and its gauge-noninvariance under $A\to g^{-1}Ag+g^{-1}dg$ is the central disputed point; whether connection-dependent quadratic forms are routinely non-invariant is gauge-theory canon. |
| `info-geometer` | The vertical block is the Fisher-Rao score outer product; its canonical legitimacy (Amari–Nagaoka score form, Cencov uniqueness) is the strongest single piece of the construction and must be anchored to external canon. |
| `variational` | Sub-point 4 (perceived space $=G^{(s)}$, the structural/slow tier) turns on whether Friston2017 hierarchical active inference actually locates structural perceptual organization in parameter learning vs state inference — pure FEP/variational territory. |
| `philosophy-of-science` | MANDATORY. Frame-checks the "$L^2$ vs bundle-metric" distinction for relabeling, polices manuscript-as-authority circularity, and tests whether "perceived space $=G^{(s)}$" admits a discriminant or is interpretive preference. |

Discounted: `transformer-ml`, `ml-engineer`, `numerical-analyst`, `implementation-engineer`, `code-quality` — this is pure theory mode with no entry point, config key, attention form, or numerical-stability content in the claim span (:2710–2818). The engineering lenses have nothing to defend here.
