# Action — pifb-pullback-mechanism (Debate 1 of 6)

**From verdict:** REMAND (chief judge, Reconciliation Rule 2 — scope override for an equivocated conjunction; canon-strict first-pass was RED_WINS, scope was REMAND).

## What survives (no edit)

- **SC2 (BLUE, settled):** $g_{E_q}$ (Eq. bundle_metric, :2734) is *a* legitimate Riemannian metric on the total space $E$ (a smooth symmetric positive-definite block sum), and $G^{(q)} = \sigma^* g_{E_q}$ is a genuine section-pullback [Lee 2013; Nakahara §5.4; Kobayashi–Nomizu §I.5]. The four tiers $G^{(q,p,s,r)}$ are well-defined symbolic objects. The vertical Fisher-Rao block is Cencov-canonical and unassailable.

## Recommended manuscript edits (SC4)

1. **`:2726` and `:2784` — reword the "bona-fide / earns the name" framing.** Decisive external canon (canon-cop-verified Kaluza–Klein line element, `02b_canon_cop.md:16-22`): the standard fiber-bundle metric routes the connection one-form into the *vertical/fiber* block with the pulled-back *base* metric in the horizontal block. The manuscript's $g^{\mathrm{tw}}_{\mu\nu}=\kappa(A_\mu,A_\nu)$ in the horizontal slot *inverts* this canonical layout (it has no base metric on $\mathcal{C}$ to pull up — the "it from bit" setting forbids one). So the claim that specifying $g_{E_q}$ "earns the name pullback of a fiber-bundle metric" over the "merely $L^2$ pullback of scores" overclaims against the canonical object. Reword to: $g_{E_q}$ is *a* Riemannian metric on $E$ (not *the* canonical Kaluza–Klein/Sasaki bundle metric), with a deliberately non-canonical horizontal block dictated by the absence of an a-priori base metric — which the manuscript should state as the novel move it is, not as recovering a standard construction. (SC1, RED-settled: the layout does not match canonical KK/Sasaki.)
2. **Correct the implicit Cencov framing.** Cencov/Chentsov uniqueness governs the metric on the statistical manifold (the fiber $\mathcal{B}$, where the vertical block correctly invokes it), NOT pullbacks to the base $\mathcal{C}$. Ensure no text suggests base-pullback uniqueness from Cencov. (This is the one point where blue, not red, had the canon right.)

## Out of mathematical scope (SC5)

- **`:2795–2797` "perceived space = structural tier $G^{(s)}$"** is a non-falsifiable interpretive identification (both sides conceded: no discriminator, frozen slow channel). Keep it explicitly flagged as metaphysical interpretation, not a mathematical result. The [Friston2017] citation is read past its source (parameter-vs-state learning does not establish the perceived-space identification); soften or drop the implication that Friston supports it.

## Genuinely contested — spawn follow-up (SC3)

- Whether the horizontal frame-twist $\kappa(A,A)$ is *generically nonzero* in realized runs (i.e. $A\neq0$, frames actually twist across $\mathcal{C}$) or whether $A=0$ every run (collapsing the bundle pullback to the bare score-only $L^2$ form). If the latter, the bundle framing is vacuous *in practice* even if valid in principle. This is an empirical/code question; the relevant code is in **MAgent_Model-main** (per [[project_pifb_codebase_split]]), not V13. Spawn as a separate code/experiment debate if pursued.

## Follow-up debates in this series

Debates 2–6 proceed independently. SC2 (the construction is a valid pullback) is the load-bearing positive result that the rest of the emergent-spacetime span builds on; the signature debate (2) inherits the same $\kappa(A,A)$ horizontal block.
