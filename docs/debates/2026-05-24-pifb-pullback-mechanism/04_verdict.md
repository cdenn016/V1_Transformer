# Verdict — pifb-pullback-mechanism (binding, chief reconciliation)

## First-pass verdicts

| Judge | Outcome | Decisive evidence |
|-------|---------|-------------------|
| canon-strict (`04_judge_canon_strict.md`) | RED_WINS | Primary-source-verified Kaluza–Klein line element `ds² = g_μν dx^μ dx^ν + φ²(A_ν dx^ν + dx^5)²` (`02b_canon_cop.md:18-22`) places the connection in the vertical block and the pulled-back base metric horizontally; the manuscript's `g_{E_q}` (:2734/:2739) inverts this with a standalone `κ(A_μ,A_ν)` horizontal block, and Blue concedes the inversion (`03_blue_rebuttal.md:7`). |
| code-truth | N/A — not dispatched | No code implements the emergent-spacetime pullback construction; it is the speculative-theory span of the manuscript. Nothing to trace at `path:line`. |
| scope (`04_judge_scope.md`) | REMAND | Blue's explicit redefinition at `02_blue_opening.md:13` ("a metric on the total space E means *any* Riemannian metric on E — not specifically the Kaluza-Klein recipe") set against the canon-cop primary-source verification (`02b_canon_cop.md:16-22`): the central term "bona-fide fiber-bundle metric" is unanchored, and the verdict on conjunct (1) flips depending on which definition is in force. |

## Reconciliation rule applied

Rule 2 — Scope override for REMAND on equivocation. The scope judge declared REMAND because the claim packs four separable propositions joined by semicolons (`04_judge_scope.md:7`), and the single load-bearing term "bona-fide fiber-bundle metric … not merely an L² pullback" is unanchored, meaning the canonical KK/Sasaki form when Red attacks and "any Riemannian metric on E" when Blue defends. Rule 1 does not fire (scope explicitly states this is not OUT_OF_SCOPE — every sub-claim is answerable; only the welding is malformed, `04_judge_scope.md:39`). Rule 2 is the first rule that fires; it precedes Rule 3 (majority) in priority order. The binding outcome is REMAND with scope's sub-claim decomposition adopted as the spawn list.

## Decisive evidence (binding)

The equivocation on the claim's central term, verified against external canon. Canon-cop confirmed against the primary Kaluza–Klein line element `ds² = g_μν dx^μ dx^ν + φ²(A_ν dx^ν + dx^5)²` (`02b_canon_cop.md:16-22`) that the canonical bundle metric routes the connection one-form into the vertical/fiber block and carries the pulled-back base metric in the horizontal block; the manuscript's `g_{E_q}` inverts this by placing a standalone `κ(A_μ,A_ν)` trace form in the horizontal slot. Blue had to redefine "bona-fide fiber-bundle metric" to mean "any smooth positive-definite (0,2)-tensor on E" (Lee2013 Ch. 13, `02_blue_opening.md:13`, `03_blue_rebuttal.md:25`) to defend the claim at all. The strong reading (canonical-form match) is RED on verified canon and conceded by Blue (`02_blue_opening.md:11`, `03_blue_rebuttal.md:7`); the weak reading (`g_{E_q}` is *a* Riemannian metric, `G^{(q)}` *a* pullback) is BLUE on verified canon and undisputed by Red (`02_red_opening.md:13` grants "well-defined as symbolic objects"). One conjunct yielding opposite verdicts under two definitions of its own central term is the canonical REMAND pattern, not a single answerable proposition.

## Outcome (binding)

REMAND

## Reasoning

Rule 2 fires: the scope judge declared REMAND on equivocation, the chief defers to scope on REMAND for packed claims (`debate_methodology.md:294`), and Rule 2 precedes the canon-strict majority-eligible vote in priority order. The canon-strict RED_WINS is not overridden on its substance — it is correct on every conjunct it scored, and its decisive Kaluza–Klein evidence is adopted verbatim as the disposition of sub-claim SC1. What Rule 2 changes is the *unit of adjudication*: canon-strict scored the four-conjunct conjunction as a single `A ∧ B ∧ C` proposition and resolved the equivocated term by binding the manuscript's `:2726`/`:2747` "bona-fide" stipulation to the strong canonical reading under the source-of-truth precedence rule (the correct move for a single-proposition verdict), whereas scope found that the conjunction welds two crisp propositions with opposite verdicts (SC1 RED, SC2 BLUE) to a third that is empirical (SC3) and a fourth that is non-falsifiable metaphysics (SC5), so the useful output is to split the welds rather than declare a single winner. Both judges agree on the substance: the math core is valid as *a* Riemannian metric on `E_q` and *a* genuine pullback (canon-strict Part 2 BLUE/agreed; scope SC2 BLUE-established on Lee2013 + Nakahara §5.4), and the manuscript's `:2726`/`:2784` "bona-fide / earns the name / not merely L²" framing overclaims against the canonical bundle metric (canon-strict Part 1 RED; scope SC1 RED; Blue conceded the label is unearned and should be rewritten at `03_blue_rebuttal.md:9`), and "perceived space = G^{(s)}" is interpretive, discriminator-free, and not Popper-falsifiable (canon-strict Part 3 RED; scope SC5 out-of-mathematical-scope; both sides conceded — Blue `03_blue_rebuttal.md:11`, Red `03_red_rebuttal.md:21`). The binding evidence is the verified Kaluza–Klein layout plus the verified Cencov scope (Chentsov's theorem governs the statistical fiber `B`, not the pullback to the base `C` — `03_blue_rebuttal.md:15-19`, primary-source confirmed, the one point on which Blue not Red has the canon right). No BLUE_WINS rests anywhere on manuscript authority; the surviving narrow proposition rests on Lee2013 and Nakahara, external canon.

## Action

Decompose the four-conjunct claim into the scope judge's sub-claims. Disposition:

- **SC1 (settled — RED, no new debate).** The construction `g_{E_q}` does NOT match the canonical Kaluza–Klein / Sasaki bundle-metric form: the canonical form routes the connection one-form into the vertical/fiber block and carries the pulled-back base metric horizontally, whereas the manuscript places a standalone `κ(A_μ,A_ν)` horizontal block. Verified against the primary KK line element (`02b_canon_cop.md:16-22`); conceded by Blue (`02_blue_opening.md:11,:26`; `03_blue_rebuttal.md:7`). Record as refuted.

- **SC2 (settled — BLUE, no new debate).** Within the body regime (`so(3)`, compact, `κ = −tr`, :2744/:2766), `g_{E_q} = κ(A,A) ⊕ g_B` is a smooth positive-definite (0,2)-tensor on `E_q` and `G^{(q)} = (σ^{(q)})* g_{E_q}` is the standard pullback of a (0,2)-tensor by a section (Lee2013 Ch. 13 metric definition; Nakahara2003 §5.4 / KobayashiNomizu Vol. I §I.5 pullback). Red does not dispute (`02_red_opening.md:13`). Record as established. This is what survives and may be relied on in the rest of the emergent-spacetime series: `g_{E_q}` is *a* Riemannian metric on `E_q` and `G^{(q)}` *a* genuine pullback of it; the four tiers `G^{(q,p,s,r)}` are well-defined symbolic objects.

- **SC3 (genuinely contested — spawn as a code/experiment-mode sub-debate).** Is the additive `κ(A^{(i)}_μ, A^{(i)}_ν)` term generically nonzero in the framework's realized operative regime, or is `A^{(i)} = 0` in every realized configuration? This is the live empirical question — Blue's `:40` "additive content exactly κ(A,A) and nothing more" vs Blue's `:27` non-vacuity defense, with the slow channel frozen at :2799 and Blue's own falsification condition 4 (`03_blue_rebuttal.md:38`). It is a question about the realized frame fields `U_i(c)` in the simulations (whether runs ever execute with `A^{(i)} ≠ 0`), not a pure-theory question. Spawn it against the empirical code (note: per the codebase split, oscillator/empirical PIFB code lives in MAgent_Model-main, not V13).

- **SC4 (manuscript edit — not a debate).** Reword the framing language, not the math. At `:2726` remove "does not earn the name … pullback of a fiber-bundle metric" and "bona-fide"; at `:2784` remove the assertion that "a metric on the noumenal substrate is the pullback of a fiber-respecting metric" as stated. Replace with a plain statement: within a fixed gauge, `g_{E_q}` is a valid block-diagonal Riemannian metric on `E_q` whose horizontal block is a non-standard, connection-built (NOT Kaluza–Klein-canonical) substitution forced by the it-from-bit setting (no pre-existing base metric to pull up), disclosed gauge-noninvariant, and whose only numerical content over the L² score-pullback form is the additive `κ(A,A)` frame-twist term. Separately, correct the Cencov framing so the theorem's scope is stated as the fiber statistical manifold `B`, not the base pullback to `C` — the point on which Blue, not Red, has the canon right (`03_blue_rebuttal.md:15-19`).

- **SC5 (out of mathematical scope — fold into the broader speculative span, do not adjudicate as a theory claim).** "Perceived / ambient geometry = structural tier `G^{(s)}`" is a non-falsifiable, discriminator-free interpretive proposal the manuscript already declines to adjudicate (:2797, "we propose … most naturally" at :2795). Both sides conceded it (Blue `03_blue_rebuttal.md:11`; Red `03_red_rebuttal.md:21`). Either accept the manuscript's already-hedged framing as correctly fenced, or — to upgrade "well-posed" — require the manuscript to name a discriminant (e.g. a frozen-slow-channel agent reporting stable perceived geometry under fluctuating `G^{(q)}`, Blue's suggested ablation). Do not treat the Friston2017 parameter/state-localization cite as establishing the tier assignment; canon-strict flagged it as read past the source (`04_judge_canon_strict.md:25`).
