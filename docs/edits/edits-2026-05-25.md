# Edits — 2026-05-25

## Prune to frozen-legacy: delete `vfe/`, `aif/`, `pure_vfe/`

The living `transformer/vfe/` implementation (and the `aif/` active-inference
package that rides on it) has been moved to a separate standalone repository.
This repo is now the **frozen legacy** Gauge-Transformer (`core/`-centered).
Branch: `chore/strip-vfe-aif-pure-vfe-legacy-2026-05-25` off the local HEAD
(the local repo is the source of truth; `main` was 316 commits / 3 weeks stale
and predated `aif/`, so it was not used as the base).

Per-construction safety check before deletion (two parallel investigators + a
verifier): `transformer/core/**` and `transformer/__init__.py` have **zero**
functional imports of `vfe`/`aif`/`pure_vfe`; the dependency layering is
one-directional (`core` never imports the deleted packages). The only
keep-set coupling was in training infrastructure and one mixed test file.

### Whole trees deleted
- `transformer/vfe/` (entire package, incl. `semantic_clustering/`)
- `transformer/aif/` (entire package)
- `transformer/pure_vfe/` (entire package)

### vfe/aif/pure_vfe-only tests deleted
- `tests/transformer/vfe/` (13 files)
- `tests/transformer/test_aif_package.py`
- `tests/transformer/test_vfe_package.py`
- `tests/transformer/test_vfe_new_features.py`
- `tests/transformer/test_vfe_numerics.py`
- `tests/transformer/test_vfe_cross_coupling.py`
- `tests/transformer/test_phi_mstep_gradient.py`
- `tests/transformer/test_phi_traceless_under_projection.py`
- `tests/smoke_test_entropy.py`

### vfe-only scripts deleted
- `scripts/diagnostic_autograd_gradient_scale_probe.py`
- `scripts/diagnostic_rope_attention_pattern_probe.py`

### vfe-only docs deleted
- `docs/audits/audit-2026-05-24-vfe.md`
- `docs/audits/audit-2026-05-24-vfe-fullcov.md`
- `docs/audits/audit-2026-05-24-vfe-semantic-clustering.md`
- `docs/superpowers/plans/2026-05-24-vfe-semantic-clustering.md`
- `docs/superpowers/specs/2026-05-24-vfe-semantic-clustering-design.md`

### Keep-set files surgically scrubbed
- `transformer/training/experiment_runner.py`: removed the three top-level
  `pure_vfe` imports, the cross-coupling visualization methods
  (`_build_cross_coupling_adapter`, `save_cross_coupling_visualization`) and
  their call site, and the entire `pure_vfe` experiment function family
  (`_validate_pure_vfe` … `run_pure_vfe_experiment`). The kept training path
  `run_single_experiment` references none of them. Module docstring updated.
- `transformer/train_publication.py`: removed the `run_pure_vfe_experiment`
  import, the `PURE_VFE_CONFIG` dict, the `'pure_vfe'` argparse choice, and the
  `elif mode == 'pure_vfe':` dispatch branch; corrected the mode comment, the
  `--mode` help string, and the "Valid modes" error message.
- `tests/transformer/test_omega_gradient.py`: removed the 8 test functions that
  imported `transformer.pure_vfe.gauge` (`natural_grad_omega`,
  `lie_algebra_clip_grad` ×3, `init_omega` ×2, `regularize_omega_conditioning`,
  `monitor_omega_health`); kept the 15 `core/`-and-torch tests, including
  `test_retract_omega_preserves_invertibility`, which sat between them.
- `transformer/core/prior_bank.py`: removed a dangling docstring cross-reference
  to the deleted `transformer.vfe.prior_bank`.
- `transformer/__init__.py`: reworded the try/except comments that justified the
  guard by the (now-deleted) `pure_vfe` subpackage; the guard mechanism is
  retained.

### Kept deliberately (filename traps and shared artifacts)
- `tests/transformer/test_vfe_gradients.py`, `test_vfe_utils.py`,
  `test_vfe_closed_form.py` — import `transformer.core.*`, not the deleted
  packages.
- `scripts/vfe_*.py` family and `transformer/visualization/vfe_dynamics_plots.py`
  — `core/`-driven, no deleted-package dependency.
- `docs/debates/**`, `docs/audits/repo-split-2026-05-24-*` — research/migration
  records.
- `.claude/agents/vfe-*` + `vfe-knowledge/` — dev tooling (the knowledge base
  also serves the manuscript reviewers).

### Verification
- `import transformer` succeeds; `__all__` populated
  (`GaugeTransformerLM`, `Trainer`, …).
- Legacy modules import: `experiment_runner`, `train_publication`,
  `core.model`, `core.prior_bank`.
- Repo-wide grep: no `.py` under `transformer/`, `tests/`, `scripts/`,
  `math_utils/` imports `transformer.{vfe,aif,pure_vfe}`.
- `pytest --collect-only`: 891 tests collected, **0 collection errors**.
- `pytest tests/transformer/test_omega_gradient.py`: 15 passed.
- `pyproject.toml`/`pytest.ini` needed no edits (glob package discovery, no
  console_scripts).

## Documentation re-centered on `transformer/core/`

- `CLAUDE.md`: rewrote the RoPE × MahalanobisNorm known-gap to cite
  `core/blocks.py` + `core/block_config.py` (core *warns* on `rope_full_gauge
  != 'off'` under diagonal covariance rather than forbidding it); removed the
  `omega_direct × exact full-cov` known-gap entirely (`exact_full_cov_decode`
  does not exist in `core/`, so the mutual exclusion is moot in legacy);
  rewrote the E-step LR section to the core `self.lr` / `sigma_lr`
  (`raw_sigma_lr`, cosine-decayed) scheme in place of the vfe/ `e_*_lr` fields;
  dropped the `vfe_default` row and its footnote from the `em_mode` table.
- `README.md`: re-centered from `transformer/vfe/` onto `transformer/core/`
  `GaugeTransformerLM`. Preserved the manuscript-anchored theory verbatim;
  rewrote the architecture/forward-pass, mechanisms, usage, project-structure,
  and numerical-stability sections for core; removed the active-inference
  generation section and the vfe/ placeholder results table; reframed the
  former "legacy variants" section as core research variants. All 13 referenced
  `core/` modules verified to exist.

## Deep-audit deprecation/quality fixes (afternoon, 2026-05-25)

Five-investigator + verifier deep audit (report: `docs/audits/audit-2026-05-25.md`).
32 findings CONFIRMED, 3 REFUTED, 1 INCONCLUSIVE. Fixes applied below; the
REFUTED (E, Y, AC) and INCONCLUSIVE (W) findings were deliberately NOT touched.

### Functional fixes
- `scripts/verify_active_inference.py:275` — corrected the broken import: the
  EFE helper `_compute_active_inference_gradient` lives in
  `transformer/core/active_inference.py`, not `variational_ffn.py`. The script
  would have raised `ImportError` on first use.
- `transformer/core/vfe_utils.py` `RecorderProtocol` — removed the `record_step`
  method from the protocol. The core forward pass only ever calls `start_forward`
  (+ `enabled`/`record_*` hooks) on the global recorder; `record_step` is a
  training-loop method on `PublicationMetrics` (which is never registered as a
  global recorder). `TrajectoryRecorder` (the sole conformer) implements
  `start_forward` and now satisfies the protocol. Updated the docstring.
- `transformer/visualization/interactive_belief_viz.py` — deleted the dead
  `shap_vfe_component_attribution` function (no callers anywhere; it read a
  non-existent `output.vfe_components`).

### Deprecated dead code removed
- `transformer/training/config.py` — removed the `'pure_fep'` arm from the
  `training_mode` Literal and deleted the unused `get_pure_fep_config` preset
  (never dispatched, never called).
- `transformer/core/types.py` — removed the dead `BeliefState.omega` field
  (never constructed in surviving code; it backed the deleted omega-direct path)
  and the stale `transformer/vfe/` / `VFEConfig` docstring references.
- `transformer/train.py` — removed the deprecated `alpha`, `lambda_beta`,
  `alpha_phi` alias kwargs from `compute_free_energy_loss` and their mapping
  block; all six live call sites use the canonical `M_alpha`/`M_beta`/`mass_phi`.
  The independent `config.get('alpha_phi', ...)` back-compat fallback in
  `experiment_runner.py:1991` was intentionally LEFT (live consumer of saved configs).

### Stale comments/docstrings repointed to deleted `transformer/vfe/`
- `transformer/core/active_inference.py:231` (efe.py reference removed),
  `math_utils/transport.py:389` (e_step.py reference removed),
  `transformer/core/vfe_gradients.py:1701` (`VFEConfig`→`BlockConfig`, `/vfe`→`core`),
  `transformer/train_publication.py:716` (`--mode` help: dropped nonexistent
  "amortized", listed real choices), `transformer/visualization/vfe_dynamics_plots.py`
  (replaced the phantom `--file/--mode` CLI usage with the actual click-to-run
  CONFIG-dict pattern).
- Removed orphaned scratch artifacts from `scripts/` (`probe_output.txt`,
  `rope_attention_comparison*.png`) — their generating diagnostic scripts were
  deleted in `866305a8`.

### Performance (verified, hot E-step path)
- `transformer/core/vfe_gradients.py:455` — collapsed the full-covariance KL
  trace `tr(Σ_j⁻¹ Σ_i)` from a two-step `einsum('bijkk', einsum('bijkl,bijlm->bijkm'))`
  (which materialized the full d×d product) to a single-pass
  `einsum('bijkl,bijlk->bij', ...)`. Added regression test
  `tests/transformer/test_vfe_gradients.py::test_trace_einsum_single_pass_matches_two_step`
  asserting equivalence to the two-step form and to an explicit batched trace.
- `transformer/core/variational_ffn.py:1427` — replaced a per-block `torch.eye`
  allocation in `_build_block_exp_pairs` (gauge_param='omega' path) with the
  cached `self._get_eye(...)` helper.
- `transformer/core/variational_ffn.py:2375,2391` — gated the per-iteration
  `grad_phi/grad_omega .norm().item()` diagnostic stores behind `_is_final_iter`,
  eliminating an n−1 CUDA sync per forward (matches the documented intent at L2281).

### Type hints (CLAUDE.md: hints on all signatures)
- `variational_ffn.py`: `e_step_early_exit_tol: float→Optional[float]`;
  fully annotated `_forward_vfe_body` (18 params + return); annotated
  `_compute_multihead_vfe_gradients` (`alpha_effective: Union[float, torch.Tensor]`,
  `_alpha_c0: Optional[torch.Tensor]`, `_precomputed_block_exp_pairs: Optional[list]`,
  + verified 6-tuple return — the 5th element is `Optional[torch.Tensor]`, not
  `Optional[list]`). The `alpha_effective`/`_alpha_c0` annotations were applied to
  the two sibling signatures (`_closed_form_e_step`, `_vfe_iteration`) sharing the
  identical block, for consistency. Added `Union` to the typing import.
- `model.py`: `_resolve_gauge_mode -> Tuple[str, int, str, str, bool, bool]`;
  `_apply_cross_head_perm` Union 2-/3-tuple return; `p_flow_update`,
  `phi_flow_update`, `delta_rule_update_w_out` → `-> None` (side-effecting delegators).
- `prior_bank.py`: `forward` return type
  `Union[3-tuple, 4-tuple (encode), torch.Tensor (decode)]`.

### Deliberately NOT changed (with rationale)
- Finding F (`TrainingConfig.sigma_ce_scale`): re-examined as a **documentation
  clarification, not a functional bug**. The model reads `sigma_ce_scale` from the
  entry-point flat CONFIG dict (`model.py:1010`), and the `TrainingConfig` field
  round-trips through checkpoints via `vars()`; no caller sets it directly. Left all
  three defaults unchanged (TrainingConfig 0.7 / model fallback 0.01 / PriorBank 0.1)
  and added a clarifying comment at `config.py:247`. Changing the default would alter
  training dynamics or the checkpoint round-trip.
- Findings H/T (`use_layernorm`): load-bearing checkpoint/config back-compat shim;
  removal risks old-checkpoint loading.
- Finding C (`trusted=True`): documented self-saved-checkpoint design.
- Finding V (`C=N` chunk loop) and AA (duplicate post-loop BEP): skipped — V is a
  cosmetic single-iteration loop (near-zero gain, large risky dedent); AA risks
  altering detach/autograd semantics (the hoisted BEP may be detached and built by a
  different builder) for a one-matrix_exp/forward gain.
- Findings E, Y, AC (verifier-REFUTED) and W (INCONCLUSIVE): not touched.

### Verification
- Full suite: **889 passed, 3 skipped, 0 failed** (was 888 + the new trace test).
- Checkpoint/resume/training suites confirmed green (24 passed):
  `test_checkpoint.py`, `test_evaluate_test_set.py`, `test_training.py`.

### Audit-accounting addendum (reconciliation)

- Finding G (`ffn_mode` not validated) — FIXED: added a `ValueError` guard in
  `transformer/core/block_config.py::__post_init__` raising on
  `ffn_mode != 'VFE_dynamic'`. Verified safe: `BlockConfig`/`GaugeTransformerBlock`
  are constructed only by `GaugeTransformerLM` (`model.py:351`, `blocks.py:1126`);
  the `'standard'`/`'hybrid'` ffn_mode values route to separate model classes
  upstream and never build a BlockConfig. No test/config uses another value
  (test_model.py iterates only `['VFE_dynamic']`). 49 BlockConfig-exercising tests
  pass with the guard.
- Findings S (`ffn_lr` config fallback) and U (`training_mode` pass-through) —
  NOT changed: both are saved-checkpoint round-trip slots (read via `config.get`
  fallbacks / `vars()` merge on resume); removing them risks loading older configs.
- AB perf guard side effect (noted for the next reader, not a bug): gating the
  `grad_phi/grad_omega .norm().item()` stores behind `_is_final_iter` means
  `self._e_step_grad_norms['grad_phi']` is only refreshed on the final iteration.
  If `grad_phi is None` on the final iter (but was non-None earlier), the dict now
  carries the prior forward's value rather than this forward's last earlier-iter
  value. Diagnostic-only; does not affect training or inference outputs.
