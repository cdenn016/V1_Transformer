# Edits — 2026-05-21

## Scope

Audit of `Attention/GL(K)_attention.tex` (2360 lines) and `Attention/GL(K)_supplementary.tex` (1451 lines) for redundancies, banned patterns, and British spellings. Five vfe-manuscript-reviewer agents dispatched in parallel (main §1–§3, main §4–§7, supp §A–§D, supp §E–§I, cross-document). American-spelling sweep performed mechanically with `grep`.

## Edits applied

### 1. American-spelling conversions (13 occurrences)

`Attention/GL(K)_attention.tex`:

- `specialisation` → `specialization` (3 occurrences: L677, L964, L1163)
- `minimisation` → `minimization` (1: L677, in `$\beta$-minimisation`)
- `factorised` → `factorized` (1: L857, in "the temperature is factorised as $\tau = \kappa\sqrt{K}$")
- `Generalisation` → `Generalization` (1: L1060, in `\paragraph{Generalisation to R\'enyi $\alpha$-divergence.}`)
- `generalisation` → `generalization` (1: L1067)
- `realisation` → `realization` (1: L1127)
- `factorises` → `factorizes` (2: L1157, L1739)
- `factorising` → `factorizing` (1: L1745)

`Attention/GL(K)_supplementary.tex`:

- `realises` → `realizes` (1: L556, in the polar-decomposition surjectivity argument)
- `realised` → `realized` (1: L1290)
- `neighbour` → `neighbor` (1: L1290, in "the neighbour beliefs")

Post-edit verification: `grep -iE '\b(specialisation|factoris...|realis...|neighbour|colour|centre|fibre)\b'` returns zero matches in both files.

### 2. Scratch artifact removed

`_audit_eq_punct.py` (project root) — left behind by Agent A (main §1–§3 reviewer). Deleted.

### 3. Redundancy fixes (5 surgical edits)

Applied at the user's request after the audit consolidation, targeting the highest-severity items identified by the agents.

**Fix 1 — main `:857`** (entropy-term-essential restatement, 4× repeat reduced to 3×).
Removed the sentence "The full (un-minimized) alignment energy includes the attention entropy/prior term, which is essential: it is this term that makes the $\beta$-optimization yield the softmax rather than an arg-min." The same point is already established by the convexity argument at L744 and the canonical $\tau$-form derivation at L766–771.

**Fix 2 — main `:656`** (flat-bundle/vanishing-holonomy restatement, 3× repeat reduced to 2×).
Removed the redundant opening sentence "This is a defining property of the vertex-frame parameterization, not a derived dynamical statement: the choice $\Omega_{ij} = g_i g_j^{-1}$ encodes a globally trivial principal $G$-bundle, and Lemma~\ref{thm:vanishing_holonomy} states the cocycle identity that any such trivialization satisfies." — this restates the Lemma at L640–650. Kept the path-independence elaboration that follows; the paragraph now opens directly with the substantive content "For $\Omega_{ij} = \exp(\phi_i)\exp(-\phi_j)$, the cocycle identity therefore holds regardless of the learned $\phi_i$…".

**Fix 3 — supp `:659–663`** (SO(3) Rodrigues $c_1, c_2$ coefficients restated with only the sign of $c_1$ flipping).
Removed the duplicate display equation `$Q_a^{L} = T_a - c_1(\theta) \mathrm{ad}_X(T_a) + c_2(\theta) \mathrm{ad}_X^2(T_a)$` and the re-stated $c_1, c_2$ definitions. Replaced with a cross-reference: "$Q_a^{L}$ is obtained from $Q_a^{R} = \mathrm{dexp}_\phi(T_a)$ of Eq.~\eqref{eq:dexp_so3} by this adjoint action; the only change is the sign of the first-order ad-coefficient (from $+c_1(\theta)\mathrm{ad}_X$ to $-c_1(\theta)\mathrm{ad}_X$), with $c_1, c_2$ and the small-angle Taylor cutoffs taken directly from Eq.~\eqref{eq:dexp_so3} and the surrounding §C.2 discussion." The §C.2 SO(3) Rodrigues block (L454–467, right-trivialised) remains canonical.

**Fix 4 — supp `:960–970`** (stale single-Conjecture form vs main's Theorem + Conjecture split).
Removed the entire `\begin{conjecture}[RG universality of the transformer limit] ... \end{conjecture}` block (clauses i–v) with label `conj:rg_universality_supp`. Replaced with a one-paragraph pointer summarising main's Theorem~\ref{thm:rg_conditional} (i.i.d.-conditional fixed-point content) and Conjecture~\ref{conj:rg_universality} (the open empirical claim on trained-transformer attention graphs). The supp now defers to the main paper for the structured claim and retains only the scaling-dimension derivation (`eq:rg_scaling_supp`, `eq:rg_matrix_supp`) and the numerical-validation subsection (`sec:rg_numerical_supp`). Pre-flight verification: the removed label `conj:rg_universality_supp` had no incoming references, and the main paper's labels `sec:rg_universality`, `thm:rg_conditional`, `conj:rg_universality` all exist (cross-document `\ref` works under the existing combined-compilation pattern used by `\ref{thm:glk_invariance}`, `\ref{eq:base_priors}`, etc. elsewhere in the supp).

**Fix 5 — supp `:122`** (broken literal "Eq.~7 of the main paper" cross-reference).
Replaced with `\eqref{eq:free_energy_full_supp}` (defined at supp L1085) and updated the prose to read "the full two-channel variational free energy \eqref{eq:free_energy_full_supp}". Cross-doc agent had identified the intended target as the two-channel $\mathcal{F}_{\text{full}}$ (the surrounding paragraph mentions $\gamma_{ij}$, the model-channel coupling, which only appears in the two-channel form), and that equation lives inside the supp, so an intra-document `\eqref` is the correct form. Verified: zero remaining "Eq.~7"-style literal main-paper references in either file.

Post-edit cross-checks:
- `grep -n 'Eq\.~?7 of the main\|conj:rg_universality_supp'` — both empty.
- All referenced labels resolve: `eq:dexp_so3` (supp L458), `eq:free_energy_full_supp` (supp L1085), `sec:rg_universality` (main L2231), `thm:rg_conditional` (main L2259), `conj:rg_universality` (main L2271).
- Paragraph flow at supp L955–963 reads cleanly: §F.3 closing → "Emergent anisotropy and LayerNorm" `\paragraph` → "Compute crossover" `\paragraph` → §F.4 "Testable Predictions". No orphan environments.

## Findings (audit report)

### Banned patterns

**LaTeX spacing macros (`\;`, `\,`, `\!`)**: **0** in both files. Verified by fixed-string `fgrep` plus three independent agents (A, C, D). Agent B reported 971 + 87 hits in main §4–§7 — this is a **hallucination**; its grep used ERE mode that treats `\\,` as just `,` and counted ordinary commas. Spot-checked at L1027 (a line Agent B claimed had both `\;` and `\,`); the line is pure prose with `\Omega_{ij}`, `\operatorname{...}`, and contains zero spacing macros.

**Horizontal rules**: **0** in both files. The 27 + 1 `---` substrings in prose are LaTeX em-dashes within sentences (e.g., L51, L757), not block-level separators.

**Claude-isms** (key insight, crucially, critically, notably, importantly, worth noting, interestingly, fundamentally, in particular, underscores): **0** in both files.

**`leverages` / `leveraging`**: **1** hit, `GL(K)_attention.tex:2309` ("By leveraging gauge theory coupled with information geometry, we can introduce geometric features…"). Verb form, banned. **Not auto-fixed**; left to user to decide rewrite. Suggested: "Coupling gauge theory with information geometry introduces geometric features that constrain model behavior…" (drops both "leveraging" and "we can").

**Soft non-banlist hits** (not banned but adjacent in spirit):
- `GL(K)_attention.tex:564` "It is important to point out that…" — empty hedge.
- `GL(K)_attention.tex:857` "…which is essential: it is this term that makes…" — boilerplate cleft.
- `GL(K)_attention.tex:1356, 1872, 1998, 2175` "An important distinction / caveat" — variants of the banned "importantly".
- `GL(K)_attention.tex:194` TikZ source comment `% CRITICAL:` (figure source, not manuscript prose).

### List-environment style violations

Five `enumerate`/`itemize` blocks read as continuous prose and should be reformulated per CLAUDE.md "minimize itemizations":

| File | Lines | Block | Severity |
|---|---|---|---|
| Main | 2328–2334 | Five "falsifiable predictions" in §7.5.6 RG future work, each `\item \emph{...}` lead | medium |
| Supp | 963–969 | Conjecture clauses (i)–(v) inside `enumerate` in §F.3 | medium |
| Supp | 981–987 | Five workstation-scale predictions | low |
| Supp | 990–994 | Three at-scale predictions | low |
| Supp | 296–303 | Hand-rolled `(i)/(ii)/(iii)` paragraph-list in §B.1 Homogeneous limit | low |

Acceptable (formal theorem/conjecture clauses): main 2261–2265 (Theorem `thm:rg_conditional`), main 2273–2276 (Conjecture `conj:rg_universality`).

### Equation punctuation issues

| File | Line | Issue |
|---|---|---|
| Main | 1969–1971 | `\theta_{k+1} = \theta_k - \eta \tilde{\nabla}_\theta \mathcal{F}(\theta_k)` ends with `)`, missing terminal `,` (companion eq at 1975 has comma) |
| Supp | 161 | `\Omega_{ij}(c) = \exp[\phi_i(c)]\exp[-\phi_j(c)] \in G` ends without terminal punctuation; lead-in is "the inter-agent gauge transformation:" — append `.` |
| Supp | 378 | Unmatched `\right[` in `\partial^2 D_{\mathrm{KL}}/\partial \Sigma_1 \partial \Sigma_1` (LaTeX compile bug, not punctuation) |
| Supp | 692 | `\alpha_{ij} = \operatorname{softmax}_j(Q_i K_j/\sqrt{d})` — no terminal punctuation before paragraph break |
| Supp | 698 | `\beta^{(\mathrm{flat})}_{ij} = \operatorname{softmax}_j(-\|Q_i-K_j\|^2/\tau)` — no terminal punctuation |
| Supp | 1221 | `q_i^*(c) \propto p_i(c)^{1/2}\prod_j[\Omega_{ij}(c)q_j(c)]^{\beta_{ij}/2}` — no terminal punctuation (theorem statement continues; comma expected) |
| Supp | 1286 | `f'(q_i/(\Omega_{ij}q_j)) = \log(...) + k` — no terminal punctuation (text continues "for some constant $k$…"; comma expected) |

Cosmetic: `$$ ... $$` display at main L861–864 (envelope-theorem gradient) uses TeX `$$` rather than `\[ \]` / `equation*` — discouraged in `amsmath` documents.

### Intra-document redundancies — `GL(K)_attention.tex`

| Severity | Lines | What is repeated |
|---|---|---|
| high | 1330–1351 vs 1965–1983 | §6.1 Summary restates the three-limit reduction already derived in §4.1.4 + §4.3 with no new content |
| medium-high | 510–512, 640–650, 656 | Flat-bundle / vanishing-holonomy story told 4× (narrative + Lemma + post-Lemma narration) |
| medium-high | 730, 734, 744, 766–771, 857 | "Why the attention-entropy term is needed for softmax" stated 4× across §3 |
| medium | 398, 479, 615, 645 | `\Omega_{ij} = \exp(\phi_i)\exp(-\phi_j)` definition appears 4× (notation table, comparison table, eq, holonomy proof) |
| medium | 859–874 vs 969–975 | Envelope-theorem / autograd-vs-reduced-gradient story stated twice |
| medium | 608, 677, 977 | Model-channel deferral declared 3× in different subsections |
| medium | 1862–1872 vs 1845 | RoPE "partial relaxation of Limit~2" framing repeated |
| medium | 1747–1759 (§4.10) vs 2218–2219 (§7.2) | Lie-algebra decomposition + per-head temperature identification stated twice |
| medium | 2075–2077 (§5.2) vs 2206 (§7.1) | BERT headline numbers ($\bar{r}=0.804$, entropy 1.076) restated; pick one owner |
| medium | 2143, 2163, 2167, 2208, 2291 | "No MLPs, no $W_Q$/$W_K$/$W_V$, no activations" architectural claim restated 5× |
| medium | 2167–2170 vs 2208 vs 2287 | Embedding-matched / parameter-matched PPL numbers restated 3× |
| low | 47, 66, 991 | Abstract / Intro / §3.10 Summary triple-state principal claim (conventional IMRAD; tighten Summary) |
| low | 370–420 vs 465–503 | Notation table and Gauge-vs-Transformer comparison table overlap on $\Omega_{ij}$, gauge group, frame field rows |
| low | 2289 vs 1350 | "Explanatory theory; not SOTA-LM" disclaimer stated twice |
| low | 2304–2305 vs 1779–1783 | Non-uniform head-dimension irrep remark restated |
| low | 1565 vs 1947–1949 | Centered $\partial\beta/\partial\Sigma$ / row-sum vanishing identity narrated twice from different angles |

### Intra-document redundancies — `GL(K)_supplementary.tex`

| Severity | Lines | What is repeated |
|---|---|---|
| high | 454–467 vs 660–663 | SO(3) Rodrigues `c_1, c_2` coefficients and Taylor cutoffs stated twice (right- vs left-trivialised; only the sign of $c_1$ flips) |
| medium | 67–94 vs 96–130 | Agent / Meta-Agent / "epistemically dead" + belief-consensus equations stated twice in §A.2 / §A.2.1 |
| medium | 446 (`eq:dexp_series`) vs 600 (`\Psi(z)`) | Differential of the matrix exponential introduced twice under different notation; no cross-reference |
| medium | 590 (`eq:killing_metric`) vs 665 | Cartan-involution-modified bilinear form fully restated inline in §D.3 |
| medium | 608 (`eq:pullback_metric`) vs 665 | Pullback natural-gradient metric restated verbatim inline |
| medium | 681 (eight-phase protocol enumeration) vs 703–893 (eight subsections that each re-introduce their phase) | Protocol overview at L681 could collapse to one sentence |
| medium | 705 vs 749–757 | $\tau \approx 2\sqrt{d}$ temperature derivation + 19% deviation argument stated twice |
| medium | 751 vs 757 | Factor-of-two derivation restated within §E.3 |
| medium | 1071–1095 (Model-Channel Formalism) | Subsection sits inside §G but its content is the canonical $\mathcal{F}_{\text{full}}$; placement looks editorial. Consider moving or deleting |
| medium | 1101/1106/1316 | Forward-KL uniqueness claim restated three times in §H |
| low | 161 vs 399 vs 436 vs 541 vs 653 | $\Omega_{ij}=\exp(\phi_i)\exp(-\phi_j)$ definition repeated 5× across §A.3 / §C / §D |
| low | 273, 293, 385 | Envelope-theorem vanishing of $\sum_j \partial\beta_{ij}/\partial\Sigma_i$ stated 3× across §B |
| low | 547–552 vs 558–614 | §C.4 says "no metric correction is applied" then §C.5 immediately develops four metric-correction preconditioners |
| low | 199 | Subsubsection title promises "Gaussian KL under GL(K) Transport" but eq is the standard Gaussian KL identity |
| low | 622–633 (§D.1) | Natural-gradient form sits in a different appendix from the §B Euclidean derivation it converts; no cross-reference |
| low | 707–709 vs 902–906 | Grand mean $\bar{r}=0.804$ restated alongside posterior $\bar{r}=0.867$ |
| low | 769–773 vs 752–755 | Key-norm CV concentration argument restated |
| low | 1334–1339 vs 1415–1418 | Decode logit formula $\ell_{i,v} = -D_{\mathrm{KL}}/\tau$ typeset twice in §I |
| low | 1370 vs 1352–1354 | Gaussian log-likelihood expansion restated within §I |
| low | 1444 vs 1340 | §I intro and §I.5 recap list the same two "canonical pieces" |

### Cross-document redundancies — main ↔ supplementary

Most overlap is **legitimate split** (main states result; supp derives in detail). Real consolidation targets:

| Topic | Verdict | Severity | Recommended action |
|---|---|---|---|
| RG `(g_1, g_2, g_3)` definitions | partial-redundancy | medium | Trim supp `:929–934` to pointer at main `:2238–2243` or align labels |
| RG meta-agent `\mu_A, \Sigma_A` | partial-redundancy | medium | Trim supp `:939–943` to pointer |
| RG scaling exponents `n^{-1/2}, n^{-1}, n^{-1}` | partial-redundancy | medium | Trim supp `:949–952` |
| RG conjecture/theorem statements | partial-redundancy + stale | medium-high | Main has Theorem-plus-open-Conjecture split (post-`4bc4de5e`); supp `:960–970` still carries the older five-clause single Conjecture. **Align the supp version to mirror the main's split, or replace with a pointer.** Two `\label`s (`conj:rg_universality` vs `conj:rg_universality_supp`) make this readable, but the two statements differ in rigor level |
| RG testable predictions | partial-redundancy | medium | Trim main `:2325–2334` to pointer, or trim supp `:978–994` to at-scale-only |
| Meta-agent scaffold cross-ref | broken | medium | Supp `:122` says "Eq.~7 of the main paper" as a literal "Eq.~7" string; main currently has no permanent Eq. number 7. Replace with `\ref{eq:free_energy_final}` or `\ref{eq:free_energy_full_supp}` and verify intended target |
| BERT "algebraic identity" caveat | legitimate-redundancy | low | Restated 3× (main `:1998`, supp `:676`, supp `:746–747`). Intentional but could be consolidated |
| `\Omega_{ij}=\exp(\phi_i)\exp(-\phi_j)` | partial-redundancy | low | Main `:614–617` and supp `:160–163`. Supp restatement is the endpoint of its self-contained §A bundle preamble; keep both |
| Free energy functional (single-channel main vs two-channel supp) | legitimate-split | low | Keep both |
| Free energy (surrogate vs canonical) | legitimate-split | low | Keep both |
| Sandwich `\Omega\Sigma\Omega^\top` push-forward | legitimate-split | low | Keep both |
| Softmax-from-mixture derivation | legitimate-split | none | Only in main §3.3; supp §E quotes the result for empirical comparison |
| KL gradient through transport | legitimate-split | none | Only in supp §C.3 |
| SPD retraction (`sec:retraction`) | legitimate-split | none | Only in supp §D.2 |
| Symmetry breaking (theory vs sims) | legitimate-split | none | Main does theory, supp does sims |
| Multi-head block-diagonal `\mathrm{GL}(K)` | legitimate-split | none | Only in main §4.4 |
| Prior-bank decode | legitimate-split | none | Main §4.10.2 + §6 mention; supp §I derives from Bishop §4.2 |

## Audit method (for reproducibility)

Five `vfe-manuscript-reviewer` agents dispatched in parallel, each with an isolated context and an explicit slice + banned-pattern list. One `vfe-cross-manuscript-consistency` agent for cross-document redundancy. Ground-truth banned-pattern counts cross-checked by direct `fgrep` from the main session (this is what caught Agent B's `\,` / `\;` hallucination). American-spelling sweep used a curated regex of common British forms (`-ise/-isation`, `-yse`, `-our`, `-re`, `-ence`, `-lled`/`-lling`, `-ogue`, `whilst/amongst/aluminium`) with care to exclude false positives (`characteristic`, `categorical`, `hierarchical`, `hypothesis`, `synthesis`, `analysis`, `emphasis` — all identical in both English variants).

## Not applied (left for user decision)

- Single Claude-ism hit: `leveraging` at main L2309 — needs prose rewrite, not a mechanical swap.
- Redundancy consolidations above — most require editorial judgment on which copy is canonical.
- Equation-punctuation tweaks — wait for user sign-off on commas-vs-periods.
- Stale supp §F.3 RG conjecture statement — needs to be aligned with main's Theorem/Conjecture split.
- Supp `:122` literal "Eq.~7" cross-reference — verify intended target first.
- Soft Claude-isms (off-list hedges and clefts) — advisory only.

## Files touched

- `Attention/GL(K)_attention.tex` — 11 in-place spelling fixes.
- `Attention/GL(K)_supplementary.tex` — 3 in-place spelling fixes.
- `_audit_eq_punct.py` (project root) — deleted scratch artifact.
- `docs/edits/edits-2026-05-21.md` — this file.
