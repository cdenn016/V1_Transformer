# Edits — 2026-05-24

## Scope

Merged a new theory result into `Attention/Participatory_it_from_bit.tex` §"Time as Information Flow": a scale-dependent meta-agent information clock formalizing Bateson's "a difference which makes a difference," with a graph-Laplacian derivation tying the cross-scale slowdown to the consensus coupling $\beta_{ij}$. Investigation and verification artifacts in `docs/notes/2026-05-24-*`.

## Edits applied

### 1. `Attention/Participatory_it_from_bit.tex`

- **§Bit-Counting Time** — added one paragraph + display equation `eq:kl_arclength`. Introduces the Cramér–Rao / Fisher just-noticeable-difference ellipsoid reading of the "1 bit" quantum (cites `CoverThomas2006`, `AmariNagaoka2000`), the Bateson attribution (`Bateson1972`), and the exact relation $\mathrm{KL}=\tfrac12 ds^2$ reconciling the bit-counting clock with the Fisher arc-length clock (one JND unit $ds=1$ is $\tfrac12$ bit, not one bit).
- **§Fisher Arc Length** — appended one sentence linking the arc length to the JND through `eq:kl_arclength`.
- **New subsubsection §Scale-Dependent Resolution and the Meta-Agent Clock** (`sec:scale_dependent_time`) — the bit-production ratio $Z_I$ (`eq:bit_production_ratio`), the coherent/idiosyncratic decomposition $Z_I=(\text{coherent fraction})+O(1/N)$ (`eq:Z_decomposition`), the covariance-aggregation consistency note (averaging form vs product-of-experts), the graph-Laplacian reduction of the belief coupling (`eq:coupling_laplacian`), the three spectral roles (zero mode → barycenter/$Z_I=1/N$; nonzero modes → $\mathcal{I}_{s\to s+1}$ via `eq:Ilow_eq_dispersion`; spectral gap $\lambda_2$ → cluster-formation timescale and `eq:culture_closure`), and the renormalization verdict (bit is reparametrization-invariant; $Z^{(s)}$ is a scale-adapted comoving-units choice, not a critical exponent — the latter is the open RG fixed point at `sec:meta_agent_rg`).
- **§Emergent Timescale Hierarchy Across Scales** — added `\label{sec:emergent_timescale}` (the new prose `\ref`s it; was previously unlabeled).

Notation decision: kept the existing clock symbol $\tau_i$ from §Bit-Counting Time rather than renaming to $\mathcal{T}$ (a manuscript-wide rename collides with $\tau=\kappa\sqrt K$ and $\tau_q,\tau_s$ and is left as a separate editorial pass). The draft's $\mathcal{I}(\theta)$ for Fisher information was changed to the manuscript's own $g_{\mathcal{B}}$ to avoid collision with $\mathcal{I}_{s\to s+1}$.

### 2. `Attention/references.bib`

- Added `@book{Bateson1972}` (Steps to an Ecology of Mind) and `@article{Fiedler1973}` (Algebraic Connectivity of Graphs).

### 3. `docs/notes/` (supporting artifacts)

- `2026-05-24-meta-agent-time-bateson.md` — full analysis (KL-vs-Fisher fork, coherence-filter result, Laplacian spectral derivation, constraints, status of claims).
- `2026-05-24-time-subsection-draft.tex` — the reviewed drop-in draft that this merge is built from.

## Verification

- Numerical/symbolic (sympy + Monte-Carlo): Gaussian $\mathrm{KL}=\tfrac12\delta\mu^\top\Lambda\delta\mu=\tfrac12 ds^2$ exact; $Z_I$ values $1/N$ (incoherent) and $1$ (coherent) for the averaging convention, $1$ and $N$ for product-of-experts; $N=1$ recovers the §2588 clock; Laplacian zero-mode projection $=1/N$, nonzero-mode energy $=\mathcal{I}_{s\to s+1}$ (machine precision), stationary dispersion $=\sum_{a\ge2}D/(4\eta\lambda_a)$.
- LaTeX: all new labels/refs/cites resolve; `\begin/\end` environments balanced; `pdflatex` processed the new section and wrote `equation.180/182–185` and `subsubsection.5.1.4` to the `.aux`. No errors localized to the inserted line range. No banned words or `\,`/`\;`/`\!` spacing macros in the inserted prose.
- Pre-existing baseline issues NOT introduced by this edit: missing figure files (`Fig_*.png`), unicode-in-math, and a dangling `\ref{sec:transformer_zero_dim}` (lines 3317/3319). The manuscript does not build to a clean PDF at baseline.

## Note

Running `latexmk` overwrote the local untracked `Attention/Participatory_it_from_bit.pdf` with a draft-mode partial build (figures absent). Rebuild locally with figures present for a real PDF. Aux artifacts cleaned with `latexmk -c`.

## 4. Red-blue-debate REMAND corrections (`Attention/Participatory_it_from_bit.tex`)

A `/red-blue-debate` on the new subsection (artifacts in `docs/debates/2026-05-24-pifb-scale-dependent-time/`) returned REMAND: the three results are sound but three sentences overclaimed as written. Three surgical fixes applied, none touching the physics:

- **Line ~2649 (mechanism for $Z_I=1/N$).** Replaced "incoherent injection, with power spread uniformly over all $N$ eigenmodes" with the correct reason: independent injection gives the zero mode a $1/N$ share of total power for any per-constituent covariance (variance-of-the-mean), with equipartition noted as the special isotropic case, not the requirement. Red's heteroscedastic counterexample showed per-mode power non-uniform yet zero-mode share exactly $1/N$.
- **Line ~2651 (Eq. `Ilow_eq_dispersion`, first equality).** Changed the first `=` to `≈` and added the condition ($\Omega_{i,I}\approx I$, $\Sigma_i\approx\Sigma_I$, leading order): $\mathcal{I}_{s\to s+1}$ is defined at `:2233` as a sum of transported Gaussian KLs and equals the pure-mean dispersion only on the gauge-aligned equal-covariance locus. The second equality (dispersion = nonzero-mode energy) is exact Parseval and stays `=`. The lead-in sentence was restructured to separate the exact and leading-order relations.
- **Line ~2656 (renormalization verdict, first sentence).** Removed the reparametrization-invariance-implies-no-renormalization step (it conflated Cencov coordinate-invariance with Wilsonian RG invariance across scales). Replaced with: the bit is a fixed reference unit held constant across scales by convention. The load-bearing second argument (LLN ratio, no critical point, open fixed point at `:2227`) carries the conclusion unchanged.

Verification: environments/braces balanced, all refs/cites resolve, no banned words or spacing macros introduced. Structural checks only (baseline does not build to a clean PDF); equation structure and labels unchanged, so a prose+operator edit cannot introduce a new build error of the kind the structural checks would miss.

## 5. README rewrite (vfe/-centered)

### Scope

Rewrote `README.md`, which was based on the legacy `transformer/core/` implementation, to be centered on the canonical `transformer/vfe/` package and re-anchored to the `Attention/GL(K)_attention.tex` + `GL(K)_supplementary.tex` manuscript pair. Driven by three parallel investigation agents (vfe/ codebase inventory, GL(K) manuscript extraction, aif/ + project-structure map) and one verifier agent that confirmed all 36 high-stakes claims against file:line evidence before any prose was written.

### Edits applied to `README.md`

- **Banner / citation** — retitled to the actual paper *"Attention as Gauge-Theoretic Variational Inference"* (Robert C. Dennis), per `GL(K)_attention.tex:33`. Previously the banner used the PIFB title. BibTeX key updated to `dennis2026attention`; PIFB demoted to a companion-paper line.
- **Equation citations corrected** — replaced the non-existent `eq:free_energy_functional_final` (a PIFB-only label) with the real `eq:free_energy_final` (single-channel reduced form) and `eq:F_align_canonical_tau` (row-Lagrangian alignment form). Cited real labels for GL(K) invariance (`thm:glk_invariance`, `eq:glk_invariance`), transport (`eq:gauge_frame_rotation`), sandwich product (`eq:gauge_action_gaussians`), geometric bias (`eq:geometric_bias`, uses `d_k`), attention (`eq:mixture_softmax`, `eq:per_head_temperature`), the softmax-gradient nonlinearity (`eq:softmax_gradient_nonlinearity`), and the canonical-vs-surrogate envelope gap (`eq:autograd_envelope_gap`).
- **Theory claims qualified to match the paper** — `W_Q W_K^T = sigma^{-2} Omega^{-T}` framed as a head-space bilinear identification (not parameter-level); layer norm as one mechanism imposing the constant-key-norm condition (not a unique necessity); backprop interpreted, not derived (`GL(K)_attention.tex:68`). Noted that the two-channel hierarchy (`lambda_h`, `gamma_ij`) and non-flat holonomy are deferred to the PIFB companion (`GL(K)_attention.tex:619,665,677`), and that the GL(K) paper proves vanishing holonomy on the default flat bundle (`thm:vanishing_holonomy`).
- **Architecture section rewritten to the actual vfe/ forward pass** — encode → positional BCH composition → per-layer E-step (β recomputed each inner iteration; natural-gradient on self-coupling + alignment + attention-entropy) → cross-layer prior handoff → decode. Removed the legacy "attention sublayer (optional)" and `skip_attention` framing (no such toggle exists in vfe/).
- **Mechanisms section** — every feature tied to a real `VFEConfig` field with `config.py` line numbers: `cross_couplings` (303), `use_rope`/`rope_full_gauge` (220/222), `gauge_covariant_ridge` (213), `phi_preconditioner` (150, with `'pullback'` rejected at runtime, 609–621), `gauge_parameterization='omega_direct'` (277, requires diagonal cov, 579–585), `exact_full_cov_decode` (requires full cov, 503–507) and their mutual exclusion, `use_non_flat_transport` (335, with the open `NotImplementedError` at `non_flat.py:483-487`), `E_learnable_alpha` (100).
- **Removed features that are core/-only** — `em_mode` table, DEQ (`use_deq`), `learnable_reflection`, `sigma_ce_scale`, `use_block_diagonal_kl` toggle, and the BERT/RoBERTa validation section (per user instruction: removed entirely, including `transformer_test.py`/`roberta_diagnostics.py` from usage). Verified absent from `transformer/vfe/`.
- **Generation section** — documents depth-1 EFE via `VFEModel.generate(use_efe=True)` (`vfe/efe.py`, confirmed live) and the depth-D `transformer/aif/` tree-search generalization. Corrected the memory-note confusion: the deleted file was `vfe/active_inference.py`, not `vfe/efe.py`.
- **Results** — three subsections: manuscript headline (GL(15), K=90, 1 layer, 81.4M → test PPL 71.6; 1.66× over embed-matched, 1.88× over KN-5, `tab:glk_results`); a current-`vfe/`-runs table with placeholders for the user to fill; a legacy-`core/` subsection carrying the prior ~61/76 WikiText-103 and ~24 wiki-ja numbers, attributed as not directly comparable.
- **Usage / install / project tree** — usage rewritten to the click-to-run `transformer.vfe.train_vfe` / `vfe_ablation_suite` / `run_semantic_clustering` and the aif entry points (noted `train_aif.py` is generation, `train_aif_augmented.py` is training); root `generate.py`/`inference.py` flagged as legacy core/. Removed the non-existent Lightning training command. Install rewritten from `pyproject.toml` (Python ≥3.10; `pip install -e .[viz|all]`). Project tree replaced with an accurate layout including `vfe/` and `aif/`.
- **Legacy variants** — brief closing section on `transformer/core/` (and that vfe/ imports its stateless math kernels) and `transformer/pure_vfe/`.

### Verification

- All 36 claims (manuscript titles/labels/results, vfe/ feature presence/absence, aif/ wiring, project structure, dependencies) confirmed by a dedicated verifier agent against file:line evidence prior to writing.
- README scanned for the project's banned words (`crucially`, `notably`, `in particular`, `leverages`, etc.) — none present. No `---` horizontal rules.
- Commit scoped to `README.md` and this edits doc only; pre-existing unrelated working-tree deletions were left untouched.
